---
description: 2026-09-14承認のD保存とA40完走/保存障害の分離
alwaysApply: true
---

# 検証成果物の保存先

- 最新2026-09-14ユーザー承認：PR #26はG2限定の診断・検証資材としてマージまで実施する。保存監査未完をPASSへ変更しない。マージ後は停止し、G3実走・本番採用・追加の自動開発は開始しない。この承認が下記の旧「mergeなし」より優先する。

- ユーザー承認により、今後新規の検証ログ・JSON/JSONL・検収票は `D:/puyo_analyzer/verify/` 配下に保存する。検証用コード/PR文書は既存workspaceに置く。
- 新規動画は従来どおり `D:/puyo_analyzer/videos/` 配下。既存Cの動画・モデル・凍結code・検証原票を自動削除/移動/上書きしない。
- 旧runのrun_id/source_idを新保存先へ書き換えない。D上の追加検収票は原runへの参照と、後処理であることを明記する。
- A40は36900までの動画処理完走と修復箇所通過を完了扱いにする。原entry1と終了書込I/O失敗は保持し、保存・検収済みへ偽装しない。I/O障害だけを理由に全動画を再走しない。
- PRには完了根拠・独立検収・未回収の保存票/残存制約を区別して記す。PR＋修正一覧コメント後はG3文書準備で停止、merge/採用/G3実走なし。
