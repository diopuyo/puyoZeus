# 同call palette 証拠に限定した診断終了契約

default OFF。旧 proof/REFERENCE/C6 と元 verify/guards/inputs/digest/read/sha の関数は不変。
有効時は旧 prove の code を私有 globals へ束縛し、report/replay の実差分だけを検査する。
palette.finish を旧 finish より先に一度実行する。実 history 保存、ENGINE、実 child exit/finalizer は元のまま。

全 raw/CNN/NEXT/Counter と非 confirmed 値を成功済み同背景 atomic run へ照合する。
前 C6 の上流は旧固定 chigiri 正本へ、全色留保を同 call の票・journal・context へ束縛して照合する。
逆投影は仮想対照であり、旧盤面の物理的正しさ、原 producer の再実行、現在盤面の認証を意味しない。
後続補正で失われた留保や未説明の派生変更は透過させない。prefix false は false のまま保存する。

元 collector の全下流採否を再生し、元 accumulator.append で全 21 非ラベル列を照合する。
画像タグは保存実値であり画像正解の認証ではない。ラベル列は読まない。
失敗 run の成功化は禁止。CPU fixture の run_id 再束縛と不足 receipt 作成は人工と明示し新排他出力だけを使う。
会計・物理真値・品質・本番権限は常に false。
