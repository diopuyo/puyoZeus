# セッション引継ぎ 2026-05-11

## 現状サマリー
- **目的**: 720p+ 既知/未知動画での puyo cell 認識精度を向上
- **最新 viz**: `data/test_unknown/v91_match1_75s_viz_FIX_V.mp4` (= cycle 70)
- **テスト基準動画**: `data/test_unknown/v91_match1_75s_720p.mp4` (= v91 から match1 切り出し、 720p 60fps 75s)
- **回帰確認動画**: v89 (720p)

## 実装済 cycle 一覧

| Cycle | 内容 | 効果 (v89 1P landing acc) |
|---|---|---|
| 65 | merged_default RED バグ修正 (= H=0-180 → 除外) | 基準 |
| 66 | NEXT 累積色制約 + in-flight tsumo queue | 0.450 |
| 67 | deficit-only force fill + 多 cell 範囲 | 0.450 |
| 68 | YELLOW V_min 180→120 | 0.432 |
| 69-A | cell_sample_rect 下寄せ (visible_row 0-1) | 0.518 |
| 69-B | ColorClassifier sub-region vote | (同上) |
| 70-A | OJAMA_S_THRESHOLD 60→30 (黄色 OJAMA 誤分類解消) | 0.515 |
| 70-B | sample 下寄せ範囲 0-1 → 0-3 | (同上) |
| 70-C | stable_frame_count 6→3 (= 遅延 100→50ms) | (同上) |

v89 累積改善 (FIX_L → FIX_V):
- 1P plc 20→34 (+70%), acc 0.450→0.515
- 2P plc 20→31 (+55%), acc 0.425→0.484

## ユーザーの残課題 feedback (= cycle 71 で対応必要)

1. **連鎖中の推論不全 + エフェクト誤認** (= まだ未解決)
2. **ぷよ置いた直後の未検出/誤認多い** (= まだ未解決)
3. 全体的に検出弱い (= 改善は出てるが目標未達)

## 根本原因仮説 (= 未確認、 ユーザーレビュー必要)

### 仮説 A: 連鎖前の置き誤認 → 連鎖中 freeze で固定
- TSUMO_FALL→STABLE 着地時に `_compute_landing_inferred` が間違った色を確定
- 連鎖発火 → freeze → 間違った色のまま連鎖中見える

### 仮説 B: 連鎖検出遅延 → 発火前 animation を CNN が読む
- 連鎖開始直後の数 frame は state=STABLE のまま
- CNN が chain animation を puyo として読む
- majority vote で confirmed_board に animation 由来の色入る

抽出済 chain frame: `data/test_unknown/chain_frames/chain_1P_f01400_23.3s.png` 等

## 次セッション優先タスク (= 提案)

### 最優先: 根本原因究明
1. **連鎖前後の各 frame で confirmed_board が変わる瞬間を log** (= 仮説 A/B 判別)
2. **TSUMO_FALL→STABLE 遷移が起きない placement の頻度を計測**
3. **CNN raw 出力と HSV-only 出力の差分を per-cell で可視化**

### 改善候補 (= 先行研究より)
1. **bpinzone Tetris "close eyes"**: 連鎖中は state machine 信頼性低なので、 NEXT pair 履歴ベースで完全予測盤面を表示
2. **添島・山口 2019 色順序対称性**: cell CNN を 24x augment で再訓練
3. **Multi-frame consensus 強化**: 着地後 5-10 frame の HSV/CNN 出力を集約

### 構造的改善
- ChainSimulator final_board を **より積極的に活用**: 連鎖直後の confirmed_board を必ず ChainSimulator 出力で上書き
- **DRY rule**: 連鎖発火 → 連鎖前 board snapshot → simulator で final 計算 → final で上書き (= 既実装だが境界条件で外れる可能性)

## 重要ファイル
- `src/recognition_pipeline.py`: メインロジック、cycle 66/67 制約コード, cycle 69 cell sample
- `src/image_reader.py`: HSV ranges, OJAMA_S_THRESHOLD, cell_sample_rect 下寄せ
- `src/board_state_machine.py`: state transitions, `_merge_diff_only` で UNKNOWN 維持
- `src/chain_detector.py`: VideoChainTracker (puyo count diff>=4)
- `src/chain_phase_detector.py`: ChainPhaseDetector (score delta 補強)
- `models/ui_templates/telop_challenger_decisive.png`: cycle 65 で追加

## 検証 script
- `scripts/visualize_recognition.py`: viz 生成
- `scripts/diagnose_cell_classification.py`: cell-level diag
- `scripts/eval_landing_via_next_history.py`: NEXT 履歴 acc 評価
- `scripts/extract_chain_frames.py`: 連鎖発火 frame 自動抽出

## メモリ参照
- `project_resolution_aware_recognition.md`: 解像度依存処理 (cycle 63)
- `feedback_recognition_target_995.md`: 99.99% 認識目標
- `reference_puyo_ai_recognition.md`: 先行研究調査
- `project_recognition_improvement_candidates.md`: 改善 backlog

## 注意点
- **720p+ regression なし** が必須 (= v89 で確認)
- **テストパス必須**: `tests/test_recognition_pipeline.py` ほか 38 件
- **MEMORY 注意**: 並列ジョブで OOM リスク (= 過去 WSL クラッシュ実績あり)

## 次セッションキックオフプロンプト

```
puyo_analyzer プロジェクト続行。 直近のセッション引継ぎは
`docs/HANDOFF_2026-05-11.md` に記載。

最新状態:
- cycle 70 完了 (FIX_V)
- 残課題: 連鎖中誤認 + 置いた直後の未検出/誤認

次の最優先タスク (= cycle 71):
1. 連鎖前後の confirmed_board 変化を per-frame log
2. 仮説 A (置き誤認→freeze) vs 仮説 B (chain detection 遅延) を判別
3. ChainSimulator final_board の境界条件確認

並行で多角度検証 (CPU 並列活用)。 root cause 確定したら適切な fix を実装。

ユーザー review が必要なら都度依頼。 viz path は
`C:\Users\ryouj\.gemini\antigravity\scratch\puyo_analyzer\data\test_unknown\` 配下。
```
