# 148動画 再収集 (2026-08-18 新本番構成) — 走行中メモ

## これは何か

2026-08-18 に user 承認された **8フラグ全群採用** 後の本番構成で、148動画を
まるごと収集し直すジョブ。学習データの土台を新認識構成で作り直すのが目的。

- 起動: 2026-08-18 06:52 JST
- 実行スクリプト: `scripts/_regen148_recollect_2026-08-18.py`
  (起動シェル `scripts/_run_regen148_recollect_2026-08-18.sh`、WSL detach)
- 認識構成の単一情報源: `src/production_config.py` の `collect_flags()`
  (2026-08-18 時点で23フラグ、うち今回採用の8フラグを含む)
- マニフェスト: `data/verify/regen_2026-08-11_manifest.tsv` (148本、8/11版を流用)
- 出力 npz: `data/indicators_v2/boards_lean_phase_l_2026-08-18/`
- ログ: `logs/regen_2026-08-18.log` (全体) / `logs/regen_2026-08-18_per_video/` (動画単位)
- 進捗: 本ディレクトリの `status.tsv` (再開可能。同じ起動コマンドで続きから走る)

## 走行中の禁止事項 (重要)

収集プロセスは起動のたびに `src/` と `scripts/collect_boards_lean.py` を読み込む。
**走行中にこれらを変更すると、148本の中で認識構成が混ざり、学習データが
汚染される**。148本が全て終わるまで、以下は変更しないこと。

- `src/` 配下 (特に `production_config.py`、`recognition_pipeline.py`、
  認識・状態機械まわり全般)
- `scripts/collect_boards_lean.py`
- `scripts/_regen148_orchestrator_2026-08-11.py` および本ジョブのスクリプト

調査・計装は読み取り専用で行うか、収集完了後に回すこと。
学習側 (`scripts/build_labeled_win_from_npz.py` 等) は収集中に使われないため
変更しても収集は汚染しないが、収集完了前に走らせても意味がない。

## 収集後の工程 (実行順・確定版)

所要時間の実測値つき。全部で概ね5〜6時間かかる。

### 1. 品質ゲート (今回から明示的に組み込む)

```
python scripts/phase_l_video_quality_gate.py --all \
    --npz-dir data/indicators_v2/boards_lean_phase_l_2026-08-18 \
    --out-dir data/verify/phase_l_quality_gate_2026-08-18
```

**なぜ明示するか**: 前回のベースライン学習 (2026-08-14) は、入力になった
`boards_lean_phase_l_2026-08-11` に対して品質ゲートを走らせた形跡が無い
(ログを全文検索してもヒットしない)。品質ゲートを通した実績があるのは
2026-08-07 収集分だけで、そのときは148本中 FAIL=24 / WARN=26 / PASS=97 だった。
今回は8フラグ全群ONでこれがどう変わるかを比較材料にできる。
FAIL 動画の扱い (除外するか目視に回すか) は user 判断を仰ぐこと。

### 2. 学習CSVのビルド

```
python -m scripts.build_labeled_win_from_npz \
    --npz-dir data/indicators_v2/boards_lean_phase_l_2026-08-18 \
    --out data/verify/labeled_win_full148_2026-08-18/labeled_win_full148.csv \
    --profile full \
    --exclude-match-end-locked
```

- `--exclude-match-end-locked` は 2026-08-18 に標準採用として
  `src/production_config.py` の `LEARNING_DATA_BUILD_ADOPTED` に登録済み。
  ただし **既定 False で、付け忘れても何も警告されない**。収集側の
  `collect_flags()` に相当する呼び出しヘルパーが学習ビルダー側にはまだ無いため、
  手で付ける必要がある (事故防止の配線は今後の課題)
- 所要 **約4.4時間** (2026-08-14 実測15,939秒)。2026-08-13 の native 化で
  「23分に短縮」と見積もられていたが、その後に追加された列のコストで
  実測は4.4時間のまま。見積もりには実測値を使うこと
- `c26 / c30 / c58 / c69` の4本は `BROKEN_VIDEOS` として自動隔離され、
  144本が処理される

### 3. 再学習・評価

```
python scripts/_retrain148_2026-08-14.py \
    --csv data/verify/labeled_win_full148_2026-08-18/labeled_win_full148.csv \
    --out-dir data/verify/retrain148_2026-08-18
```

既定パスは 2026-08-14 のものなので、**必ず両方を明示指定**して前回結果を
上書きしないこと。所要 約32分。

**比較ベースライン** (`data/verify/retrain148_2026-08-14/summary.json`):

| 指標 | 前回値 (2026-08-14) |
|---|---|
| プールAUC (フル列) | 0.6573 |
| 動画別中央値AUC | 0.6593 (IQR 0.637-0.679) |
| 旧light63相当列のみ | 0.6261 |
| 位相別 (序盤/中盤/終盤) | 0.508 / 0.560 / 0.839 |

位相別AUCの**絶対値は信用しないこと** (W18: CSVの `tsumo` 列は実手数ではなく
STABLEスナップショットの時刻順位の近似で、実勝率が npz実測11.3% に対し
CSV経由24.9% と乖離する)。旧構成との相対比較にのみ使う。

### 4. その先

W12 根治の判定仕上げ (`docs/WORK_CHECKLIST_2026-08-15.md` の P5〜P7、
148再収集完了待ちで保留していた分) → Phase J。
W12 の新列は 2026-08-16 に CSV ビルダーへ実装済みなので、今回の再ビルドで
自動的に入る。85本サブセットでの先行検証結果
(`data/verify/retrain85_w12_2026-08-16/`) が突合相手になる。

## 前回との違い

`data/verify/regen_2026-08-11_status.tsv` の 8/11 版は 148/148 OK で完了済み
(出力は `boards_lean_phase_l_2026-08-11/`)。今回はその後に採用された8フラグ
(W25系2 / 境界RT系3 / 収集系3) が入った構成での作り直しであり、**既存の
2026-08-07 / 2026-08-11 / w12_2026-08-16 の npz には一切触れない**。
