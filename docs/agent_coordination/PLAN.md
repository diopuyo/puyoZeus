# 全体計画 — 交換エピソード会計

更新: 2026-08-24 / 状態: 計画確定、既存検証完了待ち

## 目標

`催促対応 → 相手本線 → 自分本線 → 相殺 → 着弾` を一つの交換として追跡し、
進行中の暫定火力と確定会計を重複なく照合する。交換未解決中に根拠なく
99% / 1%を断定せず、交換確定後は正しい方向へ速やかに収束させる。

## Gate 0: 現在タスクを閉じる

- 掛け算式E2E、10ケース、偽イベント、8動画×3条件バックテストを完了する。
- PM100 pytestを完了する。負荷依存の速度テスト失敗は低負荷で単独再実行する。
- PM100全8区間結果と掛け算式結果をレビュー記録へ固定する。
- 完了前に関連ソースを編集しない。

## Gate 1: 観測仕様と不変条件

- 交換は最初の発火で開始し、相手の応射・再応射を同じ episode に参加させる。
- 各連鎖へ安定した `chain_id` を持たせ、formula再トリガーによる断片を統合する。
- イベントは発火・完走時刻順で処理し、OCR確定が遅れても順序を変えない。
- 暫定生成量は確定得点で「置換」する。確定時に再加算しない。
- 数学用pendingはcap前、表示用pendingだけ従来capを維持する。
- episode終了時に暫定残差、重複イベント、未照合量が0になること。

成果物: `docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md`、代表場面のイベント表、
不変条件テスト一覧。

## Gate 2: 純粋な会計コア

- 新規 `ExchangeEvent` / `ExchangeEpisode` / `ExchangeLedger` を独立モジュールへ実装する。
- 中核は順序付きイベントを受けて符号付き純残量を再計算する純粋処理とする。
- provisional / finalized / reconciled / landed の状態を区別する。
- 既存APIは削除せず、optional引数または外部wrapperで接続する。
- 新フラグは既定OFF。既存OFF出力とのbit-identicalを固定する。

## Gate 3: 認識・表示への統合

- 掛け算式検知から安定 `chain_id` を供給する。
- `OjamaAccountingTracker` を確定会計の単一情報源とし、暫定イベントは確定時に照合する。
- `ResolvedExchangeTracker` の「両側同時chain_event必須」を、時間差応射も参加できる
  episode方式へ置き換える。ただし旧経路はフラグOFFで保持する。
- episode未解決中はkill overrideの完全上書き±100を禁止する。
- 実死亡、着弾確定、反撃不能が物理的に確定した場合だけ早期解除を許す。
- timeline dumpへ episode_id、chain_id、暫定/確定生成量、純残量、phase、
  hard-override保留理由を追加する。

## Gate 4: 検収

比較条件:

1. 既存OFF基準。
2. 現行ヒステリシスBのみ。
3. 現行規模比較Aのみ。
4. 現行A+B。
5. 交換台帳+未解決ゲート+掛け算式根治。

必須条件:

- seg01 game2の誤反転を再発させない。
- 交換終了時の生成・相殺・送付・着弾の保存則を全episodeで満たす。
- 同一chain_idの暫定値と確定値を二重計上しない。
- 1秒150pt以上の急変は現行ONの40回以下で、残る全件に新しい物理イベントがある。
- ±100張り付きはOFF基準14.0%を超えない。
- 生モデル逆符号張り付きは内部整合性ガードとしてOFF基準3.3%を超えない。
- 決着方向誤りは現行ONの7試合を超えない。
- 真の致死局面を弱めた試合0を維持する。
- 認識精度99.5%以上、全pytest成功。

## Gate 5: 採用と後続

- 数値結果・代表画面・タイムラインをユーザーへ提示する。
- ユーザー承認後のみ `src/production_config.py` に採用日と根拠を記録する。
- 採用構成を固定後、別出力先で148動画を再生成する。
- 判定基盤確定後にPhase Jへ進む。

## 2026-08-25 08:10 JST — Codex再レビューによる改訂ロードマップ

Gate 1・2の成果物とGate 3-2bの実データ検証は有用だが、独立レビューで
会計の入力境界に未検出不具合が見つかった。Gate 3-2c以降を一時停止し、
次の順序へ差し替える。既存成果物は上書きせず、新しい出力先で比較する。

### Gate 3R-0: 凍結

- 交換台帳をオーバーレイへ配線しない。
- `src/production_config.py`へ交換台帳フラグを登録しない。
- 現行A+Bを本番ONにしない。
- E1削除の考え方は維持する。I16は考え方を維持するが、実装は再検収する。

### Gate 3R-1: P1修正

1. `simulate_fallback`を許可しない既定構成では、推定値がFINALIZEだけでなく
   provisional FIRE経由でも会計へ入らないようにする。
2. growthなしの`CHAIN_SETTLED`は即時確定せず、後続のscore OCR確定値と
   同じ物理連鎖へ統合する。fallbackは「確定値が来なかった」と判断できる
   終端・期限でのみ発行する。
3. 相殺・着弾はpending残量の純差分から推測せず、同一フレームの増加と減少を
   分離できるgross eventまたはcap前累積カウンタから供給する。

### Gate 3R-2: P2と認識回帰

1. 大幅下方FINALIZEの再入力を冪等化し、後続の正常確定で保留差分を解消する。
2. FormulaStepAccumulatorはvalid観測が途切れたらpending連続確認を破棄する。
3. 新規関数は1関数50行以内へ整理する。
4. 既存の掛け算式8動画成果物を上書きせず、修正後の別出力先で比較する。

### Gate 3R-3: 測定器是正と再測定

1. user承認済みのW38を根治し、壊れた検証ツールの修正前後を比較する。
2. v51 t=460〜531で実効レート時系列を採取し、115.7%検算をやり直す。
3. P1修正後にzenchiの物理16連鎖とchain_idを1本ずつ対応表にし、
   説明不能な余剰IDを0にする。
4. 同時生成+相殺、終了合図→遅延確定、fallback-onlyを実データまたは
   地上真値付きfixtureで検収する。

### Gate 3R-4: Gate 3-2b再合格条件

- 既定構成でsimulate由来量の会計混入0。
- 1物理連鎖から複数chain_idが生まれる未説明ケース0。
- 同時応酬でgross相殺量が失われない。
- 生成量の独立検算は、実効レート・leftover・全消しを含む同じ規約で比較し、
  差±5%以内。超える場合は全差分をchain単位で説明する。
- 保存則違反、oversettled、帰属漏れ、未分類減少は母数付き0。
- 関連回帰と全pytest成功。既存OFF出力bit-identical。
- Codexが成果物と最小再現を独立レビューして合格判定する。

### Gate 3R-5: 既定OFF統合とGate 4

- 合格後のみオーバーレイへ既定OFFで配線する。
- timeline dumpへepisode、chain、gross生成・相殺・着弾、純残量、
  hard override保留理由を出す。
- PM100全8区間と代表反例をOFF/ON比較し、元のGate 4条件を適用する。

### Gate 4後: 局面別モデル比較

交換会計の学習列が信頼できる状態になってから、同一video GroupKFoldと未使用動画で
次を比較する。

1. A: 現行全局面モデル。
2. B: `match_progress`・真の`tsumo_count`・`episode_stage`入り単一モデル。
3. C: 序盤・中盤・終盤のsoft-gated専門モデル。
4. D: 催促対応→相手本線→自本線を扱うExchangeEpisode専門モデル。

hardな局面切替は禁止し、境界前後±3秒の勝率ジャンプを必須検収する。
