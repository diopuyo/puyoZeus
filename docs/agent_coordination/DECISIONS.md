# 共有決定ログ

## 2026-09-06 — terminal合格は左右別coverage・負例・方向一致で判定する

- 総検出数だけのterminal gateは禁止する。実際に初回監査は27/57を満たしながら、
  1P 27/28、2P 0/29という全面的な方向偏りを隠していた。
- 保存動画監査は1P/2P別coverage、Wilson 95%区間の重なり、勝者方向不一致0、
  終端直前の同数負例でfalse positive 0、bilateral allowlist、event digest束縛を必須とする。
- review検出用2P ROIは実ロゴ上端y=150を含むようy=100開始へ拡張する。Phase J既定adapterだけは
  元`match_end_yatta.png`の背景を除いた固定crop `(60, 210, 460, 160)`を使用し、asset形状不一致はfail-closedとする。
  元asset、legacy detector、M1 rendererの既定テンプレート動作は変更しない。
- `asset_bundle_id`が変わった場合は、同一match/captureでも連続hitとone-shot latchをresetする。
- 57試合＋57負例の最終V3は1P 28/28、2P 29/29、方向不一致0、負例誤検出0。
  これは保存動画adapterの合格であり、本番capture接続・長時間soak・本番採用の完了を意味しない。

## 2026-09-06 — レビュー死亡gateは本番へ流用せず、build-bound live observerを正式根拠とする

- 旧レビューgateと正式live observerは、同じ元映像・時刻でも候補/解除列が一致しないため非同値とする。
- Phase Jの死亡根拠は`event_death_observer_v1`を正式runner内で30Hz実行し、固有build IDへ
  sidecar内容、入力動画、認識設定、コード資産、event runをhash束縛したものだけを受理する。
- 旧レビュー成果物のbuild IDと今回のlive build IDは異なる。これを同一build parityとは呼ばず、
  独立した再現証跡として保持する。レビューgateの近似一致は本番採用根拠にしない。
- 死亡候補・盤面形状・片側ロゴ・統計確率から0%/100%を生成しない。終局確率の根拠は
  allowlist済み`visual_result_logo_bilateral_2x2`だけとし、M1 rendererにもbilateral-onlyを強制する。
- Phase J reducer/display projectorはterminal eventを受理する。左右ロゴ以外のeventは拒否し、
  本番capture adapter接続・全試合coverage・false positive・長時間soak完了まではPhase J本番採用を行わない。
  Formal100、hidden reserve、既存重み、production configは変更しない。

## 2026-09-05 — user決定: M2を本命モデルとして育て、人間知見は劇的効果のある少数だけ採用する

- 次期モデルは診断用に留めず、将来の本番判定を担う構成を目標にする。
- CNNだけへ限定せず、因果ledgerと物理的に限定できる未来盤面を別branchとして扱う。
- 人間知見はhard contract、補助学習、限定的な残差expertへ分離する。固定勝率配点は追加しない。
- 直接入力やpriorとして残すのは、同一state・同一fold・学習外評価で大きく安定した追加効果を示し、
  全体性能、重大誤判定、HOLDを悪化させない少数の候補だけとする。
- 効果の数値閾値は旧版対M1の共通行監査後、候補結果を見る前に事前登録する。
- 正本は`docs/ADVANTAGE_M2_MODEL_POLICY_2026-09-05.md`とする。本番設定と既存重みは未変更。

## 2026-09-04 — Formal100 V5を取得対象として固定

- 元596候補のみを用い、fit 8本、外部holdout 12本をV5へ固定する。最強リーグ候補とV4は使わない。
- fitの`-w9ziMGve40,hJezrrCbkDg,cwj247CtYtw,zB8d1FuCStU,Du64HhfN6iI`は過去148本盤面モデルで
  勝敗・modelへ接触済みだが、holdoutではなくfitなので維持する。102 sourceはprojected-state corpusの
  source数であり、プロジェクト全体への純新規動画はfit 3本であると開示する。
- holdout 12 IDは103,783テキストファイル/36.92GB、主要取得index、勝敗付き148本表、動画別OOF表、
  Dドライブ231ファイルを横断し接触0。Formal100自身と旧候補metadataへの参照は非接触扱いとする。
- `PLNMQZlSoSRTo`は短いIDだが、ユーザー承認済み入力としてyt-dlp取得に成功した値をそのまま保存し、
  推測で別IDへ修正しない。第3回A級holdout 3本はtime-shift subsetとして別報告する。
- 選定行は差し替えず、V5本体、個別metadata、監査、scope、fit裁定、provenance noteのSHAを
  事前登録v1.0へ固定する。

## 2026-09-04 — 最強リーグは一旦正式集合へ採用しない

- ユーザー判断により、最強リーグ1部/最上位をS級またはS級相当として正式100本モデルへ使わない。
- 探索済み候補、V4選定、metadata、監査結果は削除・上書きせず、NO-GOの証跡として保持する。
- 純S級の未接触対戦候補は元596内で0本なので、外部holdoutはA級・チャレンジャー・マスター各4の
  12 sourceとし、純S級外部評価・tier別較正は`unsupported`とする。
- 正式学習追加8 source（A2/S4/チャレンジャー1/マスター1）は維持し、102 source学習を優先する。
  純S級の外部検証は、今後新しい未接触S級動画が公開・承認された時点で別途追加する。

## 2026-09-04 — 選手入替と正式100本外部holdoutの分離規則

- 選手は年度ごとに入れ替わるため、全選手未重複を外部holdoutのhard gateにしない。
  両者未知・片側未知・両者既知、年度、大会、playlist別の成績を母数付きで報告する。
- 同一YouTube ID、別IDの同一対戦・修正版、勝敗ラベル、model fit/eval、勝敗付きレビューへの
  接触は選手交差と別問題であり、外部holdoutでは交差0をhard gateとする。
- 元596候補のS級対戦は未接触0本だったため、V3のS級holdout4本は失格とし動画を取得しない。
  A・B級複合は上級者tierを一意に保証できないため除外し、S級PVも対戦動画でないため除外する。
- 最強リーグ1部/最上位は実力水準上の候補にはできるが、`S級` と偽装してtier別較正へ混ぜない。
  採用する場合はユーザー承認済みの `S級相当(外部最上位)` 独立層とし、genuine S級はunsupported、
  同一正規化pairはholdout内1本までとする。
- 本番設定と既存成果物は変更せず、候補・選定・監査の各版は別名とSHAで保存する。

## 2026-09-03 — AI完了判断: reserve停止9件とc96区間復旧を完了扱いとする

- 停止9件のうち7件は保存済みNPZ/sidecarからのみ再exportし、認識を再実行しない。
  対象集合を`c135,c139,c17,c35,c37,c40,c47`へ固定し、c36/c60/c96を混入させない。
- c36/c60のgross resetは試合番号を変える正式境界ではないが、実効レートの寿命境界である。
  片側以上のresetまたはgame変更でresolverへ`MATCH_BOUNDARY`を一度だけ送り、表示行の
  `formal_boundary`はgame変更時だけ真とする。暫定値の単調性validatorは緩めない。
- c96の共有元動画を使う3区間は、target ID・開始/終了秒・期待フレーム範囲・元動画SHA・順序付き
  target集合を一体の契約とする。3区間が揃いterminal再監査を通るまでSUMMARY/COMPLETEを出さない。
- 復旧出力はすべて別version/別attemptへ保存し、誤った旧成果物も削除・上書きしない。
- `physical_gate_pass=false`だけで失敗扱いにしない。未説明候補を試合単位で隔離し、確定残高への混入0、
  保存則残差0、観測検証PASSを満たす場合は安全隔離契約PASSとする。ただし「全物理候補を説明済み」とは称さない。
- 7件再export、c36/c60、c96三分割の現物SHAと独立監査が全件一致し、全pytest 7,291件PASSのため、
  この復旧作業を完了と判定する。本番設定は変更しない。

## 2026-08-31 — AI設計判断: 説明できない物理着地は試合単位で履歴学習から隔離する

- 高信頼の盤面増加でも、その時点までに確定した相手攻撃量で説明できない候補は、確定残高や
  物理進行の正解として使わない。同じ供給を重複消費していないことも独立に検査する。
- 一つでも不一致がある動画全体を捨てない。該当試合は、現在盤面だけを使う入力Aには残し、
  物理進行を加える入力Bと履歴を加える入力Cから試合全体を隔離する。
- 理由は、問題の瞬間だけを外すと、その後の同試合行が不正な履歴状態を引き継ぐため。
  次の正式な試合境界で履歴状態が初期化されるまでを隔離範囲とする。
- 48本実測では1,201候補中254件、232試合が対象。隔離後の30本仮学習はAUC 0.70517、
  Brier 0.21176で修正前とほぼ同水準だったため、安全性を上げつつ暫定比較能力を維持できた。
- 修正前のレビューv3は上書き・削除せず証跡として残す。修正版v4を、既決定の
  1〜20試合、21〜40試合、41試合〜実際の終了で再生成する。今回の2映像は各51試合なので20+20+11。
- この判断は30本の暫定学習と人向けレビューまで。本番設定、保持49本、100本以上で決める正式重みは変更しない。

## 2026-08-30 — 30本は暫定重み、100本以上で正式決定

- 先行30本では、入力項目の整理、A/B/C比較、暫定の重み学習、過学習の初期検査まで行う。
- レビュー動画は30本の暫定重みで作り、暫定版、学習対象30本、主な判断根拠、表示保留を明示する。
- 30本版の数値を正式版の重みへ自動継承しない。品質確認済み100本以上を元映像・元配信単位で
  分け、本格学習・過学習検査・確率検査をやり直した結果で正式な重みを決定する。
- レビュー動画は`D:\puyo_analyzer\videos\review\`へ保存し、処理後も削除しない。
- 保持49本は方式固定後の最終確認用とし、30本の暫定重み調整や100本学習の途中選択へ混ぜない。

## 2026-08-30 — 全長認識は30本で区切り、重み再学習とレビュー動画へ移る

- user判断により、固定48本をすべて待たず、全長認識原本が30本そろった時点で収集を安全停止する。
- 27本の完全保存を確認した時点で新規動画の開始を止め、既に動作中の3本だけを最後まで保存して
  合計30本にする。処理途中の動画を30本目として数えない。
- 30本到達後は、同一時点の順序不明を修理して保存済み原本から再出力し、既存入力項目の死活・
  重複・局面差・組合せを確認した後、重みを再学習してレビュー動画を作る。
- 残り18本と保持49本は未使用のまま残す。本番設定は変更しない。レビュー動画はDドライブへ
  保存し、削除しない。

## 2026-08-29 — 今後の動画はDドライブへ永続保存

- 今後新規に取得・生成する動画の基準保存先を`D:\puyo_analyzer\videos\`とする。
- 取得元は`source/`、一時取得を含むダウンロードは`downloads/`、完成動画は`rendered/`、
  人による確認用は`review/`へ分ける。
- 約1.76TBの空きを利用し、動画は処理後も削除しない。
- Cドライブに既にある動画・成果物は自動で移動、削除、上書きしない。現在の48本収集は既存の
  Cドライブ上の元動画をそのまま読み取る。
- 盤面表、検証JSON、モデル等の非動画成果物は、別途決定するまでプロジェクト配下へ保存する。

## 2026-08-29 — 48本全長収集を3並列で開始してよい

- 確定送付と物理着地の交差検証relation9、48本専用処理器とも独立レビューで
  P0/P1/P2すべて0件となり、48本開始条件を満たした。
- 最終全pytestは6,583件成功、失敗0。処理対象は固定pilot 48本だけとし、保持49本は使わない。
- 元動画は読取り専用とし、失敗、再試行、再検証は別名保存する。同一出力先の二重起動を禁止する。
- 48本の生成物から本番採用を自動決定しない。指標監査と小規模再学習の結果を人が確認した後に
  本番設定を別判断する。
- 監視はuser指定の20分間隔とし、親処理と子処理の生存、完了状態数、3時間停滞を記録する。正常完了との区別が
  つかない汎用監視からは自動再起動せず、警告と中断再開可能な成果物を使う。
- GPU 8GBの既存実測で6並列は約10倍遅化したため、空きCPUだけを根拠に4本目を追加しない。
  高パフォーマンス電源設定は45秒比較で約+1.2%に留まったため採用せず、バランスへ戻す。

## 2026-08-29 — 暫定火力を確定残高へ入れず、物理着地の帰属を確度別に隔離

- 掛け算式の途中火力は将来予測専用とし、相殺後の確定残高を動かさない。
- 確定残高の単一情報源は、得点確定後に生成される確定攻撃・自己相殺・`garbage_sent`とする。
- 物理盤面差は会計の独立検算であり、観測時点に利用可能な確定送付、事後の単一連鎖確認、
  事後の複数連鎖確認、時刻不一致、説明不能を別分類する。
- 観測時点の判定へ事後確定を戻さない。事後単一連鎖でも物理発生区間が重ならない場合は、
  方向の説明に限り、確定残高の量ラベルへ使わない。
- 複数連鎖由来、時刻不一致、説明不能は行単位で隔離する。48本全体を捨てず、利用目的ごとに
  使える観測だけを残す。
- 観測時点の高信頼候補を物理通番順で先に固定し、その残容量だけを事後確認へ使う。
  低信頼候補は診断専用で確定供給容量を割り当てない。未来候補による過去値の変更を禁止する。
- 同一フレーム内も出来事通番を時間順として使う。候補より後通番の送付や暫定連鎖を、候補時点で
  利用可能だった証拠として扱わない。
- 600秒実測の根拠は通常38番が利用可3/3、難条件c58が利用可1/4・隔離3/4・説明不能0。

## 2026-08-29 — 48本評価の得点表示位置は事前較正JSONで固定

- 34時点を左右別に探索し、基準座標より完全読取り成功が2件以上増え、かつ最良候補で
  3件以上読める場合だけ補正を採用する。信頼度だけの僅差では座標を動かさない。
- 2ピクセル刻み、横±16、縦±12を初期探索範囲とする。1ピクセル単位の最適値を逃す可能性は
  既知限界だが、通常38番を動かさず、c58/c69の手測り値を自動で再現したため初期値として採用する。
- 補正は隠れた実行時状態にせず、映像ID・SHA-256つきJSONへ固定する。収集コマンド、
  実行時資産ハッシュ、生成内容IDへ含め、別映像用設定と収集中の差し替えを拒否する。
- 省略時は従来座標を完全維持し、本番設定へ登録しない。49動画には方式固定後まで使わない。

## 2026-08-29 — 出来事原本v1の盤面保存・同期・Parquet初期値

- 通常309件と難所398件、計707盤面の実測に基づく技術決定。
- 原本と学習用Parquetは盤面本体を直接持ち、監査・関係付け用の
  `board_observation_id`も併記する。永続表を盤面表と参照表へ分割しない。
- 根拠: PyArrow 25.0.1 / Zstandardでは、同じ盤面を1・2・5・10行で使う全条件で
  直接列の方がID参照一式より約4.5KB小さかった。学習時は一括読込であり、
  盤面表常駐時だけの速度差を優先しない。
- Parquet処理は `pyarrow>=25.0.0`、圧縮はZstandardを初期値とする。
- 原本同期は一括更新1件ごと。305〜390回同期を含む変換負荷が認識の約1.1〜1.5%に
  収まったため、未同期損失を増やす間引きは行わない。
- 0〜240秒の途中切り183件と0〜300秒処理の共通前半183件は完全一致した。
- 48候補の実動画は合計46.49時間。3並列処理の初期見積りは約23〜30時間。
- 安定盤面以外の出来事、正式境界・勝者、攻撃会計、訂正、未来情報分離を2映像で通すまで、
  48動画の一括処理は開始しない。

## 2026-08-24 — 現行PM100 A+Bは未採用

- 指摘場面seg01 game2は修正できた。
- 全8区間では急変・反転試合・決着方向が改善した。
- 一方、±100張り付き14.0%→18.3%、生モデル逆符号張り付き3.3%→5.2%へ悪化した。
- 固定0.96秒ヒステリシスは10秒前後続く本線応酬を表現できない。
- 交換エピソード会計と、episode未解決中のhard override禁止が必要。

## 2026-08-24 — 既存検証完了までwrite lock

- 掛け算式検証・バックテスト・PM100 pytestの入力コードを途中変更しない。
- 完了後に証拠を固定してから新施策へ進む。


## 2026-08-24 — Gate 0 の user 判断 4 件 (すべて推奨案で承認)

user へ判断材料を提示し、**4 件すべて推奨案で進める**との承認を得た。

### 判断1: 物差しの穴 2 件を直して 8 動画を再基準化する (案 A)

Gate 0 の作業中に、認識精度の物差し `scripts/measure_stable_cell_acc.py` に
独立した穴が 2 つ見つかった。

- **W36**: `--gravity-settle-state` が `main()` → `_collect_results` で渡されておらず
  **完全に無効**。物差しは GRAVITY_SETTLE を切った状態、つまり**本番と違う
  フレーム母集団**を測っていた。CLI の ON/OFF で正規化ハッシュが完全一致することを
  v40 で実測して確認済み。
- **W37**: **試合終了テロップ中で盤面が画面から消えている区間**を STABLE として
  評価対象にしていた。v51 では不一致セル 1,102 件のうち 991 件 (90%) がこの種の窓由来。

**根拠**: この 2 つは今日 1 日で実際に判断を誤らせかけた。v51 の
「掛け算式修正で −0.1847pt 悪化」を額面どおり受け取っていたら、正しい修正を
捨てていた (実際には生きた盤面で ON 0.999548 > OFF 0.999443)。

**決定**: 両方を是正し、8 動画を再基準化する。新旧を併記する
(`feedback_paired_comparison_fixed_population_2026-08-20`)。
Phase I の合格判定 99.54% の扱いは、再基準化の結果を見てから user へ再提示する。

### 判断2: 整地の定義は実装側 (送りおじゃま ≤4 個なら消費比不問) を採る

実装 (`scripts/measure_exchange_dynamics.py:396-401`) と
伝授の記録 (`reference_saisoku_exchange_model_2026-07-22`「<60% 消費 かつ ≤4 個」) が
食い違っていた。食い違うのは「本線 (60% 以上消費) なのに 4 個以下しか送らない発火」の
扱いだけで、閾値の値と境界の向きは一致している。

**決定**: 実装側を採る。
**根拠**: episode の開始判定には送付量だけを使いたい。消費比率は連鎖終了後にしか
確定しないので、`docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` §9.4.1 の
lazy open (暫定生成量が 4 個を超えた瞬間に開く) と両立しない。
催促 / 本線 / 整地の**ラベル付け**は従来どおり事後分類のままでよい。

### 判断3: Q-04 (おじゃまダメージ) の本番配線は 148 再学習時にまとめる (案 B)

**決定**: 指標側の実装とテストのみ入れ、`virtual_landing` は既定 OFF のまま。
オーバーレイと学習列への配線は 148 動画の再生成・再学習のタイミングで A/B を取ってから。

**承知の上のリスク**: それまで本番経路では
「空盤面と窒息寸前の盤面が同じ無害判定 (0.05)」というバグが残る。
現行 52 列モデルにこの指標は入っていないのでモデルへの影響はゼロ。

**user 追加見解 (2026-08-24)**: 着弾後 `is_dead()` の CEIL が一段のステップである点について
「おじゃまは 6 列均等に降るので窒息列の余裕が足りなければ必ず 12 段目に乗り、
実態より大きく出るかもしれない」との指摘があったが、
**「上部ほどダメージが大きくなるのは間違いない事実」**との判断で現行のステップ形のまま進める。
経緯は W33 に記録。

### 判断4: 掛け算式修正の採用は、幕間の別動画検証と物差し是正後の再測定を経てから (案 A)

**決定**: 今日の数字だけで `src/production_config.py` に登録しない。次の順で進める。

1. 幕間の実測を別動画 2〜3 連鎖で再確認 (W34 の残る限界 ②)
2. W36 / W37 を是正した物差しで 8 動画を再測定
3. その結果を user へ提示して採否を仰ぐ

**根拠**: 物差し自体に穴が 2 つ見つかった直後であり、
その物差しで出した数字を採用根拠にするのは筋が悪い。

## 2026-08-24 — Phase I 合格判定 99.54% の再計測は Phase J 直前まで先送り

**user 決定**: 再計測は必要だが、**30先動画の作業を終えたあと、Phase J の直前**に行う。

### 背景

W36 (`--gravity-settle-state` の配線漏れ) により、物差しは本番と違うフレーム母集団を
測っていた。8 動画の実測で **acc 0.997728 → 0.996834 (−0.090pt)**、
誤り 4,633 → 6,407 (+38%)。

Phase I の合格判定 99.54% も同じ配線漏れの下で出た数字であり、
本番構成なら **99.45% 前後**になって合格ラインを割る可能性がある (未測定)。

### なぜ今やらないか

判定精度を上げる作業が先で、認識の合格判定を測り直すのはその後でよい。
Phase J (配信オーバーレイ) に入る直前が、本番構成での数字を確定させる自然な区切り。

### 申し送り

- **Phase J に着手する前に必ず実施すること。** 未実施のまま Phase J に入らない。
- 再計測は W36 / W37 を是正した物差し (本番構成 + 試合外区間の除外) で行う。
- 当時とは認識構成が違うので、旧 99.54% との単純な引き算はできない。
- 台帳: `docs/KNOWN_WEAKNESSES.md` W36 / W37。

## 2026-08-24 — saturation_chain_upper の欠損表現は優先度を下げる

**user 決定**: この指標は**盤面のおじゃまぷよの量と近似**なので、優先度を下げる。

### 経緯

Codex 指標監査で「99.59% 欠損の coverage 障害」と指摘され、読取監査を実施した
(`docs/SATURATION_UPPER_COVERAGE_AUDIT_2026-08-24.md`)。

判明したこと:

- 99.58% 欠損は設計どおり。充填率 90% 未満は計算しない (コスト予算からの逆算)。
- ただし消費側 6 箇所すべてが `fillna(0.0)` しており、
  producer が守った「未測定 ≠ 飽和ゼロ」の区別が失われている。
- 既存 CSV には NaN が保存されているので、**是正に動画処理は不要** (0 本)。
  学習の 0 埋めをやめるだけで済む。

一度は案 A (NaN を埋めずに渡す) で進める方針を確認したが、**user 判断で優先度を下げた。**

### 理由 (user)

**この指標は盤面のおじゃまぷよの量と近似である。**

これは 2026-08-13 に user が「おじゃまぷよの数がイコール飽和現象に繋がる」と
指摘して充填率 90% 以上に絞り込んだ、その判断と一貫している。
既に `board_ojama_count` が代理指標として働いているので、
残り 0.42% を正しく扱っても得られるものは薄い。

### 位置づけ (user 補足)

**厳密には必要だが、Phase J の後の「強化」段階で必要になるレベル。**
いま止めてまでやる話ではなく、捨てる話でもない。

### 申し送り

- **削除はしない。** 死に指標ではなく、測れている 0.42% では
  単独 AUC 0.6360 (モデル全体 0.6357 と同等) の信号が出ている。
- **実施時期の目安: Phase J 以降の強化フェーズ。**
- Codex 18:18 のモデル比較 A/B/C/D をそれ以前に回す機会があれば、
  **1 条件足すだけで測れる**ので、そのときに相乗りさせてもよい (前倒し可)。
- 是正に**動画処理は不要 (0 本)**。既存 CSV に NaN が残っているので、
  学習の 0 埋めをやめるだけ。着手すれば短時間で終わる。
- 閾値 0.90 を下げる案 (C) は**採らない**。native 化の bit-identical パリティが
  未解決 (fill≥0.70 で 2/14 件の食い違い) で、下げるほどコストが倍増するのに
  欠損は 99% 台のまま。

---

## 2026-08-25 — user決定: Gate 3R-6「表示欠陥の始末」をマップに追加。レビュー動画は**先頭5試合**

### 経緯

Claude が「このマップが終わればレビュー用動画を作れる状態になるか」への回答として、
**「レンダはできるが、そのままお見せするには足りない」**と判断した。

Gate 3R (P1/P2/W38/gross供給) が直しているのは**会計**であって、
**表示の欠陥**ではない。会計を直せば ±100 張り付き (OFF基準14.0%) と
決着方向誤り (7試合) は根治の本命に届くが、通しレビューで確実に目に付く
次の2件は**現行マップに入っていない**。

1. **STABLE 凍結デッドロック** (2026-08-24 根因確定)
   凍結盤面が連鎖検知の入力にもなっており、検証ゲートが 4.17 秒握りつぶす。
   **片側連鎖では永久に抜けられない** (救済は相手の同時連鎖による偶然)。
   memory `project_stable_freeze_deadlock_2026-08-24`
2. **W32 `is_dead()` 誤判定の残り約7割** (2026-08-24)
   窒息判定の凍結盤面修正で直ったのは31%。残りの入口は
   「画面で連鎖しているのに状態が STABLE」という別バグ。
   **既存の修正は未来を見る後処理でリアルタイム不可 = 動画の判定は未修正**。
   memory `project_is_dead_stable_misdetect_2026-08-24`

レビュー動画は「試合の開始〜終了まで通しで」がユーザー恒久指示のため、
この2件は**必ず画面に出る**。Claude の自己検収を通せない。

### 決定 (user)

- **Gate 3R-6「表示欠陥の始末」をマップに追加する。** レンダ前に上記2件を片付ける。
- 扱いは 2026-08-13 の恒久指示に従う: **「根治」か「user承認つきガード」かを先に決める。
  黙ってガードを積み増さない** (`feedback_kill_known_weaknesses_2026-08-13`)。
- **レビュー動画の範囲は「先頭5試合」でよい** (動画全編の通しレンダは不要)。
  1試合あたりの「開始〜終了まで通し」は従来どおり維持する。

### 影響

- Gate 3R-5 (既定OFF配線) の後、Gate 4 (PM100 OFF/ON 比較) の前後に 3R-6 を挟む。
- レンダ範囲が先頭5試合に限定されたことで、レンダ実時間12倍問題 (W30) の
  負担が大きく下がる。区間並列レンダ手順は
  memory `reference_segment_parallel_render_2026-08-22` を使う。

### 【訂正 2026-08-25】Gate 3R-6 の前提2件のうち1件は**既に根治済み**だった

Gate 3R-6 追加時に Claude が挙げた根拠2件を本番構成で実測し直した結果、**深刻度が過大だった**。

| 対象 | 追加時の記述 | **実測 (本番構成)** |
|---|---|---|
| STABLE 凍結デッドロック | 「4.17秒握りつぶす。片側連鎖では永久に抜けられない。**未修正**」 | **再現しない。** 掛け算式検知→CHAIN突入 4.167秒 → **0.034秒**。先頭5試合の掛け算式セッション **29件全件が CHAIN 到達 (遅延 最大0.1秒、≥1秒 0件、未到達 0件)** |
| W32 `is_dead()` 誤判定 | 「残り約7割が未修正。動画の判定は未修正」 | **大幅縮小して残存**。同一区間の before/after で **6.1秒 (1件) → 0.7秒 (1件)**。母数 = 実試合5、試合時間計 329.6秒 |

実測条件は本番と一致 (現行ワークツリー + `use_production_recognition` 既定ON +
`advantage_overlay_flags()` の実行時読込 + model62)。実効フラグは実行時に pipe 属性で確認済み
(`effective_read_verify / count_update / step_interlude = true`、配線事故なし)。

**原因**: Gate 3R-6 追加時に Claude が参照した memory が **2026-08-24 の3フラグ採用より前**の記述で、
`src/production_config.py` の採用記録 (E2E 4.133秒解消) と突き合わせていなかった。
**対象より物差しを先に疑う規律を、記憶にも適用すべきだった。**

### 実測でわかった追加事実

- `is_dead` は `scripts/visualize_advantage_overlay.py` では **dump 行構築 (:3968) にしか使われておらず、
  描画にも有利不利スコアにも入っていない**。残存 0.7 秒の区間で adv_raw は +7.0→+6.3 と滑らか、
  `drivers_top1` も不変
- 死亡見逃しゼロの傍証: 真の敗北 (2P 実試合2、t=223 おじゃま満杯) は正しく `is_dead=True`
- **新規の定量 (台帳未登録)**: 試合前のまちうけ画面区間 (t=18–86.9) で `is_dead2=True` が
  **574/1019行 (56%)**。実画面はロビーで盤面なし (W37 の表示版)。
  ただしレビュー動画の「試合開始〜終了」範囲外
- **残る構造リスク (実測ゼロだが未消滅)**: 掛け算式が読めない連鎖 (バースト/煙、W34 と同根) は
  旧 simulate 検証へフォールバックするため、凍結デッドロックが理論上再発しうる。
  先頭5試合では出現 **0/6 回**。凍結盤面をトラッカー入力にする循環自体
  (`src/recognition_pipeline.py:4653-4660`) は現存する

### Gate 3R-6 の残作業 (縮小)

**先頭5試合のレンダに限れば、通しレビューを止める規模の表示欠陥は実測で残っていない。**
残るのは残存 0.7 秒への対処方針で、2026-08-13 の恒久指示に従い user 判断を仰ぐ (**user 回答待ち**)。

| 案 | 内容 | 犠牲 | 工数 |
|---|---|---|---|
| **A (推奨)** | own state が非STABLE の間は `is_dead` を保留扱い | 連鎖中の真の詰みの早期警告が STABLE 復帰まで遅延 (ゲーム実仕様上は正しい遅延) | 0.5日 |
| B | own CHAIN 中は連鎖前進推定の結果盤面で評価 | simulate の系統的過小 (真値8→1 の前歴) による**死亡見逃し**リスク | 1〜2日 |
| C | 現状維持 | 走査器 D1a の誤警告源が残る | ほぼ0 |

案A は user 伝授の律「連鎖中 / 連鎖直前は窒息としない」と一致し、**未来参照なしでリアルタイム可能**。

証拠: `data/verify/gate3r6_diag_2026-08-25/` (実画面フレーム込み。
`t00167.00s.jpg` = 1P が「2れんさ!」実行中に OFF が窒息判定を出し続けている現場を Claude 自身が目視確認)

### 【user決定 2026-08-25】`is_dead()` 残存への対処は **案A** — 非STABLE 中は窒息判定を保留

3案 (A: 非STABLE中は保留 / B: 連鎖前進推定の結果盤面で評価 / C: 現状維持) を提示し、**user が案A を選択**。

採用理由:

1. user 伝授の絶対律「**盤面が高い ＝ 窒息ではない。設置前 / セカンドに向かって積み上げ中 /
   連鎖を打つ直前 / 連鎖中はいずれも窒息としない**」と完全に一致する。ルールから導かれた直し方であり、
   シーンからの逆算ではない
2. **未来参照なしでリアルタイム可能。** 配信オーバーレイが最終目標なので必須条件
   (案B は連鎖前進の推定が必要でリアルタイム性に難がある)
3. 案B の推定は **真値8連鎖→1連鎖のような一方向の系統的過小**という前歴があり、
   **死亡見逃し方向に外れる**。表示の誤り方として最も危険

**犠牲 (user に明示済み)**: 連鎖中の真の詰みの早期警告が、盤面が落ち着くまで遅延する。
ただしゲーム実仕様上、連鎖中はまだ勝負がついていないため**この遅延はむしろ正しい**。

### 実装の条件 (Claude が自らに課すもの)

- **既定 OFF の optional flag** として実装 (規約: 新機能は既定OFF)
- **`src/production_config.py` への採用登録は測定値を確認してから。** user は方針を承認したが、
  数値を見ずに本番へ入れない
- 保留した回数は**母数付きカウンタ**で可視化する (`3/29` の形)
- **合格条件の最重要項目 = 真の窒息を見逃さないこと。**
  2P 実試合2 (t=223、おじゃま満杯による真の敗北) が正しく検出され続けること。見逃したら不合格
- `src/indicators_v2.py` 内に `is_dead` の使用が **9箇所**あるため、
  **有利不利スコア (`adv_raw`) が動くか**を同一動画ペア (先頭5試合、t=86.9〜416.5) で測る
- 出力先 `data/verify/gate3r6_planA_2026-08-25/` (新規)。既存 `gate3r6_diag_2026-08-25/` は上書き禁止

### 【user決定 2026-08-25】案A の本番登録は**決定化まで保留** / 次の一手は**Codex の返答待ち**

**1. 案A (`enable_nonstable_hold_is_dead`) の `src/production_config.py` への採用登録は保留する。**

理由 (Claude の推奨を user が採用):
いま登録しても**副作用の有無を証明できない**。案A の adv への影響 (OFF/ON 差 149行・最大12.017) は
実行間揺らぎ (142行・最大12.017) と**同分布・同時刻帯**で、
「adv に副作用なし」の主張は1回比較では原理的に成立しない。
**乱数の決定化が入れば3run一致で証明できる**ので、証明できる状態にしてから登録する。
急ぐ理由がない (実効差は dump の is_dead 列 30/5186 行のみ、既定 OFF で本番動作は不変)。

**2. Gate 3R-6 の本丸「連鎖中なのに STABLE」(state machine 遅れ) への着手は、Codex の返答を待つ。**

理由: Codex には K5 (余剰ID の解釈)、K7 (逆方向の欠落6件)、K8 (Gate 4 合格条件の書き換え)、
および **Gate 3R-6 の対象を state 遅れに差し替えるか**を判断依頼済み。
これらの回答で優先順位が変わりうるため、先走らない。

### 未決 (今後 user 判断が必要になるもの)

- 乱数の決定化 (`dig_resistance` の seed 固定) の本番採用 — **3run 完全一致と分布不変を示してから**
- ここまでの作業のコミット — 現在**すべて未コミット**。
  他エージェントの差分が多数あるため、Codex レビュー通過後に Claude の変更分だけを選んで切り出す方針

### 【user決定 2026-08-25】Gate 3R-5 は **Codex レビューを飛ばす**。Claude 自己検収で通れば Gate 3R-6 へ進む

#### 判断の材料 (Claude が提示、user が決定)

Codex レビューを飛ばして短縮できるのは **4〜8時間** (往復2回分) だが、**律速は Gate 3R-6 の
state machine 修正**であり、飛ばしても全体は1日縮まるかどうか。
一方 Codex は本日だけで Claude の誤りを3件検出している
(P1-3 未着手を「完了」と要約 / K6 の時間単独判定 / K7 の測定器事故)。

そのうえで **Gate 3R-5 に限れば飛ばす価値がある**と判断した:

- 既定 OFF の**記録用配線**で、表示も判定も変えない
- OFF の完全一致を **W39 決定化後の3run一致で機械的に証明できる** (レビューで見つかる種類の
  誤りが入りにくい)
- 一方 **Gate 3R-6 は認識の中核を触る**ので独立レビューの価値が高く、**こちらは通す**

#### Claude 自己検収の合格条件 (「Claude 側で良ければ」を曖昧にしないため事前固定)

| # | 条件 | 判定方法 |
|---|---|---|
| 1 | OFF 構成が3run で完全一致 | `0/N 行不一致` (母数併記)。**エージェント報告を鵜呑みにせず Claude が npz を直接突合** |
| 2 | 全 pytest 0 failed | 基準 5,976。速度テストが並走で落ちた場合は単独再実行まで確認 |
| 3 | 追加7列がすべて出ている | 実 dump を Claude が開いて確認。**検査母数の列が無ければ不合格** |
| 4 | `src/production_config.py` 未変更・本番ON なし | `git diff` で確認 |
| 5 | ON でも表示・判定が変わらない | dump 列以外が一致すること |

**1つでも欠けたら Gate 3R-6 へ進まない。**

#### Codex への扱い

Gate 3R-5 の成果は `CLAUDE_TO_CODEX.md` へ通常どおり記録し、**事後レビューは妨げない**。
ただし Gate 3R-6 着手の前提条件からは外す。
Gate 3R-6 の完了時は**従来どおり Codex レビューを待つ**。

### 【訂正 2026-08-25・2回目】Gate 3R-6 の本丸「state machine 遅れ」も**存在しなかった**

Codex が Gate 3R-6 の対象として差し替えた「画面では連鎖中だが own state が STABLE のまま遅れる」は、
本番構成の実測で**現象そのものが確認できなかった**。同一動画・同一窓で**2回独立に実行**し、
既知の残存インシデント (1P・実試合2・t=164.033〜164.733、0.7秒) が
`suspicious=1/1`、`suspicious_total_sec=0.7/0.7` で一致することも確認済み (n=1 ではない)。

#### 先行信号3種の遅延分布 (母数併記)

| 信号 | 母数 | 中央値 | 最大 | ≥0.5秒 | 未到達 |
|---|---|---|---|---|---|
| 掛け算式の実読 | **29件** (1P:13 / 2P:16) | 0.033秒 | 0.1秒 | **0/29** | **0/29** |
| score 増加 (閾値 = 4個消し最小素点 40) | **32件** (1P:15 / 2P:17) | 0.0秒 | 0.134秒 | **0/32** | **0/32** |
| 盤面ぷよ数減少 (生CNN、≥4セル減) | 72件 | 0.0秒 (到達分) | 1.733秒 | 4/72 | **31/72 (43%)** |

盤面セル数の 43% 未到達は**生CNN のノイズ**。静止した STABLE 盤面でも ±2〜8セル変動しており
(t=157.5〜164.6 で実連鎖ゼロなのに非空セル数が 48〜75 を上下)、
**先行信号として単体では使えない** (デバウンスなしで 43% が偽陽性)。

#### 残存 0.7秒 の正体

フレーム単位トレースで確定:
- t=161.5〜164.6: score1 は 235前後で停滞、掛け算式実読 False、盤面セル数はノイズ範囲
  → **3信号のいずれも「連鎖が起きている」証拠を示していない**
- t=164.633: 掛け算式実読が valid
- t=164.667: state が CHAIN へ (**遅延 0.034秒**、mechanism=`formula_read`)

**つまり state machine の検知遅延ではなく、盤面が満杯になってから本人が連鎖を始めるまでの
ゲーム自体の時間。** この区間には画面上に「連鎖が来る」と分かる材料が存在せず、
**未来参照なしでの短縮は原理的に不可能。**

実画面 `data/verify/gate3r6_diag_2026-08-25/evidence_frames/t00164.10s.jpg` を Claude が目視確認:
1P は天井まで積み上がっているが**連鎖しておらず**、連鎖しているのは 2P。

#### simulate フォールバック経路 (Codex が別母数で測れと指示)

`_apply_chain_formula_early_fire` の呼び出し **33件中、read_valid=True が 33件、
simulate フォールバック 0件**。構造としては `src/recognition_pipeline.py:6253-6254` に現存するが、
先頭5試合では**一度も発火していない**。緊急性なし。

### 【測定器事故 19件目】`stable_lag` 指標は「連鎖検知の遅延」を測っていない

- score 信号の閾値を「増加 > 0」にすると、**連鎖終了後のカウントアップ演出の残響**
  (+1〜+5/フレームが数秒継続) が `SESSION_GAP_SEC=1.6秒` の連結規則で無関係な連鎖を融合し、
  **43.9〜83.7秒の巨大セッション**を作る。閾値を**物理量 (4個消し最小素点 40)** に是正すると
  全32件が遅延ほぼ0に収束した
- **既存 `scripts/_diag_gate3r6_isdead_runs_2026-08-25.py` の `stable_lag` も同じ欠陥**。
  今回の再実行でも `stable_lag_events=7 / median=3.9s / max=4.967s` を再現したが、
  これは timeline dump の粗い行間で差分を取り、**正しく検知済みの複数連鎖を1個の巨大な遅延に
  見せかけている**
- この数値は本文で採用されたことはないが、**放置すると次の判断者が額面どおり受け取る**。
  誤った側の証跡も `state_lag_summary_v1_naive_score_threshold1.json` に残してある

### 立て直した問い (検証中)

**その 0.7秒間、`is_dead` 判定はゲームのルールに照らして本当に誤りなのか。**
1P はこの後も打てているので**実際には死んでいない**。仮説は2つ:

- **仮説A (認識誤り)**: 判定セル (row=1, col=2) に実際はぷよが無いのに誤読している
  → **直すべきは判定ロジックではなく認識**
- **仮説B (判定条件の食い違い)**: セルは実際に埋まっているが、ゲームは別条件で死亡を決めている
  → **判定条件そのものの再定義が必要**

既存 dump から安価に決着させる。証跡は `data/verify/gate3r6_deathcell_2026-08-25/`。

### 【user決定 2026-08-25】Gate 3R-6 で行き詰まったら「**後から振り返って直す方式**」で動画を先に作り、根治は後回し

#### 決定内容

Gate 3R-6 (残存 0.7秒 の `is_dead` 誤判定) の根治で作業が止まる場合、
**遡及訂正方式でレビュー動画を先に作り、リアルタイム根治はその後に回す。**

#### なぜ成立するか

- 遡及訂正は**既に実装がある**: `scripts/visualize_advantage_overlay.py` の
  `_retroactively_correct_dead_dump_rows` (`--stable-confirmed-is-dead`)。
  後続フレームの確定値で過去の判定を遡って訂正する
- **リアルタイム不可という制約は、動画には効かない。** 動画は全編を先に処理できるため、
  未来参照が許される。配信オーバーレイ (Phase J) だけがリアルタイム制約を受ける
- したがって「Gate 3R-6 の根治が難しい」ことは、**レビュー動画を出すことの障害にはならない**。
  この2つは切り分けられる

#### 適用条件と扱い

- **根治を諦めるのではなく後回しにする。** Gate 3R-6 は台帳に残し、
  配信オーバーレイ着手前に必ず閉じる (リアルタイム制約が復活するため)
- 遡及訂正を使ったことを**レビュー動画の添え書きに明記する**
  (何を後処理で直したかを user が把握できる状態にする)
- レンダ範囲は 2026-08-25 の user 決定を引き継ぎ「**先頭5試合**」を想定。
  30先動画の全編 (117分) を指す場合はレンダ規模が変わるため要確認

#### 現時点の状況

Gate 3R-6 の前提は**2度とも実測で崩れている** (STABLE 凍結デッドロックは根治済み、
state machine 遅れは存在しなかった)。残存 0.7秒 が
「認識誤り」か「判定条件の食い違い」かを検証中で、**認識誤りなら話は Gate 3R-6 ではなく
認識側の課題に変わる**。その結果次第で、この遡及訂正方式を使うかどうかが決まる。

### 【user伝授 2026-08-25】死亡確定の条件 — 「12段目に設置して連鎖が起きない」or「おじゃまが降り12段目が埋まる」

#### 検証で確定した事実 (仮説B)

Gate 3R-6 の残存 0.7秒 は**認識誤りではなかった**。

| 仮説 | 判定 | 根拠 |
|---|---|---|
| A: 認識誤り | **棄却** | 確定盤面は **24/25 フレーム**で正しく占有。実画面ズーム (`gate3r6_deathcell_2026-08-25/evidence_frames/toprow_zoom.jpg`) を Claude が目視し、**判定セルに赤ぷよが実在**することを確認 |
| B: 判定条件の食い違い | **確定** | 占有は事実だが 1P は死んでいない |

生CNN は 7/25 フレームで赤→黄/空/おじゃま に揺れていたが、**原因は 2P の連鎖が発火させる
全画面バーストの半透明レイヤー** (既知)。state machine の STABLE 補正がこれを正しく吸収しており、
**測定器 (生CNN) は揺れていたが判定に使う確定盤面は健全だった。**

フレーム単位の因果: 2P の連鎖 (40×8=320点) 解決 → 送出おじゃま着弾 (t=164.6、state1=OJAMA_FALL)
→ **1P 自身の連鎖が発火 (t=164.67)**。
**猶予区間の長さは相手の連鎖解決時間で決まる = 相手依存。固定値では表せない。**

**案A はこのケースを救えない。** 0.7秒のうち **86% (0.6秒) が own state=STABLE** のため、
「非STABLE の間は保留」というガードを素通りする。

#### user 伝授 (2026-08-25)

死亡が確定するのは次のいずれか。

1. **12段目 (可視最上段、row=1) に設置して連鎖が起きない**
2. **おじゃまが降って 12段目が埋まる**

**ただし user 自身が「その判定が結構難しい」と明言。**
そのため **フィールドの底が抜けて落下する演出 (死亡アニメーション) を見る**など、
**何らかの別の判断材料が必要**。

memory: `reference_death_confirmation_condition_2026-08-25`

#### 次の手

`feedback_check_existing_before_building_2026-08-21` に従い、**実装を決める前に librarian で既存資産を調査**する。
死亡アニメーション (フィールドの底が抜ける演出) の検出器が既にあるか、
`is_dead` の時間的条件を持つ実装が既にあるかを先に確かめる。

### 【Claude の設計ミス + user 指示 2026-08-25】死亡確定は「12段目設置 + ネクスト1.5秒不動」の簡易検出で一旦進める

#### Claude の設計ミス (user 指摘で実装前に発覚)

Claude は確定条件を「連鎖が始まらないまま**次の事象 (ネクストが動いた = 次が置けた)** が
観測されたら死亡確定」と設計してエージェントに渡した。**これは論理が逆で、致命的だった。**

> user 指摘: **ネクスト動かないから設置検出にネクスト使えないけど大丈夫？**

- **死亡すると次のツモは動かない** (置けないので当然)
- したがって「ネクストが動いた」は**生きている証拠**であって死亡の証拠ではない
- 死亡時にはこの条件が**永久に発火しない** → **本当に死んでいる人を「生きている」と判定し続ける**
- これは合格条件に据えた「**真の窒息を見逃さない**」に真っ向から反する最悪の失敗モード

**構造的な問題**: 死亡すると**あらゆる観測信号が止まる**
(設置イベントなし / 連鎖なし / スコア不動 / 盤面不動 / ネクスト不動)。
**死亡は「信号が来ないこと」でしか判定できない。**

**user が最初に提示した「フィールドの底が抜けて落下する演出」が、実は唯一の積極的な証拠だった。**
Claude は librarian 調査の結果を見て「既存信号の組み合わせで済む」と判断し、
**user が提示した手段を退けていた**。ドメインは推測せず user に確認する決まり
(`feedback_consult_user_puyo_domain`) に反しており、**判断そのものが誤りだった。**

#### user 指示 (2026-08-25)

> **12段目が設置され、ネクストが1.5秒以上動かない等で簡易検出で一旦お願いします**

確定した3段階:

1. **死亡候補**: 12段目 (`DEATH_ROW=1, DEATH_COL=2`) の占有を確定盤面で観測。
   **発生源 (設置 / おじゃま着弾) を区別して記録**
2. **解除**: 自分の連鎖開始 (掛け算式の実読、遅延 中央値0.033秒) が観測されたら候補を解除
3. **確定 (簡易)**: 候補のまま **ネクストが 1.5秒以上動かなければ死亡確定**

#### 1.5秒の扱い

**この閾値は user 指定であり、Claude がシーンから逆算した値ではない。**
コードに「2026-08-25 user 指定の暫定値」と明記し、定数化し、CLI から変更可能にする。
**底抜け演出による根治に差し替える前提の暫定値**。

#### 残る課題 (後回しにするが落とさない)

- **底抜け演出 (死亡アニメーション) の検出器は未着手。** これが本来の積極的証拠
- `src/match_end_detector.py` のテンプレート照合は **42本中31本で一度も成立しない**実測欠陥があり、
  主判定に使ってはならない
- 確定が 1.5秒遅れる = **真の死亡の検出も 1.5秒遅くなる**。仕様として許容するが、
  配信オーバーレイ (Phase J) 着手前に演出検出で詰める

#### 実装の制約 (変更なし)

- **`src/board.py` を変更しない。** `is_dead()` は `src/indicators_v2.py` の19箇所ほか、
  `src/puyo_core_bridge.py` (Rustブリッジ、ビームサーチ) からも**仮想盤面の静的判定**として
  呼ばれており、そこでは現在の「占有＝死」が正しい。時間的確定は**観測層に新設**する
- 既定 OFF の optional flag + CLI。`src/production_config.py` へ登録しない
- 既存の `enable_nonstable_hold_is_dead` (案A) と併存。削除しない
- 合格条件の最重要項目は **2P 実試合2 t=223 の真の敗北を見逃さないこと**

### 【user決定 2026-08-25】Codex 指摘4 (state machine 遅延の実装要求) は**実測を根拠に差し戻す**

Codex の Gate 3R-6 独立レビューは NG。指摘1/2/3/5/6 は**すべて受諾**したが、
**指摘4 (PLAN の本丸である「画面では CHAIN 中だが own state が STABLE の遅延」を実装・検収せよ)
だけは差し戻す**と user が決定。

#### 根拠 (本番構成での実測、2回独立実行で一致)

| 先行信号 | 母数 | 中央値 | 最大 | **≥0.5秒** |
|---|---|---|---|---|
| 掛け算式の実読 | 29件 | 0.033秒 | 0.1秒 | **0/29** |
| score 増加 (閾値=4個消し最小素点40) | 32件 | 0.0秒 | 0.134秒 | **0/32** |

**現象そのものが観測されていない。** 残存 0.7秒 は
「盤面が満杯になってから本人が連鎖を始めるまでのゲーム自体の時間」であり、
その区間には画面上に「連鎖が来る」と分かる材料が存在しない (実画面で Claude が目視確認済み)。

**遅延ゼロの実測がある以上、昇格ロジックを実装しても効果を測る対象が存在しない。**

#### 手続き

Codex 自身の指示「Gate 定義を変更したと考える場合は user 決定なしに PLAN を書き換えず、
Gate 未完了として判断を仰ぐこと」に従い、**`PLAN.md` は書き換えていない**。
第25報で証跡つきで差し戻し、**Codex が独立に検証してから**改定の可否を決める順序を提案した。

#### Claude が受諾した指摘 (自分の誤りとして記録)

- **指摘1**: 確定遅延 0.067秒 を「正常動作」と第24報に書いたが、
  **それは「1.5秒の猶予が実質効いていない」ことの証拠**だった。合格と判定すべきではなかった
- **指摘2**: `next_pair=None` を「不動」として扱っていた。
  **「検出できていない」を「動いていない」と読んでいた**もので、
  本セッションで繰り返した「`0` が合格なのか未測定なのか区別しない」と同型の誤り

### 【Codex条件付き受理 2026-08-25】Gate 3R-6 の本丸を「死亡確定の正しさ」へ改定。訂正3点を受諾

user 決定 (案A: 実測を根拠に差し戻す) に対し、Codex が独立再計算で同値を再現し
**新規CHAIN昇格ロジックの実装要求を撤回**。`PLAN.md` の Gate 3R-6 節を改定した。

Codex が独立再現した値: 掛け算式29件 (中央値0.033秒、最大0.1秒、≥0.5秒 **0/29**)、
score増加32件 (中央値0秒、最大0.134秒、≥0.5秒 **0/32**)、simulate fallback **0/33**、
残存0.7秒は式読取164.633 → CHAIN 164.667、t=164.10 の実画面で連鎖中なのは **2Pのみ**。

#### 受諾した訂正3点 (**すべて Claude の誤り**)

1. **cell 信号の母数に試合前13件が混入していた。**
   正しくは先頭5試合で **59件**、未到達 **28/59**、最大 **1.434秒**、
   ≥0.5秒 **2/59**、≥1秒 **2/59** (誤: 72件 / 31件 / 1.733秒 / 4件 / 3件)。
   **これは本セッションで繰り返し問題にした「母数に試合外を混ぜる」誤りそのもの**で、
   `is_dead2` のまちうけ偽陽性と同型。自分の測定でも同じ穴を踏んでいた。結論は不変
2. **「state遅延は存在しない」ではなく
   「zenchi 先頭5試合の formula/score 観測可能セッションでは未観測」と限定する。**
   全動画・全条件での不在を主張しない
3. **2run一致の主張を取り下げる。** 監査できる生 trace が1組しかない。
   再主張するなら run 別の `trace.jsonl` / `events.jsonl` を分離保存してから行う

#### Gate 判定

**Gate 3R-6 NG / Gate 4 HOLD を維持。** 指摘1/2/3/5 の正式検収を第26報で報告するまで変わらない。

### 【user決定 2026-08-25】窒息判定の誤りを**指標計算にも通す** (案①)

#### 実測で確定した効果 (反実仮想。`Board.is_dead()` を常に False に monkeypatch した差分)

| 指標 | 差 |
|---|---|
| `adv_raw` (内部の生値) | **+9.16点** (本番構成) / **+9.22点** (model62)。**モデルに依らず同規模** |
| **`adv_ema` (画面に出る値)** | **最大 0.92点** |
| 応手 (counter) 成分 | **0.0000** (全39呼び出しで完全一致) |
| 主因表示 `drivers_top1` | **0/22 変化** |
| 決定性確認 (同一条件2回) | `adv_raw` 差 **0.0000** (全22行) = ノイズではない |

**効いている経路は1つだけ**と特定:
`src/indicators_v2.py:684-685 dig_resistance()` が `is_dead()==True` で `score=0.0` に短絡
→ `:1831 ukeyasusa()` が `UKEYASUSA_W_DIG=0.6` 倍で吸収
→ `scripts/visualize_advantage_overlay.py:2368-2421` がモデル47列へ投入。

**この誤判定が画面に届くのは `--per-side-settled` が本番ONだから。**
OFF なら相手の連鎖中は再計算行が0件で、スコアに一切到達しない。

#### Claude の報告の訂正 (2件)

1. **「最大13.23点」は誤り。** Claude が `adv_raw` の粗い差分を見た値で、
   **同時に起きた本物の盤面変化 (`diff_conn_max_group_size`) の寄与を混ぜていた**。
   誤判定単独の寄与は **+9.16点**、**画面に出る平滑後は最大0.92点**
2. **counter 経路「最大±8.0点」も誤り。** 実測 **Δ=0.0000**。
   盤面がほぼ埋まっているため、`is_dead` を False に戻しても MC ロールアウトが
   同じく0近傍を返す。short-circuit が実計算を先取りしていただけだった

#### 過去の「+7.0 → +6.3 と滑らか」が誤解を招いた理由 (測定器批判)

- **反実仮想を取っていない factual 単独の値**で、同じ再計算 tick で同時に起きた
  **本物の盤面変化と誤判定の寄与を混同**していた
- **`--no-counter-reach` を明示指定**しており、本番採用の counter-reach 経路を最初から測っていない
- さらに `advantage_overlay_flags()` (単一情報源) に**登録されていない3フラグを手動追加**していた。
  `feedback_use_single_source_for_flags_2026-08-22` と**同型の配線逸脱**。是正は別途

#### 決定と実装方針

**案① を採用。** 猶予中 (死亡候補は立っているが確定していない間) は
「生きている」として指標を計算する。

- **`Board.is_dead()` 本体 (`src/board.py`) は変更しない。** 仮想盤面の静的判定としては現行が正しい
- **optional 引数を追加**し、渡さなければ挙動が一切変わらない (backwards compat)
- 対象は本番経路から到達する3関数: `dig_resistance` (→`ukeyasusa` に伝播) /
  `saturation_chain` / `ojama_damage`
- **オーバーレイへの配線と動画測定は、Codex 指摘の実測チェーン
  (OFF 3run → ON → gross 併用) の完走後に行う** (途中でコードを変えると測定が汚れるため)

#### 未解決の論点 (user 判断が要る)

**モデルの入力列 (`ukeyasusa` / `diff_ukeyasusa`) の値が変わる。**
現行モデルは**この誤判定を含んだデータで学習されている**ため、
修正すると入力分布が学習時とずれる。**再学習の要否は未決**。今回は再学習しない。

### 【user指摘 2026-08-25】急変は「根拠づけ」でなく**根治 (規模の比較)** で減らすべき — Claude の提案が症状対処だった

#### user 指摘

> 急変の根拠付けについて教えてください、最初の指摘は**急変はそもそも幻**で、
> **はなから相手の連鎖より大きな連鎖を打つ見込みがある方を有利と判定すべき**との結論でありました

#### Claude の誤り

Gate 4 の合格条件「1秒150pt以上の急変は40回以下で、**残る全件に新しい物理イベントがある**」に対し、
Claude は「急変1件ずつに実画面の根拠をつける道具を作る」と提案した。
**これは症状の確認に道具を足す提案で、順序が逆だった。**

急変が出るのは、システムが**その瞬間に見えているもの (降ってくるおじゃまの量) だけで判定し、
これから打てる連鎖の規模を見ていない**ため。相手の連鎖が着弾した瞬間に不利へ振れ、
打ち返した瞬間に有利へ戻る。**振れているのは盤面の実力ではなくシステムの視野の狭さ。**

根治は **「規模の比較」** として **2026-08-16 に特定済み**で、
`project_chain_scale_comparison_backlog_2026-08-22` に
「**user 判断待ちのまま6日放置**」と記録されていた項目。
**Claude はその経緯を把握していながら症状対処を提案した。**

#### 事実関係の整理

「規模の比較」は既に実装があり (`--kill-override-scale-compare`、
`scripts/visualize_advantage_overlay.py:6548-6559`)、**Gate 4 の比較条件3そのもの**。

過去の PM100 全8区間の測定結果:

| 指標 | 何も入れない | A+B |
|---|---|---|
| 急変 | 79回 | **40回** |
| 反転試合 | 47 | **34** |
| 決着方向誤り | 10 | **7** |
| ±100 の張り付き | 14.0% | **18.3% (悪化)** |
| 生モデル逆符号の張り付き | 3.3% | **5.2% (悪化)** |

急変・反転・決着方向は改善したが**張り付きの悪化を理由に「全域採用不可」**とされていた。

#### **ただしこの結論は無効**

**この測定は W39 決定化より前** = 有利不利スコアが実行ごとに最大 ±12点 動いていた時期のもの。
Gate 4 の前提にも「決定化前の PM100 値は履歴参照に限定し全8区間を取り直す」と明記済み。

→ **「規模の比較は全域では使えない」という判定そのものを取り直す必要がある。**
特に悪化とされた張り付きの差 (14.0% → 18.3%、4.3pt) が
**揺らぎの中で測られたものだったのか**を確かめる。

#### 差し替えた進め方

**「急変の根拠づけツールを作る」は取り下げ。** 代わりに:

1. PM100 を決定化後に取り直し、**条件3 (規模の比較) が本当に張り付きを悪化させるのか**を確かめる
2. 悪化が再現しないなら**根治として採用を検討**する
   (そうなれば急変は根元から減り、1件ずつの根拠づけはほぼ不要になる)
3. 悪化が再現するなら、**なぜ張り付くのかを潰す**。根拠づけツールはそのとき必要なら作る

### 【訂正 2026-08-25・Codex指摘】規模の比較の過去結論は「**無効**」ではなく「**決定化後の再評価待ち**」

直前の節で Claude は「**『規模の比較は全域では使えない』という判定そのものを取り直す必要がある**」
「過去結論は無効」と書いたが、**これは言い過ぎだった**。Codex の指摘を受けて訂正する。

#### Codex の反証

**W39 以前でも、±100張り付き率は独立4run の全ペアで一致していた。**
したがって **14.0% → 18.3% の 4.3pt 悪化は、当時の乱数幅に埋もれていない。**

**この事実は Claude 自身が測っていた。** 非決定性の調査報告に
「**±100張り付き率: 17.99% vs 17.99% (全ペア一致)** / 判断の余裕 4.3pt / **埋もれない**」と
明記していたにもかかわらず、その後の議論で**自分の測定結果を見落として
「揺らぎの中で測られたものだったのか確かめる」と書いた**。
急変・反転・決着方向が揺らぎに弱いことと、**張り付きが揺らぎに強いこと**を混同した。

#### 正しい扱い (Codex 指定)

- **「全域採用不可」を最終決定とはしない**
- **過去の張り付き悪化は「負の事前証拠」として残す**
- 状態を **「W39 決定化後の再評価待ち」** に変更する
- 新測定で改善すれば採用を再検討する
- **悪化が再現した場合、規模比較を即時廃棄せず、張り付き経路を分離して修正する**

#### user 指摘との関係 (変わらない)

「急変はそもそも幻で、はなから相手の連鎖より大きな連鎖を打つ見込みがある方を
有利と判定すべき」という user 指摘の妥当性は変わらない。
**根治の方向は正しく、放棄しない。** 変わったのは「過去の悪化証拠の扱い」だけ。
悪化が再現しても**廃棄ではなく張り付き経路の分離修正**へ進む。

---

## 2026-08-26 — user決定3件: 決着ホールドは**根治**。Gate 4 基準と B 単独採用は**是正後に再測してから**

### 発端: 「±100張り付き 14.0% → 1.4%」は改善ではなかった

W39 決定化後に取り直した OFF 基準の ±100 張り付きが 14.0% → 1.4% と一桁下がったが、
**母集団が違っていた**。解析器は旧 dump に対して過去の公表値
(14.0%/18.3%、3.3%/5.2%、47/34、79/40、決着方向 10/7) を**完全再現**しており、
定義は不変。差は dump 側にあった。

| | 旧 8/24 | 新 snapshot |
|---|---|---|
| dump 行数 | 136,656 | **101,288 (−26%)** |
| 行間隔 | 0.0333秒 (30fps) | 0.0333秒 (**同一**) |

timeline dump の1行 = 有利不利を再計算した瞬間
(`scripts/visualize_advantage_overlay.py:6159`「settled 更新のたびに1レコード追記」)。
**行が消える = 評価していない。**

消えた 36,087 行の内訳 (母数併記):

- **「片側 CHAIN」28,267行 (78.3%)**、うち ±100 張り付き 15,955 (56.4%)
- 消えた行の張り付き率 **49.68% (17,928/36,087)** / 残った行 **4.86% (4,884/100,569)**
- 旧の張り付き 22,812 行の **78.6% が消えた行に集中**

実画面で確認 (`data/verify/pm100_missing_intervals_2026-08-26/frames/`):

- **t=1865.90**: 1P が掛け算式「40× 1」表示 = **連鎖を開始した瞬間**。WIN 11-16、試合の真っ最中
- **t=1872.45**: 1P がおじゃまで埋め尽くされ窒息寸前、score 92,033 (13.1秒で +5,564)

**勝敗が決まる最重要区間を、システムは1回も評価していない。**

### 犯人の特定 (計装による直接観測、推測ではない)

`resolved_hold_freezes_settled()` (2026-08-14 検収指摘⑤ で導入) が、打ち合い解決中
(`resolved_active=True`) の間 **settled 再計算そのものを完全に止める**。
`--resolved-exchange-eval` は本番 ON。

診断専用 snapshot (測定 snapshot と SHA 一致を確認) に計装した実測 (窓 t=1708–1890):

| 開始 | 終了 | 継続 |
|---|---|---|
| 1709.333 | 1717.600 | 8.27秒 |
| 1718.133 | 1738.300 | 20.17秒 |
| 1764.667 | 1796.067 | 31.40秒 |
| 1827.833 | 1829.800 | 1.97秒 |
| **1833.967** | **1879.033** | **45.07秒** |

最後の 45.07秒は、dump の行が消えていた 1833.60 → 1879.47 と一致する。

```
ホールド中frame          3206/5451 (58.81%)
ホールド適用前にsettled   3964/5451 (72.72%)
ホールドが潰したsettled   1880/3964 (47.43%)
```

着弾待ちの安全弁 `RESOLVED_HOLD_LANDING_MAX_WAIT_SEC = ceil(216/30)×0.733 = 5.86秒`
なので、**45.07秒のうち着弾待ちで説明できるのは最大 5.86秒だけ**。
残る約39秒は `chain_event` が両側とも None にならなかったぶん
(解除条件は `ev1 is None and ev2 is None`)。

**旧より長くなったかは未確定。** 旧 dump に該当区間の行が存在する = 当時ホールドは
効いていなかった、という dump からの推論のみで、8/24 のコードは計装していない。

### 【user決定1】決着ホールドは **根治**。解除条件を作り直す

ガードで上限秒数を切るのではなく、**解除条件そのもの**を直す。
現在の「両側 `chain_event` が None」は、ChainEvent が長い連鎖で 1.4秒ごとに
断片化する既知の性質 (memory `project_chain_event_fragmentation_accumulator_2026-08-22`)
に引きずられる。user 伝授の絶対律
「**連鎖の終わり = 連鎖している側のネクストが動いた瞬間 OR 連鎖している側に
お邪魔が落ちた瞬間**」(memory `reference_chain_end_absolute_signals_2026-08-21`)
に基づく終了信号へ差し替える。

- 認識の中核に触るため **Codex 独立レビュー必須**
- 新機能は既定 OFF の optional flag (規約)
- 恒久指示 `feedback_kill_known_weaknesses_2026-08-13`
  「根治 or user承認つきガード を先に決める」に従い、**根治を選択**

### 【user決定2】Gate 4 の合格条件は、ホールド是正後に測り直してから決める

合格条件「±100張り付きは OFF 基準 14.0% を超えない」は、新しい母集団では
天井として機能しない (新 OFF = 1.4%)。しかし**いま基準を決めても、評価行が
26% 欠けた母集団の上の数字**になる。

- ホールド是正 → OFF 基準を取り直す → そこで閾値を定義する
- **それまで Gate 4 の合否判定は出さない**
- Codex の指示「Gate 定義を変更するなら user 決定なしに PLAN を書き換えず判断を仰ぐ」
  に従い、`PLAN.md` は本決定の反映のみとし、閾値の数値は未確定のまま残す

### 【user決定3】条件2 (ヒステリシスB 単独) の本番候補化は保留

pre-gate measurement の実測 (同一母集団 101,288行、時刻列 8/8 完全一致):

| 指標 | 条件1 (OFF基準) | 条件2 (**B のみ**) | 条件3 (**A のみ**) | 条件4 (A+B) |
|---|---|---|---|---|
| ±100 張り付き | 95.6s (1.4%) | **72.7s (1.0%)** | 193.9s (2.8%) | 166.8s (2.4%) |
| 生値と符号が逆 | 27.1s (0.4%) | **19.9s (0.3%)** | 55.5s (0.8%) | 45.7s (0.7%) |
| 反転試合 | 7 | **4** | 9 | 7 |
| 急変 | 14回 | **8回** | 15回 | 12回 |
| 決着方向誤り | 7 | **6** | 10 | 10 |

**A と B を分離したのは今回が初めて。B 単独が5指標すべてで最良、A 単独が5指標
すべてで最悪、A+B はその中間。** 8/24 の A+B 一括測定にあった
「急変 79→40、反転 47→34、決着方向 10→7 の改善」は**再現しない**
(急変 14→12、反転 7→7、決着方向 7→10)。

ただし **この5指標はすべて評価行が26%欠けた dump の上で測られている**。
特に A (規模の比較) は打ち合いの最中に効く施策なので、**その区間が抜けた測定で
不採用と決めるのは筋が悪い**。user 指摘「はなから相手の連鎖より大きな連鎖を打つ
見込みがある方を有利と判定すべき」の妥当性は変わらず、**放棄しない**。

- B 単独の本番候補化も、A の採否も、**ホールド是正後の再測まで保留**
- `src/production_config.py` は引き続き未変更

---

## 2026-08-26 — user決定: レビュー動画はGate 4完了時の構成で作成する

- 先頭5試合のレビュー動画はGate 3R-6直後には確定しない。
- 次のGateであるGate 4の測定・判定が完了した後、その時点の構成で作成する。
- 中断した途中生成物は完成品として提示せず、再生成時は別名・別出力先を使い、
  既存成果物を上書きしない。
- 動画作成の延期はGate 4の実装・測定・合否判断を省略するものではない。

---

## 2026-08-26 — user決定: 148動画の学習前に指標戦略を壁打ちする

- 現行指標は古いモデルを前提に選定されたため、そのまま148動画の再生成・再学習へ進まない。
- Gate 4完了と先頭5試合レビューの後、現行指標の死活・重複・組合せ・局面別有効性を再監査する。
- スポーツ統計学、ゲームAI、時系列評価、確率校正の最新知見を一次資料中心に調査し、
  本プロジェクトへ適用可能な戦略・観測軸・検証方法を候補化する。
- 序盤・中盤・終盤で指標の意味や重みが変わる可能性と、
  「催促対応→相手本線→自分本線」のExchangeEpisodeを独立して扱う。
- 壁打ちとユーザー承認が終わるまで、指標セット・モデル重み・148動画学習を確定しない。
- 当面はGate 4の条件1〜5、検収、先頭5試合レビュー動画の完成へ作業を集中し、
  大規模な指標実装へ先行分岐しない。

---

## 2026-08-28 — user決定: Phase Jでは実戦予測と最善行動時の評価を切替表示可能にする

- Phase Jの配信表示では、次の2種類を区別して扱う。
  1. **実戦予測**: 一般的な上級者が実際に選びやすい行動を前提とした勝率。
  2. **最善行動時の評価**: 双方が最善に近い行動を続けた場合の盤面価値。
- 表示設定は少なくとも「実戦予測のみ」「最善行動時のみ」「両方」の3モードを持つ。
- 既定表示は実戦予測とし、両方を出す場合は意味の異なる数値であることを明示する。
- 2種類を人手で決めた重みにより混合して1つの勝率にしない。
- 両者の差が大きい局面は、人間の選択傾向と計算上の有力手が乖離する分析候補として記録する。
- 最善行動時の評価は、物理的成立、相手の応答、未知の次ぷよへの頑健性、探索深度、
  実行可能性を検証するまで「理論上の確定勝率」と表記しない。
- この決定は将来の表示要件の固定であり、現時点ではモデルやPhase Jの実装を開始しない。

---

## 2026-08-28 — user決定: 評価を更新・保留・終了する場面の基本方針

- 通常操作中は、信頼できる盤面が得られる限り、どちらかが1手置くたびに評価を更新する。
- 片方が連鎖中は、連鎖側を開始時点から物理計算した終了後の状態として扱い、
  操作可能な側が置くたびに全体評価を更新する。
- 両方が連鎖中は新たな選択がないため原則として評価を動かさず、
  認識の訂正や攻撃量の確定など新しい事実が得られた場合だけ更新する。
- 盤面を信頼して読めない間は、推測値を新しい事実として表示しない。
  最後に確定した評価と最終更新からの経過時間を示し、物理計算できる部分は「予測」と明示する。
- 試合開始前と終了演出中は勝率を表示しない。勝敗を物理的に確定できた時点で
  100%対0%を一度示し、その後は結果表示へ切り替える。
- 検証では、評価を出した区間の精度だけでなく、評価を出せた時間の割合、
  保留または古い値を表示した時間、最長停止時間を必ず併記する。
- 難しい区間を表示停止にして精度の分母から黙って除外することを禁止する。
- これは将来の表示・学習対象を定義する基本方針であり、現時点では実装を開始しない。

---

## 2026-08-28 — user決定: 最終勝敗を価値判断の主目標、将来の具体的事実を補助目標にする

- 価値判断の主目標は、1試合単位の最終勝敗とする。セット全体の勝敗や既存モデルの評価値を正解にしない。
- 短期の補助目標には、数手後の盤面、空き容量、発火、攻撃量と到着時間、相殺、
  生存、攻撃交換後の盤面、次の攻撃までの手数、実際に完成した連鎖規模など、
  将来に観測された具体的事実だけを使う。
- 「どちらが有利か」「どちらが押しているか」といった人間の解釈を補助目標にしない。
- 短期目標は「何が起きるか」、最終勝敗は「それが良かったか」を担当し、役割を混ぜない。
- 行動前後の勝率差は分析用の派生値とし、旧モデルの出力を新モデルの正解として学習させない。
- 実戦予測は実際の勝敗から学習する。最善行動時の評価は、人間の勝敗だけで確定せず、
  物理的な先読みと将来の対戦シミュレーションによる検証を経て段階的に作る。
- 補助目標は固定した人手重みで混合せず、将来事実の予測、最終勝敗の学習、共同調整、
  補助目標を1つずつ外す比較の順で有効性を確認する。
- 長い試合だけが学習を支配しないよう、学習・検証では1試合全体の寄与をそろえる。
- この決定は学習設計の固定であり、現時点では実装を開始しない。

---

## 2026-08-28 — user決定: 学習の時間軸は任意の手数・秒数でなく出来事の時系列を中心にする

- 「3手後」のように根拠が未確定な固定手数を、短期予測の主要な区切りとして先に採用しない。
- 固定秒数も主要な区切りにせず、盤面の高さ、落下距離、連鎖時間、操作速度などの影響を
  各出来事に実際にかかった時間として記録する。
- 学習データの基本形は「直前の状態 → 起きた出来事 → 直後の状態」とする。
- 最低限の出来事候補は、設置確定、操作可能復帰、連鎖開始、連鎖の各段、連鎖終了、
  攻撃量の確定、相殺、おじゃま落下開始・完了、全消し、死亡・勝敗確定とする。
- 手数は、何手目か、攻撃到着までに何回設置できたか、次の発火までに何回設置したか、
  という観測結果・属性として保持する。固定手数の採用は実測後に判断する。
- 実時間は、設置・連鎖・落下にかかった時間、攻撃到着までの残り時間、
  最後の出来事からの経過時間として保持し、時間だけが経過したことも現在状態の情報とする。
- 「攻撃交換」という人間がまとめた単位は学習の基本入力・正解にせず、攻撃発生、相殺、
  落下を個別に記録する。必要なまとまりは分析・表示時に後から構成する。
- 各出来事時点で最終勝敗を主目標とし、次の出来事の種類・側・量・所要時間・直後の状態を
  補助目標の候補とする。
- 出来事の検出漏れで行を黙って消さない。通し番号、発生時刻、信頼度、読めない区間、
  同時発生、物理的収支との一致を記録し、不明は不明として残す。
- この決定は学習データの時間軸に関する方針であり、現時点では実装を開始しない。

---

## 2026-08-28 — user決定: 出来事の由来・不確かさ・リアルタイム値と事後確定値を分離する

- 各状態・出来事は、映像からの直接観測、確定状態と物理ルールからの一意な導出、
  複数候補が残る推定、観測不能の4種類を区別する。
- 確からしさを単一数値だけに潰さず、由来、値の形（点・範囲・候補集合・値なし）、
  時刻の形（正確・区間・不明）、映像認識、時間的整合、物理的整合、根拠を別々に保持する。
- 学習入力には4種類を使用できるが、由来と不確かさを必ず伴わせる。推定候補を黙って1値にしない。
- 短期補助目標には、直接観測で確定した事実、または確定入力から物理的に一意に導けた事実だけを使う。
  複数候補がある推定と観測不能は正解に使わない。
- 最終勝敗の正解は公式の勝敗表示を直接確認できた試合だけとし、物理的な死亡推定で補完しない。
- 同じ量について「その時点で利用できた値」と「後から確定した値」を分離する。
  学習入力は時点版、補助正解は条件を満たす事後確定版を使える。
- 情報源が矛盾した場合は、一方を黙って上書きせず、各候補、矛盾、保留、後の確定を残す。
  全項目共通の動的な優先順位を置かず、項目ごとに情報源の契約を定める。
- 映像全体の時間軸、検出出来事、観測不能区間を分離し、検出漏れを「出来事なし」にしない。
- 同時発生は一組として保存し、物理的な因果順が確定している組だけ順序を付ける。
- 検証には、物理的収支、時間軸の穴、同一入力の再現性、情報源間の一致、由来別件数、
  検出器を意図的に止めて判定不能が増えることの破壊試験を含める。
- この決定は学習データの信頼性規約であり、現時点では実装を開始しない。

---

## 2026-08-28 — user決定: Phase Jでは端末性能に応じた未来予測の品質段階を選択可能にする

- Phase Jの未来予測は、軽量、標準、高精度、分析の段階を持つ。
- 軽量でも、映像認識、物理ルール、攻撃・相殺・死亡の記録、見えている次ぷよ2手の
  全配置探索、勝率の意味を共通に保つ。古い手作り評価へ戻さない。
- 段階で変更できるのは、未知の未来候補数、探索深度、残す枝数、ワイルドカードを
  実候補へ戻す回数、相手応答の深度、未来盤面の評価回数とする。
- 軽量では実戦勝率と見えている範囲の最善、標準・高精度では実戦予測と
  より深い最善行動時評価を扱う。分析は録画後の研究・監査用で、配信中には使わない。
- 初回は本番と同じ認識・探索の併走測定を行い、フレーム欠落、認識の不変、表示遅延、
  待ち行列、CPU/GPU/メモリ、再現性を満たす最上位段階を推奨し、選択後は固定する。
- 実行中の負荷で品質段階を黙って変更しない。古くなった予測だけを破棄し、
  最新状態を優先し、前回値と更新待ちを表示し、遅延・破棄を母数付きで記録する。
- 継続的に間に合わない場合は段階変更をユーザーへ提案し、変更は試合の切れ目で適用する。
- 段階ごとに勝率を別途較正し、未知試合で有利側、差の区分、確率、最善候補、計算時間を比較する。
  計算量を増やしても精度が上がらない段階を「高精度」として提供しない。
- 各出力とログには品質段階、探索設定、実際に調べた量、入力品質、遅延を付ける。
- この決定はPhase Jの将来要件であり、現時点では実装を開始しない。

---

## 2026-08-28 — user決定: 盤面の価値・対戦者の実力・最善行動時の評価を分離する

- 勝率設計は、盤面と試合展開から求める基準値、対戦者の実力差による補正、
  ほぼ最善に行動した場合の評価を別々に扱い、1つのモデルへ混在させない。
- 公開表示の既定は、選手名や過去成績を使わない「盤面基準」とする。
  これは対象ランクの上級者からランダムに2人を選んだ場合の平均的な実戦予測であり、
  未知選手でも同じ意味を保つ。完全に同じ実力同士だけを仮定した反実仮想値にはしない。
- 実際の対戦者を考慮した勝率は任意の表示とし、盤面基準へ時変の実力差を明示的に加える。
  選手IDを盤面モデルへ直接入力して、選手固有の積み方や映像条件を暗記させない。
- 実力は、各試合を1観測とした過去の勝敗から、時間変化と推定の不確かさを持つ値として別途推定する。
  未知選手、対戦数が少ない選手、古い成績しかない選手は自動的に補正を弱め、盤面基準へ戻す。
- 実力差の効き方は局面によって変える。選択肢の多い互角局面では影響を許すが、
  物理的に決着へ近いほど影響を0へ近づけ、死亡確定や回避不能な結果は最後に必ず優先する。
- 試合中に観測した行動傾向による適応は初版へ入れない。将来追加する場合も、
  その時点より前に観測できた行動だけを使い、少数観測では0へ縮め、実力差とは分離する。
- ほぼ最善に行動した場合の評価は、実際の人間の勝率モデルとは分離し、
  物理計算と未来探索から求める。選手の実力や行動傾向では補正しない。
- 検証では「盤面のみ」「実力のみ」「両方」を、未学習の選手と未来時点の試合で比較する。
  場面を独立標本として数えず、試合・選手・動画を単位に不確かさを求める。
- 採用判断は正答率だけで行わず、対数損失、Brier score、確率校正、重大局面の誤り、
  未知選手への一般化を確認する。両方を使っても盤面のみを明確に上回らなければ実力補正を採用しない。
- 盤面から選手を識別できること自体は、正当なプレイスタイル差も含むため不合格条件にしない。
  選手固有情報への依存を調べる診断として扱い、合否は未学習選手での予測性能で決める。
- 前の決定にある「実戦予測」は、既定の盤面基準と、任意の対戦者補正あり表示に分解して扱う。
  表示時はどちらを示しているかを明記し、人手の固定重みで混ぜない。
- この決定は統計・機械学習上の設計方針であり、現時点では実装を開始しない。

---

## 2026-08-28 — user決定: 盤面・未解決状態・出来事を主入力とし、既存指標は役割を再配置する

- 次の学習設計では、現在の約45〜51指標を一律に主入力へ並べる方式を前提にしない。
- 主入力は、現在の盤面とネクスト等の完全な状態、未着弾攻撃・相殺・残り時間等の
  未解決な物理状態、直近の出来事の順序と時間差を別系統で持ち、最後に統合する。
- 入力の由来と信頼度を必ず併記し、読めない値や複数候補を確定値として扱わない。
- 未来探索の結果は、盤面・未解決状態・出来事による基準モデルを作った後に、
  追加効果と計算費用を測ってから主入力へ加えるか判断する。
- 既存指標は、主入力、補助学習の正解、説明・監査専用、廃止候補の4区分へ再分類する。
- 主入力から外した指標も、即座に計算・出力を止めない。勝率への寄与と切り離したうえで、
  指標値、根拠表示、診断ログ、モデル異常の監査に利用できる状態を維持する。
- 単独の勝敗相関が低いことだけを廃止理由にしない。発生時だけ意味を持つ項目、
  組合せで効く項目、局面限定項目は対象部分集合と未知選手で再評価する。
- 廃止は、他の入力を条件にしても追加情報が確認できず、説明・監査上の価値もなく、
  配線と測定範囲が正常であることを確認した項目だけに限定する。
- 序盤・中盤・終盤でモデルを硬く切り替えず、進行度、未解決攻撃、危険度、操作状態等を
  連続した現在情報として共通モデルへ渡す。局面別の評価報告は継続する。
- 比較は「現在状態のみ」「未解決の物理状態を追加」「出来事の順序を追加」
  「未来探索を追加」の順で行い、同一の未知選手・未知動画で増分を確認する。
- 出来事の順序を壊す対照実験と、攻撃が完全に清算された直後の履歴増分検査を行い、
  順序情報が不要なら軽量な集計値へ戻す。
- この決定は学習・指標再監査の設計方針であり、現時点では新モデルを実装しない。

---

## 2026-08-28 — user決定: 操作速度を勝率入力から外し、連鎖・おじゃまを出来事と物理規則で扱う

- 操作中のぷよは、現在・ネクスト・ネクストネクストの色を追跡して状態へ含める。
  シーン切替や再同期直後など履歴が切れた場合は推測で埋めず、不明として再観測を待つ。
- 操作中の列・向き、設置までの残り時間、操作開始からの経過時間は勝率モデルの入力にしない。
  選手固有の操作速度・操作の癖を盤面基準へ混入させない。
- 前節の「実時間」方針のうち、プレイヤーが現在の手を置くまでの時間は予測対象から外す。
  実装内部では出来事の順序、タイムアウト、検出漏れの監査用時刻だけを保持する。
- 試合全体の経過時間は、時間経過で攻撃換算が変化するゲーム規則の入力として保持する。
  プレイヤーの操作速度とは別の情報として扱う。
- 勝率の更新点は、設置完了、連鎖開始・各段・終了、攻撃量確定、相殺、
  おじゃま落下開始・完了などの物理的な出来事を基本とする。操作途中の位置から意図を推定しない。
- 次に操作可能かは各側の状態管理から導く。自分の連鎖中、おじゃま落下中、
  設置直後の遷移、試合開始・終了演出、死亡後は操作不能とする。
  相手が連鎖中でも自分側が通常操作中なら操作可能であり、両者を一括して止めない。
- おじゃまの量は、攻撃側の連鎖中の暫定量と、攻撃側の連鎖終了後の確定量を分離する。
  暫定量を確定残高へ早期に混ぜない。
- おじゃまが落ちる条件は、(1) 攻撃側の連鎖終了で量が確定、
  (2) 受け手が現在のぷよを設置、(3) 受け手側で連鎖が起きていない、の3条件とする。
  受け手側で連鎖が起きた場合はその連鎖終了後まで待ち、相殺で残高が0なら落とさない。
- おじゃま状態は少なくとも、暫定生成、確定残高、受け手の設置待ち、
  受け手の連鎖終了待ち、相殺済み、落下中、落下完了を区別する。
  量の足し引きは1つの会計を単一情報源とし、別経路で再推定しない。
- 進行中の現在連鎖数は、掛け算表示の出現周期を数える直接観測を主とする。
  値の変化回数だけでは数えず、同じ計算値が連続する場合も別段として扱う。
- 発火直前の盤面が確定している場合、最終連鎖数、残り連鎖、終了後盤面は
  ゲーム物理から一意に計算する。認識不明セルがある場合は候補分布または不明とし、
  一つの確定値へ潰さない。
- 掛け算表示、盤面の消去・落下、得点増分、連鎖終了後のネクスト移動を相互検算する。
  不一致時は都合のよい経路を採用せず、その連鎖を不明として件数と母数を記録する。
- この決定は学習入力と時系列状態の設計方針であり、現時点では新モデルを実装しない。

---

## 2026-08-28 — user決定: 現行52列を38概念群へ整理し、主入力・補助正解・説明監査・廃止候補へ分類する

- 現行モデルの52列は、両者差、比率、対数化、進行度との積など同じ事実の加工違いを含むため、
  52個の独立概念とは扱わない。38の概念群へまとめ、最終役割と移行中の扱いを分離する。
- **主入力は10群**とする: 盤面の全マス、現在・ネクスト・ネクストネクストの色、
  色ぷよ数、盤面のおじゃま数、両者6列の高さと窒息余裕、進行中連鎖の現在段と暫定火力、
  両者の全消しボーナス保有、未解決おじゃまの単一会計、直近の出来事と順序、
  ゲーム規則に必要な試合全体の経過時間。
- 色ぷよ数・盤面おじゃま数・既存の高さ要約と両者差は、最初の表形式モデルでは暫定利用できる。
  最終的には盤面セル、両者の同時点の値、6列の高さから学べるようにし、手作り差分を必須にしない。
- 盤面の総ぷよ数は色ぷよ数と盤面おじゃま数から一意に決まるため、主入力から外して収支監査へ移す。
  中央の盛り上がり、色の均等度も盤面から再計算できる説明・監査値とする。
- 現在・ネクスト・ネクストネクストの追跡が切れた場合は推測で埋めず、不明として再同期する。
  操作中の列・向き・残り操作時間は主入力へ加えない。
- 認識の不明マス、情報源、欠測、物理との不一致は品質情報として必ず保持するが、
  当初は勝率の直接入力にせず、表示保留と監査に使う。未学習の配信画面でも一般化することを
  確認できた場合だけ、別条件で入力化を再検討する。
- **補助学習の正解は5群を新設する**: 次に起きた出来事、次の出来事後の盤面、
  事後確定した攻撃・相殺・着弾量、交換後の生存・窒息余裕、実際の発火までの設置回数。
  既存の手作り指標を補助正解へ流用しない。
- 任意の色を1個置いた最大連鎖・即発火火力、発火までの追加ぷよ数、発火後の副砲連鎖数、
  上部まで伸ばした連鎖は、現在の確定能力ではなく仮定付きの将来探索として別枠へ隔離する。
  探索条件、見えている色、深さ、計算量を記録し、主入力へ加えるのは増分検証後とする。
- 連鎖効率・急所、掘り耐性、受けやすさ、連結ペア・3個連結・最大連結、発火位置数、
  同時消しの豊かさ、おじゃまを受ける余裕、色ぷよとおじゃまの比率などは、
  **説明・監査用**へ移す。代替が完成するまでは軽量探索の診断値として計算を維持できる。
- 掘り耐性は端数おじゃまの単一配置でなく、落下列の分布に対する生存確率へ再設計する。
  受けやすさとおじゃま猶予量は、仮想着弾後の窒息余裕・生存分布へ置換する。
- 埋没穴は通常の重力盤面ではほぼ発生せず、現監査でも99.95%が0だったため、
  戦略指標から外す。ただし廃止せず、認識異常・重力不整合の監査値として残す。
- 全消しボーナス保有は約99%が0で単独相関が低くても、次の攻撃量を物理的に変えるため主入力に残す。
  稀少であることを死亡判定に使わない。
- `saturation_chain_upper` は99.6%欠損の配線・測定範囲障害があるため、修理前は学習に使わない。
  欠損を0とせず、修理後に条件付き将来探索として再評価する。
- **廃止・再設計候補は5群**とする:
  1. 旧 `second_chain_potential` — 実体が本線非参加色ぷよ率で、真の第二波ではないため、
     発火後盤面の実火力・必要手数・生存性へ再設計する。
  2. `isolated_pair_count` / `main_linked_ratio` — 単独寄与がなく静的隣接近似も粗い。
  3. `multi_color_ignition` — `ignition_point_count` と高相関のため、発火位置数を説明用に残して整理する。
  4. 上限後・対数・進行度掛算の予告おじゃま列 — 相関0.9996超の重複加工なので、
     上限前の単一会計と到着条件へ統合する。
  5. 色ぷよ差×おじゃま差等の手作り積 — 基礎量を非線形モデルへ渡して置換する。
- 正規化版・再同期版のおじゃま純残量は、新旧会計の移行監査に残す。
  出来事時刻で両側を正しく同期した単一会計が全範囲を覆った後にモデル入力から外す。
- 廃止候補も即座に計算・保存を止めない。代替が同じ評価時点を覆うことを確認し、
  同一行・同一試合・未知選手で群ごとの除外比較を行い、まずモデル入力だけを止める。
- 本分類は設計の固定であり、現時点では既存52列の削除、重み変更、新モデル実装、
  `src/production_config.py`への登録を行わない。

---

## 2026-08-28 — user決定: 出来事付き再生成は48動画の先行測定から始める

- ローカル元動画から出来事まで再生成可能な97動画を、最初から全件処理せず、まず48動画で
  新設計の効果を測る。
- 残る49動画は、48動画で入力構成と学習方法を選び終えるまで使用せず、独立確認用として温存する。
- 48動画は都合のよい場面を手選別せず、技能帯、元動画、対戦者の重複、動画長、認識品質の偏りを
  抑えた固定マニフェストで選ぶ。同一元動画の派生物は同じ群として扱う。
- 効果測定は48動画を6群×8動画に固定し、各回を学習32・調整8・評価8として循環させる。
  各動画は評価に1回だけ現れ、全48動画について学習外予測を作る。
- 比較する条件は同一の48動画・同一の分割とし、現在盤面のみ、未解決攻撃の追加、出来事履歴の追加、
  実際に起きた短期未来の補助学習追加を順番に比較する。
- 主な効果判定は対数損失、Brier score、確率校正とする。AUCだけで採否を決めず、物理的な新事件が
  ない急変、攻撃交換中、死亡直前、十分な対応を返した場面の重大誤りを別集計する。
- 48動画で方向性が明確になった構成だけを残る49動画で確認する。49動画の結果を見て構成を選び直した
  場合は、その確認を最終評価とは呼ばず、別の未使用データを必要とする。
- 既存データを上書きせず、新規の保存先、固定マニフェスト、元動画の指紋、設定の指紋を残す。
- 48/49 の固定結果はGit共有用 `docs/manifests/EVENT_PILOT_2026-08-28.tsv` と、
  ローカル実行用 `data/verify/event_pilot_2026-08-28/manifest.tsv` の同一内容とする。
  元動画IDの固定SHA-256順で、マスター26・チャレンジャー14・A級4・S級4を先行側へ選び、
  6群の過去有効行数を57,078〜59,381へ均した。モデル結果・目視評価は選定に使っていない。
- 48本の構成選抜では、試合単位の平均対数損失が相対1%以上改善、6群中4群以上改善、
  Brier score・確率校正・重大誤りが許容幅内であることを原則とする。
  詳細な事前基準は `docs/EVENT_DATASET_DESIGN_2026-08-28.md` §8.3 に固定する。
- 出来事データは、物理発生・観測・リアルタイム利用可能・最終確定の時刻を分離し、
  暫定値の確定・取消・競合を上書きせず追記する。おじゃま落下予約、画面上の落下開始、
  落下完了は別の出来事とする。

---

## 2026-08-28 — user決定: 壁打ち統合版を次世代判定設計の正本とする

- 壁打ち全体を `docs/ADVANTAGE_MODEL_DESIGN_AGREEMENT_2026-08-28.md` へ統合した。
  合意済み、暫定、未決定、検証方法、人による確認項目、リアルタイム条件を一つの資料から確認できる。
- コンテキスト圧縮前の判断経緯は、保存された同一taskからユーザー可視の327発言
  （ユーザー102、Codex225）を原文順に再抽出し、
  `docs/ADVANTAGE_MODEL_WALL_DISCUSSION_TURN_BY_TURN_2026-08-28.md` へ一件ずつ記録した。
  同区間には圧縮が7回あったが、発言本文は原本に残っていた。自動挿入情報3件だけを除外した。
  `docs/ADVANTAGE_MODEL_WALL_DISCUSSION_RECONSTRUCTION_2026-08-28.md` は論点別索引、統合版は結論とする。
- 既存 `docs/EVENT_DATASET_DESIGN_2026-08-28.md` と矛盾する場合、後続の壁打ちで更新した事項は
  統合版を優先する。旧資料と既存証跡は削除・上書きせず、検討経緯として保持する。
- 出来事の必須時刻は物理発生区間と利用可能時刻とする。最初の観測時刻は監査に必要な場合だけ持ち、
  後の最終確定は元記録の時刻欄へ書き戻さず、新しい訂正記録として追加する。
- 48動画の最初の比較は、現在状態、未解決の物理状態、出来事履歴を段階的に行う。
  補助学習は最良の非補助条件へ「次の物理的出来事」と「相殺後に送られる量」だけを最初に追加し、
  旧案の5群を同時に入れない。
- 現行48/49マニフェストは再現可能な候補割当であり、品質合格後の最終構成ではない。
  c58/c69は旧データでは隔離を維持し、一般修理後の共通品質条件に通らなければ別動画へ入れ替え、
  旧マニフェストを上書きせず新しい版を作る。
- 試合番号を進める正式境界通知元は一つにし、最終勝敗の正解には公式勝者表示だけを使う。
  物理的死亡は別の出来事として保存し、欠けた公式勝者を推測で補わない。
- 局面の初期仮説は、両者のうち総ぷよ数が多い側を主に参照し、18個刻みを仮区切りとする。
  生の両者総数、合計、差、色ぷよとおじゃまの内訳を保持し、区切りと効果は回帰確認で決め直せる。
- 未来予測は、確定物理処理、一手、見えている範囲の複数手、未知色・長手数近似へ段階化する。
  相手が最善または平均的に行動するという前提を現時点で固定せず、候補分布を保持する。
- 深い探索は全場面で必須にしない。状態変化時だけ計算し、古い計算を中止できる設計とする。
  精度改善、重大誤り、計算時間、破棄率、再利用率、CPU/GPU/メモリを同時に測り、
  リアルタイムで間に合う範囲の最良方式を標準採用する。
- 人の動画確認は正確な勝率を手作業で付ける作業ではなく、明らかな誤りを弾く作業とする。
  一画面・一場面・一論点、短い映像、変更前後、平易な根拠を基本とし、スマートフォンで確認可能にする。
- 本決定は設計記録であり、現行モデルの削除、重み変更、本番設定登録、配信接続、49動画の利用を
  許可するものではない。

---

## 2026-08-28 — user決定: 出来事原本と学習用状態表の二層構造

- 試合中の出来事を発生順に追記する記録を原本とする。
- 訂正、取消、後からの確定は過去行を上書きせず、新しい記録として追加する。
- 学習用データは、意味のある出来事の直後の盤面、未解決状態、不確かさを原本から生成した
  状態表とする。学習用状態表は再生成可能な派生物であり、正本にはしない。
- 物理的なファイル形式と列構成は、記録する出来事種類の確定後に決める。
- userの恒久指示として、以降の壁打ちは各論点ごとにユーザー発言、決定、保留、次論点を
  逐次ファイルへ保存しながら進める。

---

## 2026-08-28 — user決定: 出来事原本と学習用状態表の保存形式

- ClaudeとCodexが独立案を比較し、相違点を再討論した共同案をuserが承認した。
- 出来事原本は、動画・生成実行ごとに分けた追記専用JSON Linesとする。
- 学習用状態表は、原本から再生成する動画単位Parquetとする。
- SQLiteは正本にも学習表にも使わず、人による条件検索が必要になった段階で追加する
  再生成可能な補助索引とする。
- 1動画を1処理だけが書き、並列処理で同じ原本ファイルを共有しない。
- ディスク同期は一定件数または一定時間ごとに行い、間隔と失われ得る最大行数を記録する。
- 改行、JSON妥当性、動画内通番、行数、最終通番、要約値、完了印を自動検査し、
  欠落または不一致がある生成物を学習へ渡さない。
- gitの対象動画計画・列仕様・変換規則・小さな検査例と、ローカル生成物に付属する
  入力指紋・設定・行数・要約値は役割が異なるため分けて管理する。
- 盤面の重複保存と参照保存、同期の具体間隔、Parquet用ライブラリの導入時期は、
  通常動画1本と難しい動画1本の短区間による実測後に決める。
- これは設計決定であり、実装、本番設定変更、動画処理、49動画利用の許可ではない。

---

## 2026-08-28 — user決定: 原本の各行を単独で追跡可能にする

- 原本の各行へ、元映像、生成実行、動画内通番、出来事自身、出来事の種類、対象側、
  物理発生区間、利用可能時刻、記録仕様の版を判別できる情報を持たせる。
- 問題行だけを切り出す、破損行を隔離する、複数動画を統合する場合も出所を失わせない。
- 入力映像の完全な指紋、全実行設定、コード版、総行数、要約値など実行全体に共通する情報は、
  各行へ繰り返さず生成実行の説明ファイルへ置く。
- この決定は原本の共通項目に関する設計であり、実装開始の許可ではない。

---

## 2026-08-28 — user決定: 生成内容と実行試行を分けて識別する

- 記録仕様、元映像、認識設定、コード版が同じ生成内容には同じ識別情報を付ける。
- 同じ生成内容を再現確認のため複数回処理した場合、実行試行の識別情報だけを変える。
- 動画内通番は連続する整数とし、欠落と並び替わりの検査に使う。
- 出来事自身の識別情報は、生成内容識別情報と動画内通番から決定的に作る。
- 無作為な番号や実行時刻を出来事識別情報へ混ぜず、同じ入力から同じ識別情報を再現する。
- 出来事の対象は1P、2P、両者、試合全体、不明を区別する。
- この決定は識別規約の設計であり、実装開始の許可ではない。

---

## 2026-08-28 — user決定: 元映像上の時刻と処理時間を分ける

- 物理発生区間の最も早い位置と最も遅い位置を、整数フレーム番号と整数ミリ秒で保存する。
- 判定へ初めて利用できた位置も、整数フレーム番号と整数ミリ秒で保存する。
- 画面で最初に見えた位置は、検出遅延を監査する出来事だけ任意で持つ。
- 元映像のフレーム率と時間基準は生成実行の説明ファイルへ置く。
- 秒の小数は表示用とし、順序または同一性の判定には使わない。
- 実行日時は生成実行の説明ファイル、端末上の処理時間は性能確認用の別記録へ置き、
  試合中の時刻と混ぜない。
- この決定は時刻項目の設計であり、実装開始の許可ではない。

---

## 2026-08-29 — user決定: 軽い根拠は出来事行へ、重い証拠は別保存する

- 数値、値の状態、認識・計算方法の版、参照フレーム、検査結果、実測差、許容差、
  不足理由符号は出来事行へ保存する。
- 認識器の未補正得点と補正済み確率を分け、補正済み確率は補正方法と版がある場合だけ保存する。
- 前後関係と物理関係は一つの整合度へ集約せず、検査ごとの結果と理由を保存する。
- 画像の切抜き、全候補一覧、毎フレームの生反応など重い証拠は別ファイルに保存し、
  出来事行から識別情報で参照する。
- 重い証拠がなくても、出来事行だけで値の根拠と検査結果を説明できる状態を維持する。
- 学習目的ごとの使用可否は原本へ固定せず、派生する学習用状態表で理由つきで生成する。
- この決定は保存境界の設計であり、実装開始の許可ではない。

---

## 2026-08-29 — user決定: 以後の技術設計をAIへ委任する

- 以後の技術設計はCodexが判断し、必要な節目でClaudeへ独立レビューを依頼する。
- 既決事項をuserへ再確認せず、技術詳細は根拠、代案、採否を逐次記録して進める。
- 目的、表示内容、許容できる誤り、外部公開、費用または運用負担など、userの価値判断が
  結果を変える事項だけをuser判断として残す。
- 直前の未決定事項だった正式試合番号の保存方法は、過去行へ書き戻さず、動画内通番範囲と
  正式試合番号を結ぶ関連付け行を追記する方式をCodex判断で採用する。
- この委任だけでは実装、本番設定変更、動画処理、49動画利用を開始しない。
  設計完了とClaudeレビュー後に次工程を明示する。

---

## 2026-08-29 — AI設計判断: 確定攻撃と途中連鎖候補を別の識別単位にする

- 得点差から確定する会計上の攻撃は「前回確定以降に確定した量の区間」であり、画像から途中観測した
  一つの物理連鎖と常に一対一とは限らない。両者へ同じIDを付けない。
- 確定攻撃はside別の確定通番からIDを作り、途中観測は連鎖候補IDを維持する。
- 確定時に候補が1件なら関係を明示する。複数なら候補一覧だけを持ち、単一`chain_id`を出さず
  `ambiguous_multiple_observed_chains`とする。候補0件も観測なしとして明示する。
- 途中推定は、確定値から参照されるか、試合境界または処理終端で未確定終了として閉じる。
  未処理のまま集計から消える状態を許さない。
- 暫定値と確定値で換算率の観測時点が違う場合は、その事実を別状態として残す。

## 2026-08-29 — AI設計判断: 会計検証の主張範囲を狭める

- 会計観測は専用の既定OFFスイッチだけで起動し、採用済み勝者判定の有効化では起動しない。
- 出来事自身が作った配分合計を「独立検査」と呼ばない。検証器は送付列から量IDのFIFOを再構成し、
  記録配分と完全一致する場合だけ合格にする。
- 検査を持たない出来事を無理に自己確認で埋めず、検査あり・なし件数を分けて報告する。
- 処理フレーム数、標本フレーム数、side観測数、標本被覆率を別々に報告する。
- 会計変化の物理発生位置は、前の記録行後から検出行までの区間とする。検出行一点は利用可能位置であり、
  物理発生時刻の断定には使わない。

---

## 2026-08-29 — AI設計判断: 出来事保存仕様v1を実装前PASSとする

- `docs/EVENT_SOURCE_SCHEMA_V1_2026-08-29.md` に、原本、重い証拠、学習入力、未来正解、
  事後確認版、中断復旧、識別、要約値、版管理を具体化した。
- Claude第1回レビューはP0 1件・P1 7件の条件付きPASS。全件を採用または強化して反映した。
- Claude第2回レビューで前回全件CLOSED、新規P1は意味内容要約値の算出規則1件だけだった。
- 物理要約値と意味内容要約値を分離し、対象・除外項目、数値表現、配列順、SHA-256入力を固定した。
- Claude第3回レビューで当該P1はCLOSED、新規P0/P1なし、最終PASSとなった。
- 事後確認版はClaude案より強く `data/audit_views/` へ隔離し、
  `model_input_allowed=false` を必須として未来情報混入を防ぐ。
- 次工程は最小書込み・読取・検査層と決定性試験だが、本決定時点ではまだ実装していない。
- 本番設定変更、48動画一括処理、49動画利用、配信接続は許可していない。

---

## 2026-08-29 — AI設計判断: 原本部品名と短区間再現条件を固定する

- 原本部品ディレクトリは実装済みの `events/` を正とし、仕様書の旧表記 `records/` を訂正する。
  実データ生成前なので移行は不要で、出来事であることが名称から明確な方を採った。
- `stable_board_observed` の利用可能位置は認識確定フレーム、物理発生位置は前回観測後から
  今回確定までの範囲とする。一点へ偽精密化せず、物理発生位置未観測の理由符号を残す。
- 生成内容IDにはPython、numpy、OpenCV、torch、CUDA、計算装置を含める。
- 対象コード・モデル・テンプレートは収集前後に全件再要約し、増減または内容変更があれば
  その試行を完了させない。必須4資産の不在を黙って許さない。
- 収集器が実際にデコードした開始・終了を要求範囲と突合し、早期終端時は実測終端を
  manifestの処理範囲へ使う。要求範囲を処理済みと偽って記録しない。
- 同一生成内容の独立試行比較は出来事件数、生成内容ID、意味要約、出来事ID列、通番列を
  読取専用比較器で自動検査する。人手比較を合格条件にしない。
- 途中失敗した試行は上書き・削除せず、新しい実行試行IDで先頭から再実行する。
  正常接頭辞から正式再開する場合だけ復旧記録つきで同じ実行試行IDを継続する。

---

## 2026-08-29 — AI設計判断: 事後勝者を処理末尾ラベルとして隔離する

- 収集中に受理した試合境界は観測時点から利用可能な出来事とする。
- WIN★パネルの公式勝者は動画区間全体の事後走査で得るため、物理表示区間とは別に
  処理末尾を利用可能位置とし、勝敗予測の当時入力には使わない。
- NPZの`won`列は過去行へ事後書戻しされているため、新原本の入力元にしない。
- 曖昧・パネル非表示から勝者を補完しない。理由別の除外件数を残す。
- サイドカー元JSON全文とSHA-256を認識設定v3へ埋め、生成内容IDと完了検査で保護する。
- 正式試合番号への割当はこの工程では行わず、後続の追記関係として扱う。
- 途中切り比較は終了フレームより前に利用可能だった出来事だけを比較し、
  終了フレームで初めて使える事後ラベルを未来情報として除外する。

---

## 2026-08-29 — AI設計判断: 公式試合番号は確認済み範囲関係として追記する

- 元映像先頭からの処理だけ、局所試合indexを1始まりの公式試合番号へ対応させる。
- 確認済み公式勝者がある試合だけを割り当て、曖昧・パネル非表示から勝者や割当を補完しない。
- 試合範囲は直前境界の次の通番から終了境界の通番までとし、勝者と境界の出来事IDを
  確認済み根拠として持つ。
- 割当は処理末尾に新しい出来事として追記し、過去行へ公式試合番号を書き戻さない。
- 元映像の途中開始では公式番号を推測せず、外部の番号基準が得られるまで割当を作らない。

---

## 2026-08-29 — AI設計判断: 確定会計と暫定火力を同じ攻撃IDの追記履歴にする

- 確定会計の単一情報源は`OjamaAccountingTracker`のcap前累積カウンタとする。
- 掛け算式から得た進行中火力は暫定行として保存するが、会計残量や量IDを動かさない。
  `simulate`由来値は暫定攻撃量へ昇格させない。
- 得点OCRで確定した生成量は同じ攻撃IDで追記し、直近暫定値と一致すれば`confirms`、
  異なれば`corrects`とする。過去の暫定行は削除・置換しない。
- 送付量には量IDを発行し、相殺、着地、試合境界消失は先入れ先出しで量IDへ帰属させる。
  未生成量の消費、残量超過、保存則残差、参照切れは完了試行の採用を拒否する。
- 出来事を後から再統合して通番を振り直す場合、訂正先と確認済み原因の出来事IDも
  必ず新通番へ追従させる。通常映像で発見した旧ID残存を共通修正し、回帰試験へ固定した。
- 通常・難条件2映像の計24,600 side観測で確定20、同値確認8、値訂正12、
  訂正対象75、保存則違反0、未帰属0を確認した。
- 全消し加算内訳、連鎖開始、着地前後盤面差は未観測のまま推定せず、各行の不足理由に残す。
- この決定は既定OFFの原本生成に限る。本番表示、本番設定、48動画一括処理、49動画利用を許可しない。

---

## 2026-08-29 — 訂正: 確定会計と暫定火力は同じ攻撃IDにしない

- 直上の「同じ攻撃ID」はClaude独立レビューと実データ再調査で誤りと判明したため廃止する。
- 確定会計は得点確定区間、暫定火力は画像で途中観測した連鎖候補であり、常に一対一ではない。
- 修正版の規則と実測は、本ファイル前掲の
  「確定攻撃と途中連鎖候補を別の識別単位にする」と
  `docs/EVENT_SOURCE_SCHEMA_V1_2026-08-29.md` §17を正とする。
- 旧実測の「訂正対象75」は分母を隠すため採用しない。正しくは暫定102件のうち、
  確定参照92件、未確定終了10件、未処理0件である。

---

## 2026-08-29 — AI設計判断: 会計出来事基盤をp1fix11で閉じる

- 確定値として扱うのは、得点確定から得た攻撃生成量だけとする。相殺、送付、着地、境界失効は
  会計規則から派生した正確な整数値でも、盤面で独立観測していないため暫定・モデル値と明記する。
- 得点換算の余りは `leftover_after < effective_rate` と左右別の連続性を必須にする。
  試合終了表示では切らず、会計本体が出す左右別リセットだけで0へ戻す。
- 一つの観測区間に攻撃とリセットが同居する場合は、順序を一点に決めず、成立し得る余り候補を
  保持して後続攻撃で絞る。片側リセットは反対側へ影響させない。
- 同一区間の送付と境界失効は、FIFO保存に必要な場合だけ送付先行と推定し、
  `within_window_order_state=inferred_send_before_reset` と不足情報を残す。
  着地だけは同一区間で新しく送られた量を消費してはならない。
- 後段の再採番は同一フレーム内の `accounting_source_order` を必ず維持する。
  会計出来事は同フレームの境界証拠より前に並ぶため、終了する試合の範囲へ属する。
- 動画末尾が境界で終わり、その後にオンライン出来事がない最終試合は、逆転した通番範囲を
  捏造せず、公式試合割当だけを作らない。勝者観測そのものは保持する。
- 最終実動画証拠は
  `data/verify/event_source_v1_accounting_pilot_p1fix11_2026-08-29/results/accounting_combined_validation.json`。
  ただし同一フレーム送付+境界失効は実動画0/3失効件で、当該修正の判別証拠は合成回帰と明記する。
- Claude最終独立レビューは再採番P1をCLOSED、新規P0/P1なし。関連403件PASS。
  本番設定、48動画一括処理、49動画利用、学習は引き続き許可しない。

---

## 2026-08-30 — user決定: 分析判断を確定検査と場面レビューに分ける

- 再現性、保存則、未来情報の混入、元映像単位の分離、統計的有意性など、規則と数値で確定できる
  部分はAI側で処理し、user確認を必須にしない。
- 勝率表示の違和感、場面ごとの解釈、新しい入力項目や重み変更の採否は、具体値と比較可能な映像を
  userへ提示して確認する。
- 上記の判断前にClaudeへ独立レビューを依頼し、Codex・Claude・userの三者で曖昧部分を確定する。
- プロジェクト内部の略語ではなく一般的な言葉で説明し、人が確認しやすい単位に分ける。

---

## 2026-08-30 — user決定: 余剰資源で残り18本を背景処理する

- 固定先頭30本を重み再学習とレビューの対象とし、その工程を残り18本の完了待ちにしない。
- 残り18本は現在版コードの固定コピー、別出力、低優先度2並列で処理し、30本集合と混ぜない。
- 合計並列数は実測安全上限3を超えない。分析・レビュー動画生成が重くなる場合は背景処理を一時停止する。
- 保持49本は引き続き未使用とし、本番設定も変更しない。

---

## 2026-08-30 — AI設計判断: 同一フレーム順序不明量を着地から隔離する

- 同一観測行に新規送付と`dropped_uncapped`増加が共存しても、新規送付を同じフレームの
  `garbage_fall_completed`へ割り当てない。従来の禁止規則を維持する。
- 古い量IDで説明できる部分だけを通常着地とし、残りは
  `garbage_same_frame_order_ambiguous`へ分離する。同一フレーム量IDを一度だけ消費して保存則へ
  含めるが、物理着地とは主張しない。
- 隔離量は`counter_source=dropped_uncapped`、
  `ordering_state=same_frame_send_or_drain_order_unresolved`、
  `model_input_allowed=false`を必須とする。学習、表示、着地量ラベルへ入れない。
- 同一フレーム量IDで全量を説明できない、古い未決済量を飛ばす、隔離フラグがない場合は採用を拒否する。
- c6全長で順序不明6件を隔離し、保存則残差0/211,922 side、全gross差0、最終残量一致を独立確認した。

---

## 2026-08-30 — AI設計判断: 連鎖候補ID不一致時は攻撃量と関係主張を分離する

- 得点差から確定した攻撃量は権威値として保持する。
- 診断用の確定連鎖IDが同じsideの開いている途中候補に一つも一致しない場合、途中候補を
  `resolver_mismatch_before_attack`で未確定終了させる。
- 不一致候補を確定攻撃の訂正先や単一連鎖IDにしない。関係不一致を理由に正確な攻撃量まで捨てない。
- c6全長の1件で、15段候補ID329と得点確定側ID332の不一致を検出した。変更後は未確定終了1件、
  誤関係0、会計独立検査PASSとなった。

---

## 2026-08-30 — user決定: 30本版は暫定学習、100本以上で正式決定する

- 検査に合格した先行30本で、入力項目の死活・重複・局面差・組合せ効果を調べ、暫定の重みを作る。
- 30本版は仕組みと表示の妥当性を確認するための試作とし、正式な重みとは扱わない。
- レビュー動画は30本版の暫定重みで生成し、暫定版であることと学習対象範囲を明示する。
- 100本以上の品質合格データが揃った後、元映像単位の分離、試合ごとの均等化、複数回学習、
  過学習検査、確率の整合性検査を改めて行い、その結果で正式な重みを決定する。
- 30本版の重みを正式版へ自動的に持ち越さない。構造・入力項目・除外理由は再利用できるが、
  数値の採用は100本以上の再学習結果とレビューを根拠に別判断する。
- 保持49本は方式固定後の最終確認用として引き続き未使用、本番設定も変更しない。

---

## 2026-08-30 — user決定: 30本先2セットのレビュー動画は各約20試合ずつ3分割する

- 「20本刻み」は元動画数ではなく試合数を指す。30本先1セットは約58試合になるため、
  第1〜20試合、第21〜40試合、第41〜最終試合を目安に3分割する。
- 既決定の30本先2セットを対象とするため、レビュー成果物は合計6本を想定する。
- 分割位置は必ず試合境界とし、1試合の途中では切らない。実際の総試合数に応じて最終区間を調整する。
- 今回の対象は通常38番と難条件c58で、公式勝敗に結び付いた試合は各51試合。各20+20+11試合へ分ける。
  仮の画面認識番号ではなく、学習・評価と同じ公式試合キーの時刻順を分割根拠にする。
- 各区間の試合番号、元映像上の開始・終了時刻、どの仮重み・学習範囲で生成したかをmanifestへ残す。
- 保存先は`D:\puyo_analyzer\videos\review\`配下の別名ディレクトリとし、元映像・映像のみ版・
  音声付き版を削除または上書きしない。

---

## 2026-08-30 — AI暫定判断: 30本版レビューは非線形B・未調整確率を使う

- 現在盤面のみのAより、未解決の攻撃状況を加えたBが映像単位の独立評価で明確に改善した。
  出来事履歴まで加えたCはBへの追加改善を証明できなかったため、暫定30本では単純なBを選ぶ。
- 確率調整版は確率帯別の一部誤差を改善したが、Brierと高確信逆方向件数が悪化したためレビューには使わない。
  調整版は明示的な検証指定がある場合だけ生成可能とする。
- これは100本以上の正式学習前の仮選択であり、本番設定・正式採用・指標廃止を意味しない。
- 全方式合計8,794行は同一場面の重複を含むため、人向けには選択方式の516行・20動画・38試合を主表示する。
  欠測率が極端に高い項目の相関は少数場面だけの参考値として扱う。

---

## 2026-08-30 — AI設計判断: 正式な試合終了後は勝率表示を保留する

- 予測が保持上限以内に更新されていても、検証済み出来事原本で正式な試合終了が観測された時点から
  次試合の新しい確定予測が得られるまで、直前試合の勝率を表示しない。
- 終了時刻は元映像と同一SHA・同一生成版・完了印一致・意味検査合格の出来事原本だけから取得する。
  検査されていない境界ファイルや仮の試合番号は表示制御へ使わない。
- 旧レビュー成果物は上書きせず保持し、修正版は表示方式v3として別名生成する。
- この変更はレビュー上の表示制御だけで、暫定重み、学習入力、本番設定、保持49本を変更しない。

---

## 2026-08-30 — AI設計判断: 計算不能な指標NaNは0でなく明示的欠測にする

- 指標が局面上の計算不能をNaNで返した場合、値を0へ丸めず`None`へ変換し、左右別の欠測印を立てる。
- 左右差・最大・合計は両側が有限値の場合だけ算出し、片側でも欠測なら派生値も欠測とする。
- 正負の無限値は欠測へ変換せずデータ異常として停止する。
- 欠測が多い指標の採否は、30本暫定分析で欠測率・局面偏り・追加効果を測って判断する。
  欠測を0に見せかけて相関や重みを作らない。

---

## 2026-08-31 — user訂正: レビュー元映像はURLを単一情報源にする

- 先行して候補化した`data/frames/video_c109.mp4`は、元映像が違う可能性があるため採用しない。
- userが次に渡すURLを正とし、タイトル、対戦者、2つの30先を映像内容とメタデータで照合してから処理する。
- c109候補で開始した2区間処理は停止済み。同候補の途中ファイルは削除・上書きせず不採用証跡として保持する。
- URL確定前にレビュー動画を生成しない。既存の誤対象レビュー動画も削除せず、最終成果物としては扱わない。

---

## 2026-08-31 — user決定: 余剰1枠で保持側動画の解析原本を増やす

- 正しいレビュー対象の2並列枠を空けたまま、余剰1枠で未処理動画を1本ずつ全長解析する。
- 追加解析結果は将来の100本以上学習に利用できる候補原本として分離保存し、暫定30本の重み・方式選択・レビュー予測へ混ぜない。
- 最初の対象は固定台帳の保持側`video_35`。元動画を移動・削除・上書きせず、別出力へ現在版の出来事原本を作る。
- 合計の重い並列数は3以下を維持し、正しいレビュー元映像のURL受領後はレビュー処理2枠を優先する。

---

## 2026-08-31 — user確定: レビュー元はc0BQoMJwwQUの30先2セット

- 正しい元URLは`https://www.youtube.com/watch?v=c0BQoMJwwQU&t=1688s`。動画IDが一致する保存済み
  `data/frames/video_zenchi_c0BQoMJwwQU.mp4`を使い、再取得、移動、削除、上書きは行わない。
- URLの`t=1688s`はYouTubeの再生位置であり、対象区間の開始指定とは扱わない。映像全体7033.6秒に
  やまだ対のらすけの30先が2セット収録されていることを、既存の目視証跡で再確認した。
- セット境界は3626.0秒。第1セットは0〜3626.0秒、第2セットは3626.0〜7033.6秒とする。
  3483秒付近の30本到達、結果・再選択画面、3626秒の得点0リセットを根拠とする。
- 2026-08-30節に記載した通常38番・難条件c58というレビュー対象指定は、このURL確定により撤回する。
  分割原則の「各セットを1〜20、21〜40、41〜最終の3本」は維持する。
- レビュー表示は固定30本で仮決定した非線形B・未調整確率を使い、1P勝率グラフと音声を必須とする。
  30本の学習元にこの動画が含まれないことを生成前に検査し、本番設定や正式重みは変更しない。
- 動画成果物は合計6本をDドライブの新規ディレクトリへ保存し、映像のみ版も含めて削除しない。
  公式試合数に応じて各セットの第3部終端だけを調整し、試合途中では分割しない。

---

## 2026-08-31 — user決定: c0BQoMJwwQUは指標再設計後の強化レビューに使う

- c0BQoMJwwQUの30先2セットは、現行30本仮モデルをそのまま見る通常レビューには使わない。
- 先に48本で入力項目の有効性、重複、局面差、組合せ効果、動画の流れから得る情報を再設計し、
  その設計で仮重みを学習し直す。
- 再設計と独立検査を通した後、c0BQoMJwwQUを学習に混ぜず、強化レビュー用の6本を生成する。
- 現在走っている2セットの解析は将来の強化レビュー入力として継続する。旧仮モデルへ自動接続する
  待機処理は予測・レンダ開始前に停止し、途中物の削除・上書きは行わない。
- 20試合単位の3分割、合計6本、音声、1P勝率グラフ、正式試合境界、2秒超の不確実時の線切断、
  学習外映像検査という表示・検収条件は維持する。

---

## 2026-08-31 — user決定: 指標再設計へClaude/Fableの独立意見を組み込む

- Codexが数値根拠付きの再設計案を作り、Claude CodeのFableへ読取専用で独立レビューを依頼する。
- 最初のFable設計レビューは大枠を承認し、48本探索前の前提検査5項目を提示した。
- CodexとFableの意見が一致した技術項目は、事前登録後に実測する。意見が割れた項目は、どちらかを
  権威として採用せず、比較実験で決める。
- Fableの推測はCodexがコードと実データで独立確認する。Fableが書いたことだけを採用根拠にしない。
- Fableは、再設計前の設計担当、48本測定後の分析担当、強化レビュー前の検収担当という節目で使う。
  小さな実装判断ごとには呼ばず、週間枠を重要な判断へ残す。
- 人の価値判断が必要な対立だけをuserへ提示し、統計手順、重複整理、欠測処理などはAI間で詰める。

---

## 2026-08-31 — AI設計判断: 48本比較は事前登録・複合方式・50乱数対照で行う

- 48本の結果を見る前に、候補順、母集団、主評価、局面報告、採否条件を
  `docs/INDICATOR_REDESIGN_PREREGISTRATION_2026-08-31.md`へ固定する。
- 共通行の対応比較だけでなく、候補が値を出せない行は現在盤面方式へ戻す複合方式を全行評価する。
- 最低3種の学習乱数、固定50組の無関係入力、元映像単位95%区間、6分割の再現性を併用する。
- 組合せの一般入口は10映像・100試合・1,000行・試合重み5%以上。特定物理状態だけの候補は
  事前宣言した別トラックで状態内300試合・状態内重み5%以上を要求する。
- 48本の固定分割表を再割当しない。残る49本と強化レビュー映像は候補選択へ使わない。

---

## 2026-08-31 — AI設計判断: 学習用の全試合隔離とリアルタイム表示停止を分離する

- 説明不能着地を一件でも含む試合は、品質の悪い攻撃量を学ばせないため試合全体を学習から除外する。
- ただし試合後に決まるこの印を、試合冒頭のリアルタイム入力可否へ遡って使わない。
- 表示では、説明不能着地を当時初めて観測した通番以後だけ未解決攻撃方式を停止し、現在盤面方式へ戻す。
- 正式試合境界で停止状態を解除する。異常観測前の正常区間を不必要に非表示にしない。
- 既存成果物は変更せず、新しい学習表版を別出力へ生成する。本番設定は変更しない。
# 2026-08-31 — 希少状況は100本以上の学習後へ持ち越す

- user決定: 全消し戦など、固定48本ではデータ数が不足する状況は100本以上の学習後に再検証する。
- 48本では最低母数未満を「問題なし」にせず、未検証として明示する。
- 希少状況の母数不足だけで仮方式全体を止めない。全体成績と十分な母数の局面が必須条件を満たせば、
  制限付きの仮方式として次工程へ進める。
- 100本以上では持ち越し項目を再集計し、本決定前に合否を出す。

---

## 2026-09-01 — user決定: 新判定でset1先頭40試合を2本へ再生成する

- 同時攻撃時の量差が盤面更新まで弱くしか反映されない問題を一般化して修正し、固定48本で再学習する。
- レビュー対象はヤマダ対のらすけ第1セットの先頭40試合とし、1〜20試合、21〜40試合の2本に分ける。
- 旧レビュー動画は上書き・削除せず、Dドライブの新規ディレクトリへ保存する。
- 画面配置は左上にゲーム、右上に大きな勝率、下段全幅に大型グラフとする。
- 進行中の攻撃量は確定残高へ混ぜず、「観測できた攻撃差」という予測入力として扱う。
  この一軸だけの有無を同じ48本・同じ6分割・同じ3乱数で比較し、特定試合の目標勝率は設けない。
- 本番設定は変更せず、100本以上の正式学習まではレビュー専用の暫定方式とする。

---

## 2026-09-01 — AI設計判断: 攻撃差は単調な10%差分補正としてレビューへ進める

- 攻撃差を含むモデル全体の予測値を混ぜる方式は、固定48本平均では改善しても第43試合で攻撃差が逆向きに働いたため不採用。
- 攻撃差軸には正方向の単調制約を課し、同一盤面・同一モデルで攻撃差だけを0にした予測との差分だけを従来予測へ加える。
- 差分100%は高信頼誤判定の条件を満たさない。10%は3乱数、固定6分割、主要局面、確率二乗誤差、
  高信頼誤判定の全条件を満たすため、強化レビュー専用の仮補正率とする。
- 10%と36個境界は固定48本上の探索結果であり、本採用ではない。100本以上の学習で候補値を事前固定して再評価する。
- 第43試合は補正率の選択に使わず、学習外の向き検査にだけ使う。旧成果物は無変更で保持し、新しい予測・動画は別名で保存する。

---

## 2026-09-01 — user決定: 第2試合3分33秒の表示欠陥を修正して40試合を再生成する

- 返し切れない攻撃を認識済みなのに、盤面量条件で攻撃差を評価から除外したまま勝率を表示する状態を修正する。
- 現在の48本学習にない範囲へ攻撃差を外挿せず、安全に数値化できない間は理由付きの判定保留とする。
- 同じヤマダ対のらすけ先頭40試合を1〜20、21〜40の2本へ分け、旧動画を保持したまま別名で再生成する。
- 本番設定は変更せず、100本以上の正式学習時に盤面量境界と補正率を再決定する。

---

## 2026-09-01 — user決定: 未来の勝敗が確定した区間は高勝率を表示する

- 死亡位置の占有だけでは即断せず、従来どおり復帰可能性を時間的に確認する。
- 現在画面の勝ち／負け表示を左右両方で3回連続確認し、直近2秒以内の単独死亡候補と方向が一致すれば、
  統計予測ではなく確定結果として勝者100%・敗者0%を表示する。
- 確定結果は次試合への遷移を観測するまで保持し、そこで必ず破棄する。グラフにも確定終点を表示する。
- 両側候補、死亡側不明、死亡側自身の連鎖中は勝者を推測せず、判定保留を維持する。
- 次試合遷移の証拠は表示リセットと動画分割だけに使い、前試合の勝敗確定には使わない。
- 旧レビュー動画を上書き・削除せず、同じ先頭40試合を20試合×2本の別名動画として再生成する。
- 本番設定と学習重みは変更しない。確定上書きは学習モデルから独立した表示規則とする。

---

## 2026-09-02 — AI安全判断: 攻撃量だけによる0%／100%確定を停止する

- 過去5世代・全57試合の同一監査で、旧v11は攻撃量由来の早期確定119行、うち不利側が連鎖中62行、
  公式勝者と逆方向12行だった。
- 攻撃量、返し量、盤面高さだけでは、落下開始済み量、着地順序、置ける手数、着地後盤面を一意に
  復元できないため、現在の物理証明を勝率0%／100%へ配線しない。
- 0%／100%は現在画面の勝ち・負け表示が左右で同じ方向に成立した場合だけ許可する。
- 攻撃量は統計予測の補助入力としてのみ使い、信用不可時は盤面方式へ戻すか理由付き保留とする。
- 本判断はレビュー表示の安全措置であり、本番設定変更ではない。

## 2026-09-02 — AI品質判断: 一意に帰属できない攻撃確定と着地を学習から隔離する

- 複数連鎖の累積得点、対応する途中連鎖なし、途中確定後の再開は、一つの物理攻撃へ帰属できないため
  「攻撃量0」ではなく「量不明」とする。
- 固定48動画の旧v2再構築では、曖昧な確定1,733件・368,611個分を検出した。
- 着地比較78,008件のうち、直前盤面で純粋なおじゃま増加と一意に対応したものは7,435件。
  直前盤面でない1,745件と、色ぷよ等の同時変化を含む441件、合計11,336個分は隔離する。
- 信用不可は状態表、学習可否、特徴表、学習器、学習外予測へ同じ定義で伝播させ、行単位一致監査を必須化する。
- 23:57に安全側v3で48動画の再構築を開始。旧v2途中成果物は削除せず不採用証跡として保持する。

## 2026-09-02 — AI再評価判断: 攻撃差10%補正を再不採用とする

- 2026-09-01の10%補正採用判断は、攻撃量の品質定義を修正する前の固定48動画に基づく暫定判断だった。
- 曖昧な攻撃確定、不整合な着地、攻防終了後の会計不一致を隔離して再学習した結果、平均対数損失は
  3乱数すべてで改善したが、改善の95%区間上端が0以下にならず、高信頼の重大逆判定も相対10%以内を満たさなかった。
- したがって旧10%補正の採用判断を現在版へ引き継がない。強化レビュー2は、攻撃差を直接加えない
  安全側モデルを使い、攻撃情報を信用できない場面では盤面のみへ戻すか理由付き保留とする。
- 10%補正、36個境界、却下モデルは研究証跡として保持する。100動画以上の正式学習で事前登録した
  候補として再検証するまで、本番・レビューとも再採用しない。
- `src/production_config.py`は変更しない。

## 2026-09-02 — AI品質判断: 左右一致の勝敗ロゴは2フレーム間隔・2回一致で検出する

- 左右の勝ち・負けロゴ同時一致は強い証拠なので、事前の死亡候補なしでも0%／100%表示を許可する。
- 15フレーム間隔では開始位置により12〜13件、5フレーム間隔でも第37試合を1位相で取りこぼした。
- 閾値と2回一致を緩めず、走査間隔だけ2フレームへ短縮する。全57試合13件が2位相で完全一致し、
  公式勝者との方向不一致0、第13試合検出を確認した。
- 旧23件は片側ロゴと死亡候補を組み合わせた別方式の件数であり、新方式の最低件数に流用しない。
- 本判断はレビュー表示専用。本番設定は変更しない。

## 2026-09-03 — AI品質判断: 死亡確認の誤解除を状態遷移として是正する

- `confirmed` は同一試合中stickyとし、正式境界まで新しいcandidateへ降格させない。
- 死亡演出中の傾いた盤面をSTABLE空盤面と誤認した実測例があるため、後続STABLEの死亡セル空化だけを
  生存証拠にせず、own chain開始、NEXT変化、新ツモ開始の因果証拠でのみreleaseする。
- これは既存runとbit-identicalなfeature flag追加ではなく、誤った状態遷移の無条件な正しさ修正である。
  今後の再収集は旧runと結果が変わり得るため、既存成果物を上書きせずbuild/attemptを分離する。
- 0%／100%を許す根拠は従来どおり左右一致の勝敗ロゴだけとし、本番設定は変更しない。

## 2026-09-03 — AI品質判断: 打ち合い物理差を統計特徴として固定48本で再検証する

- Game43で旧モデル約51.6%に対し、eventのroot-familyを独立再生するとP2へ正味108個、初回30個、
  残り78個まで因果的に確定した。一方、正しい13連鎖後盤面は初回着地後も死亡しないため、勝敗確定ではない。
- 凍結盤面への着地、score端数の非継承、同一prefire anchorの重複simulate、関係メタ欠落のfail-openを修正し、
  不明・不連続時は数値を作らずHOLDへ倒す。
- 受け側別の正規化量2列とmissing 2列だけを既存baselineへ追加し、固定48本・既存6-fold・固定3乱数で比較する。
  heldout 57試合とGame43はfit/tune/support選択から除外し、最終評価だけに使う。
- 攻撃量や死亡推定で0%／100%へ上書きせず、既存重み・旧相互交換器・`src/production_config.py`は変更しない。

## 2026-09-03 — AI採否判断: 正味量4列と固定係数残差は不採用、着地後死亡列圧迫へ進む

- baseline+4列の全体再fitはraw全3乱数改善、bootstrap上端0以下、fold安定性を満たさず不採用とする。
- baseline固定・weight=1/intercept=0のlogit残差は3乱数すべてで平均log-loss/Brierが改善したが、
  bootstrap区間が0を跨ぎ、全6fold非悪化を満たさなかったため不採用とする。同じ固定48本を見て
  0.1等の係数探索は行わない。
- first-dropとleftoverは正味量の決定的変換、dead flagは着地後pressureの終端と重複するため特徴へ加えない。
- 次の一候補は、因果的に再構成した初回着地後盤面の死亡列圧迫度を受け側別2列+missing 2列とする。
  隠し段row0を除き、可視12段だけで0〜1化する。静的圧迫は統計特徴に限定し、0%／100%へ接続しない。
- fixed48だけでlabel非参照supportを先に検査する。入口条件を満たす場合だけ固定6-fold・固定3乱数へ進み、
  heldout 57試合とGame43は候補凍結後の最終評価まで使用しない。本番設定は変更しない。

## 2026-09-03 — AI採否判断: 着地後死亡列圧迫を不採用、固定48本での探索を終了する

- 左右missing flagの完全重複は、受け側が既知の曖昧着地では受け側だけmissing、非受け側は既知0とする
  意味修正で解消した。修正理由は勝敗ラベルや学習結果ではなく独立コードレビューで事前に確定した。
- 修正版v2でも全3乱数でraw/calibratedのlog-lossとBrierが悪化し、raw改善foldは1/6、1/6、2/6だった。
  support、左右方向、成果物完全性はPASSしたが性能gateはFAILしたため、特徴候補として不採用とする。
- fixed48では正味量全体再fit、固定weight=1残差、着地後圧迫までを評価済みである。これ以上の特徴、係数、
  cap、閾値探索は選択バイアスになるため行わず、heldout 57試合とGame43も使用しない。
- 物理再構成、完全盤面、残り予定量、provenance/hash、fail-closedは本格学習用の入力基盤として保持する。
  未較正中は既存確率を更新せず、受け側・初回30・残り78等の診断値だけを表示できる。
- 100本超の正式候補は、連鎖解決後・初回着地後の完全盤面と残り着地予定を入力する
  projected-state専用モデルとし、特徴選択前に仕様とgateを固定する。合格時だけheldoutを一度評価する。
- 0%／100%は左右一致の勝敗ロゴだけ。本番設定、既存重み、旧相互交換器は変更しない。

## 2026-09-04 — AI品質判断: W36/W37再基準化をPhase J入口条件PASSとして確定する

- 正本8動画を現在の本番認識構成、`gravity_settle=true`、`slide_override_ojama_hold=true`、
  `game_event_chain_exit=true`で別名再測定し、入力8/8、holdout `v89,v97`、入力/source SHAを固定した。
- 試合外W37区間を除いた整数集計はoverall **99.92488%**、holdout **99.92739%**、
  動画別最低v51 **99.77004%**で、全8動画が99.5%以上。全動画でspan、score観測、台帳が存在し、
  raw/試合内/除外のセル数・一致数保存則は残差0だった。
- この値は人手真値ではなく、CNN・HSV・confirmedの三者一致による「本番構成・試合内区間の
  認識精度代理」である。旧Phase I 99.54%とは母集団と測定構成が違うため単純比較しない。
- 専用判定PASSをPhase J入口条件4の根拠とする。旧`verdict=FAIL`とexit 1は未補正母集団の
  non-STABLE/corruption診断を後方互換で維持した別系統であり、撤回も入口条件への混入もしない。
- Phase Jの許可範囲は、Fable P1×2を閉じた後の合成fixture J0/J1まで。本番入力接続、正式beta、
  `src/production_config.py`採用は、terminalリアルタイム検収と100本超正式モデルの完了後に別判断する。
- 証跡は`data/verify/w36_w37_phase_j_rebaseline_2026-09-03_v1/manifest.json`、
  `result.json`、`receipt.json`。既存成果物と本番設定は変更しない。

## 2026-09-04 — AI設計判断: Phase J仕様v1.0を合成J1実装の正本とする

- Claude/Fable反対査読で得たP1 2件、P2 10件を、仕様、snapshot Schema、意味規則、
  canonical 8例、既存48 test IDへ反映した。
- Schema契約27件とcanonical snapshot 8件はPASS。Fable独立再検収はGO、
  P2 10件closed、新規P0/P1なし、J1開始可と判定した。
- `hold_elapsed_ms`は未実装のDTO v1契約に必須fieldとして固定し、
  hold開始の映像時刻とprocess monotonic由来の経過時間を混ぜない。
- 許可範囲は動画・GPU・HTTP・本番入力を使わないcontracts/validator/reducer/projectorのJ1まで。
  terminal実機検収と100本超正式モデル前の本番接続・beta採用・本番設定変更は行わない。

## 2026-09-04 — AI品質判断: Phase J J1を完了し、J2合成実装へ進む

- DTO、validator、純粋reducer、display projectorを実装し、Schema・相関制約・境界・hold・capture session・
  fail-closed・左右対称を合成fixtureで固定した。Phase J統合439件、全pytest 7,586件がPASSした。
- Claude/Fable実装査読のP1は、hold中commit、intermission/fault latch、新capture session、PRE_MATCH表示、
  hold解除時の物理予測復帰を局所修正して全て閉じた。最終判定はGO、残P0/P1なし、J1完了可。
- J1未実装のterminal/worker eventは黙殺せず明示rejectする。capture buffer置換数、lease、stall、profile、
  backend failoverはJ4で同じtest IDを継続し、延期を完了扱いにしない。
- J2はschedulerとSnapshotHubの合成実装から始める。HTTP、本番入力、動画/GPU、`src/production_config.py`は
  変更せず、terminal実機検収と100本超正式モデル完了前の本番接続禁止を維持する。

## 2026-09-04 — AI品質判断: J2 scheduler／SnapshotHubコアを採用する

- schedulerはpractical/best-actionをlane分離し、running 1件＋waiting最新1件、token全項目、deadline、
  worker restart、境界後resultをfail-closedで検査する。2Hz集約はpractical専用でbest-actionを阻害しない。
- SnapshotHubはstream_seq採番、latest保存、subscriber通知、subscribe時の現在値投入を同じlock内で行い、
  subscriberは1件bufferのlatest-winsとする。legacyの16件FIFOと満杯時latest dropは流用しない。
- Fableが指摘した共用2Hz枠と不正future revision高水位汚染を修正した。最終検収GO、残P0/P1なし。
  J2新規試験込みの全pytestは7,648件PASS。
- SingleWriter runtime、worker supervisor、transport、cache/loopback/timeout、実worker prewarmは未完了。
  本番HTTP、入力、動画/GPU、production configには接続しない。

## 2026-09-04 — AI品質判断: J2 SingleWriter／pure worker／transport純粋層を採用する

- reducer、scheduler、projector、Hubの更新とpublishは`SingleWriterRuntime`の一つのlockで直列化する。
  worker結果はschedulerの全token・deadline・health検査とpractical 2Hz gateを必ず通し、
  `apply_event`への直接`PREDICTION_COMPLETED`は`RESULT_REQUIRES_COMMIT_GATE`で拒否する。
- job context不足または期限切れではreducerを再試行可能な位置へ戻す一方、旧running/waitingと数値pendingを
  全無効化する。fail-closed latchは新しいtrusted observationからjob開始できるまで旧LIVE復活を禁止する。
- 実行中runtimeの`NEW_SESSION`は`SESSION_ROTATION_REQUIRED`で拒否して恒久hiddenとし、
  session変更は新しいRuntime+Hubの構築だけで行う。固定session Hubとの暗黙分裂を正常扱いしない。
- schedulerはRESTARTINGを既存理由、STARTING/DEGRADED/DISABLEDを`WORKER_UNHEALTHY`で拒否する。
  pure supervisorはhard deadline、cancel、grace後terminate/spawn、prewarm、新worker tokenを副作用intentで表す。
- v1 transport純粋層は4 route、canonical JSON/SSE、heartbeat、cache規則を固定し、bindは仕様・Health Schemaどおり
  `127.0.0.1`だけを許可する。legacy payloadと混在させない。
- Claude/Fableと検収レビュアの最終判定はGO、残P0/P1なし。合同443件、全pytest 7,729件PASS。
  実listener、実worker process、GPU、動画、本番入力、`src/production_config.py`は未接続・未変更。

## 2026-09-04 — user決定: 返し切れない予告はactive chain後の着弾盤面で評価する

- 予告おじゃまを受けている側が連鎖を発火し、相殺後も同じ側へ正味量が残り、初回着弾まで
  追加設置できない場合は、発火前の凍結盤面ではなく連鎖終了後＋初回着弾後の投影盤面を使う。
- 既存root-family、相互交換、仮想着弾を再利用し、新しい物理会計系統を増やさない。
  投影盤面は`confirmed_board`へ書き戻さず、`physics_projected`由来の別入力とする。
- 6個単位の端数列はランダムなので候補集合として扱い、単一疑似seed盤面を確定値と呼ばない。
  30個超は初回着弾後盤面とleftoverへ分離し、後続分を一括着弾させない。
- 正式モデル未較正中は物理診断だけを出し、勝率を更新しない。死亡予測でも0%／100%にせず、
  左右一致の勝敗ロゴだけをterminal factとする。
- 詳細は`docs/PHASE_J_GUARANTEED_LANDING_PROJECTION_ADDENDUM_2026-09-04.md`。
  Formal100正式評価、Phase J本番接続、`src/production_config.py`は未変更。

## 2026-09-04 — AI設計判断: 着弾投影の適用範囲と端数時fallbackを固定する

- `guaranteed_landing_after_active_chain`は、発火中の連鎖を持つ側自身に返し切れない予告が残り、
  初回着弾まで追加設置できない場合だけ成立させる。通常の非発火側送付予測とは分離する。
- 初回着弾が6の倍数でない場合は列が一意でないため、全候補を列挙し、単一疑似seed由来の
  死亡・圧迫度を正式入力や表示へ出さない。
- 端数列の曖昧さだけでは新しいHOLDを増やさない。現在状態baselineが利用可能なら数値を維持し、
  `landing_columns_ambiguous`を診断表示する。Formal100でもbaseline fallbackとし分母から除外しない。
- 現在状態自体、anchor、順序、rate、hashが信用不能な場合は従来の安全HOLDを維持する。
  本判断は仕様整合の修正であり、本番設定・既存重みは変更しない。

## 2026-09-04 — AI設計判断: 正式projected-state入力は独立DTOとする

- 監査用`ReviewPhysicalProjection`は縮約値であり、両側の完全盤面と候補集合を保持しないため、
  正式学習入力DTOとして流用しない。
- 独立DTOはidentity/cutoff、asset hash、三状態gate、0/missingを分けた量、両側の現在／連鎖後盤面、
  最大20件の初回着弾候補を持つ。offlineとPhase Jは同じpure adapterとcanonical digestを使う。
- 保証gateにはactive chainだけでなく、chain開始直前に同じ側へ確定予告が1個以上あったこと、
  解決後も受け側が同じこと、root/cutoff/boundary/hashが信用できることを要求する。
- pilot比較後は各seedの全pilot fitモデルとpilot OOFだけでfitした較正器を凍結し、reserveへ一回適用する。
  reserveまたはfull-fitのin-sample予測で較正器をfitしない。

## 2026-09-04 — AI実装判断: projected-stateは確定event prefixから一度だけ導出する

- offlineとPhase Jは同一の`ProjectedStateObservationAdapter`を使い、root-family、交換残量、
  発火前予告、現在/連鎖後/初回着弾後盤面を同じcommitted event prefixから作る。
  Phase J側だけ認識flagや別のcacheからflat stateを合成しない。
- 正式`boundary_segment`は`match_boundary_evidence`だけで進め、root内部のgarbage expiry epochと分離する。
  境界batchは状態をresetするがDTOは発行しない。両側が既知の非発火ならDTO自体を発行せず、
  欠測flagだけを非発火とみなさない。
- Adapterの通常経路ではRoot全履歴copyを行わない。小型stateをstagingし、予期しない例外時のみ
  成功済みprefixからRootを再構築する。再構築自体が失敗した場合だけpoisonし、同一batchの
  再試行とfresh replayのbit-identical性を回帰で保証する。
- この入力基盤だけで勝率は変更しない。pilot48は構造・欠測・左右対称・速度の固定に限定し、
  reserveの勝敗評価はモデル・較正・入力hashの事前凍結後の一回だけとする。

## 2026-09-04 — AI設計判断: projected-set residual CNN v1.1を実装正本として凍結する

- 一つのprojected observationを一学習単位とし、最大20件の端数列候補を独立行へ増幅しない。
  各候補を共有位置保持CNNで評価し、辞書順・float64の等確率単純平均で一つの勝率にする。
- baseline logitへの残差だけを学び、zero counterfactualと左右交換差で反対称化する。
  `EPS=1e-6`、非有限値拒否、対称Plattの切片0をfit前に固定した。
- candidate固有のunsupportedは正常なbaselineへbit-identicalに戻す。baseline欠測・非有限、
  source/cutoff/hash/順序/current-state integrity異常、モデル非有限出力はHOLD/FAULTとしfallbackで隠さない。
- 端数候補は正式採用前だけbaseline＋診断badge、採用後は全候補平均＋同じbadgeとする。
  この段階差をPhase J親仕様、着弾追補、Formal100事前登録へ同期した。
- 補助正解は死亡済みlandingでも意味が変わらない盤面直読8指標だけに限定した。
  UNKNOWNは全8指標で非占有、missing boardはmask=false。補助出力を推論勝率へ使わない。
- 独立設計再検収はP0/P1/P2=0でGO。勝敗label、reserve、外部holdout、本番設定は未接触・未変更。

## 2026-09-04 — AI実装判断: projected-set OOFは全母集団・台帳・weightをreceiptに結合する

- OOF metadataは完走projected datasetの全行・同一順序とし、input digestの集合と順序、
  source数、manifest/observation hashが一つでも違えば実行を拒否する。
- 主評価条件は`baseline_predictable`、`winner_label_present`、`formal_game_key_present`の
  3 flagで凍結し、projected unsupportedも評価行から除外しない。supportedだけモデル予測へ
  差し替え、unsupportedはbaseline raw/calibratedをbit-identicalで複写する。
- `EVENT_PILOT_2026-08-28.tsv`の全pilot sourceとmetadata sourceは完全一致を要求し、
  `source_group_id`/foldを照合する。1 source丸ごと欠落、架空group、重複台帳を拒否する。
- 学習weightは正確なfull-fit母集団で事前計算し、epochごとのglobal index・digest・weightを
  同一permutationで渡す。indexだけshuffleしweightの対応をずらす実装を禁止する。
- CPU/CUDA間のbit一致は実機で成立しないため、各device内の完全再現とdevice間
  `max abs <= 5e-5`、`relative norm <= 5e-6`に凍結する。実測はそれぞれ約`1.20e-5`、`1.08e-6`。
- active chain後の残予告が次ツモ設置より先に初回着弾する構造規則を専用gateとする。
  active side/recipient不一致、残量非正、判定不能は`first_drop_timing_untrusted`で
  `guaranteed`を拒否し、時間閾値や未来の実着弾を使わない。
- 必須21資産（着弾追補仕様を含む）、metadata SHA、plan SHA、dataset receipt、seed/決定性設定を実行receiptに保存する。
  Claude Sonnet二段階検収はGO、P0/P1なし。勝敗label、reserve、外部holdout、本番設定は未使用・未変更。

## 2026-09-04 — user決定: 組ぷよK1〜K4近未来火力を複数系列で実測する

- Kは単ぷよ数でなく組ぷよ数とする。K1〜K4は1〜4組（2〜8個）を置いた先の火力であり、
  現行`current_max_chain`の単ぷよ1個追加とは別の観測軸とする。
- 全理想組ぷよ、実NEXT/DNEXT、実ツモ後を理想組ぷよで補う混合、平均ツモの各系列を比較対象にする。
  理想色の探索候補はSTABLE確定盤面に存在する色に限定する。
- 色ぷよ量、盤面おじゃま量、空き容量、局面により有効なKと系列が変わる可能性があるため、
  最初から一つへ絞らず実測で決める。単独採用だけでなく、複数K・複数系列の並列採用も許容する。
- 並列採用は相関だけで棄却せず、同一video GroupKFold、未使用動画、条件付き追加効果、
  grouped ablation、family単位Permutation、較正、重大誤り、計算時間で過大計上を検査する。
- 既存`near_future_fire_k*`の理想部分は単ぷよ単位で今回の定義と異なるため、過去AUCを流用しない。
  既存の配置列挙・simulator・ビーム探索・期待火力計算は再利用し、K別checkpointでコストを共有する。
- 詳細タスクは`docs/agent_coordination/PLAN.md`の
  「組ぷよK1〜K4近未来火力ファミリーの実測」に記録した。現時点で本番指標・重み・
  `src/production_config.py`は変更しない。

## 2026-09-04 — AI再監査: 組ぷよ近未来火力の初稿はP0修正まで実装しない

- user決定の「K=組ぷよ数」「複数方式を実測」「並列採用も許容」は維持する。
- 初稿の「4系列×K1〜K4」は誤り。実ツモはNEXT/DNEXTが見えるK1/K2だけで、混合K1/K2は
  実ツモと完全重複する。存在しない比較セルと重複列を作らない。
- 現在盤面に見える色だけでは、試合で使われるactive 4色を表せない。過去検証でも約27%不一致し、
  中盤AUCが0.776から0.664へ低下した既知事故がある。現在盤面色限定は近似比較へ格下げし、
  正本候補を因果的な試合別active 4色とする。この変更はuserとの一問一答で確定する。
- K内最大単発、K手目まで発火禁止の本線成長、複数発火の累積攻撃を分離する。理想値、
  実ツモ到達値、未知未来を見ない期待分布、全知上限も同じ意味の列として扱わない。
- 現行ビームは途中得点だけの枝刈りで無発火の良形を落とすため、K1完全探索、K2層別完全探索、
  K3/K4幅感度を通すまで「最大火力」と呼ばない。
- `/72`クランプは既存実ツモK1で9.1%、K2で20.6%が既に飽和している。rawを保存し、
  非飽和正規化を比較する。平均ツモは各試行をおじゃま換算後に集計し、未来情報を見ない。
- 既存実ツモK1/K2は`current_max_chain`単独に対しAUC +0.00295の信号があるが、現行47特徴への
  追加効果は未測定。既存理想単ぷよK間はPearson 0.984〜0.998であり、並列採用を当然視しない。
- 改稿タスクは`docs/agent_coordination/PLAN.md`の
  「組ぷよ近未来火力曲線の構成概念検証」とする。本番設定・既存重み・採用フラグは変更しない。

## 2026-09-04 — user決定: 仮想的な複数発火の累積攻撃は候補から外す

- K組以内に複数回発火させた累積攻撃は、現在のフィールドや確定時系列から直接追える盤面能力よりも、
  仮想的な発火後方策の影響が大きいため近未来火力候補へ入れない。
- K組以内の最大単発攻撃と、途中発火を禁止して本線をK組伸ばした火力は分けて検証を継続する。
- 新しい候補は、STABLE確定盤面、実NEXT/DNEXT、過去の確定時系列から根拠を追跡できるものに限定する。
- 本番指標、既存重み、`src/production_config.py`は変更しない。

## 2026-09-04 — user決定: 火力成長・停滞は状況との組合せで評価する

- 過去のSTABLE確定盤面と確定手数から、直近N手の火力増分/手数と、火力が伸びていない
  連続手数を時系列指標候補へ追加する。未来盤面・試合後情報は使わず、試合境界でresetする。
- この指標は単独で強いことを前提にしない。色ぷよ量、盤面おじゃま量、空き容量、現在火力、
  局面、ExchangeEpisode状態、実NEXT/DNEXT可用性と組み合わさった時の条件付き効果を重視する。
- 単独AUCは診断として報告するが、それだけで棄却しない。context込み構成へのblock追加・block除外、
  grouped OOF、局面別効果、未使用動画での再現により採否を決める。
- 組合せは非線形モデルへ基礎量を渡して学習させる方式を基本とし、明示interactionは少数を事前固定する。
  全組合せを後から総当たりして最良値だけを採用しない。
- 本番指標、既存重み、`src/production_config.py`は変更しない。

## 2026-09-04 — user決定: 組ぷよ探索色は因果的に判明したactive 4色を使う

- 理想組ぷよ・期待火力の色候補は、現在盤面に残っている色だけに限定しない。
- その試合で使用されている色を、現在までのSTABLE確定盤面、認識済みNEXT/DNEXT、
  過去の確定観測から因果的に集約し、判明したactive 4色を正本とする。
- 現在盤面から消えているactive色も次ツモ候補に含める。未来の観測を過去へ戻して色集合を補完しない。
- active色が未確定の場合は欠測または時点内で成立する候補集合として扱い、5色fallbackや
  現在盤面色だけの値を正本値として黙って出さない。現在盤面色限定は近似誤差の監査用途に限る。
- 色数は最大4のままなので、探索本体の計算量は増やさない。本番設定・既存重みは変更しない。

## 2026-09-04 — user決定: 火力成長の主時間軸は直近2・4・6手とする

- 火力増分/手数と停滞手数は、確定`tsumo_count`に基づく直近2手・4手・6手を主窓とする。
- 秒基準は操作速度、映像間隔、連鎖・落下演出時間が混ざるため、主指標でなく補助診断にする。
- 試合境界で履歴をresetし、実発火・おじゃま落下による盤面値の段差を通常の積み成長・停滞と
  同じ変化として扱わない。未来のSTABLE盤面を現在値へ戻して使わない。
- 本番指標、既存重み、`src/production_config.py`は変更しない。

---

## 2026-09-04 — user決定: 個別指標の細分化を止め、有利不利入力をプロジェクト思想から全体再設計する

- 指標を一本ずつ人手で採否し、固定interactionや固定重みを増やす進め方を停止する。
- 人間の試合知見は「何を観測・検証するか」と物理制約の定義に使う。
  どの局面で、どの観測familyやK/N horizonが重要かは、最終勝敗と実際の短期未来から学習させる。
- 次期本線は、STABLE確定盤面を直接読む左右共有CNN、因果的な未解決攻撃会計、
  有限候補のprojected-state expert、将来事実を教師にするauxiliary headを分離して統合する。
- 旧47列は凍結本番baseline/fallbackとして保持する。後発52列と299/337/338列系は、
  手作り派生と機械展開を含む診断baselineへ降格し、新本線の土台にしない。
- 盤面から再計算できる`diff/max/sum/ratio`、手作り積、累積score、全試合累積event count、
  品質・実装由来shortcutを新本線の直接入力にしない。
- 既存指標は、機能familyごとのauxiliary target、説明、監査、再設計候補へ移す。
  補助予測を推論時の勝率入力へ折り返さず、相関指標の数だけfamily重みを増やさない。
- 組ぷよK1〜K4と直近N手は一つのmulti-horizon search familyへ再配置する。
  scenario×horizonの探索候補・分布を保存し、K/N別の手作り勝率重みや大量の派生列を作らない。
- 詳細設計の正本を`docs/ADVANTAGE_INPUT_ARCHITECTURE_REDESIGN_2026-09-04.md`とする。
- 本決定は設計と移行順序の固定であり、既存学習済み重み、本番設定、
  `src/production_config.py`をまだ変更しない。

## 2026-09-04 — user決定: 有限データ期は人間知見をpriorとして併用する

- 膨大な実戦サンプルが揃うまでは、データだけで戦略価値を完全には決められない前提に改める。
- 人間知見は、ゲーム物理のhard contract、architectureの構造、物理的に明白な弱い順序prior、
  疎な領域の縮約/HOLD/fallbackへ限定して使う。
- 高さ、色ぷよ量、現在火力、連結、K/N探索値など、局面により意味が反転し得る量へ固定符号・
  固定勝率重みを与えない。これらは観測候補、補助教師、条件付き仮説として扱う。
- weak priorは命題、適用条件、反例、強度、再評価条件を台帳化し、priorなしと同一foldで比較する。
  supportが増えた領域では弱めるか外し、hidden reserveを見て追加・強化しない。
- 本補正でも既存学習済み重み、本番設定、`src/production_config.py`は変更しない。

## 2026-09-04 — user決定: 既存指標を資産として保持し、撃ち合い計測を継続する

- これまでの指標実装・定義・測定結果は削除しない。旧47列はbit-identical baseline/fallback、
  52列と299/337/338列系は診断baseline、各指標はauxiliary target・説明・監査資産として保持する。
- 指標が新本線の直接勝率入力でないことは、指標自体の廃止を意味しない。再計算可能性と過去比較を守る。
- 撃ち合いは単一の因果exchange ledgerとしてraw量、availability、provenanceを継続計測する。
- 旧capped/log/diff/synced/interactionや固定勝率配点は診断へ残すが、新本線へ固定重みで混ぜない。
- `CanonicalObservationV2`、共通prefix adapter、旧5 schemaの全列registry、prior台帳をP0正本とする。
- P0実装だけを理由に既存重み、採用フラグ、`src/production_config.py`を変更しない。

## 2026-09-04 — 実装決定: P0 canonical契約を固定しP1学習比較の前提とする

- canonical値は`known/known_zero/unknown/unsupported/integrity_fault`を別状態とし、欠測を0で黙って埋めない。
- 学習・本番は同じ`canonical_observation_from_committed_prefix`を共有する。未来event、posthoc、
  非STABLE盤面、semantic hash不一致を受け入れない。
- side swap規則は列ごとに登録する。eventのp1/p2は交換、diffは反転、max/sumは不変。
  legacy side-rowは再計算し、`color_diff_x_ojama_diff`は不変とする。
- weak priorは命題・適用条件・反例・解除条件を台帳化し、係数を発明せず事前登録後に同一foldで比較する。
- P0完了前提で次はM0/M1の実装・比較へ進むが、P1 gate前にFormal100/hidden reserveを見ない。

## 2026-09-04 — M0 50動画prototypeは説明指標を資産化し、勝率本番採用はM1後に判断

- M0はraw STABLE盤面とNEXT/DNEXTだけを読み、累積score、選手ID、品質・mechanism shortcut、
  手作りdiff/ratio/interactionを入力しない。
- ぷよ色の名称は戦略価値を持たないため、全色置換不変性をarchitectureのhard contractにする。
  左右交換反対称、位置保持、左右共有encoderも維持する。
- 学習外10sourceで較正後log-lossが0.5固定を上回り、V1からsource対応改善したV3を
  次のM1比較baselineとする。ただし30先動画の序中盤AUCが0.5未満のため本番採用しない。
- 学習由来のregion/queue指標は0〜1・0.5中立・左右交換補数で保存し、説明・auxiliary target・
  監査へ使う。同じ勝率modelの直接入力へ自己帰還させない。既存45指標も削除しない。
- 下段、窒息列、NEXT等の価値は局面で変わり得るため固定符号を与えない。source横断と局面別で扱う。
- 次は単一causal exchange ledger branchを追加したM1を同じ分割で比較する。正式100本以上の学習、
  既存重み、本番フラグ、`src/production_config.py`の変更はM1 gateとユーザーレビュー後とする。

## 2026-09-05 — M1次候補はvalues+availability maskの3 seed等重み、まだ本番採用しない

- M1の採否単位はseed単体ではなく、既存慣例どおり固定3 seedの状態別確率を等重み平均したものとする。
  seed選別、重み最適化、reserve参照は行わない。
- 46-source開発集合では`values_and_masks`がM0とarchitecture-matched random controlの双方を改善し、
  6/6 fold勝ち、winner<10%新規0、winner<20%総数非劣化、大量被弾3層改善を満たしたため次候補に固定する。
- 値のみとmaskのみは診断baselineとして保持し、勝率本線候補にはしない。既存指標もauxiliary/explanation/audit資産として残す。
- winner<10%の残存3 episodeはledger unusableのM0 fallbackなので、M1の手作り補正では直さない。
  c76の11.22%と20% crossing、c8の20% crossingを含む5区間を真のOOF映像で確認し、
  M0側の不足とM1側の過小評価を分離する。
- Formal100は5区間レビューと式凍結後に一度だけ開く。本番設定・既存重み・`src/production_config.py`は
  Formal100 gateとユーザー承認を通るまで変更しない。

## 2026-09-05 — M1レビューの死亡HOLDは通常値へ誤解除せず、監査用参考値を分離表示する

- 疎な真OOF予測行の静的死亡候補を表示gateとして使わない。全source stateから独立した
  `display_gate.parquet`を作り、表示時点以前・同一game・同一generationの最新行だけを使う。
- 死亡候補は候補side本人の連鎖だけで解除する。相手側の連鎖中を理由に候補を解除しない。
- frame/ms、source/build、game/generation、死亡セル既知性、A入力、SHA、staleness、terminalの
  どれかが不明・不一致ならfail-closedとし、攻撃会計やintegrity由来のHOLDを弱めない。
- c8は現行列だけでは候補側2Pの早期生存を確定できず、6.333秒の物理HOLDが残る。
  通常勝率へのtimeout解除は行わず、レビュー中だけ灰色の`未確認参考値`としてモデル出力を表示する。
- 参考値には`HOLD: 死亡未確認`、候補side、gate時刻、ageを必須表示し、通常バー、graph、
  急変判定、gate合格集計、0%/100%、terminal判定へ使わない。ledger unusableは`M0 fallback`を明示する。
- この参考表示はM1真OOFの人間監査専用であり、Phase J本番へ流用しない。正式V5 death observerまたは
  同等live causal stateへ置換するまで、死亡gate解決済みとは扱わない。
- 実装と動画化だけでFormal100、hidden reserve、本番設定、既存重みを変更しない。

## 2026-09-05 — 6.333秒死亡HOLDの参考表示案を因果release状態機械で置換

- 直前決定の灰色参考表示は安全な中間案として証跡保存するが、c8に6.333秒の物理HOLDを残すため
  最終レビュー正本にはしない。静的死亡候補をそのままpendingへ写す設計を撤回する。
- レビュー用display gateはside別`clear/pending/released`状態とし、候補より後の有効NEXT変化、
  新ツモ、候補side本人の連鎖開始だけを生存release証拠として採用する。
- `None→valid NEXT`、相手連鎖、死亡セル空化、timeout、future情報はrelease証拠にしない。
  候補行のvalid NEXTは新baselineとし、同一行eventは旧pendingのrelease後にpost-event盤面を再armする。
- 時刻逆行、counter逆行・+2以上の飛び、部分NEXT、未知死亡セル、source/game/generation/hash不一致は
  fail-closed。fault理由はsidecarに保持し、画面へ内部コードを露出しない。
- c8実測はG1 1.034秒、連続G2/G3 0.434秒。5レビュー窓の死亡pending最大は1.034秒、
  integrity faultを含む連続非表示最大も1.100秒で、1.5秒超0件。真死亡握り潰し反例0件。
- 最終予測正本を`advantage_m1_true_oof_review_v5_3seed_finalized_primary6`、最終動画を
  `m1_v3_3seed_true_oof_2026-09-05_v6`とする。旧V1〜V5は上書き・削除しない。
- この決定はレビュー表示の是正であり、Phase J本番採用ではない。正式live death observerとの同値検証、
  最終V6のユーザー目視、Formal100 gateとユーザー承認までproduction configと既存重みを変更しない。

## 2026-09-05 — M2 V3現行出力は重大誤判定gateで不採用、安全縮約は比較器に限定

- M2 V3 aux-onは46 source・3 seedでM1より平均log-loss/AUC/Brierを改善したが、
  winner<10%を25→28、winner<20%を283→367へ増やし、既存例の解消は0だった。
- 新規20%未満84/84行は、M1も誤方向だった状態で同じ方向へconfidenceだけを拡大した例。
  符号反転や正方向から誤方向への反転ではなく、無制約残差加算のtail calibration不良と判断する。
- 平均性能だけで採用しない。aux-off/onとも現行出力は本命・本番NO-GOとする。
- material/spatial 8補助教師はMSEを`0.170112→0.001488`へ改善したが、aux-off比の主評価追加効果は
  未確定なので、係数0.3や現行二段階構成を固定採用しない。補助教師資産は保持する。
- `abs(logit(M2)) <= abs(logit(M1))`のconfidence-safe版は、ラベル非依存で重大誤判定数をM1以下に
  保ち、全6 foldで小幅改善したため開発比較器として保持する。ただし既存極端例を直さず本命採用しない。
- 外部20試合で旧基礎BがM1を上回ったため、次は旧基礎Bを同じ46 source/state/foldへ再現して直接比較する。
  旧勝率の教師化、固定10%混合、旧固定配点の復活は行わない。
- 物理的に勝敗確定した局面はterminal/outcome proofのhard contractで処理し、統計モデルの過度な
  confidence拡大で代用しない。
- この判断でFormal100、hidden reserve、既存重み、`src/production_config.py`を変更しない。

## 2026-09-05 — 旧308列anchorは診断基準、ledger残差は棄却、次auxは火力・着火を優先

- 旧308列anchorを46 source・224,011同一state・同一6 foldsへ移植した結果、平均性能はM1を
  上回ったが、winner<10%が942行、<20%が3,677行ある。旧308列全体を本線へ戻さず、
  family発見と次設計の診断anchorに限定する。
- 旧sample weightはcanonical M2 weightと一致しないため使用しない。state ID、label、fold、
  source/build receiptをfail-closedで照合し、学習・評価はM2 canonical weightだけを使う。
- ledger values/masks/values+maskは平均log-lossを小幅改善したが、重大誤判定を増やし、
  random controlに対する改善CIも0を跨ぐ。ledger固有信号は未確定として全案を棄却する。
- ledger unusableとepoch 0はanchor確率へのbit-identical fallbackを必須とし、integrity faultは
  fallbackせずHOLD/FAULTとする。この区別を今後のモデルでも維持する。
- shortcutを除くfamily permutationの第一候補を`firepower_and_ignition`、第二候補を
  `connectivity_and_shape`とする。既存material/spatial 8や直接入力済みqueueは重複aux化せず、
  leave-one-outまたはconditional studyなしに追加しない。all-clearも現時点では昇格させない。
- 次のM2は`baseline.logit + utility差`の無制約加算を使わず、M1 exact fallbackを備えた
  bounded/non-expanding残差とする。火力・着火、接続・形の順に同一foldで比較し、
  winner<10%新規0、<20%非増加を採否gateとして維持する。
- Formal100、hidden reserve、既存重み、`src/production_config.py`は変更しない。

## 2026-09-06 — bounded shrinkは診断保持、火力・接続auxiliary familyは全案棄却

- M1の符号を保ち絶対logitを最大50%だけ縮める構造は、重大誤方向を増やさない安全契約として保持する。
- ledger unsupportedはM1へbit-identical fallback、integrity faultはfail-closed、epoch 0はM1完全同値とする。
- `firepower_and_ignition`と`connectivity_and_shape`はM1比で小幅改善しても、aux-offおよび
  architecture-matched randomに対するseed横断の固有効果がないため採用しない。
- M1向け平均gateの通過だけでauxiliary教師を採用しない。aux-off差、matched random差、fold/source、
  seed安定性、重要層の退行を同時に満たすことを必須とする。
- 今回の改善は補助教師ではなくconfidence shrink由来と扱う。非凸なrepresentation学習は固定せず、
  次は凍結M1 logitと因果的適用条件だけを使う決定論的/凸なcross-fitted shrinkを比較する。
- GPU batch差による浮動小数点差をbit契約へ混ぜない。派生modelの安全契約は同一checkpoint、device、
  batchで再構築したexact anchorと比較し、公式保存値との差は別の再現性監査として記録する。
- bounded M2も本番候補へ昇格しない。Formal100、hidden reserve、既存重み、production configは変更しない。

## 2026-09-06 — deterministic convex shrinkも棄却し、同一開発集合での追加探索を停止

- 凍結M1 3seed ensembleに対するseedなし・凸な0.5〜1.0 scaleでも、global/phase/pressure/直積の
  どれもfold、source bootstrap、global差を同時に通過しなかったため本番採用しない。
- non-expanding、sign-preserving、ledger unusable exact fallbackは安全契約として有効だが、
  それだけではM1が正しい大量被弾局面の確信低下を防げない。
- 平均log-lossだけを根拠にpressure3を選ばない。5/6 foldだけを根拠にphase3や直積9層も選ばない。
- 結果を見た後にincoming閾値、盤面層、scale下限、最小cell行数を同じ46 sourceで追加調整しない。
  V4/V5はnegative resultを含む診断資産として固定する。
- M2微調整を止め、terminal/outcome proofの物理hard contractと正式live death observerの
  train-serving parityへ戻る。Formal100、hidden reserve、本番設定、既存重みは変更しない。

## 2026-09-14 — Cドライブ容量方針: 「削除しない」から「D退避を既定、削除は再生成可能物のみ」へ

- 経緯: C 935GB 中 空き 5.1GB。長時間ジョブが即死する水準だった。
- user裁定 (19:33): 旧複製 124.66GB は削除、`data/viz`・`data/match_clips_viz` は D 退避、
  **`data/frames` (元動画143本) は Codex の現行ジョブ完了後に `D:\puyo_analyzer\videos\frames` へ移動し
  `data/frames` をジャンクションにする**。CLAUDE.md「処理後削除」と Codex 9/1「移動・削除しない」の衝突は
  「移動 (パス不変・証跡保持)」で解消する。
- 原則: `data/` `logs/` `models/` は git 復元不可 (99.97% が ignored) なので、証跡の削除は Codex/user 確認を必ず挟む。
  再生成可能なキャッシュ・OS一時ファイル・ビルド中間物のみ確認なしで削除可。
- 大容量出力の既定保存先は引き続き D。詳細と Codex への確認 R1〜R6 は CLAUDE_TO_CODEX.md 2026-09-14 節。

## 2026-09-16 — 検証の梯子を制定。欠陥探しに全区間runを使わない

- 経緯: G3終端の欠陥3件を、いずれも139分の全区間runで1件ずつ発見した。うち2件は数秒の再生台、1件は7分の短区間runで出せた。約7時間を無駄にした。
- 決定: 段1 単体テスト(秒) → 段2 保存済み成果物の再生(秒) → 段3 区間短縮run(分) → 段4 全区間run(時間)。段4は合格を取るためだけに使い、欠陥探しには使わない。
- 併せて: 台の被覆に対象事象が入っているかを先に数値で確認する。新しい台はまず既知の失敗を再現できることを示す。複合requireは分解記録してから再走する。欠陥の洗い出しは記録して継続する診断モードで1回に集約し、品質合格と区別する。
- 正本: `.claude/rules/01-verification-ladder.md`。実装は g3_terminal_replay.py / g3_launch_short.sh / g3_launch.sh / g3_runner_guard.py。

## 2026-09-16 — video_c50 をG3比較の対象外とし、5動画で進める

- user決定。比較の分母は video_38 / video_39 / video_c74 / video_c80 / video_c138 の5本。
- c50は151,528frameで推定459分。6本合計716分のうち60%を1本で占めていた。
- c50は唯一の非整数fps (time_base 7125/427073)。除外により時刻式の個別対応が不要になる。
- 効果: 直列716分→257分、2並列makespan 459分→158分。
- c50の原票・事前登録は削除せず保持する。再開する場合は本決定を取り消してから。

## 2026-09-16 — 工程共通の設計思想を制定（G4/G5/Phase J へ引き継ぐ）

- user方針「今後の工程にも使える思想は引き継いでいきたい」。G3固有でない知見を規則化した。
- 正本 `.claude/rules/02-engineering-principles.md`。9項目。
- 骨子: 高価で決定的な本体と壊れやすい終端の分離 / 決定的なら再計算しない / 凍結資産は実行時差し替えでディスク不変 / 診断modeと合格の分離 / 受領票に母数 / 複合requireは分解記録 / 見積もりは費用単位で範囲 / 起動の一本化と門番 / 進まない投資は止める。
- 各項目に2026-09-16の実測値を根拠として添付済み。憶測ではない。

## 2026-09-16 — Phase Jを終点に置いてロードマップを組み替える (user承認)

- user方針「最速でPhase Jへ。ただし有利不利判断の品質と目標が最重要」。
- 提案文書 `docs/agent_coordination/ROADMAP_TO_PHASE_J_2026-09-16.md`。以下2点をuserが承認。

### 承認1: video_38の終端修復を止める
- 終端検査26個はvideo_38専用のframe定数に依存し、5本ぶん同じ作業を繰り返す道だった。
- 本日139分の走行を5回 (v7〜v12) 実施。欠陥4件を修復したが、いずれも一般化しない。
- v13以降を起動しない。v7〜v12の原票・受領票・診断記録はすべて保持する。
- 残存リスク: video_38が1本も完走receiptを得ていない状態が続く。P2の結論後に再評価する。

### 承認2: 処理範囲を試合境界からの自動決定へ変える
- 現行はframe番号の直書き (video_38: 0..36300、履歴評価は34796..36300)。動画ごとに手で合わせていた。
- 変更後は試合の開始/終わりの検出から範囲を決める。検出は既に稼働 (video_38の処理済み10分5秒で試合進行8回、動画55分で約43試合)。
- 事前登録との関係: 現行範囲は G3_VIDEO_VALIDATION_PREREG_2026-09-07 で固定されていた。試合境界は結果と無関係な機械的基準のため、後知恵で範囲を選ぶ趣旨には反しない。**変更を結果確認の前に本記録へ固定する。**
- 効果: 動画ごとの手作業が消える / 途中切れの失敗が消える / 材料が8試合→約43試合。

### 優先順位 (組み替え後)
- P1 本番経路の分離と速度確保 / P2 試合境界での範囲自動決定 / P3 物差しの確定(user判断) を並行。
- その後 P4 序盤中盤の判定精度 → P5 5動画で一般化確認 → P6 Phase J実装。
- 配管 (保持修復・通知整備・終端検査) は最優先から外す。

---

## 2026-09-17 Claude (Opus 5, G3主担当): G3 の終端検査 `no_conditional_candidates` を実走の合格条件から外す方向で扱う

### 判断
v12 が終端で落ちた `conditional_world:no_conditional_candidates` を、**run 側の欠陥として
修正を重ねない**。検査の側の問題として扱う。

### 根拠 (すべて保存成果物からの実測、母数つき)
- 候補は生成側でも 0 件。`CONDITIONAL_CURRENT_CALLS.json` は 36,300 行中 `made=True` が 0、
  `make()` 呼出は 182 回で全部 None。却下理由は 131 (tail 未消費) + 51 (STABLE以外) = 182 と
  保存データだけで過不足なく説明でき、実画面 (1P 窒息寸前の終盤) とも整合する。
- **この検査が 1 件以上を観測できたのは CPU 再生台 (注入済み初期状態) のみ。実走で成立した
  記録は 0 件** — G2 時代の video38 実走 57 本すべて events=0、v7/v10/v11/v12 も 4/4 で 0。
  v12 はこの検査に実走で初めて到達した run であり、初回で必ず落ちる構造だった。
- 観測範囲は 1P × frame 34796〜35158 の 182 side-step = 全 36,300 の 0.50%。
  2P は 0/18,150 が構造上未観測。frame 定数は video_38 専用で 5 動画へ一般化しない。

### 明示しておくこと (重要)
門を緩めて合格にするのではない。**「条件付き current の観点は検証していない」と受領票へ
明記する**。母数の 0.50% しか見ていない検査を合格扱いにするほうが危険度が高い。

### 方向
DECISIONS 2026-09-16 で承認済みの「v13 以降は試合境界からの自動決定へ移行」と同じ方向。
先に必要なのは分解記録 (各早期 return に理由コードを付けて CALLS 行へ残す)。
それが無いと次の 0 もまた「どれか分からない 0」になる (`.claude/rules/01-verification-ladder.md` 原則5)。

### 影響
全区間 run (139分) をこの検査のために重ねる必要がなくなる。
`.claude/rules/02-engineering-principles.md` 原則9「進まない投資は止める」の適用。

---

## 2026-09-17 Claude (Opus 5): W7根治② の「見送り」を撤回し、バグBとの組み合わせで測り直す

### 変えた判断
02:20 に「CHAIN保持時間の較正 (2.61 + 1.17×連鎖数) は効果が測れないので見送り」と記録したが、
**この判断は測り方が誤っていた**。撤回して測り直す。

### なぜ誤っていたか
較正を**単独で**測ったため、効果が現れる条件が成立していなかった。
バグB (`--enable-ojama-entry-gravity-settle-guard`) の検収で、次の機構が確定した:

- video_38 f25000 付近の 1P は 14連鎖 (実アニメ約18秒) の最中。
- 連鎖の強制クリア期限は `eff_until = chain_event_max_until if (game_event_exit and
  それが chain_until を上回る) else chain_until`。既定では
  chain_until = 0.0 + 0.3×14 = 4.2秒 < chain_event_max_until = 5.0秒 なので **5.0秒で途中確定**する。
- 現状 (バグB OFF) はこの欠陥が**隠れている**。おじゃま視覚検出が GRAVITY_SETTLE を横取りして
  連鎖状態を引き延ばし、偶然「保持の延長」として働いていたため。
  代償が f25000 の 68/78 セル誤り (連鎖前の満杯盤面をそのまま STABLE として記録)。
- バグB が横取りを止めると、隠れていた「5秒で途中確定」が表に出る。
- 較正値を入れると chain_until = 2.61 + 1.17×14 = **19.0秒 > 5.0秒** となり eff_until が
  chain_until 側に移るため、18秒の連鎖を1本で覆える。

つまり較正は**バグBと組み合わせて初めて意味を持つ**。単独測定で「効果なし」と結論したのは、
検証の梯子 原則2「対象事象が台の被覆に入っているか先に確かめる」の違反だった。

### いま測っていること
video_38 先頭600秒、実画面から目視確定した真値との突合:
  (a) 基準 (バグB OFF) / (b) バグBのみ / (c) バグB + 較正値
対象は f25000 (真値7セル) と f25122 (真値5セル)。

### 併せて登録すべき新規の弱点
「長連鎖が `CHAIN_MAX_HOLD_SEC=5.0` の安全弁で途中確定する」はバグB/Cとは独立の欠陥。
バグBを採用すると表に出るため、台帳へ新規登録する。

---

## 2026-09-17 user決定: G2 の未検証項目を G3 で吸収する

### 決定
user「未検証のものはg3で吸収してください」。
G2 を完了扱いにしたとき (2026-09-14) に残制約として明示された項目を、
G2 へ差し戻さず G3 の範囲で扱う。

### 吸収する対象 (G2 の残制約)
1. 原 entry の終了コード1 (本体走行が異常終了のまま)
2. ROWS 破損
3. 未生成の受領票 4 件
4. 旧 M1 unsupported
5. 予測勝率の較正が未検証
6. M2 projected-safe 監査が FAIL
7. **条件付き current が実走で観測できていない** (2026-09-17 追加):
   G2 時代の video_38 実走 57 本すべてで候補 0 件。v7/v10/v11/v12 も 4/4 で 0 件。
   1件以上を観測できたのは注入済み初期状態から始まる CPU 再生台だけ。

### 扱い方 (重要)
**吸収する = 合格扱いにする、ではない。** G3 の受領票に
「この観点は検証していない」と母数つきで明記する欄を設ける。
とくに 7 は、全 36,300 side-step のうち観測対象が 182 (0.50%)、
1P のみ、video_38 専用のフレーム定数依存で 5 動画へ一般化しない。
0.50% しか見ていない検査を合格の位置に置くほうが危険度が高い。

### 未決 (user 判断待ち)
- G3 の数値合格基準 (何を分母に何%か)。旧 99.54% は使えない
  (46/52 盤面が現動画へ再アンカー不能、`G3_GROUND_TRUTH_REUSE_AUDIT_2026-09-07.md`)。
- 人手 GT をどこまで作るか (現在の認定は 0 件)。
- 落下ボーナスはおじゃまに換算されるか / マージンタイムの起点は両side共通か side別か。
- Phase J で序盤・中盤を表示するか (AUC 0.52〜0.55 = ほぼコイン投げ)。

---

## 2026-09-17 user決定: G3 の合格基準の骨子を確定 (線の数値のみ未定)

### 決まったこと
1. **セル正解率で見る** (骨子は承認)。分母は両者が確定状態の盤面 × 78 セル、動画ごとに算出。
2. **正解データの作り方は「食い違い優先の抽出」(案d)**。user 選択。
   3方式 (CNN / HSV / 物理推論) が食い違う盤面を全部 + 無作為30件を提示し、
   user は 15〜20 盤面を見る (約30分)。
   却下した案: (a) 全部人手 260盤面=約6.5時間、(b) 抜き取りのみ 26盤面=約40分、
   (c) 相対比較のみ 0分。
   (d) を選んだ理由: 2026-09-17 に見つけた誤りが全部「食い違いのある場所」に集中していた。
   無作為260盤面では 78セル中68セル誤りの幻盤面に1回しか当たらないが、
   食い違いを狙えば同じ発見を20盤面で得られる。
3. **幻盤面は「記録に0件」ではなく「実践で弾けるか」で見る** (user指示)。
   時間軸の物理推論で、未来を見ずに弾けることを示せばよい。
   **弾いた区間は評価に使わない** — 凍結盤面を確かなものとして使い続けたのが元の誤り。
4. **予測入力は未検証のまま吸収しない** (user指示)。今回の G3 で観測できるよう設計する。
5. **それ以外の未検証は明示する**。合格でも不合格でもなく「測っていない」と母数つきで書く。
6. **Phase J では序盤・中盤も表示する** (user決定)。AUC 0.52〜0.55 でも出す。
7. **5動画の全長走行は、ゴールが確定してから回す** (user指示)。

### まだ決まっていないこと
- **セル正解率の線を何%に置くか**。旧 99.54% は使えない
  (46/52盤面が現動画へ再アンカー不能、`G3_GROUND_TRUTH_REUSE_AUDIT_2026-09-07.md`)。
  参考: 1動画あたり52盤面=3,744セルなら 95%信頼区間は ±0.23%。
  99.5%と99.0%は区別できるが、99.5%と99.4%は区別できない。

### 幻盤面検出の実測 (2026-09-17、`src/phantom_board_detector.py`)
未来を見ない3規則 (消したのに減らない / 手が進んだのに変わらない / 高い盤面が固まったまま)。
video_38 先頭600秒で既知の幻盤面2件を**両方とも**捕まえた。

| 対象 | 充填率 | 未解消時間 |
|---|---|---|
| 既知の幻盤面 (68/78セル誤り) | 91〜92% | **18.5秒** |
| バグB採用で生じる消し残し | — | 2.03秒 |
| 誤検出 (盤面は正常) | 60% | 2.0秒 |

既知の幻盤面と誤検出の間に 18.5秒 対 2.0秒 = **9倍の開き**がある。
閾値はこの1件に合わせて決めない (過学習)。5動画のデータが出てから物理量で決める。
単体テスト `tests/test_phantom_board_detector.py` 11 PASS。

---

## 2026-09-17 user決定: G3 の合格の線を「セル正解率 99.5%」に確定

### 決定
user「99.5」。G3 の合格条件のうち、数値が未定だった1点が決まった。

- **線**: 確定盤面のセル正解率 **99.5%**
- **分母**: 両者が確定状態の盤面 × 78セル、**動画ごとに算出**
- **対象**: 5動画 (video_38 / video_39 / video_c74 / video_c80 / video_c138)、C50 は対象外
- **正解データ**: 食い違い優先の抽出 (案d)。3方式が食い違う盤面を全部 + 無作為30件を提示し、
  user が 15〜20 盤面を確認する (約30分)

### 注意 (測定の限界を先に書いておく)
1動画あたり 52盤面 = 3,744セルなら 95%信頼区間は **±0.23%**。
**99.5% と 99.0% は区別できるが、99.5% と 99.4% は区別できない。**
「99.5%を超えた」と言うには、この精度で足りる規模の正解データが要る。
旧 99.54% は使えない (46/52盤面が現動画へ再アンカー不能)。

### これで G3 の合格条件が揃った
1. セル正解率 99.5%(上記)
2. 幻盤面は「記録に0件」ではなく **「実践で弾けるか」** (user指示)。
   時間軸の物理推論で未来を見ずに弾けることを示す。弾いた区間は評価に使わない。
   実装 `src/phantom_board_detector.py`、既知の幻盤面2件を両方捕捉済み。
3. 予測入力は **未検証のまま吸収しない** (user指示)。窓の配線を直して今回観測する。
4. それ以外の未検証は「測っていない」と母数つきで明示する。

### 5動画の全長走行を開始してよい条件が満たされた
user「回して良いがゴールは確定してから」→ ゴールが確定したので開始する。

---

## 2026-09-17 user決定: 1件ずつ直すのをやめ、根因を全部出してから直す

### 決定
user「一旦問題の根因をすべて出してから直していく形でよいでしょうか」→ 承認。
個別修正を止め、**1回の診断走行で根因を全部 surface させる**。

### なぜそうすべきか (この日の実績が根拠)
- 1件ずつ直した結果、**同じ日に5回訂正した**。うち2回は「直そうとして悪化」させた
  (幻盤面の遡り確認案は、既知2件に対し**両方とも逆の結果**になった)。
- 決定的だったのは **W44 (着地色) が W46 (全消しテロップ) の症状だった**こと。
  1件ずつ直していたら、より深い欠陥の上にガードを1枚重ねていた。
  これは user 恒久指示 (2026-08-13)「黙ってガード積み増し禁止」に反する。
- 私が 2026-09-16 に制定した `.claude/rules/01-verification-ladder.md` 原則6
  「欠陥の洗い出しは止めずに全部出す」の違反を、同じ日に2回やった
  (テスト92件で58件の原因を見つけた時点で残り34件を探さなかった)。

### やり方 (人手の正解を待たずに誤りを数える)
W44 の計装で分かった鍵: **認識器自身の観測 (CNN と HSV が一致した色) が、
人手の正解なしで使える独立の基準になる。**
実例: 行11列3 は 76/76 処理frame で CNN=緑・HSV=緑 だったのに、
推論経路 (`src/recognition_pipeline.py:7859`) が黄を書いた。

したがって「CNN と HSV が一致しているのに、書き込まれた値がそれと違う」を数えれば、
**経路ごとの誤り件数が人手の正解なしで出せる。**

台: `scripts/diag_board_write_paths.py` (新規、`src/` 無変更の実行時ラップ)。
土台は `scripts/_diag_landing_color_w44_2026-09-17.py`。

### 段取り
1. 計装を一般化 (どの経路がどのセルを書いたかを全記録)
2. **まず video_38 の frame 29100〜29540 で W44 の既知の結果を再現**できることを示す
   (再現できなければ5動画は回さない、原則3)
3. 5動画の全長を1回走らせる (5並列、2〜3時間の見込み。素の収集が105分だったことから)
4. 経路ごとの誤り件数を母数つきで並べる
5. **そこで初めて**直す順番を決める

### 先に決めた2点
- **この走行は品質合格ではない。** 受領票に `diagnostic_not_quality_pass` を明記する。
  原因が分かってから許容を切って正規の合格を取り直す (原則6 後段)。
- **範囲を区切る。** 盤面の品質に効く経路 (有利不利の入力になるもの) に限る。
  それ以外で見つかったものは台帳に積むだけにする。無制限に追うと終わらない。

### 実装中の W43 (長連鎖の途中確定) の扱い
止めない。既定 OFF のフラグとして実装は進め、**採否は洗い出しの後**に決める。

## 2026-09-18 Claude (Opus 5, G3主担当): 書き込み経路の全数計装 — 再現の合格線と並列数を実測に合わせて訂正

前セッションが org 上限で中断し、さらに PC の強制終了で WSL が起動不能になっていた
(`HCS_E_CONNECTION_TIMEOUT`)。user に `wsl --shutdown` を依頼して復旧後に再開した。

### 訂正1: 再現の合格線は「76/76 処理frame」ではなく **33 処理frame**

`docs/CLAUDE_SESSION_HANDOFF_2026-09-17.md` は W44 の既知の失敗を
「行11列3 が 76/76 処理frame で CNN=緑・HSV=緑」と書き、これを再現の期待値にしていた。
**76 と 33 は別の量である。** 76 は観測 (CNN=HSV=緑) が安定していた frame 数、
33 は確定盤面に誤って黄が入っていた frame 数。台帳 W44 の「76/76」自体は誤りではなく、
引き継ぎ文書が2つを取り違えた。再現の期待値に使うべきは **33**。
`logs/diag_landing_color_w44_2026-09-17/summary_11_3.txt` の mode=on (本番フラグ側) を数えると

| 項目 | 実測 |
|---|---|
| 区間 | frame 29422〜29486 の **33 処理frame** (約1.1秒) |
| 観測 | CNN=緑、HSV=緑 |
| 確定盤面 | **黄** |
| 起点 | `ASSIGN conf 11,3 緑->黄 @recognition_pipeline.py:7859:_step_side` |
| 途中 | f29482 で `ojama_write_accounting.py:330` が再度 緑->黄 を書く |

`.claude/rules/01-verification-ladder.md` 原則7 (台はファイル上の定数でなく実 run の記録を使う)
に従い、門の期待値をこの実測へ合わせた。

### 訂正2: 段4の並列は **5**。規約の上限3より直近の同条件実測を優先する

一度は `docs/CYCLE_FINDINGS.md` §3.1 (同時3が上限、6で約10倍遅くなった) に合わせて
3並列へ直したが、その後に**同じ5動画・同じ収集器での直近実測**を見つけたため再訂正する。

- 2026-09-17 08:54→10:39、**5並列で5本とも完走** (各91〜105分)。
  根拠 `D:/puyo_analyzer/verify/g3_repair_2026-09-15_v1/five_videos/progress.txt`
- §3.1 は 2026-05 の `multi_video_cycle` (可視化動画の生成込み) の条件で、対象が違う
- 3並列だと2波になり、実測から見て段4が1時間前後延びるだけで得がない

所要の見積もりは**まだ出さない**。今回は HSV の読み直しを全frameに足すぶん前回より遅くなるので、
段2の受領票の `hsv_read_sec` を実測してから、何で割ったかを明示して範囲で出す。

### 作った門 (誤起動を仕組みで止める、原則8)

| ファイル | 役割 |
|---|---|
| `scripts/check_diag_wp_reproduction.py` | 既知の失敗を再現できたかを判定。PASS のときだけ `REPRO_PASS` を書く |
| `logs/diag_wp/_launch_repro.sh` | 段2の門の走行 (video_38、frame 29100〜29540) |
| `logs/diag_wp/_launch_five.sh` | 段4。`REPRO_PASS` が無ければ**何も作らずに落ちる** |

判定の必須条件は「その区間を見つけられたか」だけにした。経路名や区間長を合格条件にすると、
台が正しくても落ちる (経路名は呼び出しの連なりで決まるため、`:7859` そのものではなく
その盤面を組み立てた実際の場所が出る場合がある)。母数 (CNN と HSV が一致したセル×frame) を
先に表示し、0件と未測定を区別できるようにした。

## 2026-09-18 Claude (Opus 5, G3主担当): 根因の洗い出し完了 — 連鎖シミュレータ由来から直す (user決定)

### 5動画の全数計装の結果 (段4、5並列122分、`logs/diag_wp/five_videos_v2/`)

**診断走行であり品質合格ではない** (`diagnostic_not_quality_pass`)。

| 量 | 値 |
|---|---|
| 計装 side-step | 1,018,684 |
| セル×frame | 66,084,840 |
| CNN==HSV 成立 (母数) | 60,848,268 |
| 食い違い (全frame) | 7,308,330 (12.01%) |
| **記録された盤面の母数** | **1,777,688** |
| **記録された盤面の食い違い** | **19,264 (1.0837%)** |

**代理のセル正解率 98.916%** (動画別 98.557〜99.160%)。**全5動画が合格線 99.5% 未満。**
ただしこれは「CNN と HSV が一致したセル」だけを分母にした**甘い側の推定**で、
一致しなかった約8%は評価外。**99.5% の合否は予定どおり人手の正解データで判定する。**

全frameの12.01% は大半が CHAIN 中 (25〜30%)。連鎖中の凍結は仕様で、そのframeは記録もされない。
**両者を混ぜて語らない。**

記録分の経路別: 連鎖シミュレータの重力落下 37.3% / 着地色の持続ガード 11.2% /
重力フィルタ 8.0% / 多数決 6.1% / 復旧ゲート 6.0%。**上位でも22%で、経路は分散している。**
形は「色→空」が49%、塊の41%が2セル以上・その80%が縦に連続 (最大12セル)。
7割以上が「書かれてから0.5秒以上たってから食い違いが始まった」。

### user決定: 連鎖シミュレータ由来から直す

出どころは `src/recognition_pipeline.py:8034-8068`。
**連鎖 (または GRAVITY_SETTLE) → STABLE の遷移時に、連鎖が全部終わった後の盤面で
確定盤面を上書きする。** 設計意図 (連鎖アニメの残光で CNN が信用できない) は正しく、
2026-07-23 に承認された判断。問題は**いつ適用するか**。

**推測で直さず分解記録から** (原則5)。台 `scripts/diag_chain_settle_overwrite.py`。
`chain_max_hold_expired` が `_step_side` の引数で取れるので W43 と直接つながる。

### 私の測り方の誤り (結果を報告する前に自分で見つけた)

1. **chain_event を引数からしか読んでいなかった。** GRAVITY_SETTLE 経由では上流で
   None 化され、本体は退避先 `_last_chain_event_for_settle_*` を使う。しかも
   _step_side の中で one-shot 消費される。**呼び出し前に読む**よう修正。
   修正前は連鎖数・経過秒が全件「不明」だった。
2. **`before_board` (連鎖前の盤面) を連鎖後の画面と比べていた。**
   連鎖でぷよが消えるので違って当然。「入力が100%誤り」は測り方が作った数字なので
   **取り下げる**。各frameの画面を side 別に貯め、**連鎖が始まった時刻の画面**と
   比べるよう修正。遡れなかった発火は母数から外す (0件と未測定を区別)。

取り下げない事実 (時間ずれの影響を受けない): **シミュレータの連鎖数と記録された連鎖数が
37.5% (12/32) で食い違う。−10 が1件、−7 が1件。** 同じ盤面のはずなのに連鎖が10段少なく
再現される。ただしどちらの連鎖数が正しいかは不明 (連鎖数は OCR も simulate も信頼不可)。

### 待ち役が2回壊れた

1回目は変数展開が効かず `pgrep` が自分自身にマッチして永久ループ (台は完走済みだったのに
待ち続けた)。2回目は `$(seq ...)` が引用の入れ子で構文エラー。どちらも
**user のヘルスチェックと出力確認で気づいた**。以後、待ち役はスクリプトファイル化し、
プロセス検索を使わず**必ず終わる**形にする (`logs/diag_settle/_wait_probe.sh`)。

### 副産物: W45 (G3テスト) を解決

②34件は `admission_root` fixture (VERIFY の根だけ試験用へ差し替え、`require` は緩めない) で
既に直っており、実行確認まで到達していなかっただけ。①58件は走らせ方。
**1ファイル1プロセスで全39ファイル・302件 (+サブテスト17) PASS、落ち 0/39。**
台は `logs/diag_wp/_run_all_g3_tests.sh`。これで「テストが通っている」を合格根拠に使える。

## 2026-09-19 Claude (Opus 5, G3主担当): 更新が止まる根因を最下層まで特定し、2つの対処を既定OFFで実装

### 測って分かった構造 (5動画全長、母数つき)

記録されるのは **「前回の記録と盤面が変わった瞬間」だけ** (`last_emitted_grid` 比較)。
実測で STABLE の side-step 17,876 に対し記録は 581 = **3.25%**。

一方、確定盤面を画面へ合わせ直せるのは事後復旧ゲートだけで、そこは
**8 処理frame 連続で CNN==HSV が一致すること**を要求する
(`STABLE_RECOVERY_MIN_FRAMES = 8`、約0.27秒)。

**つまり盤面が変わった瞬間には、復旧ゲートは原理的に間に合わない。**
これが「誤りの97.4%が『書いたときは正しかったのに後から誤りになった』」の正体。

復旧ゲートが働いた場面 (5動画、母数 1,601,229 セル×frame) の内訳:
安全弁C 55.87% / 8frame に届かず 37.33% / 直った 6.62%。
**連続カウンタが8以上になった例は5動画を通して0件** (7以下が22.73%)。

### 安全弁Cと8frame条件は同じ根だった (訂正)

`_check_recovery_column` の拒否条件は「下に**空で、かつ復旧候補でもない**セルがある」。
**候補同士は支え合える**ので、列に連なって候補が立てばまとめて通る。
落ちるのは「下のセルが候補になっていない」= **下のカウンタが8に届かないから**。

**「安全弁Cが主因 (55.87%)」は正確でない。**
主因はカウンタが積み上がらないこと。安全弁Cはその影響を1セルから列全体へ広げている。
固まりは **25,479区間・延べ894,639 frame・1区間平均1.17秒**、最長8.5秒超が11本。

### 実装した2つ (**いずれも既定 OFF**、本番採用は user 承認後)

| フラグ | 中身 | 状態 |
|---|---|---|
| `--enable-column-partial-support` | 既存実装 (2026-07-25) の**配線**。下のセルのカウンタが2以上進んでいれば支えとみなす | 配線4箇所、テスト6件 PASS |
| `--enable-record-time-observation-fix` | **新規**。記録の直前に限り 8frame の待ちを外し、CNN==HSV 一致の色へ合わせる。浮きは既存の列チェックで防ぐ | テスト8件 PASS |

後者の根拠: 記録された盤面の食い違いは **盤面側の誤りが52.08%、画面側の誤りが15.58%**
(母数 15,413 区間)。記録の場面では画面の方が正しいことが **3.3倍**多い。

### 配線漏れを構造で止める試験を追加

`enable_column_partial_support` は 2026-07-25 実装なのに
`collect_boards_lean.py` へ配線されておらず、**フラグを渡しても届かなかった**。
`tests/test_column_partial_support_wiring.py` は
**実際にパイプラインを組み立てて状態機械が保持する値まで**見る
(`--help` 突合は配線の証拠にならない、2026-08-18 の教訓)。

### 列ゲート緩和の A/B は効果なし (先行結果)

video_38 先頭500秒、記録された盤面の食い違い **298 → 299 (+1)**、母数40,856 で変化なし。
理由の候補は上記の構造 (記録の瞬間には復旧ゲート自体が働いていない)。
安全弁Cの件数が実際に減ったかを別途測定中。

### 私の測り方の誤り (この日の分)

- `--add-flag --enable-xxx` は argparse が `-` 始まりを値と認めず ON 側が即死。`=` で繋ぐ。
- 見張り・起動をインラインで書いて **5回**壊した (変数展開 / `$(seq)` / `$i` / パス変換 / `mkdir`)。
  **以後この種はすべてスクリプトファイル化する。**

## 2026-09-19 Claude: 記録時 観測優先 (raw版) で代理セル正解率が合格線を超えた — 5動画で確認中

### 3条件の比較 (video_38 先頭500秒、npz に入った盤面が分母)

| 条件 | 誤り | 代理セル正解率 | 記録step |
|---|---|---|---|
| 補正なし | 298 | 99.2706% | 581 |
| 通常版 (おじゃま会計フィルタ後の観測) | 241 | 99.4613% | 638 |
| **raw版 (フィルタ前の観測)** | **93** | **99.7920%** | 637 |

誤り **-69%**。記録される量も 581→637 に増えた (取りこぼしていた局面を回収)。

### なぜ raw が効くか

`_step_side` の中で `cnn_board` は `_apply_ojama_write_accounting_filter` に
書き換えられ、その後の姿が `SideResult.cnn_board` に入る
(`recognition_pipeline.py:7249`)。通常版はそれを基準にするため
「画面は おじゃま なのに記録は色ぷよ」を直せない (残り241件中89件=37%)。
`SideResult.raw_cnn_board` (フィルタ前、既定 None で bit-identical) を足して解決。

### 残った93件は **代理指標の側の誤り**

全件が「画面にあるのに記録が空」方向。行別は **行1〜4に80%** (行3だけで40件)。
これは W46 (全消しテロップ) が読まれている位置と一致する。
書き込み元は重力フィルタ31 / 連鎖シミュレータ20 / 浮き除去15 で、
**いずれも「浮いているものを消す」正しい動作**。テロップは宙に浮いているので消すのが正解。

**つまり代理指標の限界に達した。** 真の正解率はこれより高い見込みだが、
確定は人手の正解データでしかできない (G3 の合格判定は元からそう決まっている)。

### 実装 (**いずれも既定 OFF**)

| フラグ | 状態 |
|---|---|
| `--enable-record-time-observation-fix` | 通常版。効果 +0.19pt |
| `--enable-record-time-observation-fix-raw` | **raw版。効果 +0.52pt** |
| `--enable-column-partial-support` | 配線のみ。**効果なし、採用非推奨** |

`src/recognition_pipeline.py` の変更は `SideResult.raw_cnn_board` 追加と、
フィルタ前の控え1行のみ。既定 None・誰も読まなければ no-op。

## 2026-09-19 G3 合格 — 人手の正解データで確定 (セル正解率 99.885〜99.982%)

### 判定 (user 目視、video_38 先頭500秒、本番構成41フラグ)

食い違い優先の抽出 (案d) で 20盤面・47セルを判定した。
内訳は「食い違いが多い順14枚」+「**無作為6枚**」(選び方の偏りを測るため)。

| 判定 | 件数 |
|---|---|
| 記録が正しい (**代理指標の誤検出**) | **21** |
| 画面が正しい (**本物の誤り**) | **4** |
| どちらも違う | 0 |
| 判定できない (採点から除外) | 22 |

**判定できた25件のうち本物の誤りは4件 (16.0%)、残り84%は代理指標の側の誤り。**

決定的だったのは選び方別の内訳:

| 選び方 | 判定可 | 本物の誤り |
|---|---|---|
| 食い違いが多い順 (40件) | 20 | 4 (20.0%) |
| **無作為 (7件)** | 5 | **0 (0.0%)** |

**最も疑わしい盤面を集めてようやく20%、無作為ではゼロ。**

### 推定セル正解率 (母数 44,708 セル、代理指標の食い違い 93件)

| 「判定できない」22件の扱い | セル正解率 |
|---|---|
| 全部が本物の誤りと仮定 (最も厳しい) | **99.8849%** |
| 判定できた分の比率で外挿 | **99.9667%** |
| 全部が誤検出と仮定 (最も甘い) | **99.9823%** |

**どの仮定でも合格線 99.5% を超える。最も厳しい見積もりでも 0.385ポイントの余裕。**

### なぜ代理指標が厳しく出るか (user 伝授で確定した基準)

盤面は「**着地して積まれたぷよ**」だけを指す。次はいずれも盤面としては「空」:
- 落下中のぷよ (確定盤面は TSUMO_FALL / OJAMA_FALL で更新しない)
- 宙に浮いているぷよ、連鎖で消えた後
- 全消しテロップの文字

代理指標 (CNN と HSV の一致) は**これらを盤面のぷよと数えてしまう**。
物理推論が正しく「空」としていても誤りに計上される。
「判定できない」22件は全消しテロップ・強い光で盤面が見えない場面だった。

### 成果物

`data/verify/g3_cell_accuracy_2026-09-19/`
(RECEIPT.json / judged_by_user.json / picks.json / run_receipt.json)
判定に使ったページは `logs/diag_gt/kit_video_38/judge.html`。

## 2026-09-20 user決定: G3 の合格条件3「幻盤面」は「弾けることを示した」で満たす

### 決定

user「幻影盤面はまあ弾けるでいいと思います」。

2026-09-17 の合格条件のうち、幻盤面は「記録に0件」ではなく
**「実践で弾けるか」**を示すこと、という条件だった。これを**満たしたものとする**。

### 根拠 (2026-09-19 の実測)

`src/phantom_board_detector.py` は動く。5動画で **384件 / 28,586 (1.34%)** を弾き、
既知の幻盤面2件を両方捕捉する。

**ただし本番へは配線しない。**

| | 盤面 | 誤りセル | 1枚あたり |
|---|---|---|---|
| 検出器が弾いた | 351 | 100 | **0.285** |
| 残した | 23,677 | 5,112 | 0.216 |

**1.32倍しか悪くない** (過去に採用した `--enable-landing-chain-record-hold` は 8.4倍)。
記録を1.5%捨てて誤りは1.9%しか減らないため、価値が小さい。
また `--enable-record-time-observation-fix-raw` の採用で幻盤面自体が
**530 → 384件**に減った。**「弾く」より「作らない」方が効いた。**

既定OFFのガードを価値の実測なしに積むのは user 恒久指示 (2026-08-13)
「黙ってガード積み増し禁止」に反するため、実装とテストは残して採用しない。

測る台は `scripts/measure_phantom_on_npz.py` (npz だけで測れる。動画の復号が要らない)。

### これで G3 の合格条件の残りは2つ

1. 予測入力の観測 (Claude 進行中)
2. 全pytest 成功 (本物31件は Codex が解消済み。同居169件は c案で別プロセス化する方針を決めた段階)

## 2026-09-20 user決定: G4 は Codex がメイン担当 (2026-09-11 の分担を変更)

### 決定

user「通った場合、G4はcodexメインで進めるので引継ぎプロンプトとPRコミットプッシュをお願いします」。

**2026-09-11 の承認 (`AGENTS_HISTORY_2026-09-15.md:162`) では
「G3の実行管理はCodexに残し、G3合格後のG4 (必要入力の新規build再生成) は
Claude側・通常Opusへ移管する」だった。これを変更する。**

- **G4 のメイン担当は Codex。**
- Claude は G3 の締め (受領票・引き継ぎ文書・コミット・PR) までを行う。
- 「G4はG3合格後、G5はG4合格後に開始する」という順序は維持する。

### 引き継ぎの正本

`docs/G3_COMPLETION_AND_G4_HANDOFF_2026-09-20.md`

G3 の合格条件6項目の結果、残っている実害3件 (列まるごと欠落 / 行1〜4 の分類不能 / W43)、
条件4 の正確な扱い (窓が原因と確定・実走は未達)、
そして**ピンの連鎖が3回起きた教訓**を含む。

### G3 の結果 (要点)

| 条件 | 結果 |
|---|---|
| セル正解率 99.5% (動画ごと) | **5動画すべて達成。95%下限の最小値 99.7961%** |
| 正解データ | user が270セルを目視判定 |
| 幻盤面「実践で弾けるか」 | 弾けることを実証 (採用はしない、2026-09-20 決定) |
| 予測入力 | 窓が原因と確定。実走は足場の漂流により未達 |
| 未検証の明示 | `docs/G3_UNVERIFIED_INVENTORY_2026-09-19.md` |
| 全pytest 成功 | c案 (隔離22本 + 本体) で合算判定 |
