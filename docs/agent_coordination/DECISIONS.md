# 共有決定ログ

## 2026-08-24 — 現行PM100 A+Bは未採用

- 指摘場面seg01 game2は修正できた。
- 全8区間では急変・反転試合・決着方向が改善した。
- 一方、±100張り付き14.0%→18.3%、生モデル逆符号張り付き3.3%→5.2%へ悪化した。
- 固定0.96秒ヒステリシスは10秒前後続く本線応酬を表現できない。
- 交換エピソード会計と、episode未解決中のhard override禁止が必要。

## 2026-08-24 — 既存検証完了までwrite lock

- 掛け算式検証・バックテスト・PM100 pytestの入力コードを途中変更しない。
- 完了後に証拠を固定してから新施策へ進む。


## 2026-08-24 — Gate 0 の user 判断 4 件 (すべて推奨案で承認)

user へ判断材料を提示し、**4 件すべて推奨案で進める**との承認を得た。

### 判断1: 物差しの穴 2 件を直して 8 動画を再基準化する (案 A)

Gate 0 の作業中に、認識精度の物差し `scripts/measure_stable_cell_acc.py` に
独立した穴が 2 つ見つかった。

- **W36**: `--gravity-settle-state` が `main()` → `_collect_results` で渡されておらず
  **完全に無効**。物差しは GRAVITY_SETTLE を切った状態、つまり**本番と違う
  フレーム母集団**を測っていた。CLI の ON/OFF で正規化ハッシュが完全一致することを
  v40 で実測して確認済み。
- **W37**: **試合終了テロップ中で盤面が画面から消えている区間**を STABLE として
  評価対象にしていた。v51 では不一致セル 1,102 件のうち 991 件 (90%) がこの種の窓由来。

**根拠**: この 2 つは今日 1 日で実際に判断を誤らせかけた。v51 の
「掛け算式修正で −0.1847pt 悪化」を額面どおり受け取っていたら、正しい修正を
捨てていた (実際には生きた盤面で ON 0.999548 > OFF 0.999443)。

**決定**: 両方を是正し、8 動画を再基準化する。新旧を併記する
(`feedback_paired_comparison_fixed_population_2026-08-20`)。
Phase I の合格判定 99.54% の扱いは、再基準化の結果を見てから user へ再提示する。

### 判断2: 整地の定義は実装側 (送りおじゃま ≤4 個なら消費比不問) を採る

実装 (`scripts/measure_exchange_dynamics.py:396-401`) と
伝授の記録 (`reference_saisoku_exchange_model_2026-07-22`「<60% 消費 かつ ≤4 個」) が
食い違っていた。食い違うのは「本線 (60% 以上消費) なのに 4 個以下しか送らない発火」の
扱いだけで、閾値の値と境界の向きは一致している。

**決定**: 実装側を採る。
**根拠**: episode の開始判定には送付量だけを使いたい。消費比率は連鎖終了後にしか
確定しないので、`docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` §9.4.1 の
lazy open (暫定生成量が 4 個を超えた瞬間に開く) と両立しない。
催促 / 本線 / 整地の**ラベル付け**は従来どおり事後分類のままでよい。

### 判断3: Q-04 (おじゃまダメージ) の本番配線は 148 再学習時にまとめる (案 B)

**決定**: 指標側の実装とテストのみ入れ、`virtual_landing` は既定 OFF のまま。
オーバーレイと学習列への配線は 148 動画の再生成・再学習のタイミングで A/B を取ってから。

**承知の上のリスク**: それまで本番経路では
「空盤面と窒息寸前の盤面が同じ無害判定 (0.05)」というバグが残る。
現行 52 列モデルにこの指標は入っていないのでモデルへの影響はゼロ。

**user 追加見解 (2026-08-24)**: 着弾後 `is_dead()` の CEIL が一段のステップである点について
「おじゃまは 6 列均等に降るので窒息列の余裕が足りなければ必ず 12 段目に乗り、
実態より大きく出るかもしれない」との指摘があったが、
**「上部ほどダメージが大きくなるのは間違いない事実」**との判断で現行のステップ形のまま進める。
経緯は W33 に記録。

### 判断4: 掛け算式修正の採用は、幕間の別動画検証と物差し是正後の再測定を経てから (案 A)

**決定**: 今日の数字だけで `src/production_config.py` に登録しない。次の順で進める。

1. 幕間の実測を別動画 2〜3 連鎖で再確認 (W34 の残る限界 ②)
2. W36 / W37 を是正した物差しで 8 動画を再測定
3. その結果を user へ提示して採否を仰ぐ

**根拠**: 物差し自体に穴が 2 つ見つかった直後であり、
その物差しで出した数字を採用根拠にするのは筋が悪い。

## 2026-08-24 — Phase I 合格判定 99.54% の再計測は Phase J 直前まで先送り

**user 決定**: 再計測は必要だが、**30先動画の作業を終えたあと、Phase J の直前**に行う。

### 背景

W36 (`--gravity-settle-state` の配線漏れ) により、物差しは本番と違うフレーム母集団を
測っていた。8 動画の実測で **acc 0.997728 → 0.996834 (−0.090pt)**、
誤り 4,633 → 6,407 (+38%)。

Phase I の合格判定 99.54% も同じ配線漏れの下で出た数字であり、
本番構成なら **99.45% 前後**になって合格ラインを割る可能性がある (未測定)。

### なぜ今やらないか

判定精度を上げる作業が先で、認識の合格判定を測り直すのはその後でよい。
Phase J (配信オーバーレイ) に入る直前が、本番構成での数字を確定させる自然な区切り。

### 申し送り

- **Phase J に着手する前に必ず実施すること。** 未実施のまま Phase J に入らない。
- 再計測は W36 / W37 を是正した物差し (本番構成 + 試合外区間の除外) で行う。
- 当時とは認識構成が違うので、旧 99.54% との単純な引き算はできない。
- 台帳: `docs/KNOWN_WEAKNESSES.md` W36 / W37。

## 2026-08-24 — saturation_chain_upper の欠損表現は優先度を下げる

**user 決定**: この指標は**盤面のおじゃまぷよの量と近似**なので、優先度を下げる。

### 経緯

Codex 指標監査で「99.59% 欠損の coverage 障害」と指摘され、読取監査を実施した
(`docs/SATURATION_UPPER_COVERAGE_AUDIT_2026-08-24.md`)。

判明したこと:

- 99.58% 欠損は設計どおり。充填率 90% 未満は計算しない (コスト予算からの逆算)。
- ただし消費側 6 箇所すべてが `fillna(0.0)` しており、
  producer が守った「未測定 ≠ 飽和ゼロ」の区別が失われている。
- 既存 CSV には NaN が保存されているので、**是正に動画処理は不要** (0 本)。
  学習の 0 埋めをやめるだけで済む。

一度は案 A (NaN を埋めずに渡す) で進める方針を確認したが、**user 判断で優先度を下げた。**

### 理由 (user)

**この指標は盤面のおじゃまぷよの量と近似である。**

これは 2026-08-13 に user が「おじゃまぷよの数がイコール飽和現象に繋がる」と
指摘して充填率 90% 以上に絞り込んだ、その判断と一貫している。
既に `board_ojama_count` が代理指標として働いているので、
残り 0.42% を正しく扱っても得られるものは薄い。

### 位置づけ (user 補足)

**厳密には必要だが、Phase J の後の「強化」段階で必要になるレベル。**
いま止めてまでやる話ではなく、捨てる話でもない。

### 申し送り

- **削除はしない。** 死に指標ではなく、測れている 0.42% では
  単独 AUC 0.6360 (モデル全体 0.6357 と同等) の信号が出ている。
- **実施時期の目安: Phase J 以降の強化フェーズ。**
- Codex 18:18 のモデル比較 A/B/C/D をそれ以前に回す機会があれば、
  **1 条件足すだけで測れる**ので、そのときに相乗りさせてもよい (前倒し可)。
- 是正に**動画処理は不要 (0 本)**。既存 CSV に NaN が残っているので、
  学習の 0 埋めをやめるだけ。着手すれば短時間で終わる。
- 閾値 0.90 を下げる案 (C) は**採らない**。native 化の bit-identical パリティが
  未解決 (fill≥0.70 で 2/14 件の食い違い) で、下げるほどコストが倍増するのに
  欠損は 99% 台のまま。

---

## 2026-08-25 — user決定: Gate 3R-6「表示欠陥の始末」をマップに追加。レビュー動画は**先頭5試合**

### 経緯

Claude が「このマップが終わればレビュー用動画を作れる状態になるか」への回答として、
**「レンダはできるが、そのままお見せするには足りない」**と判断した。

Gate 3R (P1/P2/W38/gross供給) が直しているのは**会計**であって、
**表示の欠陥**ではない。会計を直せば ±100 張り付き (OFF基準14.0%) と
決着方向誤り (7試合) は根治の本命に届くが、通しレビューで確実に目に付く
次の2件は**現行マップに入っていない**。

1. **STABLE 凍結デッドロック** (2026-08-24 根因確定)
   凍結盤面が連鎖検知の入力にもなっており、検証ゲートが 4.17 秒握りつぶす。
   **片側連鎖では永久に抜けられない** (救済は相手の同時連鎖による偶然)。
   memory `project_stable_freeze_deadlock_2026-08-24`
2. **W32 `is_dead()` 誤判定の残り約7割** (2026-08-24)
   窒息判定の凍結盤面修正で直ったのは31%。残りの入口は
   「画面で連鎖しているのに状態が STABLE」という別バグ。
   **既存の修正は未来を見る後処理でリアルタイム不可 = 動画の判定は未修正**。
   memory `project_is_dead_stable_misdetect_2026-08-24`

レビュー動画は「試合の開始〜終了まで通しで」がユーザー恒久指示のため、
この2件は**必ず画面に出る**。Claude の自己検収を通せない。

### 決定 (user)

- **Gate 3R-6「表示欠陥の始末」をマップに追加する。** レンダ前に上記2件を片付ける。
- 扱いは 2026-08-13 の恒久指示に従う: **「根治」か「user承認つきガード」かを先に決める。
  黙ってガードを積み増さない** (`feedback_kill_known_weaknesses_2026-08-13`)。
- **レビュー動画の範囲は「先頭5試合」でよい** (動画全編の通しレンダは不要)。
  1試合あたりの「開始〜終了まで通し」は従来どおり維持する。

### 影響

- Gate 3R-5 (既定OFF配線) の後、Gate 4 (PM100 OFF/ON 比較) の前後に 3R-6 を挟む。
- レンダ範囲が先頭5試合に限定されたことで、レンダ実時間12倍問題 (W30) の
  負担が大きく下がる。区間並列レンダ手順は
  memory `reference_segment_parallel_render_2026-08-22` を使う。

### 【訂正 2026-08-25】Gate 3R-6 の前提2件のうち1件は**既に根治済み**だった

Gate 3R-6 追加時に Claude が挙げた根拠2件を本番構成で実測し直した結果、**深刻度が過大だった**。

| 対象 | 追加時の記述 | **実測 (本番構成)** |
|---|---|---|
| STABLE 凍結デッドロック | 「4.17秒握りつぶす。片側連鎖では永久に抜けられない。**未修正**」 | **再現しない。** 掛け算式検知→CHAIN突入 4.167秒 → **0.034秒**。先頭5試合の掛け算式セッション **29件全件が CHAIN 到達 (遅延 最大0.1秒、≥1秒 0件、未到達 0件)** |
| W32 `is_dead()` 誤判定 | 「残り約7割が未修正。動画の判定は未修正」 | **大幅縮小して残存**。同一区間の before/after で **6.1秒 (1件) → 0.7秒 (1件)**。母数 = 実試合5、試合時間計 329.6秒 |

実測条件は本番と一致 (現行ワークツリー + `use_production_recognition` 既定ON +
`advantage_overlay_flags()` の実行時読込 + model62)。実効フラグは実行時に pipe 属性で確認済み
(`effective_read_verify / count_update / step_interlude = true`、配線事故なし)。

**原因**: Gate 3R-6 追加時に Claude が参照した memory が **2026-08-24 の3フラグ採用より前**の記述で、
`src/production_config.py` の採用記録 (E2E 4.133秒解消) と突き合わせていなかった。
**対象より物差しを先に疑う規律を、記憶にも適用すべきだった。**

### 実測でわかった追加事実

- `is_dead` は `scripts/visualize_advantage_overlay.py` では **dump 行構築 (:3968) にしか使われておらず、
  描画にも有利不利スコアにも入っていない**。残存 0.7 秒の区間で adv_raw は +7.0→+6.3 と滑らか、
  `drivers_top1` も不変
- 死亡見逃しゼロの傍証: 真の敗北 (2P 実試合2、t=223 おじゃま満杯) は正しく `is_dead=True`
- **新規の定量 (台帳未登録)**: 試合前のまちうけ画面区間 (t=18–86.9) で `is_dead2=True` が
  **574/1019行 (56%)**。実画面はロビーで盤面なし (W37 の表示版)。
  ただしレビュー動画の「試合開始〜終了」範囲外
- **残る構造リスク (実測ゼロだが未消滅)**: 掛け算式が読めない連鎖 (バースト/煙、W34 と同根) は
  旧 simulate 検証へフォールバックするため、凍結デッドロックが理論上再発しうる。
  先頭5試合では出現 **0/6 回**。凍結盤面をトラッカー入力にする循環自体
  (`src/recognition_pipeline.py:4653-4660`) は現存する

### Gate 3R-6 の残作業 (縮小)

**先頭5試合のレンダに限れば、通しレビューを止める規模の表示欠陥は実測で残っていない。**
残るのは残存 0.7 秒への対処方針で、2026-08-13 の恒久指示に従い user 判断を仰ぐ (**user 回答待ち**)。

| 案 | 内容 | 犠牲 | 工数 |
|---|---|---|---|
| **A (推奨)** | own state が非STABLE の間は `is_dead` を保留扱い | 連鎖中の真の詰みの早期警告が STABLE 復帰まで遅延 (ゲーム実仕様上は正しい遅延) | 0.5日 |
| B | own CHAIN 中は連鎖前進推定の結果盤面で評価 | simulate の系統的過小 (真値8→1 の前歴) による**死亡見逃し**リスク | 1〜2日 |
| C | 現状維持 | 走査器 D1a の誤警告源が残る | ほぼ0 |

案A は user 伝授の律「連鎖中 / 連鎖直前は窒息としない」と一致し、**未来参照なしでリアルタイム可能**。

証拠: `data/verify/gate3r6_diag_2026-08-25/` (実画面フレーム込み。
`t00167.00s.jpg` = 1P が「2れんさ!」実行中に OFF が窒息判定を出し続けている現場を Claude 自身が目視確認)

### 【user決定 2026-08-25】`is_dead()` 残存への対処は **案A** — 非STABLE 中は窒息判定を保留

3案 (A: 非STABLE中は保留 / B: 連鎖前進推定の結果盤面で評価 / C: 現状維持) を提示し、**user が案A を選択**。

採用理由:

1. user 伝授の絶対律「**盤面が高い ＝ 窒息ではない。設置前 / セカンドに向かって積み上げ中 /
   連鎖を打つ直前 / 連鎖中はいずれも窒息としない**」と完全に一致する。ルールから導かれた直し方であり、
   シーンからの逆算ではない
2. **未来参照なしでリアルタイム可能。** 配信オーバーレイが最終目標なので必須条件
   (案B は連鎖前進の推定が必要でリアルタイム性に難がある)
3. 案B の推定は **真値8連鎖→1連鎖のような一方向の系統的過小**という前歴があり、
   **死亡見逃し方向に外れる**。表示の誤り方として最も危険

**犠牲 (user に明示済み)**: 連鎖中の真の詰みの早期警告が、盤面が落ち着くまで遅延する。
ただしゲーム実仕様上、連鎖中はまだ勝負がついていないため**この遅延はむしろ正しい**。

### 実装の条件 (Claude が自らに課すもの)

- **既定 OFF の optional flag** として実装 (規約: 新機能は既定OFF)
- **`src/production_config.py` への採用登録は測定値を確認してから。** user は方針を承認したが、
  数値を見ずに本番へ入れない
- 保留した回数は**母数付きカウンタ**で可視化する (`3/29` の形)
- **合格条件の最重要項目 = 真の窒息を見逃さないこと。**
  2P 実試合2 (t=223、おじゃま満杯による真の敗北) が正しく検出され続けること。見逃したら不合格
- `src/indicators_v2.py` 内に `is_dead` の使用が **9箇所**あるため、
  **有利不利スコア (`adv_raw`) が動くか**を同一動画ペア (先頭5試合、t=86.9〜416.5) で測る
- 出力先 `data/verify/gate3r6_planA_2026-08-25/` (新規)。既存 `gate3r6_diag_2026-08-25/` は上書き禁止

### 【user決定 2026-08-25】案A の本番登録は**決定化まで保留** / 次の一手は**Codex の返答待ち**

**1. 案A (`enable_nonstable_hold_is_dead`) の `src/production_config.py` への採用登録は保留する。**

理由 (Claude の推奨を user が採用):
いま登録しても**副作用の有無を証明できない**。案A の adv への影響 (OFF/ON 差 149行・最大12.017) は
実行間揺らぎ (142行・最大12.017) と**同分布・同時刻帯**で、
「adv に副作用なし」の主張は1回比較では原理的に成立しない。
**乱数の決定化が入れば3run一致で証明できる**ので、証明できる状態にしてから登録する。
急ぐ理由がない (実効差は dump の is_dead 列 30/5186 行のみ、既定 OFF で本番動作は不変)。

**2. Gate 3R-6 の本丸「連鎖中なのに STABLE」(state machine 遅れ) への着手は、Codex の返答を待つ。**

理由: Codex には K5 (余剰ID の解釈)、K7 (逆方向の欠落6件)、K8 (Gate 4 合格条件の書き換え)、
および **Gate 3R-6 の対象を state 遅れに差し替えるか**を判断依頼済み。
これらの回答で優先順位が変わりうるため、先走らない。

### 未決 (今後 user 判断が必要になるもの)

- 乱数の決定化 (`dig_resistance` の seed 固定) の本番採用 — **3run 完全一致と分布不変を示してから**
- ここまでの作業のコミット — 現在**すべて未コミット**。
  他エージェントの差分が多数あるため、Codex レビュー通過後に Claude の変更分だけを選んで切り出す方針

### 【user決定 2026-08-25】Gate 3R-5 は **Codex レビューを飛ばす**。Claude 自己検収で通れば Gate 3R-6 へ進む

#### 判断の材料 (Claude が提示、user が決定)

Codex レビューを飛ばして短縮できるのは **4〜8時間** (往復2回分) だが、**律速は Gate 3R-6 の
state machine 修正**であり、飛ばしても全体は1日縮まるかどうか。
一方 Codex は本日だけで Claude の誤りを3件検出している
(P1-3 未着手を「完了」と要約 / K6 の時間単独判定 / K7 の測定器事故)。

そのうえで **Gate 3R-5 に限れば飛ばす価値がある**と判断した:

- 既定 OFF の**記録用配線**で、表示も判定も変えない
- OFF の完全一致を **W39 決定化後の3run一致で機械的に証明できる** (レビューで見つかる種類の
  誤りが入りにくい)
- 一方 **Gate 3R-6 は認識の中核を触る**ので独立レビューの価値が高く、**こちらは通す**

#### Claude 自己検収の合格条件 (「Claude 側で良ければ」を曖昧にしないため事前固定)

| # | 条件 | 判定方法 |
|---|---|---|
| 1 | OFF 構成が3run で完全一致 | `0/N 行不一致` (母数併記)。**エージェント報告を鵜呑みにせず Claude が npz を直接突合** |
| 2 | 全 pytest 0 failed | 基準 5,976。速度テストが並走で落ちた場合は単独再実行まで確認 |
| 3 | 追加7列がすべて出ている | 実 dump を Claude が開いて確認。**検査母数の列が無ければ不合格** |
| 4 | `src/production_config.py` 未変更・本番ON なし | `git diff` で確認 |
| 5 | ON でも表示・判定が変わらない | dump 列以外が一致すること |

**1つでも欠けたら Gate 3R-6 へ進まない。**

#### Codex への扱い

Gate 3R-5 の成果は `CLAUDE_TO_CODEX.md` へ通常どおり記録し、**事後レビューは妨げない**。
ただし Gate 3R-6 着手の前提条件からは外す。
Gate 3R-6 の完了時は**従来どおり Codex レビューを待つ**。

### 【訂正 2026-08-25・2回目】Gate 3R-6 の本丸「state machine 遅れ」も**存在しなかった**

Codex が Gate 3R-6 の対象として差し替えた「画面では連鎖中だが own state が STABLE のまま遅れる」は、
本番構成の実測で**現象そのものが確認できなかった**。同一動画・同一窓で**2回独立に実行**し、
既知の残存インシデント (1P・実試合2・t=164.033〜164.733、0.7秒) が
`suspicious=1/1`、`suspicious_total_sec=0.7/0.7` で一致することも確認済み (n=1 ではない)。

#### 先行信号3種の遅延分布 (母数併記)

| 信号 | 母数 | 中央値 | 最大 | ≥0.5秒 | 未到達 |
|---|---|---|---|---|---|
| 掛け算式の実読 | **29件** (1P:13 / 2P:16) | 0.033秒 | 0.1秒 | **0/29** | **0/29** |
| score 増加 (閾値 = 4個消し最小素点 40) | **32件** (1P:15 / 2P:17) | 0.0秒 | 0.134秒 | **0/32** | **0/32** |
| 盤面ぷよ数減少 (生CNN、≥4セル減) | 72件 | 0.0秒 (到達分) | 1.733秒 | 4/72 | **31/72 (43%)** |

盤面セル数の 43% 未到達は**生CNN のノイズ**。静止した STABLE 盤面でも ±2〜8セル変動しており
(t=157.5〜164.6 で実連鎖ゼロなのに非空セル数が 48〜75 を上下)、
**先行信号として単体では使えない** (デバウンスなしで 43% が偽陽性)。

#### 残存 0.7秒 の正体

フレーム単位トレースで確定:
- t=161.5〜164.6: score1 は 235前後で停滞、掛け算式実読 False、盤面セル数はノイズ範囲
  → **3信号のいずれも「連鎖が起きている」証拠を示していない**
- t=164.633: 掛け算式実読が valid
- t=164.667: state が CHAIN へ (**遅延 0.034秒**、mechanism=`formula_read`)

**つまり state machine の検知遅延ではなく、盤面が満杯になってから本人が連鎖を始めるまでの
ゲーム自体の時間。** この区間には画面上に「連鎖が来る」と分かる材料が存在せず、
**未来参照なしでの短縮は原理的に不可能。**

実画面 `data/verify/gate3r6_diag_2026-08-25/evidence_frames/t00164.10s.jpg` を Claude が目視確認:
1P は天井まで積み上がっているが**連鎖しておらず**、連鎖しているのは 2P。

#### simulate フォールバック経路 (Codex が別母数で測れと指示)

`_apply_chain_formula_early_fire` の呼び出し **33件中、read_valid=True が 33件、
simulate フォールバック 0件**。構造としては `src/recognition_pipeline.py:6253-6254` に現存するが、
先頭5試合では**一度も発火していない**。緊急性なし。

### 【測定器事故 19件目】`stable_lag` 指標は「連鎖検知の遅延」を測っていない

- score 信号の閾値を「増加 > 0」にすると、**連鎖終了後のカウントアップ演出の残響**
  (+1〜+5/フレームが数秒継続) が `SESSION_GAP_SEC=1.6秒` の連結規則で無関係な連鎖を融合し、
  **43.9〜83.7秒の巨大セッション**を作る。閾値を**物理量 (4個消し最小素点 40)** に是正すると
  全32件が遅延ほぼ0に収束した
- **既存 `scripts/_diag_gate3r6_isdead_runs_2026-08-25.py` の `stable_lag` も同じ欠陥**。
  今回の再実行でも `stable_lag_events=7 / median=3.9s / max=4.967s` を再現したが、
  これは timeline dump の粗い行間で差分を取り、**正しく検知済みの複数連鎖を1個の巨大な遅延に
  見せかけている**
- この数値は本文で採用されたことはないが、**放置すると次の判断者が額面どおり受け取る**。
  誤った側の証跡も `state_lag_summary_v1_naive_score_threshold1.json` に残してある

### 立て直した問い (検証中)

**その 0.7秒間、`is_dead` 判定はゲームのルールに照らして本当に誤りなのか。**
1P はこの後も打てているので**実際には死んでいない**。仮説は2つ:

- **仮説A (認識誤り)**: 判定セル (row=1, col=2) に実際はぷよが無いのに誤読している
  → **直すべきは判定ロジックではなく認識**
- **仮説B (判定条件の食い違い)**: セルは実際に埋まっているが、ゲームは別条件で死亡を決めている
  → **判定条件そのものの再定義が必要**

既存 dump から安価に決着させる。証跡は `data/verify/gate3r6_deathcell_2026-08-25/`。

### 【user決定 2026-08-25】Gate 3R-6 で行き詰まったら「**後から振り返って直す方式**」で動画を先に作り、根治は後回し

#### 決定内容

Gate 3R-6 (残存 0.7秒 の `is_dead` 誤判定) の根治で作業が止まる場合、
**遡及訂正方式でレビュー動画を先に作り、リアルタイム根治はその後に回す。**

#### なぜ成立するか

- 遡及訂正は**既に実装がある**: `scripts/visualize_advantage_overlay.py` の
  `_retroactively_correct_dead_dump_rows` (`--stable-confirmed-is-dead`)。
  後続フレームの確定値で過去の判定を遡って訂正する
- **リアルタイム不可という制約は、動画には効かない。** 動画は全編を先に処理できるため、
  未来参照が許される。配信オーバーレイ (Phase J) だけがリアルタイム制約を受ける
- したがって「Gate 3R-6 の根治が難しい」ことは、**レビュー動画を出すことの障害にはならない**。
  この2つは切り分けられる

#### 適用条件と扱い

- **根治を諦めるのではなく後回しにする。** Gate 3R-6 は台帳に残し、
  配信オーバーレイ着手前に必ず閉じる (リアルタイム制約が復活するため)
- 遡及訂正を使ったことを**レビュー動画の添え書きに明記する**
  (何を後処理で直したかを user が把握できる状態にする)
- レンダ範囲は 2026-08-25 の user 決定を引き継ぎ「**先頭5試合**」を想定。
  30先動画の全編 (117分) を指す場合はレンダ規模が変わるため要確認

#### 現時点の状況

Gate 3R-6 の前提は**2度とも実測で崩れている** (STABLE 凍結デッドロックは根治済み、
state machine 遅れは存在しなかった)。残存 0.7秒 が
「認識誤り」か「判定条件の食い違い」かを検証中で、**認識誤りなら話は Gate 3R-6 ではなく
認識側の課題に変わる**。その結果次第で、この遡及訂正方式を使うかどうかが決まる。

### 【user伝授 2026-08-25】死亡確定の条件 — 「12段目に設置して連鎖が起きない」or「おじゃまが降り12段目が埋まる」

#### 検証で確定した事実 (仮説B)

Gate 3R-6 の残存 0.7秒 は**認識誤りではなかった**。

| 仮説 | 判定 | 根拠 |
|---|---|---|
| A: 認識誤り | **棄却** | 確定盤面は **24/25 フレーム**で正しく占有。実画面ズーム (`gate3r6_deathcell_2026-08-25/evidence_frames/toprow_zoom.jpg`) を Claude が目視し、**判定セルに赤ぷよが実在**することを確認 |
| B: 判定条件の食い違い | **確定** | 占有は事実だが 1P は死んでいない |

生CNN は 7/25 フレームで赤→黄/空/おじゃま に揺れていたが、**原因は 2P の連鎖が発火させる
全画面バーストの半透明レイヤー** (既知)。state machine の STABLE 補正がこれを正しく吸収しており、
**測定器 (生CNN) は揺れていたが判定に使う確定盤面は健全だった。**

フレーム単位の因果: 2P の連鎖 (40×8=320点) 解決 → 送出おじゃま着弾 (t=164.6、state1=OJAMA_FALL)
→ **1P 自身の連鎖が発火 (t=164.67)**。
**猶予区間の長さは相手の連鎖解決時間で決まる = 相手依存。固定値では表せない。**

**案A はこのケースを救えない。** 0.7秒のうち **86% (0.6秒) が own state=STABLE** のため、
「非STABLE の間は保留」というガードを素通りする。

#### user 伝授 (2026-08-25)

死亡が確定するのは次のいずれか。

1. **12段目 (可視最上段、row=1) に設置して連鎖が起きない**
2. **おじゃまが降って 12段目が埋まる**

**ただし user 自身が「その判定が結構難しい」と明言。**
そのため **フィールドの底が抜けて落下する演出 (死亡アニメーション) を見る**など、
**何らかの別の判断材料が必要**。

memory: `reference_death_confirmation_condition_2026-08-25`

#### 次の手

`feedback_check_existing_before_building_2026-08-21` に従い、**実装を決める前に librarian で既存資産を調査**する。
死亡アニメーション (フィールドの底が抜ける演出) の検出器が既にあるか、
`is_dead` の時間的条件を持つ実装が既にあるかを先に確かめる。

### 【Claude の設計ミス + user 指示 2026-08-25】死亡確定は「12段目設置 + ネクスト1.5秒不動」の簡易検出で一旦進める

#### Claude の設計ミス (user 指摘で実装前に発覚)

Claude は確定条件を「連鎖が始まらないまま**次の事象 (ネクストが動いた = 次が置けた)** が
観測されたら死亡確定」と設計してエージェントに渡した。**これは論理が逆で、致命的だった。**

> user 指摘: **ネクスト動かないから設置検出にネクスト使えないけど大丈夫？**

- **死亡すると次のツモは動かない** (置けないので当然)
- したがって「ネクストが動いた」は**生きている証拠**であって死亡の証拠ではない
- 死亡時にはこの条件が**永久に発火しない** → **本当に死んでいる人を「生きている」と判定し続ける**
- これは合格条件に据えた「**真の窒息を見逃さない**」に真っ向から反する最悪の失敗モード

**構造的な問題**: 死亡すると**あらゆる観測信号が止まる**
(設置イベントなし / 連鎖なし / スコア不動 / 盤面不動 / ネクスト不動)。
**死亡は「信号が来ないこと」でしか判定できない。**

**user が最初に提示した「フィールドの底が抜けて落下する演出」が、実は唯一の積極的な証拠だった。**
Claude は librarian 調査の結果を見て「既存信号の組み合わせで済む」と判断し、
**user が提示した手段を退けていた**。ドメインは推測せず user に確認する決まり
(`feedback_consult_user_puyo_domain`) に反しており、**判断そのものが誤りだった。**

#### user 指示 (2026-08-25)

> **12段目が設置され、ネクストが1.5秒以上動かない等で簡易検出で一旦お願いします**

確定した3段階:

1. **死亡候補**: 12段目 (`DEATH_ROW=1, DEATH_COL=2`) の占有を確定盤面で観測。
   **発生源 (設置 / おじゃま着弾) を区別して記録**
2. **解除**: 自分の連鎖開始 (掛け算式の実読、遅延 中央値0.033秒) が観測されたら候補を解除
3. **確定 (簡易)**: 候補のまま **ネクストが 1.5秒以上動かなければ死亡確定**

#### 1.5秒の扱い

**この閾値は user 指定であり、Claude がシーンから逆算した値ではない。**
コードに「2026-08-25 user 指定の暫定値」と明記し、定数化し、CLI から変更可能にする。
**底抜け演出による根治に差し替える前提の暫定値**。

#### 残る課題 (後回しにするが落とさない)

- **底抜け演出 (死亡アニメーション) の検出器は未着手。** これが本来の積極的証拠
- `src/match_end_detector.py` のテンプレート照合は **42本中31本で一度も成立しない**実測欠陥があり、
  主判定に使ってはならない
- 確定が 1.5秒遅れる = **真の死亡の検出も 1.5秒遅くなる**。仕様として許容するが、
  配信オーバーレイ (Phase J) 着手前に演出検出で詰める

#### 実装の制約 (変更なし)

- **`src/board.py` を変更しない。** `is_dead()` は `src/indicators_v2.py` の19箇所ほか、
  `src/puyo_core_bridge.py` (Rustブリッジ、ビームサーチ) からも**仮想盤面の静的判定**として
  呼ばれており、そこでは現在の「占有＝死」が正しい。時間的確定は**観測層に新設**する
- 既定 OFF の optional flag + CLI。`src/production_config.py` へ登録しない
- 既存の `enable_nonstable_hold_is_dead` (案A) と併存。削除しない
- 合格条件の最重要項目は **2P 実試合2 t=223 の真の敗北を見逃さないこと**

### 【user決定 2026-08-25】Codex 指摘4 (state machine 遅延の実装要求) は**実測を根拠に差し戻す**

Codex の Gate 3R-6 独立レビューは NG。指摘1/2/3/5/6 は**すべて受諾**したが、
**指摘4 (PLAN の本丸である「画面では CHAIN 中だが own state が STABLE の遅延」を実装・検収せよ)
だけは差し戻す**と user が決定。

#### 根拠 (本番構成での実測、2回独立実行で一致)

| 先行信号 | 母数 | 中央値 | 最大 | **≥0.5秒** |
|---|---|---|---|---|
| 掛け算式の実読 | 29件 | 0.033秒 | 0.1秒 | **0/29** |
| score 増加 (閾値=4個消し最小素点40) | 32件 | 0.0秒 | 0.134秒 | **0/32** |

**現象そのものが観測されていない。** 残存 0.7秒 は
「盤面が満杯になってから本人が連鎖を始めるまでのゲーム自体の時間」であり、
その区間には画面上に「連鎖が来る」と分かる材料が存在しない (実画面で Claude が目視確認済み)。

**遅延ゼロの実測がある以上、昇格ロジックを実装しても効果を測る対象が存在しない。**

#### 手続き

Codex 自身の指示「Gate 定義を変更したと考える場合は user 決定なしに PLAN を書き換えず、
Gate 未完了として判断を仰ぐこと」に従い、**`PLAN.md` は書き換えていない**。
第25報で証跡つきで差し戻し、**Codex が独立に検証してから**改定の可否を決める順序を提案した。

#### Claude が受諾した指摘 (自分の誤りとして記録)

- **指摘1**: 確定遅延 0.067秒 を「正常動作」と第24報に書いたが、
  **それは「1.5秒の猶予が実質効いていない」ことの証拠**だった。合格と判定すべきではなかった
- **指摘2**: `next_pair=None` を「不動」として扱っていた。
  **「検出できていない」を「動いていない」と読んでいた**もので、
  本セッションで繰り返した「`0` が合格なのか未測定なのか区別しない」と同型の誤り

### 【Codex条件付き受理 2026-08-25】Gate 3R-6 の本丸を「死亡確定の正しさ」へ改定。訂正3点を受諾

user 決定 (案A: 実測を根拠に差し戻す) に対し、Codex が独立再計算で同値を再現し
**新規CHAIN昇格ロジックの実装要求を撤回**。`PLAN.md` の Gate 3R-6 節を改定した。

Codex が独立再現した値: 掛け算式29件 (中央値0.033秒、最大0.1秒、≥0.5秒 **0/29**)、
score増加32件 (中央値0秒、最大0.134秒、≥0.5秒 **0/32**)、simulate fallback **0/33**、
残存0.7秒は式読取164.633 → CHAIN 164.667、t=164.10 の実画面で連鎖中なのは **2Pのみ**。

#### 受諾した訂正3点 (**すべて Claude の誤り**)

1. **cell 信号の母数に試合前13件が混入していた。**
   正しくは先頭5試合で **59件**、未到達 **28/59**、最大 **1.434秒**、
   ≥0.5秒 **2/59**、≥1秒 **2/59** (誤: 72件 / 31件 / 1.733秒 / 4件 / 3件)。
   **これは本セッションで繰り返し問題にした「母数に試合外を混ぜる」誤りそのもの**で、
   `is_dead2` のまちうけ偽陽性と同型。自分の測定でも同じ穴を踏んでいた。結論は不変
2. **「state遅延は存在しない」ではなく
   「zenchi 先頭5試合の formula/score 観測可能セッションでは未観測」と限定する。**
   全動画・全条件での不在を主張しない
3. **2run一致の主張を取り下げる。** 監査できる生 trace が1組しかない。
   再主張するなら run 別の `trace.jsonl` / `events.jsonl` を分離保存してから行う

#### Gate 判定

**Gate 3R-6 NG / Gate 4 HOLD を維持。** 指摘1/2/3/5 の正式検収を第26報で報告するまで変わらない。

### 【user決定 2026-08-25】窒息判定の誤りを**指標計算にも通す** (案①)

#### 実測で確定した効果 (反実仮想。`Board.is_dead()` を常に False に monkeypatch した差分)

| 指標 | 差 |
|---|---|
| `adv_raw` (内部の生値) | **+9.16点** (本番構成) / **+9.22点** (model62)。**モデルに依らず同規模** |
| **`adv_ema` (画面に出る値)** | **最大 0.92点** |
| 応手 (counter) 成分 | **0.0000** (全39呼び出しで完全一致) |
| 主因表示 `drivers_top1` | **0/22 変化** |
| 決定性確認 (同一条件2回) | `adv_raw` 差 **0.0000** (全22行) = ノイズではない |

**効いている経路は1つだけ**と特定:
`src/indicators_v2.py:684-685 dig_resistance()` が `is_dead()==True` で `score=0.0` に短絡
→ `:1831 ukeyasusa()` が `UKEYASUSA_W_DIG=0.6` 倍で吸収
→ `scripts/visualize_advantage_overlay.py:2368-2421` がモデル47列へ投入。

**この誤判定が画面に届くのは `--per-side-settled` が本番ONだから。**
OFF なら相手の連鎖中は再計算行が0件で、スコアに一切到達しない。

#### Claude の報告の訂正 (2件)

1. **「最大13.23点」は誤り。** Claude が `adv_raw` の粗い差分を見た値で、
   **同時に起きた本物の盤面変化 (`diff_conn_max_group_size`) の寄与を混ぜていた**。
   誤判定単独の寄与は **+9.16点**、**画面に出る平滑後は最大0.92点**
2. **counter 経路「最大±8.0点」も誤り。** 実測 **Δ=0.0000**。
   盤面がほぼ埋まっているため、`is_dead` を False に戻しても MC ロールアウトが
   同じく0近傍を返す。short-circuit が実計算を先取りしていただけだった

#### 過去の「+7.0 → +6.3 と滑らか」が誤解を招いた理由 (測定器批判)

- **反実仮想を取っていない factual 単独の値**で、同じ再計算 tick で同時に起きた
  **本物の盤面変化と誤判定の寄与を混同**していた
- **`--no-counter-reach` を明示指定**しており、本番採用の counter-reach 経路を最初から測っていない
- さらに `advantage_overlay_flags()` (単一情報源) に**登録されていない3フラグを手動追加**していた。
  `feedback_use_single_source_for_flags_2026-08-22` と**同型の配線逸脱**。是正は別途

#### 決定と実装方針

**案① を採用。** 猶予中 (死亡候補は立っているが確定していない間) は
「生きている」として指標を計算する。

- **`Board.is_dead()` 本体 (`src/board.py`) は変更しない。** 仮想盤面の静的判定としては現行が正しい
- **optional 引数を追加**し、渡さなければ挙動が一切変わらない (backwards compat)
- 対象は本番経路から到達する3関数: `dig_resistance` (→`ukeyasusa` に伝播) /
  `saturation_chain` / `ojama_damage`
- **オーバーレイへの配線と動画測定は、Codex 指摘の実測チェーン
  (OFF 3run → ON → gross 併用) の完走後に行う** (途中でコードを変えると測定が汚れるため)

#### 未解決の論点 (user 判断が要る)

**モデルの入力列 (`ukeyasusa` / `diff_ukeyasusa`) の値が変わる。**
現行モデルは**この誤判定を含んだデータで学習されている**ため、
修正すると入力分布が学習時とずれる。**再学習の要否は未決**。今回は再学習しない。

### 【user指摘 2026-08-25】急変は「根拠づけ」でなく**根治 (規模の比較)** で減らすべき — Claude の提案が症状対処だった

#### user 指摘

> 急変の根拠付けについて教えてください、最初の指摘は**急変はそもそも幻**で、
> **はなから相手の連鎖より大きな連鎖を打つ見込みがある方を有利と判定すべき**との結論でありました

#### Claude の誤り

Gate 4 の合格条件「1秒150pt以上の急変は40回以下で、**残る全件に新しい物理イベントがある**」に対し、
Claude は「急変1件ずつに実画面の根拠をつける道具を作る」と提案した。
**これは症状の確認に道具を足す提案で、順序が逆だった。**

急変が出るのは、システムが**その瞬間に見えているもの (降ってくるおじゃまの量) だけで判定し、
これから打てる連鎖の規模を見ていない**ため。相手の連鎖が着弾した瞬間に不利へ振れ、
打ち返した瞬間に有利へ戻る。**振れているのは盤面の実力ではなくシステムの視野の狭さ。**

根治は **「規模の比較」** として **2026-08-16 に特定済み**で、
`project_chain_scale_comparison_backlog_2026-08-22` に
「**user 判断待ちのまま6日放置**」と記録されていた項目。
**Claude はその経緯を把握していながら症状対処を提案した。**

#### 事実関係の整理

「規模の比較」は既に実装があり (`--kill-override-scale-compare`、
`scripts/visualize_advantage_overlay.py:6548-6559`)、**Gate 4 の比較条件3そのもの**。

過去の PM100 全8区間の測定結果:

| 指標 | 何も入れない | A+B |
|---|---|---|
| 急変 | 79回 | **40回** |
| 反転試合 | 47 | **34** |
| 決着方向誤り | 10 | **7** |
| ±100 の張り付き | 14.0% | **18.3% (悪化)** |
| 生モデル逆符号の張り付き | 3.3% | **5.2% (悪化)** |

急変・反転・決着方向は改善したが**張り付きの悪化を理由に「全域採用不可」**とされていた。

#### **ただしこの結論は無効**

**この測定は W39 決定化より前** = 有利不利スコアが実行ごとに最大 ±12点 動いていた時期のもの。
Gate 4 の前提にも「決定化前の PM100 値は履歴参照に限定し全8区間を取り直す」と明記済み。

→ **「規模の比較は全域では使えない」という判定そのものを取り直す必要がある。**
特に悪化とされた張り付きの差 (14.0% → 18.3%、4.3pt) が
**揺らぎの中で測られたものだったのか**を確かめる。

#### 差し替えた進め方

**「急変の根拠づけツールを作る」は取り下げ。** 代わりに:

1. PM100 を決定化後に取り直し、**条件3 (規模の比較) が本当に張り付きを悪化させるのか**を確かめる
2. 悪化が再現しないなら**根治として採用を検討**する
   (そうなれば急変は根元から減り、1件ずつの根拠づけはほぼ不要になる)
3. 悪化が再現するなら、**なぜ張り付くのかを潰す**。根拠づけツールはそのとき必要なら作る

### 【訂正 2026-08-25・Codex指摘】規模の比較の過去結論は「**無効**」ではなく「**決定化後の再評価待ち**」

直前の節で Claude は「**『規模の比較は全域では使えない』という判定そのものを取り直す必要がある**」
「過去結論は無効」と書いたが、**これは言い過ぎだった**。Codex の指摘を受けて訂正する。

#### Codex の反証

**W39 以前でも、±100張り付き率は独立4run の全ペアで一致していた。**
したがって **14.0% → 18.3% の 4.3pt 悪化は、当時の乱数幅に埋もれていない。**

**この事実は Claude 自身が測っていた。** 非決定性の調査報告に
「**±100張り付き率: 17.99% vs 17.99% (全ペア一致)** / 判断の余裕 4.3pt / **埋もれない**」と
明記していたにもかかわらず、その後の議論で**自分の測定結果を見落として
「揺らぎの中で測られたものだったのか確かめる」と書いた**。
急変・反転・決着方向が揺らぎに弱いことと、**張り付きが揺らぎに強いこと**を混同した。

#### 正しい扱い (Codex 指定)

- **「全域採用不可」を最終決定とはしない**
- **過去の張り付き悪化は「負の事前証拠」として残す**
- 状態を **「W39 決定化後の再評価待ち」** に変更する
- 新測定で改善すれば採用を再検討する
- **悪化が再現した場合、規模比較を即時廃棄せず、張り付き経路を分離して修正する**

#### user 指摘との関係 (変わらない)

「急変はそもそも幻で、はなから相手の連鎖より大きな連鎖を打つ見込みがある方を
有利と判定すべき」という user 指摘の妥当性は変わらない。
**根治の方向は正しく、放棄しない。** 変わったのは「過去の悪化証拠の扱い」だけ。
悪化が再現しても**廃棄ではなく張り付き経路の分離修正**へ進む。

---

## 2026-08-26 — user決定3件: 決着ホールドは**根治**。Gate 4 基準と B 単独採用は**是正後に再測してから**

### 発端: 「±100張り付き 14.0% → 1.4%」は改善ではなかった

W39 決定化後に取り直した OFF 基準の ±100 張り付きが 14.0% → 1.4% と一桁下がったが、
**母集団が違っていた**。解析器は旧 dump に対して過去の公表値
(14.0%/18.3%、3.3%/5.2%、47/34、79/40、決着方向 10/7) を**完全再現**しており、
定義は不変。差は dump 側にあった。

| | 旧 8/24 | 新 snapshot |
|---|---|---|
| dump 行数 | 136,656 | **101,288 (−26%)** |
| 行間隔 | 0.0333秒 (30fps) | 0.0333秒 (**同一**) |

timeline dump の1行 = 有利不利を再計算した瞬間
(`scripts/visualize_advantage_overlay.py:6159`「settled 更新のたびに1レコード追記」)。
**行が消える = 評価していない。**

消えた 36,087 行の内訳 (母数併記):

- **「片側 CHAIN」28,267行 (78.3%)**、うち ±100 張り付き 15,955 (56.4%)
- 消えた行の張り付き率 **49.68% (17,928/36,087)** / 残った行 **4.86% (4,884/100,569)**
- 旧の張り付き 22,812 行の **78.6% が消えた行に集中**

実画面で確認 (`data/verify/pm100_missing_intervals_2026-08-26/frames/`):

- **t=1865.90**: 1P が掛け算式「40× 1」表示 = **連鎖を開始した瞬間**。WIN 11-16、試合の真っ最中
- **t=1872.45**: 1P がおじゃまで埋め尽くされ窒息寸前、score 92,033 (13.1秒で +5,564)

**勝敗が決まる最重要区間を、システムは1回も評価していない。**

### 犯人の特定 (計装による直接観測、推測ではない)

`resolved_hold_freezes_settled()` (2026-08-14 検収指摘⑤ で導入) が、打ち合い解決中
(`resolved_active=True`) の間 **settled 再計算そのものを完全に止める**。
`--resolved-exchange-eval` は本番 ON。

診断専用 snapshot (測定 snapshot と SHA 一致を確認) に計装した実測 (窓 t=1708–1890):

| 開始 | 終了 | 継続 |
|---|---|---|
| 1709.333 | 1717.600 | 8.27秒 |
| 1718.133 | 1738.300 | 20.17秒 |
| 1764.667 | 1796.067 | 31.40秒 |
| 1827.833 | 1829.800 | 1.97秒 |
| **1833.967** | **1879.033** | **45.07秒** |

最後の 45.07秒は、dump の行が消えていた 1833.60 → 1879.47 と一致する。

```
ホールド中frame          3206/5451 (58.81%)
ホールド適用前にsettled   3964/5451 (72.72%)
ホールドが潰したsettled   1880/3964 (47.43%)
```

着弾待ちの安全弁 `RESOLVED_HOLD_LANDING_MAX_WAIT_SEC = ceil(216/30)×0.733 = 5.86秒`
なので、**45.07秒のうち着弾待ちで説明できるのは最大 5.86秒だけ**。
残る約39秒は `chain_event` が両側とも None にならなかったぶん
(解除条件は `ev1 is None and ev2 is None`)。

**旧より長くなったかは未確定。** 旧 dump に該当区間の行が存在する = 当時ホールドは
効いていなかった、という dump からの推論のみで、8/24 のコードは計装していない。

### 【user決定1】決着ホールドは **根治**。解除条件を作り直す

ガードで上限秒数を切るのではなく、**解除条件そのもの**を直す。
現在の「両側 `chain_event` が None」は、ChainEvent が長い連鎖で 1.4秒ごとに
断片化する既知の性質 (memory `project_chain_event_fragmentation_accumulator_2026-08-22`)
に引きずられる。user 伝授の絶対律
「**連鎖の終わり = 連鎖している側のネクストが動いた瞬間 OR 連鎖している側に
お邪魔が落ちた瞬間**」(memory `reference_chain_end_absolute_signals_2026-08-21`)
に基づく終了信号へ差し替える。

- 認識の中核に触るため **Codex 独立レビュー必須**
- 新機能は既定 OFF の optional flag (規約)
- 恒久指示 `feedback_kill_known_weaknesses_2026-08-13`
  「根治 or user承認つきガード を先に決める」に従い、**根治を選択**

### 【user決定2】Gate 4 の合格条件は、ホールド是正後に測り直してから決める

合格条件「±100張り付きは OFF 基準 14.0% を超えない」は、新しい母集団では
天井として機能しない (新 OFF = 1.4%)。しかし**いま基準を決めても、評価行が
26% 欠けた母集団の上の数字**になる。

- ホールド是正 → OFF 基準を取り直す → そこで閾値を定義する
- **それまで Gate 4 の合否判定は出さない**
- Codex の指示「Gate 定義を変更するなら user 決定なしに PLAN を書き換えず判断を仰ぐ」
  に従い、`PLAN.md` は本決定の反映のみとし、閾値の数値は未確定のまま残す

### 【user決定3】条件2 (ヒステリシスB 単独) の本番候補化は保留

pre-gate measurement の実測 (同一母集団 101,288行、時刻列 8/8 完全一致):

| 指標 | 条件1 (OFF基準) | 条件2 (**B のみ**) | 条件3 (**A のみ**) | 条件4 (A+B) |
|---|---|---|---|---|
| ±100 張り付き | 95.6s (1.4%) | **72.7s (1.0%)** | 193.9s (2.8%) | 166.8s (2.4%) |
| 生値と符号が逆 | 27.1s (0.4%) | **19.9s (0.3%)** | 55.5s (0.8%) | 45.7s (0.7%) |
| 反転試合 | 7 | **4** | 9 | 7 |
| 急変 | 14回 | **8回** | 15回 | 12回 |
| 決着方向誤り | 7 | **6** | 10 | 10 |

**A と B を分離したのは今回が初めて。B 単独が5指標すべてで最良、A 単独が5指標
すべてで最悪、A+B はその中間。** 8/24 の A+B 一括測定にあった
「急変 79→40、反転 47→34、決着方向 10→7 の改善」は**再現しない**
(急変 14→12、反転 7→7、決着方向 7→10)。

ただし **この5指標はすべて評価行が26%欠けた dump の上で測られている**。
特に A (規模の比較) は打ち合いの最中に効く施策なので、**その区間が抜けた測定で
不採用と決めるのは筋が悪い**。user 指摘「はなから相手の連鎖より大きな連鎖を打つ
見込みがある方を有利と判定すべき」の妥当性は変わらず、**放棄しない**。

- B 単独の本番候補化も、A の採否も、**ホールド是正後の再測まで保留**
- `src/production_config.py` は引き続き未変更

---

## 2026-08-26 — user決定: レビュー動画はGate 4完了時の構成で作成する

- 先頭5試合のレビュー動画はGate 3R-6直後には確定しない。
- 次のGateであるGate 4の測定・判定が完了した後、その時点の構成で作成する。
- 中断した途中生成物は完成品として提示せず、再生成時は別名・別出力先を使い、
  既存成果物を上書きしない。
- 動画作成の延期はGate 4の実装・測定・合否判断を省略するものではない。

---

## 2026-08-26 — user決定: 148動画の学習前に指標戦略を壁打ちする

- 現行指標は古いモデルを前提に選定されたため、そのまま148動画の再生成・再学習へ進まない。
- Gate 4完了と先頭5試合レビューの後、現行指標の死活・重複・組合せ・局面別有効性を再監査する。
- スポーツ統計学、ゲームAI、時系列評価、確率校正の最新知見を一次資料中心に調査し、
  本プロジェクトへ適用可能な戦略・観測軸・検証方法を候補化する。
- 序盤・中盤・終盤で指標の意味や重みが変わる可能性と、
  「催促対応→相手本線→自分本線」のExchangeEpisodeを独立して扱う。
- 壁打ちとユーザー承認が終わるまで、指標セット・モデル重み・148動画学習を確定しない。
- 当面はGate 4の条件1〜5、検収、先頭5試合レビュー動画の完成へ作業を集中し、
  大規模な指標実装へ先行分岐しない。
