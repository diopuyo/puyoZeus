# PR #27 レビュー修復の記録

対象PRの元HEADは `2c1c92ceca0e01f820ba6aa182e9b0ad5fefbe97`。
隔離ブランチ `codex/pr27-review-fixes` で修復し、修復コードをコミットした。
このPRはコード・検証資材の修復として承認対象にできる。
G3全体の動画品質合格を意味しない。

## 修復内容

- 記録時のCNN/HSV補正は、支えを削除した後の盤面で追加を判定する。
  新しい浮きが生じる列を戻し、独立した列の正常な補正を残す。
  元盤面は変更せず、隠し段・UNKNOWNの既存契約を維持する。
- rawモードでraw観測が欠けた場合は補正を保留する。
  filtered観測への暗黙の切り替えを廃止した。両フラグのAPI既定値はOFFのまま。
- 採点器の全件判定不能は点推定 `None`、判定の欠測・重複・対象外は例外とする。
  偏った食い違い標本による参考外挿には `quality_gate_clear=false` と
  `confidence_interval=null` を付ける。旧95%下限とG3全体合格の記述を訂正した。
  ROOT外の原票でも表示ラベル生成で例外にしない。絶対入力は実行OSの表記
  （Windowsは `D:/...`、WSLは `/mnt/d/...`）を使い、相互変換はしない。
- 欠落していた隔離テスト、凍結Python、契約文書、fixture依存を収録した。
  元のコード・期待SHA・実走原票は変更していない。
- 実行時SHAが改行を含む8ソースは `.gitattributes` で取得時のbyte列を保つ。
  7本はCRLF、混在改行の `death_confirmation.py` は元byte列をそのまま保存する。
  正規化した本文は元版と同一。8本ともcheckout後のSHAが元版と一致した。
- 通知の実入口 `g3_notify_event.ps1` の収録漏れを補い、BOMと原byte列を保持する。
  二重通知と既存開始票の拒否は、画面表示前の元例外まで実行して確認した。
- モデル子プロセスの失敗試験は、重複tokenの元例外まで確認する。
  import失敗や資産不足だけで試験が通る偽陽性を防ぐ。
- 通常群と隔離23本を分け、実終了コード・ログ・JUnitを保存するrunnerを収録した。

## mainとの統合

main `2a87d4a6ff6d6af10b7402f0cae28a6dbae134f6` を取り込んだ。
16競合のmain側blobはG3修正前 `68a2abc` と一致したため、PR側のG3修正を保持した。
main固有の追加ファイルも取り込んだ。元workspaceのdirty変更は含めていない。

## 検証結果

- 既定の収集対象 **7,719件中7,701件合格、既存skip18件、失敗・未実行0件**。
  既定で除外されるslow 1件はこの母数に含めない。skipや除外を新設して通してはいない。
- 内訳は通常群7,299件（7,281合格・18skip）と隔離23本420件（全件合格）。
  各pytest原票をテストIDで結合し、重複・対象外・欠測が無いことを検査した。
- 記録補正は修復前の支え削除反例3件とraw欠測反例2件を再現。
  修復後の関連16件は、非空NPZの保存・再読まで合格。
- 採点器7件合格。WindowsとWSLの各OSネイティブ表記で外部原票を読めることを確認。
  旧video38の参考値3個も保存済み元データと一致した。
- モデル子プロセス6件合格。負例の元例外をassertした追加検査1件も合格。
  原票SHA・親モジュールへの非干渉・元のvideo38出力との一致を保持した。
- runnerの構文確認、既存出力先拒否・D保存域外拒否を確認した。
  23本のリストはcheckout後もLF、通知scriptはBOMと元SHAを維持する。

元の絶対パスを要求する試験は、private mount namespace内で提出checkoutを
元のパスへ割り当て、正規の既存資産をread-onlyで参照した。
元workspaceのディスク内容は変更していない。提出版との追跡入力4,140本の
SHA一致を確認した。Formal100/hidden reserveの資産は空のread-onlyディレクトリで隠した。
新しいログ・JUnit・JSONはD保存域へ記録した。
通常群の途中で、全sys.modulesを走査するfunnel試験が遅延したため正常に中断し、
JUnitを保存した。funnel全16件は独立プロセスで125.09秒で合格した。
成功済み3574件と既存skip3件を保持し、失敗した通知1件を含む残3725件だけを
別プロセスで継続した。最終集計は収集済みテストIDとの集合一致を検査し、重複を除く。
中断run単体のexit 2を成功へ書き換えていない。

## 独立レビューと残る範囲

Fableによる設計反証と最終コードレビューを実施した。採点器は全コードと全7試験を提示し、
目的範囲でコード本体の必須修正なしとの判断を得た。記録補正も原反例・修復差分・
保存経路・既定値・Board操作を提示した。

一部の独立票は、原実行と抜粋の紐付けや提示範囲を理由に条件付きとしている。
CPU全体の実行・ID集合の照合・namespace内の同一性確認は親Codexが担当した。
**独立担当が全suiteを再実行して合格した、という主張はしない。**
コードの独立レビュー、親のCPU検証、G3動画品質の判定を区別する。

実行版の対応は、通常前半が `f1928d5`、残対象が `57aa8b6`。
両版の差分は属性・通知script追加・runner・隔離リストだけで、
Board・pipeline・記録補正のSHAは前の全入力manifestと現在で同一。
採点器の最後のパス修正は影響する7件を前後SHA付きで検証した。
独立票の提示範囲による残コメントとの照合はD保存域の `review_resolution.md` に記録した。

ここでの試験合格は、コードのCPU動作と保存・子プロセス・資産接続の確認に限る。
修復後の実動画精度は未測定。G3の全体精度条件と予測入力の実走条件は未完のまま。
G4開始・学習・追加の本番採用・mergeをこのPRの検証結果から承認したものではない。

未実装の計測課題 `PR27-R1`: raw欠測での補正保留件数を、観測があり補正0件だった
場合と分けて集計する。欠測時の保存挙動はCPU検証済みだが、件数の可視化は今回の修復に含めない。

## 証拠

- 今回の検証: `D:/puyo_analyzer/verify/pr27_ready_2026-09-20_v1/`
- 初回修復・反例: `D:/puyo_analyzer/verify/pr27_fix_2026-09-20_v1/`
- 初回レビュー: `D:/puyo_analyzer/verify/pr27_review_2026-09-20_v1/`

主な原票は `main_full/`、`final_validation_receipt.json`、`isolated_final_summary.json`、`raw_before.xml`、
`raw_after.xml`、`model_final/`、`model_corrupt_guard.xml`、`namespace_input_proof.json`。
独立担当の実Read・原応答・利用量は `reviews/requests/` に保存している。
