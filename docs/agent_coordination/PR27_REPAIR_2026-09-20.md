# PR #27 レビュー修復の記録

対象は `2c1c92ceca0e01f820ba6aa182e9b0ad5fefbe97`。
隔離ブランチ `codex/pr27-review-fixes` で修復した。
元workspaceの他担当の変更・凍結原票は変更していない。

## 修復した内容

- 記録直前のCNN/HSV補正で、削除後の支えを使って追加を判定する。
  新しい浮きが生じた列は元へ戻し、独立した列の正常補正は維持する。
  元盤面・隠し段・UNKNOWNの扱いと既定OFFを維持する。
- 採点器は偏った食い違い標本の参考外挿であることを明示する。
  `quality_gate_clear=false`、`confidence_interval=null` とし、
  全件判定不能の点推定は `None`、人手判定の欠測・重複・対象外は例外にする。
  無効な95%下限と未到達の予測入力を含むG3全体合格の記載を訂正した。
- 未収録だった隔離テスト21本、記録補正テスト、G3コードとCPUテスト、
  参照する凍結コード・契約文書・固定snapshotのPythonを収録した。
  コピー元とのバイト一致を確認し、凍結コードの期待SHAやテスト期待値は変えていない。
  Python/シェルのLFと凍結資産の元バイト列を `.gitattributes` で保持する。
- 通常群と隔離22本を分ける既存c案を、実終了コード・ログ・JUnitを保存する
  `scripts/run_g3_partitioned_tests.sh` として収録した。
  失敗・未収集・空のJUnit・既存出力先・D保存域外を成功にしない。

## mainとの統合

統合対象mainは `2a87d4a6ff6d6af10b7402f0cae28a6dbae134f6`。
16競合のmain側blobはすべてG3修正前の `68a2abc` と一致したため、
PR側のG3修正を保持して解消した。main固有の追加ファイルも取り込んだ。
比較元・各blob IDは下記D保存域の `merge_comparison.txt` に保存した。

## CPU検証

- 修復前に3失敗を再現。修復後は記録補正12件と採点器6件、計18件が合格。
  支え削除の2反例は実採録入口・勝敗付与・NPZ保存・再読まで通し、
  保存が空でないこと（各1盤面）と重力整合を検査した。
- 採録・着地連鎖・試合範囲ゲートの関連統合は269件合格。
- runnerの既存出力先拒否・D保存域外拒否は2件ともexit 2。
  実際の隔離群失敗も外側exit 1として保存した。
- 隔離22本は初回一巡後、依存コード・契約文書の追加が影響する箇所だけを再試験した。
  同一テストの最新結果を統合すると **404件中357件合格、47件未検証**。
  後者は実pytestでは14 failure / 33 errorであり、skipや合格へ変更していない。

47件の内訳は、保存済みJSON原票・凍結manifest等の不足42件、
未配置の `data/frames/video_38.mp4` に依存する4件、
元workspaceの絶対パスを期待するvideo39 baselineの1件。
モデル・動画・実走原票はGit同梱外であり、正規の原環境が必要。
詳細は `docs/G3_TEST_REPRODUCTION.md` とD保存域の `final_test_summary.json` を参照。

ここでの合格はCPUの修復・接続確認の範囲に限る。
**全pytest、修復後の動画精度、G3全体合格、G4開始は未完**。
従前のG2合格や当時のローカル結果を取り消すものではなく、今回の提出版の検証範囲を示す。

## 証拠と独立検収

新規のログ・JUnit・JSON・レビュー票は
`D:/puyo_analyzer/verify/pr27_fix_2026-09-20_v1/` に保存した。
旧反例の票は `D:/puyo_analyzer/verify/pr27_review_2026-09-20_v1/` に保持する。
`before_v2.xml`、`save.xml`、`integration.xml`、`final_test_summary.json` が主なCPU証拠。

Fable mediumで設計・製造検収を行い、切れていた署名・返却部・
実行結果の未読箇所を追加提示した。各実Read・原応答・利用量は `reviews/` に保存。
確認済みの抜粋には重大コード欠陥の指摘は無かったが、最終票は証拠の結線・
一括提示の不足を理由に**独立合格を保留**している。CPUの親検収と区別する。
本コミットは修復内容の保存であり、PR承認・merge・G4開始の判断ではない。
修復後の実動画検収は行っていない。
