# E3 事前登録（結果を見る前に固定）

- 対象: q_7gc4TgFig / fcXG83vInDY / mia8KCjr52g、指定sourceの先頭0–900秒。保護集合はアクセスしない。
- 比較: 実行時に `advantage_overlay_flags()` を読み、ONだけ `--exchange-event-update` を追加。本番採用は変更しない。
- 母集団: 本番30fps正規化後の全処理フレーム（sample-interval=0）。待機・凍結・ホールドも含む表示dumpの時刻列をOFF/ONで完全一致検証。不一致は共通部分へ縮めず測定未成立。
- 撃ち合い母集団: ONのexchange_events.jsonlの全recordを固定し、OFFにも同じイベント時刻を適用。未完・欠測件数を併記する。
- M1: 最初のS3と最初の物理着地の差（秒）、差<0の割合、S3後の参加連鎖の終了合図撤回または継続の割合。早すぎS3の分母はS3発生撃ち合い数、着地前割合の分母はS3と着地が両方ある撃ち合い数。早すぎS3 ≤ 20%。撤回はend_signals.revoked_sec>S3、追加参加はobserved_sec>S3として数え、同じ撃ち合いは一度だけ数える。
- M2: 全参加連鎖の最終score_finalize_secの最大を終了時刻とする（score_readyによる代用なし）。着地後最初のG_fe確率を共通目標とし、各モードの終了直前表示から目標への変化量の50%へ初めて達した時刻との差を測る。終了時点で到達済みなら0秒。目標変化ゼロは0秒。探索上限は同試合の次の撃ち合い発火または試合末、未到達は+∞として中央値に含める。確定・G_fe欠測は件数を示す。ON中央値 < OFF中央値。
- M3: 表示1P勝率と勝敗のlog loss/AUCを全体・P_time三等分で算出。P_timeは各評価対象試合区間内の相対経過率、境界は1/3と2/3。確率clipは1e-7、AUC単一クラスは未定義。
- qの本判定はoutcome_receipt_v1/RESULT.jsonの独立勝敗4試合をcounter_binding_v1/RESULT.jsonの[start,end_exclusive)へ結合し、ASSIGNMENTS.jsonでrange_idを照合。区間内の全フレームを使い、元の疎な保存行だけには限定しない。ラベルなしフレーム数を併記。q全体log loss ON ≤ OFF + 0.005。
- fcX/miaのM3は認識由来勝敗のみ参考扱い。使用ラベルの出所・区間を明示し、無ラベルは除外数を明示する。
- M4: 全処理フレームでabs(display_adv)≥99の割合。符号反転は同試合内で0を飛ばした直前非ゼロ符号からの反転回数、分母は全処理フレーム時間/60。境界で符号履歴をリセット。ON反転/分 ≤ OFF×1.2。
- 全指標にフレーム数・撃ち合い数・試合数（該当する分母）と欠測数を併記。機械的閾値判定だけを報告し、採否は統括に委ねる。
- 早すぎS3代表例は動画リスト順・時刻昇順の先頭3件（3件未満なら実件数）。
- 細部決定: 900秒を分割せず動画×モードで保存・再開、最大3並列/nice 19、固定seed=20260926、既存本番資産を作業先へ複製、元動画は共有指定素材なので保持、短時間起動確認は本集計から除外。
