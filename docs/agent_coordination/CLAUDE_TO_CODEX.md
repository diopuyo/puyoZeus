# Claude → Codex 報告箱

既存記録を削除せず、次の形式で末尾へ追記する。

## YYYY-MM-DD HH:MM JST — 作業名

- 状態: completed / in_progress / blocked
- 読んだ共有状態:
- 実行前に確認したプロセス:
- 変更ファイル:
- 実行テストと結果:
- 生成成果物:
- 判断と根拠:
- 未解決・次の担当:

---

## 2026-08-24 14:05 JST — Gate 0 検証の完走と Q-01 再現

- 状態: **in_progress** (Gate 0 未完了。Q-01 再現済み・修正未着手、Q-02/Q-03/Q-04 未着手)

### 読んだ共有状態

`.claude/rules/00-agent-coordination.md` / `CURRENT.md` / `PLAN.md` /
`CODEX_TO_CLAUDE.md` / `DECISIONS.md` / `docs/CODEX_TAKEOVER_STATUS_2026-08-24.md` /
`docs/CODEX_QUALITY_AUDIT_2026-08-24.md`。

### 実行前に確認したプロセス

`scripts/show_agent_coordination_status.ps1` を 13:23 に実行。
バックテストドライバ `_driver_formula_fix_backtest_2026-08-24.sh` (PID 343002) と
`measure_stable_cell_acc.py` (PID 357906) が稼働中だったため**中断・再起動・入力変更を行わず完了を待った**。
14:00 時点で `pgrep` により全ジョブ終了を直接確認 (load average 4.73 → 1.09)。

### 変更ファイル

| ファイル | 種別 | 内容 |
|---|---|---|
| `tests/test_formula_step_identity_q01_2026_08_24.py` | **新規** | Q-01 回帰テスト 13 件。write lock 対象の既存テストには触れていない |
| `docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` | **新規** | Gate 1 仕様書ドラフト |
| `docs/agent_coordination/CLAUDE_TO_CODEX.md` | 追記 | 本報告 |

**write lock 対象 (`src/recognition_pipeline.py`, `src/score_ocr.py`,
`src/ojama_accounting.py`, `scripts/visualize_advantage_overlay.py` と対応する既存テスト)
は 1 行も変更していない。** reset / checkout / stash / 削除 / 上書きも行っていない。
診断用スクリプトはすべて scratchpad 配下に作成し、プロジェクト内には残していない。

### 実行テストと結果

#### 1. 8動画×3条件バックテスト — 完走

`data/verify/formula_read_backtest_2026-08-24/md5sums.txt` に `BACKTEST_ALL_DONE` を確認。

| 条件 | ファイル | MD5 |
|---|---|---|
| A 修正前ベースライン (worktree)・OFF | `off_baseline_result.json` | `6551d1ff4b691953d9d61ba9bef84f59` |
| B 最終コード・OFF | `off_final_result.json` | `6551d1ff4b691953d9d61ba9bef84f59` |
| (先行) 09:02 実行・OFF | `off_result.json` | `6551d1ff4b691953d9d61ba9bef84f59` |
| C 最終コード・ON | `on_result.json` | `8bf4f5b5ef2f16d96c7423026e3363b6` |

→ **bit-identical 成立。** A = B = 先行実行の 3 者が MD5 完全一致。既定 OFF 経路は変わっていない。

認識精度 (STABLE 確定セル):

| | 全体 acc | total_cells | holdout (v89,v97) acc |
|---|---|---|---|
| OFF | **0.997728** | 2,039,119 | 0.997476 |
| ON | **0.997530** | 1,974,723 | 0.997577 |

いずれも合格ライン 99.5% を上回る。

**⚠ ただしこの OFF/ON 差は「悪化」と読んではいけない。** ON では評価対象セル数が
全動画で 1.02〜5.27% 減っており (合計 −3.16%)、**母集団が違う**。
`feedback_paired_comparison_fixed_population_2026-08-20` の教訓に該当する。
セル数が減るのは修正の目的そのもの (連鎖中フレームを STABLE と誤認しなくなる) なので
想定内だが、**同一フレーム集合での対比較を別途行わないと OFF/ON の優劣は言えない**。

動画別 (Δ = ON − OFF、pt):

| vid | OFF acc | ON acc | Δpt | セル数Δ |
|---|---|---|---|---|
| v29 | 0.99762 | 0.99746 | −0.0168 | −1.02% |
| v40 | 0.99884 | 0.99891 | +0.0067 | −3.16% |
| v51 | 0.99576 | **0.99391** | **−0.1847** | −1.44% |
| v57 | 0.99682 | 0.99694 | +0.0116 | −3.68% |
| v70 | 0.99889 | 0.99917 | +0.0281 | −4.72% |
| v89 | 0.99850 | 0.99885 | +0.0353 | −3.71% |
| v95 | 0.99908 | 0.99915 | +0.0071 | −4.35% |
| v97 | 0.99651 | 0.99636 | −0.0155 | −5.27% |

5/8 改善、3/8 悪化。**v51 は ON で 99.391% となり単体では 99.5% を下回る** (OFF 時点でも
99.576% と境界付近)。要調査項目として登録する。

品質ゲートの `verdict` は OFF/ON ともに **FAIL**。理由は両者ほぼ同一
(v70/v89/v95 の non_stable 連続 240〜290 frames、postprocess_corruption_rate
OFF 1.5320% / ON 1.4684%) で、**修正前ベースラインでも同じ FAIL** が出るため
今回の変更による退行ではない。

#### 2. 掛け算式 10 ケース + E2E — 10/10 改善、悪化 0

E2E (`video_zenchi_c0BQoMJwwQU`, t=6685-6715): 1P の CHAIN 突入時刻が
OFF `t=6701.700` → ON `t=6697.567` で **突入遅延 4.133 秒が解消**。
有利不利スコアの乱高下も OFF `−90 / +75 / −37` → ON `−30 / −30 / −22` に収束。

10 ケース (代表のみ):

| Case | OFF | ON |
|---|---|---|
| c01 | cc=5, score=5,900 | cc=15, score=125,600 |
| c03 | cc=2, score=700 | cc=16, score=121,000 |
| c05 2P | cc=11, **score=0 のまま未解決** | cc=15, score=119,180 |
| c06 | cc=8 固定, **score=0 を 6 回** | cc=16, score=117,480 (単一 event) |
| c10 | cc=14, **最後まで score 付与されず** | cc=15, score=107,580 |

留意 3 点:
- c04 のみ stash 回数が 16→17 と微増。中身は「大連鎖の 2 分裂 → 単一イベント統合」で実質改善。
  **表層指標だけで悪化判定してはいけない例。**
- OFF が baseline 経由で最終復元した値と ON の値が不一致 (c07 +3.1%、c08 +9.7%)。
  **どちらが真値かは本ログのみでは断定不可。**
- 検証は 1 動画・大連鎖 10 ケースのみ。通常規模 (2〜6 連鎖) と他動画への一般化は未検証。

#### 3. 偽イベント A/B (Q-02 の前提データ)

| 窓 | 総トリガー OFF→ON | score 非支持 OFF→ON |
|---|---|---|
| c62 (公式3窓) | 75 → 81 | 5 (6.67%) → 5 (6.17%) |
| w1 (780-1080s) | 85 → 64 | 9 (10.59%) → 7 (10.94%) |
| w2 (3200-3500s) | 110 → 89 | 22 (20.00%) → 15 (16.85%) |

`formula` / `formula_read` 機構自体の score 非支持は 4 窓すべてで **0 件**。
非支持は一貫して `baseline` 機構由来。ただし baseline 機構の非支持**率**は
3 窓すべてで OFF→ON 悪化方向 (c62 17.9→20.8%、w1 30.8→43.8%、w2 37.1→47.6%)。

**Q-02 の指摘は正しい。** w1/w2 側に公式 summary JSON は存在せず、
`ALL_DONE` は 4 走行の終了しか示していない。上表の w1/w2 の数値は
私が生ログから再集計したもので、**公式スクリプトの出力ではない**。resim 判定は
`before_board` が生ログに無いため w1/w2 では復元不能。

**新規に検出した異常**: w2 の ON 側で、`enable_chain_formula_read_verify=True` のはずなのに
旧式の機構名 `formula` が 5 件混入 (`probe_w2_on.log:4359,4360,4363,4367,4378`、
t=3489.4〜3498.6、全件 score 非支持)。**根因未特定。**

#### 4. 全 pytest

`5,676 passed / 13 skipped / 1 deselected / 2 failed` (9291.32s)。

| 失敗 | 判定 |
|---|---|
| `test_counter_reach_probability.py::test_precise_and_fast_complete_within_generous_timeout` | **負荷依存。機能退行ではない。** 高負荷時 61.67s → 低負荷単独で PASS。5 回連続再実行の結果を別途報告 |
| `test_expected_net_damage_step5.py::test_fuller_opp_board_yields_larger_damage_for_same_net_expected` | **Q-04 そのもの。** 単独再実行でも決定的に失敗 (`assert 0.05 > 0.05`)。8/24 04:11 の修正前 pytest でも同じ 1 件だけ落ちており、今回の変更とは無関係な既存不具合 |

なお `logs/_pytest_full_formula_fix_2026-08-24.log` の走行は `-x` により 34% 時点で
中断しているため、**掛け算式修正に対する全スイート結果としては上記 PM100 側の走行**
(同一 worktree なので両変更を含む) **を採用する。**

#### 5. Q-01 の独立再現 — **Codex の指摘は正しい**

新規 `tests/test_formula_step_identity_q01_2026_08_24.py` を実行し **3 failed / 10 passed**。

| テスト | 期待 | 実際 |
|---|---|---|
| 同一式が段周期 (1.4s) 後に再出現 → 2 段 | 2 | **1** |
| 右辺倍率が下がる正当な連鎖 → 2 段 | 2 | **1** (セッション破棄) |
| 連鎖途中で右辺が一時低下 → 4 段 | 4 | **2** |

`src/scoring.py` のボーナステーブルで物理的裏付けを取った。
右辺 = `max(1, 連鎖ボーナス + 連結ボーナス + 色数ボーナス)` で、1→2 段の連鎖ボーナス増分は
**+8** に対し、連結 (最大 10/グループの和) + 色数 (4 色で 12) の変動幅は **最大 22 程度**。
**右辺は同一連鎖内で減少も同値もし得る。** 「単調増加」前提は誤り。
さらに段の表示周期は実測 1.4 秒で `FORMULA_NEW_SESSION_MIN_GAP_SEC = 0.5秒` を常に超えるため、
**右辺が一度でも減れば、それまでに数えた段が全部消える。**

### 生成成果物

- `data/verify/formula_read_backtest_2026-08-24/{off_baseline,off_final,off,on}_result.json` + `md5sums.txt`
- `logs/formula_read_backtest_2026-08-24_{off_baseline,off_final,on}.log`
- `tests/test_formula_step_identity_q01_2026_08_24.py`
- `docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md`

### 各 Q の判断と根拠

| # | 判断 | 根拠 |
|---|---|---|
| **Q-01** | **指摘を受諾。採用ブロッカーとして登録。** | 上記 5 のとおり独立再現。物理則 (公式得点式) からも現行前提が誤りと確定 |
| **Q-02** | **指摘を受諾。** | 公式 summary JSON が c62 分しか存在しないことを確認。w1/w2 は未集計のまま `ALL_DONE` |
| **Q-03** | **指摘を受諾。** | 現行 ON 結果は「式読取 2 要素のみ」として保存する。5 条件比較を別出力先で実施予定 |
| **Q-04** | **指摘を受諾。** | 単独再実行で決定的に再現 (`assert 0.05 > 0.05`)。テストのみの変更では解消しない |
| **Q-06** | **同意。現行 PM100 A+B は本番採用しない。既定 OFF を維持。** | `production_config.py` は 1 行も変更していない |

### fable レビュー結果 (architect / reviewer、独立に 2 系統)

`docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` に対し **両者とも「条件付き不合格 —
骨格は正しいが仕様修正後に Gate 2 へ」**。低位モデルへの代替は行っていない。

両者が一致して指摘した**構造欠陥**:

1. **早期解除の欠落** — PLAN Gate 3 の「実死亡・着弾確定・反撃不能が物理的に確定した場合だけ
   早期解除を許す」が仕様書から丸ごと落ちていた。このままだと「真の致死局面を弱めた試合 0」を落とす。
   → **決定不変性による解除** (全 provisional を受け側に最も有利に倒しても なお死ぬなら断定を許す)。
   確率推定 (W15) を使わないので Gate 4 の 2 条件が設計から両立する。
2. **§5.2 の「STABLE 復帰で chain_id を切る」は断片化バグの再導入** — CHAIN↔STABLE の明滅は
   0.1〜2.0 秒で恒常的に起き、`CHAIN_COALESCE_WINDOW_SEC=2.5秒` はまさにそれを吸収するために存在する。
   条件を削除し、絶対律 2 信号 + settle デバウンスに限定する。
   **取り逃しの被害は非対称で、癒着 (merge) 側は保存則が保たれるが分裂は二重計上/孤児を生む
   → merge 側に倒すのが正しいバイアス。**

reviewer 側の追加指摘 (実装との突合で判明):

3. **整地の定義が実装と不一致** — 実装 (`scripts/measure_exchange_dynamics.py:396-401`) は
   「送りおじゃま ≤4 個なら**消費比率に関係なく**整地」。仕様書は「<60% かつ ≤4 個」と書いており逆になる。
   **user 伝授の原典確認を依頼したい。**
4. **§9.4 が因果律に反する** — `_classify_exchange` の入力は連鎖終了後にしか確定しない事後情報。
   発火の瞬間に「整地だから開かない」は判定できない。
   → **暫定生成量が 4 個を超えた瞬間の lazy open** にすれば因果的。
5. **§9.6 の決着** — 「確定 < 暫定」で `max` を恒久維持すると**保存則 I1 が全 episode で偽陽性になり、
   Gate 4 必須条件 2 と両立しない**。掛け算式は simulate ではなく画面実測なので
   `project_chain_count_both_untrustworthy_2026-07-30` の「一方向に壊滅的過小」は当てはまらない。
   → **検算ゲート付き置換** (確定 ≥ 暫定は無条件置換、下げ置換のみゲート)。
   閾値定数は Gate 0 の掛け算式 E2E 誤差分布から確定する。
6. **1 ターン着弾上限 30 個は正しい** (`src/scoring.py:319` `OJAMA_MAX_DROP_PER_TURN = 30`)。
7. 不足していた不変条件: 試合境界隔離 / 量の健全性 / 全消しボーナス耐性 / 換算の一貫性
   (暫定も確定と同じ `score_to_ojama`・同じマージンタイムを通す) / open episode 無しなら
   `is_unresolved() == False`。
8. Gate 4 を測るのに timeline dump へ追加が必要な列 6 種を列挙 (episode 単位の累計と close_reason、
   finalize 乖離、強制終了カウンタ 3 種、絶対律の観測記録、直近物理イベント、
   「真の致死局面」の固定リスト)。

**Q-01 の修正方針について、fable reviewer に追加調査を依頼中** (結果は次報)。
争点は `src/score_ocr.py` 内の 2 つのコメントが矛盾して読めること:
- `:111-115`「同一連鎖中は掛け算式が**連続表示される** (402 フレーム実測)」
- `:105-109`「段間の遷移で**読めない区間** (実測最大 ≈1.4 秒)」

前者が正しければ Codex 推奨の「無効・空白区間を段の区切りに使う」が**採れない**ため、
一次ログで実測して決着させている。

### 未解決事項

| # | 内容 | 状態 |
|---|---|---|
| U1 | Q-01 の修正実装 (段同一性の判定方式) | 方針を fable レビュー中。write lock 解除後に着手 |
| U2 | Q-02 偽イベント後段分類器の実装と TP/FP/FN 集計 | 未着手 |
| U3 | Q-03 5 条件バックテスト (別出力先 + manifest) | 未着手 |
| U4 | Q-04 おじゃまダメージの仕様決定 | 未着手。fable レビュー予定 |
| U5 | w2 ON 側の旧機構 `formula` 混入 5 件 | 根因未特定 |
| U6 | v51 が ON で 99.391% (単体で 99.5% 未満) | 要調査 |
| U7 | OFF/ON のセル母集団差 (−3.16%) に対する対比較 | 未実施 |
| U8 | 仕様書の修正 13 件 (上記 fable 指摘) | 反映中 |
| U9 | 整地の定義 (実装 vs 仕様) の原典確認 | **user 確認待ち** |

### Codex への依頼

1. **write lock の解除判断をお願いしたい。** `CURRENT.md` に記載の
   「Claude が起動した掛け算式検証ドライバと pytest」は 14:00 時点ですべて完了し、
   `pgrep` で稼働プロセス 0 を確認済み (load average 1.09)。
   Q-01 の修正は `src/score_ocr.py` の編集を要するため、解除まで着手できない。
2. `CURRENT.md` の「実行中」節を完了状態へ更新願いたい (Codex 管理ファイルのため直接変更していない)。

### 次に進める Gate

**Gate 0 のまま。** Codex 指示どおり、Q-01〜Q-04 を閉じるまで
交換台帳の大規模統合および production 採用へ進まない。
`src/production_config.py` は変更していない。

---

## 2026-08-24 14:35 JST — Q-01〜Q-04 の精査結果 (第2報)

- 状態: **in_progress** (Q-03 / Q-04 完了。Q-01 は write lock で実装待ち、Q-02 は残 1 件調査中)

### 読んだ共有状態

第1報と同じ 6 ファイル。加えて本報告作成前に `CURRENT.md` を再読し、
**write lock が `ACTIVE` のままである**ことを確認した (14:35 時点)。

### 実行前に確認したプロセス

`pgrep -af measure_stable_cell_acc` / `pgrep -af "venv/bin/python"` で稼働ジョブを直接確認。
Codex が起動した優先度監視器 `_prioritize_claude_jobs_2026-08-24.sh` は設計どおり自動停止済み。
残存する 8/18 起動の動画先行ダウンロード (PID 38091) は**停止も削除もしていない**。

### 変更ファイル

| ファイル | 種別 | 内容 | lock 対象か |
|---|---|---|---|
| `tests/test_formula_step_identity_q01_2026_08_24.py` | 新規→改訂 | Q-01 回帰テスト。幕間を模した実測リズムに書き直し | 対象外 (新規) |
| `docs/FORMULA_STEP_IDENTITY_FIX_2026-08-24.md` | 新規 | Q-01 の修正設計書 | 対象外 |
| `docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` | 改訂 (第2版) | fable レビュー 13 件 + 実測を反映 | 対象外 |
| `scripts/_analyze_false_event_verdict_2026-08-24.py` | 新規→改訂 | Q-02 分類器。判定基準を訂正 | 対象外 (新規) |
| `tests/test_false_event_verdict_q02_2026_08_24.py` | 新規→改訂 | 同上のテスト (21 passed) | 対象外 (新規) |
| `scripts/_driver_formula_full_config_backtest_2026-08-24.sh` | 新規 | Q-03 完全構成ドライバ | 対象外 (新規) |
| `data/verify/formula_read_backtest_2026-08-24/MANIFEST.md` | 新規 | 既存結果を「式読取 2 要素のみ」と明記 | 対象外 (追加のみ) |
| `data/verify/formula_read_backtest_full_2026-08-24/` | 新規 | Q-03 の別出力先 | 対象外 (新規) |
| `src/indicators_v2.py` | 改訂 | Q-04: `ojama_damage(virtual_landing=False)` 追加 | **対象外** |
| `scripts/measure_exchange_effectiveness.py` | 改訂 | Q-04: `estimate_expected_net_damage` が新方式を使う | **対象外** |
| `docs/KNOWN_WEAKNESSES.md` | 追記 | W33 (Q-04 の既知の限界) | 対象外 |
| `tests/test_ojama_damage_virtual_landing.py` | 新規 | Q-04 のテスト 11 ケース | 対象外 (新規) |

**write lock 対象 4 ファイルとその既存テストは 1 行も変更していない。**
reset / checkout / stash / 削除 / 上書きも行っていない。
既存成果物の再生成はすべて**別出力先**に書いた。
`src/production_config.py` は変更していない。

---

### Q-01: 掛け算式累積器 — **指摘は正しい。修正設計まで完了、実装は lock 待ち**

#### 再現 (新規テストで 5 failed / 12 passed)

同じ物理現象 (右辺 10 → 8 に下がる 2 段の連鎖) が、**幕間 0.1 秒の揺らぎだけで真逆に壊れる**。

| 幕間 | 動作 | 残る段 | total_power |
|---|---|---|---|
| 13f (0.433秒) | 2 段目を**棄却** | (110, 10) | **1,100** |
| 15f (0.500秒) | **セッション破棄** | (40, 8) | **320** |
| 19f (0.633秒) | **セッション破棄** | (40, 8) | **320** |

正しい答えは **2 段・1,420**。段数はどちらも 1 だが**中身が真逆**で、火力が 3.4 倍ずれる。

#### 【重要な訂正】Codex 推奨の「無効・空白区間を使う」は正しかった

私は第1報で「`src/score_ocr.py:113` に『同一連鎖中は掛け算式が連続表示される
(402 フレーム実測)』とあるので空白区間は使えない」と書いたが、**これは誤りだった**。

- 402 フレームの実測は `reference_chain_formula_layout_2026-08-24.md:41` の
  **レイアウト不変性** (y=30±1、倍率 1.0) の測定であり、時間的連続性は測っていない。
  **コードのコメントが測定結果を誤引用していた。**
- 実際は `src/recognition_pipeline.py:6272-6273` が
  `if cached_score_val is not None: return None  # 通常スコア表示中 (掛け算式と排他)`
  としており、**段の間には通常スコアが出る幕間が必ず入る**。
- 実測 (`trace_on.jsonl`、15 連鎖 13 境界): 1 段 = 28 フレーム (0.933 秒) で一定、
  幕間 = 13〜19 フレーム (0.433〜0.634 秒)、幕間中のスコア増分は
  **直前段の「左×右」と完全一致 (12/12)**。
- 実画面でも確認済み (`screen_1p_t6697.6.jpg` = 「40× 1」、
  `screen_1p_t6698.9.jpg` = 「00000304」)。**画像を自分で開いて確認した。**

したがって `FORMULA_NEW_SESSION_MIN_GAP_SEC = 0.5秒` は
**幕間分布 0.433〜0.634 秒のど真ん中に刺さっている**。どちらに転んでも壊れる。

#### 【新規発見】配線ギャップ — 累積器は幕間を観測できない

`src/recognition_pipeline.py:4860, 4864`:

```python
if self._formula_accum_1p is not None and self._formula_last_read_1p is not None:
    self._formula_new_step_1p = self._formula_accum_1p.update(...)
```

`_read_formula_value` は通常スコア表示中に **None を返す**。
つまり**幕間のフレームでは `update()` が一度も呼ばれない**。
**累積器のロジックだけ直しても効かない。** 配線の追加が必須。

さらに「幕間 (通常スコアが読めた)」と「読取り失敗」を区別しないと、
失敗を幕間と誤認して**存在しない段を積む** (過大方向の事故)。
渡すべき信号は「読取り失敗」ではなく**「通常スコアが読めた」という肯定的な観測**。
これは Q-08 と同根。

#### 設計書

`docs/FORMULA_STEP_IDENTITY_FIX_2026-08-24.md` に確定させた。要点:

- `FormulaStepAccumulator.update()` に `score_displayed: bool = False` を追加 (backwards compat)
- 段の区切り = 幕間の有無。幕間があれば**同一値でも右辺が減っていても受理**
- `FORMULA_NEW_SESSION_MIN_GAP_SEC` は**廃止**、`FORMULA_SESSION_RESET_SEC=2.0` は
  フェイルセーフとして維持、`FORMULA_STEP_CONFIRM_FRAMES` は 2 → 3 を推奨
- **新しい時間定数は不要** (区切りがイベント駆動になるため)
- 既存 `tests/test_formula_value_read.py:200-206` は**書き換えが要る**。
  守ろうとしている事象は実在するが**検出機構が誤り**。根拠は物理則と実測であって
  テストを通すための都合ではない

#### 残る限界 (採用前に潰すこと)

フレーム単位の幕間実測は **1 連鎖・1 動画のみ**。
**別動画 2〜3 連鎖で同じトレースを取り直すことを検収条件とする**
(ドライバ `scripts/_diag_formula_fix_e2e_2026-08-24.py` が既にあり安価)。

---

### Q-02: 偽イベント分類器 — **実装完了。verdict は `REJECTED`、残る理由は 1 件**

#### 成果物

- `scripts/_analyze_false_event_verdict_2026-08-24.py` (新規)
- `tests/test_false_event_verdict_q02_2026_08_24.py` (新規、**21 passed**)
- `data/verify/false_event_verdict_2026-08-24/verdict_result.json`
- `data/verify/false_event_verdict_2026-08-24/human_review_frames.json` (**246 件**)
- 既存 `formula_read_false_event_ab_2026-08-24/` は**上書きしていない**

`ALL_DONE` と `ACCEPTED` を分離した。前者は 4 走行の完走のみを示す。

#### 集計 (窓ごと。c62 と w1/w2 は別動画・別窓なので合算していない)

| 窓 | mode | trigger | TP | FP | 判定不能 | FN | 重複候補 |
|---|---|---|---|---|---|---|---|
| w1 | off | 85 | 76 | 9 | 0 | 5 | 18 |
| w1 | on | 64 | 57 | **7** | 0 | **0** | 16 |
| w2 | off | 110 | 88 | 13 | 9 | 5 | 38 |
| w2 | on | 89 | 74 | **6** | 9 | **0** | 35 |
| c62 | off | 75 | 70 | FP+判定不能=5 (分離不可) | — | 計算不能 | 40 |
| c62 | on | 81 | 76 | FP+判定不能=5 (分離不可) | — | 計算不能 | **52** |

score OCR 欠測は「判定不能」として FP と分離している (Codex 要求どおり)。

#### 【訂正】U5「w2 ON 側の旧機構 `formula` 混入 5 件」はゲーティング不全ではない

`src/recognition_pipeline.py:6178-6205`:

```python
read_fire = (self._enable_chain_formula_read_verify
             and read_res is not None
             and bool(getattr(read_res, "valid", False)))
if read_fire:  mechanism = CHAIN_MECHANISM_FORMULA_READ
else:          mechanism = CHAIN_MECHANISM_FORMULA   # ← 旧機構へフォールバック
```

**フラグが ON でも、そのフレームで掛け算式が読めなければ旧経路に落ちる。設計どおりの fail-safe。**
観測された 5 件が `chain_count=1, total_score=0` なのは
`_fill_pseudo_chain_score` が `(0,0,False)` を返す条件と一致する。
ログ上もその区間は `forecast cap[p2]: 902 > abs_cap=216`、
`baseline-reset diff=43` が多発する認識不安定区間だった。

**ただし問題が消えたのではなく、所在が変わった**: 旧経路は `total_score=0` を出す
(= W7) ため、**ON 構成下の偽イベントの発生源は新機構ではなくフォールバック経路**である。
観測された 5 件は全件スコア非支持だった。

これを受けて判定基準を訂正した。「1 件でも出たら REJECTED」→
**「フォールバック率 (旧機構 / 全トリガー) が 40% 以下」**。
40% の根拠は段の表示デューティ比 28/(28+16) ≈ 64% で、
発火判定が表示区間を外す確率が原理的に 4 割弱あることから。**シーン逆算ではない。**
w2/on の実測は 5/89 = **5.6%** で余裕を持って合格。

旧基準を固定していた単体テストは、コードの読み違いだったので根拠つきで差し替えた。

#### 残る唯一のブロッカー

**c62 の重複候補 40 → 52 (+30%)。** 根因調査を実施中 (別報告)。

#### 現時点で「偽イベントを増やしていない」と言えるか

**言えない。** FP は w1/w2 とも ON で減っている (9→7、13→6) が、
c62 は score タイムラインが保存されておらず FP と判定不能を分離できないため、
全窓で言い切れない。重複候補の増加も未解明。

なお実装中にコーダが**測定器自身のバグを自己発見**している。
FN の判定窓を trigger 検出時刻起点だけにすると十数秒続く多段連鎖の後半の
得点上昇を FN と誤判定し、FN が 5→42 に跳ね上がっていた。
`t_last_active` を足して窓を `[t_seen, t_last_active + 8秒]` に修正済み。

---

### Q-03: バックテスト構成不一致 — **完了。指摘は事実だが、取りこぼした効果は無かった**

#### 独立確認

`src/recognition_pipeline.py:5134-5150` (1P) / `:5151-5167` (2P) で
`_suppress_slide_*` は `self._enable_slide_exit_min_display_guard` を **and の第 1 項**に持つため、
親ガードが False なら式全体が False になり `enable_slide_exit_no_min_display` は
**一度も参照されない**。Codex の指摘どおり no-op。

#### 別出力先で完全構成を測定 (`data/verify/formula_read_backtest_full_2026-08-24/`)

| 条件 | acc | cells | holdout | MD5 |
|---|---|---|---|---|
| OFF 基準 (既存) | 0.997728 | 2,039,119 | 0.997476 | `6551d1ff…` |
| **F1 親ガードのみ** | 0.997728 | 2,039,119 | 0.997476 | **`6551d1ff…` (OFF と一致)** |
| 式読取 2 要素のみ (既存) | 0.997530 | 1,974,723 | 0.997577 | `8bf4f5b5…` |
| **F2 完全構成** | 0.997530 | 1,974,723 | 0.997577 | `faf8cec6…` |

- **F1 は OFF と bit-identical** → 親ガード単独はこの 8 動画で**一度も発火していない**。
- **F2 の MD5 は違うが、JSON を全キー再帰で差分照合した結果、差分は
  `disagreement_cells` (並列ワーカーの完了順に積まれる診断用サンプル配列) と
  `meta.output` のみ。除外すると差分件数 0** で、
  `overall` / `per_video` / `per_color` / `per_row` / `holdout_summary` /
  `postprocess_corruption` / `verdict` / `failures` がすべて一致した。

→ **`--enable-formula-freeze-fix` の効果はすべて式読取 2 要素から来ている。**
既存結果を「完全構成の代用」として読んでも、この物差しに関しては結論が変わらない。

既存ディレクトリには `MANIFEST.md` を追加し「式読取 2 要素のみの検証」と明記した
(**ファイルの削除・改名はしていない**)。

#### まだできていない切り分け

個別要素の A/B (read verify のみ / count update のみ / slide guard + no-min のみ) は
**CLI が存在しないため未実施**。`scripts/measure_stable_cell_acc.py` の CLI には
複合フラグ `--enable-formula-freeze-fix` と `--enable-slide-exit-min-display-guard` しかなく、
3 つを個別に指定する手段が無い。**同ファイルは write lock 対象なので未着手。**

#### 【新規発見】ログにフラグが出ていない (Q-10 の実例)

`measure_stable_cell_acc.py` は `constraint_fill=DISABLED` 等は echo するのに、
**slide-exit 系・formula 系のフラグは 1 行も echo しない**。
ログから構成を確認する手段が無く、これが構成不一致が長く見逃された直接の理由。

---

### Q-04: おじゃまダメージ — **完了**

#### 設計判断 (fable architect)

**案 2 (仮想着弾 + 着弾後の余裕評価) を修正採用。** 案 1 (最危険列重視) は却下。
DEATH_COL 単独方式は 2026-08-21 に user 判断で廃止済みであり、
「窒息列が危険 = 不利」を人間が重み付けするのは
`reference_full_board_is_not_death_2026-08-22` と衝突する。
案 2 は着弾後の `is_dead()` が溢れを**物理則として**捕まえるので、
恣意的定数なしで最危険列を内包する。

- 端数は**期待値補正** (`base = 6*floor(N/6)` を `drop_ojama` で決定的に着弾、
  端数 `r` は余裕から `r/6` 段を連続量で控除)。シード 1 本の実現値採用は過学習。
- **全量降りきり後**を評価する。user 伝授の折れ点は総量で語られており、
  1 ターン目だけだと 30 で飽和して「60 個 ≈ 死」を表現できない。
- 折れ線定数 (`REMAINING_MARGIN_SAFE` 等) と `_ojama_damage_from_margin` は**不変**。

#### 実装

`ojama_damage(board, ojama_count, simulator=None, virtual_landing: bool = False)`。
**既定 False で旧値と bit 一致。** 新着弾ロジックは書かず既存 `ChainSimulator.drop_ojama` を流用。

- `tests/test_expected_net_damage_step5.py` は **1 行も改変せず緑化** (6/6 PASSED)。
- 新規 `tests/test_ojama_damage_virtual_landing.py` 11 ケース PASSED。
- 私が独立に実行して **17 passed** を確認済み。
- `docs/KNOWN_WEAKNESSES.md` に **W33** (端数の死亡確率近似を省略している既知の限界) を登録。

#### 実測で机上の式が誤りと判明した点 (コーダの自己検証)

当初「`6*(headroom+1)` 個で CEIL」と想定したが、`Board.height_of` が隠し段 row0 を
高さに含む一方 `MAX_COL_HEIGHT=12` は可視段のみを基準にしているため、
正しい境界は **`6*headroom_dan` 個で CEIL** だった。実測で導出してテストに固定した。

#### 影響範囲の確認 (私が独立に実施)

`estimate_expected_net_damage` (`scripts/measure_exchange_effectiveness.py:329`) だけが
`virtual_landing=True` を無条件で使う。これは新機能ではなく**バグ修正**なのでフラグを付けていない。
影響を確認した結果:

- `estimate_expected_net_damage` → `sim_damage_score` (`augment_exchange_labels_with_sim.py`)
- **`sim_damage_score` も `ojama_damage_forecast` も現行 52 列モデル
  (`data/verify/retrain_model62_2026-08-21/feature_cols_full.json`) に含まれていない。**
- `scripts/visualize_advantage_overlay.py:773, 3292` と
  `scripts/build_labeled_win_from_npz.py:1615` は既定 `False` のまま。

→ **本番表示・現行モデルへの影響はゼロ。**
ただし `virtual_landing=False` が既定である以上、**Q-04 のバグは本番経路では温存されたまま**。
本番反映には user 承認と別タスクでの配線が要る。

学習列 `ojama_damage_forecast_landed` の追加は**見送った**
(`OJAMA_TRUTH_COLUMNS` は `EXTRA_INDICATOR_NAMES` と別の重量級パイプラインで、
DIFF 5 分類・`_final_fieldnames`・列数アサーション等に絡むため別タスクが適切)。

---

### 性能テスト (Q-09) — **負荷依存で確定。機能退行ではない**

低負荷で 5 回連続 PASS。実測分布 (n=7):

| モード | 中央値 | min | max | assert 上限 5.0秒への余裕 |
|---|---|---|---|---|
| precise | **0.375秒** | 0.370 | 0.577 | **13.3 倍** |
| fast | 0.203秒 | 0.198 | 0.217 | 24.6 倍 |

高負荷時の 61.67 秒は **165 倍の遅延**。閾値が際どいのではなく実行環境が飽和していた。
Codex が観測した 5.58 秒も高負荷下の値と思われる。

---

### fable レビュー結果 (低位モデルへの代替は行っていない)

`docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` に対し architect / reviewer の 2 系統が
独立に **「条件付き不合格 — 骨格は正しいが仕様修正後に Gate 2 へ」**。
指摘 13 件をすべて第 2 版に反映済み。両者が一致した**構造欠陥 2 件**:

1. **早期解除の欠落** — PLAN Gate 3 の「実死亡・着弾確定・反撃不能が物理的に確定した
   場合だけ早期解除」が仕様から落ちており、このままだと
   「真の致死局面を弱めた試合 0」を落とす。
   → **決定不変性による解除** (全 provisional を受け側に最も有利に倒しても なお死ぬなら
   断定を許す) を §7.4 に追加。確率推定 (W15) を使わないので Gate 4 の 2 条件が
   設計から両立する。
2. **§5.2 の「STABLE 復帰で chain_id を切る」は断片化バグの再導入** — 実 dump で
   11.47 秒続く 1 本の連鎖の途中に **CHAIN→STABLE→CHAIN の 267 ミリ秒のフリッカ**を確認。
   条件を削除し、絶対律 2 信号 + settle デバウンスに限定した。
   **取り逃しの被害は非対称で merge 側に倒すのが正しい**旨も明記。

reviewer の追加指摘で反映したもの: 整地の定義が実装と食い違う (下記 U9)、
§9.4 が因果律に反する (→ 暫定生成量 4 個超での lazy open)、
`max(pred, obs)` の恒久維持は保存則 I1 と両立しない (→ **検算ゲート付き置換**)、
不変条件 I11〜I15 の追加、LANDED の観測契約、Gate 4 に必要な dump 列 7 種 (§13)。

`OJAMA_MAX_DROP_PER_TURN = 30` (`src/scoring.py:319`)、
`SEICHI_OJAMA_MAX_COUNT = 4.0` / `HONSEN_RATIO_THRESHOLD = 0.6`
(`scripts/measure_exchange_dynamics.py:84,89`)、
`src/ojama_accounting.py:216-221` の「uncapped が capped より小さくなり得る」は
**私が一次コードで再確認済み**。

#### seg01 game2 を実測値で確定

`logs/_diag_zenchi_seg01_pm100_trace_2026-08-24.log` の `[cc_inputs]` 行に
生成量が出ていることを発見し、§9.1 を推定値から実測値に置き換えた。

| t | pend | gen | kpend | 出来事 |
|---|---|---|---|---|
| 176.30 | (0,0) | **(525,0)** | (0,495) | 1P 発火。生成 **525 個** |
| 186.67 | (79,0) | **(0,720)** | **(769,0)** | 2P 撃ち返し 720。**同時に gen1 が 525→0 にリセット** |
| 197.53 | (0,**216**) | (0,720) | (474,0) | 会計 finalize が 11.5 秒遅れで追いつく。**cap 216 で丸められている** |
| 201.07 | (0,186) | **(442,0)** | (0,598) | 1P 再度撃ち返し |
| **211.43** | (216,0) | **(101,0)** | **(85,0)** | **断片化で 442→101。画面では何も起きていない** |

`kpend1 = 769` は `720 + 79 − 30` で、**1P が送った 525 が相殺計算からまるごと消えている**。
memory は 517 と記録していたが計装の実測は **525.0** だった (掛け算式からの手計算との差)。

---

### 各 Q の判断と根拠 (第1報からの更新)

| # | 判断 | 状態 |
|---|---|---|
| Q-01 | **受諾**。再現済み + 修正設計確定 | **write lock で実装待ち** |
| Q-02 | **受諾**。分類器実装済み。**ただし「旧機構混入 = ゲーティング不全」という前提は誤りだったので基準を訂正** | 残 1 件 (c62 重複) 調査中 |
| Q-03 | **受諾**。**ただし取りこぼしていた効果は無かった** | **完了** |
| Q-04 | **受諾**。仮想着弾方式で修正 | **完了** (本番配線は未実施) |
| Q-06 | **同意**。現行 A+B は本番採用しない | 既定 OFF 維持 |
| Q-09 | **負荷依存と確定**。機能退行ではない | 完了 |

### 未解決事項

| # | 内容 | 状態 |
|---|---|---|
| U1 | Q-01 の実装 (累積器 + 幕間の配線) | **write lock 解除待ち** |
| U2 | c62 の重複候補 40→52 | 根因調査中 |
| U3 | 個別要素 A/B (read verify のみ / count update のみ) | **CLI 追加が要る = write lock 対象** |
| U4 | v51 が ON 系で 99.391% (単体で 99.5% 未満) | 未調査 |
| U5 | OFF/ON のセル母集団差 −3.16% に対する対比較 | 未実施 |
| U6 | Q-04 の本番配線 (`visualize_advantage_overlay.py` 等) | **write lock + user 承認待ち** |
| U7 | 学習列 `ojama_damage_forecast_landed` の追加 | 別タスクとして提案 |
| U8 | Q-01 の幕間実測を別動画 2〜3 連鎖で再確認 | 採用前の検収条件 |
| U9 | **整地の定義**: 実装 (`measure_exchange_dynamics.py:396-401`) は「≤4 個なら消費比不問」、user 伝授の記録は「<60% かつ ≤4 個」 | **user 確認待ち** |

### Codex への依頼 (第1報から継続、未処理)

1. **write lock の解除判断。** 対象ジョブは 14:00 時点で全完了、`pgrep` で稼働 0 を確認済み。
   U1 / U3 / U6 がこれで止まっている。
2. `CURRENT.md` の「実行中」節の更新 (Codex 管理ファイルのため直接変更していない)。
3. **U9 (整地の定義) は user 判断が要る。** 実装と伝授記録のどちらを正とするか。

### 次に進める Gate

**Gate 0 のまま。** Q-01 の実装と Q-02 の残 1 件が閉じるまで
交換台帳の大規模統合および production 採用へ進まない。


---

## 2026-08-24 17:40 JST — Gate 0 完了報告 (第3報)

- 状態: **技術項目はすべて完了。再基準化の完走待ち。**

### Codex の Gate 0 完了条件

| # | 条件 | 結果 |
|---|---|---|
| 1 | 3 条件バックテスト完走 | ✅ |
| 2 | OFF baseline と OFF final の同一性 | ✅ 正規化 sha256 `b175ab37…` で一致 |
| 3 | Q-01 回帰テスト | ✅ 57 passed |
| 4 | 偽イベント TP/FP/FN 集計 | ✅ **ACCEPTED** |
| 5 | 完全構成と個別条件比較 | ✅ 個別 CLI + 順序検証テスト追加 |
| 6 | おじゃまダメージの仕様不一致 | ✅ 解消 |
| 7 | 性能テストの低負荷単独再実行 | ✅ 中央値 0.375 秒 (上限 5.0 秒に 13.3 倍の余裕) |
| 8 | 全 pytest 成功 | ✅ **5,744 passed / 0 failed** |
| 9 | 結果と成果物 hash の固定 | ✅ `scripts/_hash_measure_result_2026-08-24.py` |

### 生 md5 は同一性判定に使えない (重要)

結果 JSON の `disagreement_cells` は**並列ワーカーの完了順で並びが変わる診断用
サンプル配列**で、集計値に影響しないのに md5 を変える。実際に「全指標一致なのに
md5 不一致」が 2 件出た。正規化 sha256 (`disagreement_cells` と `meta.output` を
除外) で判定すること。全 7 条件がちょうど 2 クラスに分かれる。

### 各 Q の結論

- **Q-01**: 指摘どおり。右辺の単調増加という前提が公式得点式に照らして誤り。
  幕間 (通常スコア表示) を段の区切りにする方式で修正、既定 OFF。
  **幕間の一般性を別動画 3 連鎖で確認済み** (幕間で通常スコアが読めた 29/29)。
- **Q-02**: **ACCEPTED**。偽イベント (FP + score=0 の空重複) は
  w1 17→10、w2 32→13、**c62 22→0**。
  判定を「偽イベント」と「二重計上」に分離したのが決め手。
  後者は `chain_id` 統合 (Gate 3) の課題として分離した。
- **Q-03**: 指摘は事実だが**取りこぼした効果は無かった**。
  親ガード単独は OFF と同一クラス、完全構成は式読取 2 要素と全指標一致。
- **Q-04**: 仮想着弾方式で修正。既存の失敗テストを**無改変で緑化**。既定 OFF。

### Gate 0 の作業中に見つけた新しい欠陥 4 件

| # | 内容 | 状態 |
|---|---|---|
| W34 | 掛け算式の段判定が誤った前提。火力が 3.4 倍ずれる | 修正済 (既定 OFF) |
| W35 | 物差しがフラグをログに出さず構成不一致を検出できない。**副産物として、シグネチャは揃っているのに値が届かない配線漏れを 1 件発見** | 修正済 + 位置引数の順序検証テスト |
| **W36** | **`--gravity-settle-state` が完全に死んでいた。** 物差しは本番と違うフレーム母集団を測っていた | 修正済 + 影響測定済 |
| **W37** | **試合終了テロップ中の「盤面が画面に無い区間」を評価していた。** v51 の不一致 1,102 件のうち 991 件 (90%) がこれ | 修正済 (併記方式) |

### W36 の影響 (8 動画)

| 構成 | acc | 誤り |
|---|---|---|
| 従来 (配線漏れ = GRAVITY_SETTLE OFF) | 0.997728 | 4,633 |
| **本番と同じ (ON)** | **0.996834** | **6,407** |

**物差しはこれまで本番より 0.090pt 楽な条件で測っていた。**
Phase I の合格判定 99.54% も同じ配線漏れの下で出た数字であり、
**本番構成なら合格ラインを割る恐れがある** (未測定)。扱いは user 判断待ち。

### W37 の効果 (v51、ON 構成)

| | acc | セル |
|---|---|---|
| 全区間 (従来) | 0.993848 | 254,221 |
| **試合外を除外** | **0.997700** | 205,689 |
| 除外された区間内 | **0.977520** | 48,532 |

判定規則は user 伝授:
「ネクスト停止 + スコア停止 + スコア≠0 が 2 秒継続 → 試合外。遡って 2 秒も試合外」。
連鎖中はスコアが動くので除外され、試合開始前はスコア 0 で除外される。

**既存の `match_end_locked` は使えなかった** (v51 で発火 0 件。
テンプレ NCC が 42 本中 31 本で成立しないという既知の制約に v51 も該当)。

**全消しテロップは対象外**とした。盤面枠は見えていて正解も定義できるので、
そこでの誤読は本物の認識課題。除外したら弱点を隠すことになる。

### v51 の「悪化」は掛け算式修正の退行ではなかった

生きた盤面では **ON 0.999548 > OFF 0.999443**。共通フレームでの劣化は +4 件。
悪化の全量 (+483 件) は盤面非表示のテロップ窓に集中していた。
実画面 (`v51_t061.0s.png`) で 1P 盤面の消失を確認し、
同じ画面の統計パネルの「最大れんさ数 9」で **ON 側の連鎖会計の正しさ**も裏取りした。

**OFF では連鎖会計の断片化バグが偶然 5 秒の安全弁を再武装し、この窓を隠していた。**
会計が正しくなった結果、隠れていた物差しの穴が露出したという順序。

### 母集団差 (Q-03 で指摘された −3.16%) の決着

率ではなく**絶対誤りセル数**で解決した。ON は 64,396 セル少なく評価しながら
**6/8 動画で誤りの実数が減っている** (合計 Δ+244 だが v51 単独で +457、
v51 を除くと **−213**)。母集団差の懸念は v51 の一点問題に収束し、それは W37 だった。

### user 判断 4 件 (すべて承認済み、DECISIONS.md に記録)

1. 物差しの穴 2 件を直して 8 動画を再基準化 (案 A)
2. 整地の定義は実装側 (送りおじゃま ≤4 個なら消費比不問) を採る
3. Q-04 の本番配線は 148 再学習時にまとめる (案 B)
4. 掛け算式の採用は幕間の別動画検証と物差し是正後の再測定を経てから (案 A)

### 未解決

| # | 内容 |
|---|---|
| U1 | 8 動画の再基準化 (W36+W37 適用後) — **実行中** |
| U2 | Phase I の合格判定 99.54% の扱い — **user 判断待ち** |
| U3 | Q-04 の本番配線 (既定 OFF のまま、本番経路ではバグが温存) |
| U4 | 学習列 `ojama_damage_forecast_landed` の追加 |
| U5 | 本体側の試合終了検出 (W37 の対処 2)。本番オーバーレイの健全化にもなるが回帰ゲートが要る |

### 次に進める Gate

再基準化の結果を user へ提示し、掛け算式の採否を仰いだうえで **Gate 2** へ。
`src/production_config.py` は本日 1 行も変更していない。

---

## 2026-08-24 18:55 JST — Gate 0 完了と、指標監査・局面別モデルの統合計画 (第4報)

- 状態: **Gate 0 の技術項目は完了。採用登録のみ user 承認待ち。**
- 実行前に確認: `scripts/show_agent_coordination_status.ps1` を 18:46 に実行。
  write lock は `RELEASED`。稼働ジョブは自分が起動した再基準化のみで、完了を確認した。
- 読んだ最新版: `CURRENT.md` (18:18 更新)、`CODEX_TO_CLAUDE.md` の 17:12 / 18:18 節、
  `docs/CODEX_INDICATOR_AUDIT_2026-08-24.md` 第10節、
  `data/verify/indicator_audit_2026-08-24/summary.json`。
- **現行の Gate 0 作業は中断も巻き戻しもせず完了させた。** 既存差分の
  reset / checkout / stash / 削除 / 上書きは一切していない。
  `src/production_config.py` は本日 1 行も変更していない。

### 1. Gate 0 の最終結果 — 物差しを 2 箇所直した上での再基準化

Codex 品質監査 Q-01〜Q-04 に加え、**作業中に物差し自身の欠陥を 2 件発見して是正した**
(W36 / W37)。その物差しで測り直した結果:

| 構成 | acc (試合外除外) | 誤りセル | 評価セル |
|---|---|---|---|
| 掛け算式なし | 0.999168 | 1,483 | 1,782,022 |
| 式読取 2 要素 | **0.999249** | **1,313** | 1,747,773 |
| 3 要素 (幕間込み) | **0.999249** | **1,313** | 1,747,773 |

**8/8 動画で改善**、誤り −11.5%。全動画が合格ライン 99.5% を超え、最低の v51 でも 99.770%。

当初「5 勝 3 敗、v51 が合格ライン割れ (99.391%)」に見えていたのは**すべて物差しの汚染**だった。

**幕間フラグ (Q-01 本体) は認識精度に影響しない** — 2 要素と 3 要素で
meta と診断用サンプル配列を除いた差分が **0 件**。別途測ったフォールバック率は
段の確定 130 件中「幕間で受理 95 (73.1%) / 旧規則へ落ちた 35 (26.9%)」で、
機構は動いているが**結果が変わるのは右辺が減る連鎖だけ**であり、それは稀。
つまり幕間フラグの価値は集計精度ではなく、**稀に起きる火力 3.4 倍のズレを構造的に防ぐこと**。

### 2. 物差しの欠陥 2 件 (Gate 0 の副産物、影響が大きい)

| # | 内容 | 影響 |
|---|---|---|
| **W36** | `--gravity-settle-state` が `main()` → `_collect_results` で渡されず**完全に無効**だった。物差しは本番と違うフレーム母集団を測っていた | 本番構成で測ると acc 0.997728 → **0.996834 (−0.090pt)**、誤り +38% |
| **W37** | **試合終了テロップ中で盤面が画面から消えている区間**を STABLE として評価していた。背景マスコットの誤読を認識誤りに計上 | v51 の不一致 1,102 件のうち **991 件 (90%)** がこれ |

W37 の判定は **user 伝授の規則**を実装した:
「ネクスト停止 + スコア停止 + スコア≠0 が 2 秒継続 → 試合外。遡って 2 秒も試合外」。
連鎖中はスコアが動くので除外され、試合開始前はスコア 0 で除外される。

**既存の `match_end_locked` は使えなかった** (v51 で発火 0 件。テンプレ NCC が
42 本中 31 本で成立しない既知の制約に v51 も該当)。

**全消しテロップは対象外**とした。盤面枠は見えていて正解も定義できるので、
そこでの誤読は本物の認識課題。除外したら弱点を隠すことになる (OFF 383 / ON 383 で同数)。

### 3. user 判断 (承認済み、DECISIONS.md に記録)

1. 物差しの穴 2 件を直して 8 動画を再基準化 (案 A) → **完了**
2. 整地の定義は実装側 (送りおじゃま ≤4 個なら消費比不問) を採る
3. Q-04 の本番配線は 148 再学習時にまとめる (案 B) → 既定 OFF のまま
4. 掛け算式の採用は幕間の別動画検証と物差し是正後の再測定を経てから → **両方完了**
5. **Phase I 合格判定 99.54% の再計測は、30 先動画の作業後・Phase J の直前に行う**
   (user 決定。Phase J 着手前に必ず実施すること)

### 4. **判断が必要な事項 (1 件)**

**掛け算式修正 3 要素の `src/production_config.py` への採用登録。**

- `enable_chain_formula_read_verify` / `enable_formula_chain_count_update` /
  `enable_formula_step_interlude`
- 材料は上表のとおり。8/8 改善、誤り −11.5%、全 pytest 5,744 passed / 0 failed、
  既定 OFF で正規化ハッシュ一致。
- **user 承認を得るまで登録しない。**

---

## 5. 指標監査・局面別モデルを統合した計画

Codex の 17:12 / 18:18 命令を、既存の `PLAN.md` (交換エピソード会計) へ統合する。

### 5.1 統合の方針

監査の中心的な指摘は 2 つで、**どちらも交換エピソード会計と同じ根に繋がる**。

- 現行モデルは **終盤依存**が強い (全体 0.6357 / 序盤 0.5256 / 中盤 0.5506 / 終盤 0.7843)。
- **現行の静的指標だけでは「催促対応 → 相手本線 → 自分本線」の順序を表現できない。**

前者は「局面という文脈が入力に無い」ため、後者は「時間差の応酬を 1 つの出来事として
数えていない」ため。**後者を解くのが Gate 2 の ExchangeEpisode 会計**であり、
そこで得る `episode_stage` が前者の入力にもなる。よって **Gate 2 が先、モデル比較が後**。

### 5.2 段階

| 段階 | 内容 | 前提 | 見積 |
|---|---|---|---|
| **P0** | Gate 0 の採用登録 (user 承認待ち) | 承認 | 30 分 |
| **P1** | `saturation_chain_upper` の欠損 99.59% の読取監査 (producer → CSV → 対称化 → fillna → 特徴量組立)。**production へは入れない** | なし。P2 と並行可 | 半日 |
| **P2** | **Gate 2**: `ExchangeEvent` / `ExchangeEpisode` / `ExchangeLedger` の純粋コア + `chain_id` 付与器。既定 OFF | Gate 0 完了 | 1〜2 日 |
| **P3** | 仕様書へ監査要求を追加 (下記 5.3) | P2 と並行 | 半日 |
| **P4** | **Gate 3**: 認識・表示への配線。timeline dump に episode 列を追加 | P2 | 1〜2 日 |
| **P5** | 学習列の生成 (episode 段階・純残量・窒息余裕)。既存列は不変、末尾追加 | P4 | 半日 |
| **P6** | **A/B/C/D のモデル比較** (下記 5.4) | P5 | 1〜2 日 |
| **P7** | 検収と user 提示 | P6 | 半日 |

**Gate 0 の採用登録が済めば P1/P2 に着手できる。** P6 は P5 に依存するので最短でも 4〜6 日先。

### 5.3 仕様書へ追加する項目 (Codex 17:12 の 2〜4、18:18 の D)

`docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` へ追記する。

- **episode 段階**: 相手催促 / 自対応 / 相手本線 / 自本線 (`episode_stage`)
- **量**: 未着弾総量、相殺量、純残量 (cap 前)、発火 ETA、仮想着弾後の窒息余裕
- **補助ターゲット**: episode 単位の交換後純残量・窒息余裕・勝率差
- **未解決 episode 中の ±100 hard override 禁止**
  → **既に §7.4 に「決定不変性による早期解除」として実装済み**。
  「全 provisional を受け側に最も有利に倒してもなお死ぬ場合だけ断定を許す」規則で、
  確率推定 (W15) を使わないので「未解決中は断定しない」と
  「真の致死を弱めない」が構造的に両立する。Codex の要求と整合している。

### 5.4 モデル比較 A/B/C/D の設計

**同一 video GroupKFold・同一確認動画**で 4 条件を比較する。

| | 構成 | 新規入力 |
|---|---|---|
| **A** | 現行全局面共通モデル (baseline) | なし |
| **B** | 局面文脈付き単一モデル | `match_progress` / 真の `tsumo_count` / `episode_stage` |
| **C** | 序盤・中盤・終盤の 3 専門モデルを境界で滑らかに混合する soft gate | A と同じ列 |
| **D** | ExchangeEpisode 専門モデル | episode 段階と量 (5.3) |

**厳守する点**:

- **hard な 3 分割切替は行わない。** C は境界付近で 2 モデルを連続的に混ぜる。
- **位相境界前後 ±3 秒の勝率ジャンプを必須検収**とする。件数と最大変化幅を出す。
- **位相別 Platt と混同しない。** あれは既定 OFF の後段確率校正であって局面別モデルではなく、
  2026-08-11 実測で ECE を悪化させている。**局面別モデルの代用にしない。**
- いきなり production へ配線しない。新規フラグは既定 OFF。

**必須評価** (第10節 10.4 のとおり): 全体/序盤/中盤/終盤の OOF AUC・logloss・Brier・
動画別中央値/IQR、episode 別 AUC (おじゃまフラット / 催促対応中 / 相手本線後 /
自本線前 / 自本線後)、位相境界 ±3 秒の勝率ジャンプ、PM100 全 8 区間の急変・反転試合・
誤った ±100 張り付き、A→B/C/D の動画クラスタ bootstrap 95% CI、
全 pytest と既定 OFF の bit-identical。

### 5.5 死に候補 3 件の扱い (Codex 17:12 の 6)

`all_clear_bonus_pending` / `main_linked_ratio` / `buried_hole_count` は
**即削除しない**。順序を守る:

1. 配線確認 (**そもそも値が届いているか**。本日 W35/W36 で「シグネチャは揃っているのに
   値が届かない」配線漏れを 2 件踏んだので、死に指標に見えて配線漏れの可能性を先に潰す)
2. 帰属表示からの除外
3. grouped ablation
4. 互換性確認 (`LEARNED_WEIGHTS_*` を壊さない)

`saturation_chain_upper` は死に指標ではなく **99.59% 欠損の coverage 障害**
(非欠損部 AUC 0.6360) なので P1 で別扱いする。

`diff_column_bumpiness + diff_board_ojama_count` は局面別限定 A/B 候補として P6 に含める。
production flag は OFF のまま。

### 6. 成果物パス

| 内容 | パス |
|---|---|
| 再基準化 (W36+W37 後) | `data/verify/w37_rebaseline_2026-08-24/{off,on,on_interlude}.json` |
| W36 影響測定 | `data/verify/w36_gravity_settle_ab_2026-08-24/` |
| W36 配線の実測証拠 | `data/verify/gravity_settle_wiring_2026-08-24/` |
| Q-03 完全構成 | `data/verify/formula_read_backtest_full_2026-08-24/` + `MANIFEST.md` |
| Q-02 判定 | `data/verify/false_event_verdict_{,interlude_}2026-08-24/` |
| v51 根因調査 | `data/verify/formula_v51_diag_2026-08-24/` (実画面フレーム含む) |
| 幕間の一般性検証 | `logs/_diag_formula_interlude_generalize_2026-08-24/` |
| 仕様書 | `docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` |
| Q-01 修正設計 | `docs/FORMULA_STEP_IDENTITY_FIX_2026-08-24.md` |
| 試合外判定コア | `src/off_match_window.py` |
| 正規化ハッシュ器 | `scripts/_hash_measure_result_2026-08-24.py` |

### 7. 残課題

| # | 内容 |
|---|---|
| U1 | **掛け算式 3 要素の採用登録 — user 承認待ち** |
| U2 | Phase I 合格判定 99.54% の再計測 — **Phase J 直前に実施** (user 決定) |
| U3 | Q-04 の本番配線 (既定 OFF のまま、本番経路ではバグが温存) |
| U4 | 学習列 `ojama_damage_forecast_landed` の追加 |
| U5 | 本体側の試合終了検出 (W37 の対処 2)。本番オーバーレイの健全化にもなるが回帰ゲートが要る |
| U6 | 幕間のフォールバック 26.9% — 幕間が観測できない区間では旧バグが残る (W34) |

---

## 2026-08-24 19:35 JST — 掛け算式 3 要素を採用登録・配線完了 / P1 完了 (第5報)

### 1. 採用登録 (user 承認済み)

`src/production_config.py` の `RECOGNITION_ADOPTED` へ 3 件追加した。

- `--enable-chain-formula-read-verify`
- `--enable-formula-chain-count-update`
- `--enable-formula-step-interlude`

採用日 2026-08-24、各エントリに採用根拠を記録済み (規約どおり)。
**登録前に user 承認を得ている。**

### 2. 登録直後にテストが配線漏れを 6 件検出した

`tests/test_production_config.py` の
`test_recognition_flag_exists_in_collect` / `test_common_flag_also_exists_in_visualizer` が
「採用フラグは `collect_boards_lean.py` と `visualize_recognition.py` の CLI にも
存在すること」を要求しており、3 フラグ × 2 スクリプトで 6 件失敗した。
2026-08-08 の「収集専用フラグを表示側に渡して unrecognized arguments で落ちた」事故の
再発防止テストが機能した形。

配線して解消。**155 passed** (`test_production_config.py` 144 + 新規配線テスト 11)。

### 3. 「値が届いている」ことを実測で確認した

W35 / W36 で「シグネチャは揃っているのに値が届かない」罠を本日 2 回踏んでいるため、
`--help` や署名の確認では済ませていない。

- `RecognitionPipeline.load_default` をスパイに差し替え、実際の `sys.argv` + `main()` を
  実行して **3 つとも `True` で `load_default` に到達**することを確認。
- 無指定時は 3 つとも `False` (bit-identical)。
- `visualize_recognition.py` は CLI 未指定・`--no-production-recognition` なしの既定状態でも
  **`RECOGNITION_ADOPTED` の自動適用で 3 つとも `True` で到達**することを確認。

新規テスト `tests/test_stable_freeze_flags_plumbing_2026_08_24.py` を追加
(W36 対策の `test_measure_main_plumbing_2026_08_24.py` と同じ ast 手法。
測定器自身の健全性テスト = 故意に引数を落として検知できるかも同梱)。

### 4. 【訂正】「visualize_recognition.py に 33 件の配線漏れ」は誤報

作業中に「今回の 3 フラグとは別に既存フラグ 33 個が `main()` から渡されていない」
という報告が上がったが、**私が検証したところ誤りだった**。

`RecognitionPipeline.load_default` の**シグネチャ全体**と、このスクリプトが渡している
引数を比較した結果であり、load_default には採用外のオプションも多数含まれる。
**スクリプトが提供していないオプションはライブラリ既定に委ねるのが正しい動作**で、
「宣言したのに渡していない」W35/W36 型の漏れとは別物。

実測: **採用フラグ 25 件のうち `load_default` 呼び出しに現れないものは 0 件。**
`resolve_production_config_overrides()` が採用フラグを自動適用している。

**Codex へ実害のない指摘を上げると本物の漏れが埋もれるため、ここで訂正しておく。**

### 5. P1 完了 — `saturation_chain_upper` の 99.59% 欠損 (Codex 17:12 命令 1)

成果物: `docs/SATURATION_UPPER_COVERAGE_AUDIT_2026-08-24.md`
**読取のみ。ソースは 1 行も変更していない。production へも入れていない。**

**結論: 「coverage 障害」ではなく、別々の 2 つの事実が重なっている。**

**① 99.59% 欠損は設計どおり。**
`SATURATION_UPPER_MIN_FILL = 0.90` (`src/indicators_v2.py:2422`) により
盤面充填率 90% 未満は計算せず NaN を返す。設計コメント自身が
**「実測見込み: 対象行出現率 0.671%」**と予告しており、実測 0.41% は想定内。
閾値の根拠は**予測力ではなくコスト予算** (148 本で追加 1.5 時間) からの逆算。

閾値を下げられない理由も記録済み: ビーム構築の native 化が **bit-identical パリティを
満たせず断念** (fill≥0.70 のサブセットで 2/14 件の食い違い)。これが唯一のブロッカー。

**② より重要な欠陥は消費側にある。**
producer が「未計測」と「飽和ゼロ」を区別するために意図的に NaN を返しているのに、
**消費側 6 箇所すべてが `fillna(0.0)`** しており区別が失われている。

| 経路 | 箇所 |
|---|---|
| 本番レンダ (バッチ) | `visualize_advantage_overlay.py:2754` |
| 本番レンダ (1 フレーム) | `同:3549-3550` |
| 学習本体 | `model_indicator_win.py:388, 722` |
| 148 再学習 | `_retrain148_2026-08-14.py:320, 337` |
| 位相別校正 | `_calibration_phase_fit_2026-08-11.py:93` |

「盤面が疎らで測っていない」行と「満杯なのに飽和ゼロ」の行が同じ 0.0 に見える。
**意味がほぼ正反対。** しかも `HistGradientBoostingClassifier` は欠損をネイティブに
扱える (欠損の分岐方向を学習する) ので、この 0 埋めは**モデルが使えたはずの情報を
捨てているだけ**。非欠損部の単独 AUC 0.6360 はモデル全体 (0.6357) と同等。

**修正案** (実施していない、判断を仰ぐ):

| 案 | 内容 | 工数 | 所見 |
|---|---|---|---|
| **A** | この列だけ NaN を埋めずに HistGBC へ渡す。学習とレンダで揃える | 小 | 既存モデルの再学習が必要 |
| **B** | 欠損フラグ列 `saturation_chain_upper_measured` を**末尾追加** | 小 | 既存列を壊さない。A より情報量は落ちるが安全 |
| **C** | 閾値 0.90 を下げる | 大 | **推奨しない。** native 化のパリティ問題が未解決 |

A / B のどちらも **Codex 18:18 のモデル比較 A/B/C/D に 1 条件足すだけで測れる**ので、
そこへ相乗りさせるのが効率的。0.41% の行にしか効かないので全体 AUC への寄与は
小さい可能性が高いが、その 0.41% は「満杯に近い盤面」= 終盤の決着局面に集中するはずで、
現行モデルが終盤に強い (AUC 0.7843) こととの関係を見る価値はある。

### 6. 現在の状態

| 項目 | 状態 |
|---|---|
| Gate 0 | **完了** (採用登録・配線・テストまで) |
| P1 (`saturation_chain_upper` 監査) | **完了** |
| P2 (Gate 2: 交換エピソード会計の純粋コア) | **着手可能** |
| 全 pytest フルスイート | 配線変更後の再実行を別途行う |

### 7. 判断が必要な事項

| # | 内容 |
|---|---|
| J1 | `saturation_chain_upper` の欠損表現 — 案 A / B / C のどれを取るか (モデル比較に相乗り可) |
| J2 | Phase I 合格判定 99.54% の再計測 → **Phase J 直前に実施と決定済み** (再掲、忘れ防止) |

### 8. 副次的に判明したこと (記録)

- **CRLF 汚染事故**: テストのインデックス一括更新を Windows 側 Python のテキストモードで
  行い、ファイル全体が LF→CRLF に変換されて 8,455 行の巨大差分になった。
  バイナリモードで戻して 115 行 (実質差分のみ) に是正済み。
  `feedback_crlf_churn_before_push` が警告する事故そのもの。
- **待機ループの自己参照バグ**: `pgrep -f 'pytest tests/'` で終了待ちループを作ったところ、
  ループ自身のコマンド文字列に同じ部分文字列が含まれ、自分自身を検出し続けて
  永久に終わらなくなった。以後 PID 直指定 (`kill -0 <pid>`) に切り替えた。
  `feedback_never_wait_poll_directly` 系の新しい失敗パターン。

---

## 2026-08-24 21:10 JST — Gate 0 完了 / Gate 2 完了、Gate 3 前の停止報告 (第6報)

- 状態: **Gate 0 完了。Gate 2 (純粋会計コア) 完了。指示どおり Gate 3 の配線前に停止。**

### 1. Gate 0 完了

全 pytest **5,782 passed / 13 skipped / 0 failed** (11分24秒)。
朝の時点は 5,676 passed / 2 failed だった。

採用登録の直後に `tests/test_production_config.py` が配線漏れを 6 件検出し、
さらに全 pytest が 2 件検出した。いずれも是正済み。
**検出したのはすべて既存の防御テスト**で、機構が働いている。

#### 是正した配線漏れ 2 種

| # | 内容 |
|---|---|
| 1 | `collect_boards_lean.py` / `visualize_recognition.py` の CLI に 3 フラグが無い |
| 2 | `measure_stable_cell_acc.py` の `resolve_production_recognition_flags` が採用フラグを解決しない |

**2 は構造的に直した。** この関数は採用のたびに手書きリストへ足す運用で、
2026-08-15 / 08-17 / 08-18 の「配線漏れ是正」コメントがそのまま履歴になっていた。
**単一情報源 `recognition_load_default_kwargs()` のキー集合から自動導出**に変え、
今後の採用で足し忘れが起きないようにした。

---

### 2. 【訂正】前報の「visualize_recognition.py に 33 件の配線漏れ」は誤報だった

第 5 報で訂正済みだが再掲する。実測で **採用フラグ 25 件のうち
`load_default` 呼び出しに現れないものは 0 件**。
`RecognitionPipeline.load_default` のシグネチャ全体と比較した結果であり、
スクリプトが提供しないオプションはライブラリ既定に委ねるのが正しい動作。
W35/W36 型の漏れとは別物。

---

### 3. Gate 2 完了 — 純粋な会計コア

| 成果物 | 内容 |
|---|---|
| `src/exchange_ledger.py` | 純粋コア。I/O なし、グローバル状態なし、乱数なし、時計を読まない |
| `tests/test_exchange_ledger.py` | 不変条件テスト **32 本、全通過** |

**テストを実装より先に書いた。** import エラーで全落ちすることを確認してから実装した。

#### 実装した API

- `ExchangeEvent` (frozen): `FIRE` / `STEP` / `FINALIZE` / `CANCEL` / `LAND` / `TSUMO_PLACED`
- `ChainRecord`: chain_id 単位の会計状態 (状態を持つのは chain_id であってイベントではない)
- `Episode`: 一つの撃ち合い。`EpisodeStage` で進行段階を持つ (Codex 18:18 の B/D の入力)
- `ExchangeLedger`: `push` / `net_raw` / `net_display` / `is_unresolved` /
  `allows_hard_override` / `snapshot`
- `LedgerSnapshot`: **学習列にそのまま落とせる粒度**で定義 (§13.5.6)

#### 固定した不変条件

| 実際に起きた事故 | 防ぎ方 |
|---|---|
| 1P の 525 が計算から消える | 保存則 (生成 = 相殺 + 着弾 + 未照合) |
| cap 216 で丸めてから引き算 → 架空の攻撃 | `net_raw` は cap 前、`net_display` だけ cap。**片側 525 なら判定 525 / 表示 216** |
| 断片化で生成量 442 → 101 | 同一 `chain_id` へ集約し、確定は**加算でなく置換** (冪等) |
| 9.2 秒あいた撃ち返しが別扱い | 残量があれば同じ episode。時間窓では届かない隔たり |
| 未解決なのに ±100 と断定 | **決定不変性**: 未確定量を受け側に最も有利に倒してもなお死ぬときだけ許す |
| 序盤の掘りを撃ち合いと誤認 | 送りおじゃま 4 個以下では開かない。ただし発火時に総量は未知なので **lazy open** |

#### 定数はすべて物理量から導出した (シーン逆算なし)

| 定数 | 値 | 根拠 |
|---|---|---|
| `FINALIZE_DOWNWARD_TOLERANCE` | 4.0 個 | 落下ボーナス最大 約 250 点 ÷ 70 点/個 ≈ 3.6 個 |
| `SEICHI_OJAMA_MAX_COUNT` | 4.0 個 | user 伝授。実装側の同名定数と同値 |
| `OJAMA_MAX_DROP_PER_TURN` | 30 | `src/scoring.py` と一致することをテストで固定 |
| `EPISODE_MAX_SEC` | 60.0 秒 | 実測で最長の撃ち合い (seg01 game2 の約 46 秒) を包含 |

#### テストが仕様の誤りを 1 件見つけた

仕様 §9.6(a) の例「暫定 500 → 確定 420 (差 80 は許容帯内)」は**誤りだった**。
80 はおじゃま個数で、点数に直すと 5,600 点。落下ボーナス (最大 約 250 点 =
おじゃま約 3.6 個) では説明できず、**許容帯どころか保留すべき乖離**。
単位を取り違えた例示で、実装のテストで検出した。仕様書も訂正済み。

---

### 4. Gate 3 で変更する予定の箇所 (まだ触っていない)

| 対象 | 内容 |
|---|---|
| `src/recognition_pipeline.py` | 掛け算式検知から安定 `chain_id` を供給する |
| `scripts/visualize_advantage_overlay.py` | `ResolvedExchangeTracker` の「両側同時 chain_event 必須」(`:1932-1934`) を episode 方式へ。**旧経路はフラグ OFF で保持** |
| 同 | `PostChainUnregisteredSentTracker` (A案) と `KillOverrideConfidenceGate` (B案) の役割を台帳へ移す |
| 同 | timeline dump に §13 の D1〜D7 列を追加 |
| `src/ojama_accounting.py` | 確定会計の単一情報源として残し、確定値を `finalize()` の入力にする |

### 5. 不変条件 (Gate 3 で壊してはいけないもの)

§8 の I1〜I15。特に:

- **I1 保存則**: episode CLOSED 時に `生成 = 相殺 + 着弾 + unreconciled`
- **I6 cap 非汚染**: 判定は cap 前、表示だけ cap
- **I8 未解決ゲート**: `is_unresolved` かつ `allows_hard_override` が False の間は ±100 禁止
- **I11 試合境界**: episode は試合を跨がない
- **既定 OFF で bit-identical**

### 6. 既存コードとの重複・競合

| 既存 | 扱い |
|---|---|
| `cancel_own_pending_then_send_surplus` | **再利用**。相殺の式を再発明しない |
| `pending_*_uncapped` 並行帳簿 | **再利用**。`net_raw` の供給元 |
| `_classify_exchange` の 60%/4 個 | **判定式を移植**。閾値は user 伝授 |
| `CHAIN_COALESCE_WINDOW_SEC = 2.5秒` | **当面残す**。chain_id が実測で勝ってから置換を検討 |
| `_maybe_redecide` の `max(pred, obs)` | **置換へ**。max は保存則 I1 と両立しない |
| `ChainGenerationAccumulator` | 置換。**排他フラグ**で二重加算を防ぐ |

### 7. 残るリスク

| # | 内容 |
|---|---|
| R1 | 掛け算式の読み取り自体の誤り → 「素点は 10 の倍数」で検算しカウンタに出す |
| R2 | 「連鎖の終わりの絶対律」の**検出器**が既知の事故源 (連鎖中に 1.37 秒おきに誤検知した前科) |
| R3 | score OCR 破綻動画 (c26/c58/c69) で episode が恒久癒着しうる → §2.3.1 の裏付け規則で隔離 |
| R4 | `ChainGenerationAccumulator` との二重加算 → 排他フラグ |
| R6 | `is_dead` が W32 の上流バグを継承する |
| R7 | **Q-01 のフォールバック 26.9%** — 幕間が観測できない区間では旧規則に落ちる |

### 8. 判断が必要な事項

| # | 内容 |
|---|---|
| J1 | **Gate 3 (本配線) へ進んでよいか。** 指示どおりここで停止している |
| J2 | Phase I 合格判定 99.54% の再計測 → **Phase J 直前**と決定済み (再掲) |
| J3 | `saturation_chain_upper` の欠損表現 → **user 判断で優先度を下げた** (Phase J 以降の強化フェーズ)。おじゃま量と近似のため |

`src/production_config.py` には掛け算式 3 要素のみ登録済み (user 承認済み)。
**交換エピソード会計は既定 OFF で、既存経路には一切つないでいない。**

### 9. Gate 2 後の全体テスト結果 (追記)

```
5814 passed, 13 skipped, 1 deselected, 67 warnings in 665.94s (0:11:05)
```

Gate 0 完了時点の 5,782 に対し **+32 (交換エピソード会計のテスト)**、失敗 0。
**Gate 2 の追加による既存テストの退行なし**を確認した。

---

## 2026-08-24 23:40 JST — Gate 3-1 / 3-2a 完了、設計を1つ転換 (第7報)

- 状態: **Gate 3 に着手 (user承認済み)。本番ファイルは1つも変更していない。**
- 全 pytest: **5,852 passed / 13 skipped / 0 failed** (11分17秒)。朝は 5,782。

### 1. 【設計転換】認識パイプラインを一切変更しない方針へ

Gate 3 前の報告 (第6報) では
「`src/recognition_pipeline.py` の連鎖検出に chain_id を持たせる」計画だった。
**実測にもとづき撤回した。**

#### 根拠となった実測 (母数38本の物理連鎖)

計測: `scripts/_gate3_chainid_probe_2026-08-24.py`、
結果: `data/verify/gate3_chainid_2026-08-24/summary.json`。
既存ログの再パースのみで、動画の再処理はしていない。

| 測定 | 結果 |
|---|---|
| 掛け算式ON経路の段カウントの単調性 | **38/38** |
| 別々の連鎖の癒着 | **0/38** |
| 1つの物理連鎖が複数イベントに分裂 | 20/38、**上限2で頭打ち** |
| 掛け算式OFF (旧経路) の分裂上限 | **4** (11連鎖が `[777.37, 782.37, 787.37, 792.7]`) |

分裂 20/38 は乱れではなく**常に同じ構造**:
成長フェーズ (`formula_read`、trigger_sec 固定、段が単調増加) と
着地確認 (`baseline`、**新しい trigger_sec**) が別イベントとして出る。

#### 原因を特定した

`src/chain_detector.py:303` — baseline イベントは
`trigger_sec=self._last_stable_t` (最後に STABLE だった時刻) を使う。
**連鎖の開始時刻ではない。**

> **`trigger_sec` の一致で連鎖の同一性を判定してはならない。**
> これを知らずに実装すると必ず 100% 分裂する。

#### なぜパイプラインを触らないことにしたか

プローブは**パイプラインを1行も変えずに、ログの再パースだけで 38/38 を復元できた**。
必要な情報はすでにイベント列に出ており、
「認識側の文脈 (盤面・hold タイマー) が要る」という想定は実測で反証された。

`recognition_pipeline.py` 約7,000行は配線事故の常習地帯であり、
「既定OFFのはずが gravity_settle 採用で強制ON」(8/22)、
「採用登録漏れ3回」、「kill_override に別の値が配線」の前科がある。
**bit-identical をフラグ分岐の正しさに依存させる構成は、このプロジェクトでは成立しなかった。**

純関数の解決器なら **呼ばなければ何も変わらない**ことが自明になる。

fable architect の独立レビューも案B (純関数の解決器) を推奨し、
仕様書 §5.2.3 / §11 が既に「chain_id 付与器は純関数、Gate 2 成果物」と
定めていたことを指摘した。**当初計画のほうが合意済み仕様の巻き戻しだった。**

---

### 2. 追加した成果物 (すべて既定OFF・未配線)

| ファイル | 内容 | テスト |
|---|---|---|
| `src/chain_id_resolver.py` | イベント列から chain_id を復元する純関数 | 19本 |
| `src/exchange_episode_tracker.py` | resolver + ledger を合成する観測アダプタ | 24本 |
| `src/exchange_ledger.py` | 既存。**追加のみ**の拡張 | 39本 (既存32+7) |

**時間の閾値を一切使っていない。** 実測の 0.667秒 をそのまま定数にするのは
シーン逆算にあたるため、イベントの種別と順序だけで判定する。
物理導出済みの `CHAIN_ID_MAX_SEC = 30.0` (連鎖アニメ実測 `2.61 + 1.17×N`、n=418) のみ使う。

**連鎖終了の絶対律検出器 (R2) に依存させていない。**
`GROWING → BASELINE で FINALIZE` の経路で動くため、
8/22 の「連鎖中に1.37秒おきに誤検知」した検出器を踏まずに済む。

---

### 3. 【重大】仕様の自己矛盾を1つ確定し、修正した — E1 の削除

#### 何が間違っていたか

仕様 §2.4 の終了条件 E1:
> **(E1) 完全相殺で閉じる**: `provisional_residual() == 0` かつ `net_raw() == 0`

実装 (`src/exchange_ledger.py`) はこれに忠実だった。**欠陥は仕様のほうにあった。**

1. 1P が 10 個生成 (FIRE + FINALIZE)
2. 2P が 10 個生成 (FIRE + FINALIZE)
3. **相殺も着弾も1件も起きていない**
4. `net_raw = +10 − 10 = 0` なので E1 成立 → **CLOSED**
5. しかし両側の `outstanding` は 10 個ずつ残っている
6. → `生成 20 = 相殺 0 + 着弾 0 + unreconciled 0` で **不変条件 I1 (保存則) が破れる**
7. その後の実際の相殺・着弾は**別 episode** に載る → 二重計上

E1 の名前は「完全相殺」なのに、**相殺を1件も記録せずに閉じていた。**

#### なぜ致命的か

「撃ち合いが会計に載らないまま決着扱いになる」— これは
`project_pm100_display_flip_2026-08-24` で確定した
±100 張り付き (動画の14.0%・47試合で符号反転) の**根因そのもの**。
**直そうとしている病気を新しい仕様に埋め込んでいた。**

#### 判断 (fable architect の独立裁定)

決め手は次の指摘:

> 正しく配線された系では CANCEL 供給後に E2 (`_all_settled`) が必ず成立する。
> **E1 が E2 と異なる結論を出すのは「相殺が観測されていない」異常時のみ。**
> つまり E1 は正しい系では冗長で、壊れた系でだけ発火する。

`net_raw() == 0` は相殺の**運命**を示すだけで、相殺の**取引記録ではない**。
相殺は `cancel_own_pending_then_send_surplus` (`src/ojama_accounting.py:742`) が
攻撃確定の瞬間に計算する実イベント。**台帳は物理ではなく会計に従う。**

E1 を削除した。仕様書 §2.4 と §3 も訂正注記つきで修正済み。

#### 予想される副作用 (正常)

配線が完了するまで、両側が撃ち合った episode は**1本も正常に閉じない**。
すべて `EPISODE_MAX_SEC` の安全弁で強制終了になる。
強制終了は残量を `unreconciled` として明示記録するので **I1 は成立する**。

`has_settlement_input=False` の強制終了件数が
**「まだ配線していない」ことを機械的に示す指標**になる。
**配線完了後にこれがほぼゼロになることを受け入れ条件とする。**

---

### 4. 配線前に潰した「静かに間違える」経路 4 件

いずれもコーダの正直な報告から見つかった。**取り繕わない報告が欠陥検出そのものだった。**

| # | 内容 | 直し方 |
|---|---|---|
| 1 | **単位ズレ**: スコアの点数をおじゃま個数として台帳に渡していた。許容帯 4.0個 と 720点 を比較する状態 | `score_to_ojama` で換算。乖離 −720 → **−10個** |
| 2 | **経過秒の既定値 `0.0`**: 渡し忘れてもコードが動き、マージンタイムが効かず終盤の火力が黙って過小になる | **既定値を削除して必須化**。渡し忘れは `TypeError` で止まる |
| 3 | **超過決済の黙殺**: `outstanding = max(0, 生成−相殺−着弾)` の下限クリップで、過大供給が消えて保存則を素通り | `oversettled` を追加して可視化。クリップ自体は維持 |
| 4 | **時刻の偽装**: 着弾分割で重複排除をすり抜けるため t_sec を 1e-6 秒ずらしていた | `ExchangeEvent.seq` を追加し dedup_key に含める。**時刻は本物のまま** |

4 は特に重要。重複排除 (I4) は 8/22 に確定した
ChainEvent 断片化 (単発84 vs 累積840) への対策そのものであり、
時刻を細工してすり抜ける穴を作ると**本物の重複も一緒に通る**。

2 の判断根拠は `feedback_wiring_gap_vs_wiring_error_2026-08-22`。
**間違った数字を静かに出すくらいなら、大きな音を立てて止まるほうが良い。**

---

### 5. 進行中: 実データでの検証 (配線より先)

台帳・解決器・tracker は**すべて合成データのテストしか通っていない**。
実動画で保存則が成り立つか、まだ 1 件も測っていない。

**本番の表示経路に配線する前に、独立したプローブで測る。**
`scripts/_gate3_episode_probe_2026-08-24.py` (新規、本番ファイル無変更)。
Gate 3-0 と同じ動画・同じ窓を使い、chain_id 数を突き合わせて検算する。

測る対象:

| 項目 | 期待値 |
|---|---|
| 正常 CLOSED した episode の割合 | 大半 |
| 保存則違反 | 0 |
| `oversettled_total` | 0 |
| `episodes_without_settlement_input` | 0 に近い |
| 自己検算 (換算値 vs `ojama_sent`) | 小さい |
| finalize 乖離が許容帯 4.0個 を超える割合 | 分布を取る |

---

### 6. Gate 3 の残り (未着手)

| # | 内容 |
|---|---|
| a | 実データ検証の結果確定 (進行中) |
| b | `scripts/visualize_advantage_overlay.py` への配線 (既定OFF)。**数値が正しいと確認できてから** |
| c | timeline dump への D5 / D6 (フレーム単位の列)。tracker は現在バッチ設計なので別途 |
| d | PM100 8区間の OFF/ON バックテスト |

**表示・判定にはまだ一切影響していない。**
`src/production_config.py` は掛け算式3要素のみ (user承認済み) で、
交換エピソード会計は登録していない。

---

### 7. 判断が必要な事項

| # | 内容 |
|---|---|
| J1 | **E1 削除への異議はあるか。** 仕様の破壊的変更にあたるが、根拠は上記のとおり |
| J2 | 6-b (オーバーレイ配線) へ進む前に、実データ検証の結果を独立レビューするか |
| J3 | `FINALIZE_DOWNWARD_TOLERANCE` の 2 項化 (加法 4.0個 + 相対項)。fable architect の提案。**相対項の定数は D2 の分布を 38 連鎖以上で取ってから確定**する方針で、それまで現行 4.0 のまま (保留側に倒す) |

---

## 2026-08-25 03:00 JST — Gate 3-2b 実データ検証: 欠陥5件を検出し是正 (第8報)

- 状態: **配線前の実データ検証で欠陥5件を検出。うち3件は私の仕様・設計の誤り。**
- **表示・判定には一切影響していない。** `src/production_config.py` は掛け算式3要素のみ (user承認済み)。
- **本番ファイルは1つも変更していない**
  (`recognition_pipeline.py` / `chain_detector.py` / `score_ocr.py` /
   `ojama_accounting.py` / `visualize_advantage_overlay.py` / `production_config.py`)。

### 0. 結論を先に

**オーバーレイに配線する前に実データで測ったことが、そのまま正解だった。**

実データを1本通しただけで:

- 生成量の **74.5% が二重計上**
- **正常に閉じた episode がゼロ** (古い chain が残り永久に閉じられない状態)
- 「確定値」が**推定値**で、両方向に壊滅的に外れる (38個 → **745個**)

そして **保存則の検査は違反ゼロを出していた** (各 episode 単体では帳簿が合っていたため)。
**「きれいに合っている」ように見えたまま、合計が4倍狂っていた。**

配線後に測っていれば、「±100 の張り付きが直らない」という症状だけを見て、
台帳・配線・認識のどこが悪いのかを探すことになっていた。

---

### 1. 是正した欠陥 (実測とコード読解の両方で確認済み)

| # | 欠陥 | 誰の誤りか | 実測 |
|---|---|---|---|
| 1 | **E1「差し引きゼロで決着」** — 相殺を記録せず帳簿を閉じる | **私の仕様** | 第7報に記載 |
| 2 | **連鎖記録が試合を跨いで永久に残る** — `_force_close` が `self._chains` を消さない | 台帳の実装 | 生成量の**74.5%が二重計上**。5 episode で 5,025→8,965→9,771→9,825→11,450 と単調増加 |
| 3 | **「確定値」が推定値だった** — `mechanism='baseline'` は score OCR でなく `ChainSimulator` 由来 | **私の設計** | 許容帯超えの乖離 **63.6%**。38個 → **745個** が無条件採用 |
| 4 | **W38: `ChainEvent.ojama_sent` の経過秒が壊れている** | **本番コード** | **16/16** でレート1。差 108,961 を厳密に再現 |
| 5 | 単位取り違え / 時刻の偽装 / 超過決済の黙殺 / 既定値による無音の誤り | 実装 | 第7報に記載 |

#### 欠陥2 の副次被害 (コーダがコード読解で発見、私の被害報告に無かった)

古い chain が `self._chains` に残る限り **`_all_settled()` が台帳全体を見る**ため、
**以後どの episode も永久に正常 CLOSED できない**。
これが「正常 CLOSED が1件も出なかった」真因。

#### 欠陥3 の是正 (fable architect の裁定。**私の案は過剰修正だった**)

私は「掛け算式の累積を最終値にする」と提案したが**却下された**。
掛け算式には読み取れない区間が **26.9%** 残り、段を取りこぼすと**系統的に過小**になる。
最終値に固定すると相殺・着弾の実測合計と一致せず**保存則が構造的に破れる**。

> **仕様 §4.1 は最初から「確定 = score OCR 確定差分」と書いていた。壊れていたのは配線。**

確定した序列:

| 供給源 | 役割 |
|---|---|
| 掛け算式の累積 | **暫定 (PROVISIONAL) の唯一の供給源** |
| **score OCR 確定差分** (`OjamaAccountingTracker.total_generated_by_pX`) | **FINALIZE の値の唯一の供給源** |
| 推定 (`ChainSimulator`) | **連鎖終了の合図のみ。値は使わない。** 低信頼フォールバックのみ |

user 伝授「推定も simulate も不要」は **score OCR を排除していない**。
排除すべきは推定だけだった。**私はそこを取り違えて実測資産まで捨てようとしていた。**

`FINALIZE_DOWNWARD_TOLERANCE = 4.0` (落下ボーナス由来) の物理導出は
「確定 = score OCR 差分」のときだけ成立する。
**63.6% 発動はゲートの故障ではなく入力の故障だった。**

#### 追加した不変条件 I16

**台帳が `source != "score_ocr_diff"` の FINALIZE を既定で拒否し、カウンタに出す。**
配線の「間違い」型は `--help` 突合でも採用登録の確認でも検出できないため
(`feedback_wiring_gap_vs_wiring_error_2026-08-22`)、**黙って受け取らず拒否して数える。**

---

### 2. 修正前後の実測比較

| 項目 | 修正前 | 修正後 |
|---|---|---|
| 生成量 (zenchi t=780-1080) | **45,036** | **9,124** |
| 許容帯 4.0個 超えの乖離 | **63.6%** | **10%** (2/20) |
| 乖離の中身 | 707 / −518 / 794 / −1576 / −1083 | ほぼ 0、少数が ±1 / ±30 |
| 重複抑制の発動 | — | **0** (試合境界の修正が効いている) |
| 保存則違反 / 超過決済 / 帰属漏れ | 0 / 0 / 0 | 0 / 0 / 0 |
| 迷子の終了信号 / chain_id 強制打ち切り | 0 / 0 | 0 / 0 |

#### 独立検算 (代表動画 v51、マスター級30先、1ラウンド全体)

実画面でラウンド境界を確認済み
(t=458 前ラウンド決着 → t=460 新ラウンド開始 → t=531 決着 → t=534 次ラウンド)。

- ラウンド終了時スコア 55,269 + 71,627 = **126,896点** ÷ レート70 ≈ **1,813個**
- 台帳の実測 **2,107個** (16%差、同じ桁)

推定値を混ぜていたときは、これが **45,036 vs 1,813** の比較だった。

---

### 3. 残件3件 (診断中)

**「0 だから正常」と読めない数字が混ざっている。**

| # | 内容 | 実測 |
|---|---|---|
| 1 | **自己検算が1件も動いていない** | 不一致 0 だが**スキップ 23/23・9/9**。全件スキップ。「合っている」ではなく「測っていない」 |
| 2 | **着弾がほぼ観測されていない** | v51 は1ラウンド全体で決着しているのに**着弾 5個** / 生成 2,107個。未照合 84% |
| 3 | **正常 CLOSED した episode がゼロ** | 両動画とも全て強制終了。v51 は **14本の連鎖が1 episode** = 撃ち合いの区切りになっていない |

1 は**今夜3回目の fail-silent**。`0` だけでは「合っている」か「測っていない」か判別できない。
**必ず母数と並べる**こと (`0/23` と書けば一目で分かる)。

2 については、正当な未照合 (ラウンドが終われば相手の抱えたおじゃまは降らない、
`reference_ojama_landing_gated_by_placement_2026-07-29`) と観測漏れの切り分けを進めている。
**混ぜて「観測漏れ」と断定しない。**

プローブは本番の `scripts/collect_indicators_v2.py::_drive_ojama` を流用しており、
`on_state_transition` と手数増分駆動の drain を通していることは確認済み。
**台帳の設計ではなく測定ハーネスの配管の問題である可能性が高い** (要確定)。

---

### 4. 判断が必要な事項

| # | 内容 |
|---|---|
| J1 | **E1 削除 (第7報) と I16 追加への異議はあるか。** どちらも仕様の破壊的変更 |
| J2 | **W38 の根治** (`recognition_pipeline.py` の4箇所から `match_start_sec` を配線) に進むか。本番表示に影響しないため緊急性は低いが、**検証ツール2本が壊れた値を読んでいる** (`recognition_physics_review.py` / `verify_ojama_score_inference.py`)。`tests/test_chain_detector.py:126` 等の期待値見直しを伴う |
| J3 | 残件3件を閉じてから 3-2c (オーバーレイ配線) へ進む方針で良いか |
| J4 | `FINALIZE_DOWNWARD_TOLERANCE` の 2 項化 (加法 4.0個 + 相対項)。D2 の分布は乖離ほぼ0になったので、**2項化は不要になった可能性**がある。分布を38連鎖以上で取り直してから判断する |

---

## 2026-08-25 06:00 JST — Gate 3-2b 完了。会計は内部無矛盾、残る課題は生成量15.7%過大 (第9報)

- 全 pytest: **5,914 passed / 13 skipped / 0 failed**
- **本番ファイルは1つも変更していない。表示・判定に一切影響なし。**
- `src/production_config.py` は掛け算式3要素のみ (user承認済み)。交換エピソード会計は未登録。

### 1. 到達点

| 指標 | 検証開始時 | 現在 |
|---|---|---|
| 生成量 (zenchi t=780-1080) | **45,036** | **2,024** |
| 未照合 (zenchi) | 87.4% | 28.5% |
| **正常 CLOSED した episode** (zenchi) | **0/5** | **3/5** |
| chain_id 数 (zenchi、物理連鎖の実測 16) | 33 | 27 |
| `|乖離| > 4.0` の割合 (zenchi、同一定義) | 53.3% (16/30) | **15.0%** (3/20) |
| 保存則違反 / 超過決済 / 帰属漏れ / 判別不能な減少 | 0 | **0 (母数つきで確認)** |
| テスト | 5,782 | **5,914** (+132、既存の期待値変更 0件) |

### 2. 【最重要の成果】測定器が窓の切り方に騙されなくなった

実データで実証:

| 窓 | `total_unreconciled` | `ledger_residual_all` |
|---|---|---|
| v51 t1=**533** | **0.0** | **981.0** |
| v51 t1=**545** | 981.0 | 0.0 |

**同じ 981 が、窓をどこで切るかだけでラベルを変えていた。**
早く切れば「未照合ゼロ」という完璧な数字が出る。

`_build_d1()` が `closed_episodes()` だけを合算し、
まだ OPEN な episode を無視する設計だったため。
**両方を同時に出すようにし、テストで固定した。**

> **今夜、`0` が「合っている」ではなく「測っていない」だった事故が 5 回あった。**
> カウンタは必ず母数と並べる (`0/23` の形) ことを恒久ルールとして memory に記録した
> (`feedback_zero_needs_denominator_2026-08-25`)。

### 3. 是正した欠陥 (実データ検証で検出。**うち4件は私の誤り**)

| # | 欠陥 | 出所 | 実測 |
|---|---|---|---|
| 1 | E1「差し引きゼロで決着」— 相殺を記録せず閉じる | **私の仕様** | 第7報 |
| 2 | 連鎖記録が試合を跨いで永久に残る | 実装 | **74.5%** が二重計上 |
| 3 | 「確定値」が推定値 (`ChainSimulator`) だった | **私の設計** | 38個 → **745個** が無条件採用 |
| 4 | W38: `ChainEvent.ojama_sent` の経過秒が壊れている | **本番コード** | **16/16** でレート1 |
| 5 | 上限216の切り捨てが観測経路を塞ぐ | 設計の見落とし | zenchi で**総生成の52%** (4,743個) が消失 |
| 6 | ラウンド境界での無音の消滅 | 設計の見落とし | 456個が一括消滅、検知が **10.6秒**遅れ |
| 7 | 連鎖の過剰分割 | 実装 | zenchi で**総生成の23%** の水増し |
| 8 | 自己相殺が発火本人の帳簿から引かれない | 実装 | **327 / 1,765 = 18.5%** |
| 9 | `D1` が窓の切り方に依存する (上記2) | 測定器 | 同じ 981 が 0 にも 981 にもなる |
| 10 | 退役した chain の相殺・着弾も合計から落ちる | 実装 | 327 → 309 (18個が消失) |

**5 は既知の宿敵だった。** ±100 張り付きの根因の第2層「上限216の相殺汚染」そのもの。
**上限をかけない判定値を持つ台帳を作り、そこに上限をかけた観測を流し込んでいた。**

### 4. 残る課題: 生成量が独立検算の **115.7%**

v51 のラウンド (t=460〜531、実画面で境界確認済み):

- 検算値 = (55,269 + 71,627) ÷ 70 = **1,813個**
- 台帳の `raw_generation_total` = **2,097個** = **115.7%**

**窓の切り方では説明できなかった** (t=459-545 で119%、t=460-531 で115.7%、ほぼ横ばい)。
プローブは観測を `t_sec >= t0` でゲートしており、準備区間は混入していない (確認済み)。

過大分 284個のうち、全消しボーナス (`ALL_CLEAR_BONUS = 2100点 = 30個`) で
説明できるのは約30個 (**10%**) のみ。**残り 254個は未解明。**

#### 次に疑うべき候補 (優先順位つき、**未検証**)

| # | 候補 | 根拠 |
|---|---|---|
| a | `score_to_ojama` の換算経路 (繰り越しの扱い) | tracker は `prev_leftover=0` 固定。連鎖ごとに最大69点の誤差 |
| b | 掛け算式の成長フェーズの取りこぼし / 過大 | 実測で `provisional=0` の chain が存在 (chain6) |
| c | **検算値 1,813 の側の誤差** | 全消しボーナスは score に現れない。他にも score に出ない加算があるか要確認 |

**c を先に潰すべき。** 物差しの側を疑わずに対象を直すと、今夜の
「v51 の悪化が物差しの穴だった」件 (8/24) を繰り返す。

### 5. 未着手として記録した項目 (**すべてカウンタで可視化済み。黙って落としていない**)

| 項目 | カウンタ | 現在値 |
|---|---|---|
| close 後に届いた相殺・着弾が欠落する | `post_close_settlement_dropped_count` / `_amount` | 0/0 (対象なし) |
| 自己相殺のクリップ上限超え | `self_cancel_clipped_count` / `self_cancel_eligible_count` | **0/5** |
| `_fire_events_of_open_chains` / `Episode.touch` の無条件巻き込み | (未計装) | Fix4 の効果を弱めている |
| chain_id 27 vs 物理連鎖 16 の残差 | — | 5本は隣接統合で説明、**6本は未測定** |

### 6. 判断が必要な事項

| # | 内容 |
|---|---|
| J1 | **仕様の破壊的変更2件への異議はあるか** — E1 の削除、I16 (FINALIZE 供給源の限定) |
| J2 | **W38 の根治に進むか。** 本番表示は無事だが**検証ツール2本が壊れた値を読んでいる** (`recognition_physics_review.py` / `verify_ojama_score_inference.py`)。`tests/test_chain_detector.py:126` 等の期待値見直しを伴う |
| J3 | **115.7% の原因究明を続けるか、この精度で 3-2c (オーバーレイ配線) に進むか** |
| J4 | `_fire_events_of_open_chains` の無条件巻き込みを是正するか (episode の粒度が「1回の撃ち合い」でなく「ラウンド全体」になる原因) |
| J5 | `FINALIZE_DOWNWARD_TOLERANCE` の2項化は**不要になった可能性**。乖離分布が改善したため、判断は D2 の分布を広い母数で取り直してから |

---

## 2026-08-25 07:00 JST — セッション終了。引き継ぎ文書を作成 (第10報)

- **実行中のジョブなし。** 実測エージェントは中途半端な状態を残さないため停止した。
- 全 pytest **5,914 passed / 0 failed**。**本番ファイルの変更 0件。**
- **引き継ぎ: `docs/CLAUDE_SESSION_HANDOFF_2026-08-25.md`**
  次セッションはこの文書から再開する。

### user 判断 (2026-08-25)

**J2 (W38 の根治) と J3 (生成量115.7%の究明) の両方を実施する**と決定。
着手は次セッション以降。手順・リスク・確認事項は引き継ぎ文書に記載済み。

### 引き継ぎ文書に含めたもの

- W38 の根治手順 (4箇所の生成点は**自分で grep して再確認済み**)、
  影響を受けるツール一覧、`ChainEvent.ojama_sent` の値が変わることによる
  既存テスト期待値見直しの必要性
- 115.7% の**最有力仮説 (検算値の側がマージンタイムを無視している)** と、
  それを確定させる4つの測定項目、逆算した数値 (平均レート60.5、45%が低レート)
- 棄却時の次候補3件 (**b = 同一値の二重計上が有力**、11,460点=163個が
  284個のうち57%を説明しうる。未検証)
- 未着手項目4件 (**すべてカウンタで可視化済み。黙って落としていない**)
- 既存成果物の一覧 (`data/verify/gate3_*` 11ディレクトリ、**上書き禁止**)

### 次セッションへの注意

- 成果物は**未コミット**。`git status` に他エージェントの差分も多数あるため
  **一括コミットしないこと**。既存の差分・ログ・成果物を
  reset / checkout / stash / 削除 / 上書きしない。
- `data/verify/gate3_*` の11ディレクトリは**修正前後の比較に使う監査証跡**。上書き禁止。
- 速度テスト `test_exchange_predictor.py::TestPredictionSpeed` は
  **並走負荷で落ちる**。落ちたら単独で再実行して確認すること。

---

## 第10報 (2026-08-25 / Claude Code) — J3「生成量115.7%」の究明が**決着**

### 結論: 最有力仮説 (マージンタイム) は**棄却**。真因は**過剰分割による二重計上** (候補 b)。

対象は v51 のラウンド t=460〜531。同一条件で **3/3 ラン再現** (`raw_generation_total` = 2,097.0)。

#### 棄却した仮説 (実測)

| 測定項目 | 予想 | 実測 |
|---|---|---|
| `OjamaAccountingTracker._match_start_sec` | 429付近 | **461.383** (実ラウンド開始460とほぼ一致) |
| `compute_effective_rate(elapsed, 70)` の時系列 | 途中で70→52 | **全区間70のまま。変化点 0件** (換算時 elapsed 最大58.9秒 < 96秒) |
| レート70未満で換算された点数 | 約57,470点 (45%) | **0点 / 78,113点** |
| 実レートでの再検算 | 2,097に近づく | **1,115.9個** (一定70の検算と同値) |

ラウンド長71秒では `match_start_sec` が正しい限りマージンタイムに届き得ない。

#### 真因 (`ChainIdResolver.push` を monkeypatch し、resolver への全観測58件を記録して直接観測)

**問われていた事実への直接回答: chain5 の初期値は 0 ではなく 29,670 だった。** chain8 も同型で、二重計上は2箇所ある。

- **物理連鎖B (2P, t=492〜506)**: 掛け算式の段 cc=1〜10、累積 40→54,230点。
  chain4 が cc=1〜7 (暫定21,570) を保持し、t=501.93 の SCORE_FINALIZE (OCR差分308個=21,570点相当) で確定クローズ。
  **その0.17秒後 t=502.10、続きの段 cc=8 (累積29,670) が新 chain5 を開き**、cc=10 (54,230) まで成長して SUPERSEDED。
  chain5 の暫定54,230は chain4 で確定済みの21,570を丸ごと**累積包含**。
- **物理連鎖C (2P, t=516〜526)**: chain7 = cc=1〜3 (1,320点、19個確定)。
  t=520.40 に続きの cc=4 (累積3,020≠0) が chain8 を開き、cc=7 (14,540) まで成長して STREAM_END。chain7 の1,320を累積包含。

#### 過大286個の完全分解 (**誤差0個**)

| 項 | 個数 |
|---|---|
| 二重計上 chain5⊃chain4 (21,570点) | +308 |
| 二重計上 chain8⊃chain7 (1,320点) | +18 |
| chain8 の読み残し (STREAM_END が実連鎖完走前。物理連鎖C真値17,044点 vs 暫定14,540点) | −36 |
| 1P 窓内未観測の小差分 (259点) | −4 |
| **純過大** | **+286** (実測286と完全一致) |

`raw_generation_total` 2,097 の内訳も完全分解: OCR確定(chain 1,3,4,6,7)=1,115 / simulate_fallback(chain 2,9)=1 / **未確定chainの暫定換算(chain5,8)=981**。
窓内で OCR 観測できなかった実スコアは695個相当なので、**過大は全額 2P 側の未確定 chain 5・8 に所在**。

前報の「候補b は11,460点=163個で284個の57%を説明しうる」という見立ては**外れ** (実際は2箇所・合計326個の二重計上と62個の過小の差し引き)。
候補 a (`score_to_ojama` の繰り越し) は棄却方向 (leftover 実測最大60点)。
候補 c (成長フェーズの過大) は主因でない — 成長フェーズはむしろ**読み残しで −36 の過小方向**。

#### 該当コード (**修正は未実施。判断を仰ぐ**)

1. `src/chain_id_resolver.py:309` `_handle_score_finalize` — SCORE_FINALIZE が物理連鎖の途中で届くと in-flight を確定クローズする
2. `src/chain_id_resolver.py:377-383` `_open` — 続きの FORMULA_STEP (累積値) が新 chain を `provisional_score=obs.total_score` で開くため、**前半確定分を含む累積値のまま初期化**される
3. `src/exchange_episode_tracker.py:1385-1390` `_build_d7` — 未確定 chain の暫定をそのまま合算するため前半確定分が二重になる

修正案の方向 (実装していない):
- **(i) resolver 側**: 同 side が SCORE_FINALIZE でクローズした直後 (閾値例: 数秒以内) に**段番号が継続** (cc が前 chain の最終 cc+1) して開く場合、新 chain の暫定から前 chain の確定点数相当を差し引いて初期化。副作用範囲は resolver のみ、工数小。
- **(ii) D7 集計側**: SUPERSEDED / STREAM_END chain の暫定から同一物理連鎖の確定済み分を控除。副作用は D7 集計のみだが物理連鎖の対応付けが必要、工数中。

**zenchi の chain_id 27 vs 物理連鎖16 の残差6本も同一機構の可能性が高い (未測定)。**

#### 成果物 (すべて新規。既存は温存)

- `scripts/_gate3_rate_trace_2026-08-25.py` (v4プローブのコピー+計装。**`src/` は無変更**)
- `data/verify/gate3_rate_trace_2026-08-25/rate_trace.json` / `summary.md` (レート時系列)
- `data/verify/gate3_rate_trace_2026-08-25/step_trace.json` / `step_trace_summary.md` (step履歴の直接観測)
- `logs/_gate3_rate_trace_2026-08-25.log` / `logs/_gate3_step_trace_2026-08-25.log`

### J2 (W38 の根治) は着手済み・進行中

W38 の根治は測定器 (`RecognitionPipeline` 経由) の出力を変えるため、**J3 の測定を先に現状コードで完了させてから**着手した。結果は次報で報告する。

---

## 第11報 (2026-08-25 / Claude Code) — Gate 3-2b REVIEW NG を受諾。ただし**順序に4点の異議**

P1×3 / P2×2 の指摘は**すべて受諾**。反論しない。既存差分・ログ・`data/verify/gate3_*` は温存する。
Gate 3R-0 の凍結 (配線しない / production 登録しない / A+B を本番ONにしない) も遵守する。

ただし再開順序 `P1 → P2 → W38 → 115.7%再検算 → zenchi対応表` には次の4点で異議がある。

### 異議1: 再開順序 4番 (115.7%の再検算) は**すでに完了済み**。しかも P1-2 と同じ機構だった

第10報のとおり、実効レートの採取と再検算は完了している。**実効レートは全区間70で変化点0件**
(換算時 elapsed 最大58.9秒 < 96秒)、レート70未満で換算された点数は **0点/78,113点**。
マージンタイム仮説は棄却され、真因は **P1-2 と同族の「1物理連鎖 → 複数 chain_id」** だった。
過大286個を誤差0で分解済み (二重計上 +308 +18、読み残し −36、小差分 −4)。

→ **4番を独立項目として残す意味はない。「P1-2 修正後に v51 を再測して過大が消えることを確認する」
という P1-2 の検収条件に格下げすべき。**

### 異議2: W38 は 3番でなく**先頭または並行**であるべき

Gate 3R-3 は「壊れた検証ツールの修正前後を比較する」ことを求めているが、
その検証ツール (`recognition_physics_review.py` / `verify_ojama_score_inference.py`) は
**P1/P2 の検収そのものに使う**。壊れた物差しで修正の可否を判断すると、このプロジェクトで
17回起きた測定器事故を18回目にする (直近: 8/24 の v51「悪化」は物差しの穴だった)。
W38 は `src/chain_detector.py` / `src/recognition_pipeline.py` にあり **P1/P2 とファイルが重ならない**。

→ **本報の時点で W38 は着手済み・並行実行中。** 完了後に修正前後比較を出す。

### 異議3: P2-2 は P2 でなく **P1 相当**に上げるべき

P1×3 と P2-1 は**すべて既定OFFの交換台帳内**で、現在の表示に影響しない。
一方 P2-2 (`FormulaStepAccumulator`) の3フラグは `recognition_load_default_kwargs()` で
**全て True = 本番認識構成でON**。つまり **5件のうち唯一、いま本番の数値を汚しうる**。
さらに掛け算式の段は「消えるたびに出る」値で、連鎖数・火力の**両方の一次情報源**
(user伝授 2026-08-22)。ここが非連続確認で誤確定すると下流全部が汚れる。

→ **P2-2 を最優先レーンに上げ、既定OFF群と並行で進める。** 本報の時点で着手済み。

### 異議4: P1-3 は「修正」でなく**設計変更**。同じレーンに混ぜない

P1-3 は cap 前の gross 量の供給経路を新設する話で、P1-1 / P1-2 / P2-1 のような
局所修正とは規模が違う。しかも前セッションで「**上限216の切り捨てが観測経路を塞ぎ
総生成の52%が消えていた**」ことが判明しており、cap の位置関係は会計の根幹に触る。

→ **先に設計を確定させ、不変条件テストの一覧まで出してから実装に入る** (Codex 自身の
「設計を先に確定し」に沿う)。本報の時点で設計を別レーンで着手済み。

### 私が実行している改訂順序

```
レーンA (本番影響あり・最優先) : P2-2 → 掛け算式の別出力先での回帰
レーンB (既定OFF・会計)        : 失敗テスト追加 → P1-1 / P1-2 / P2-1 → v51 再測 (115.7%の検収)
レーンC (測定器)               : W38 根治 → 影響ツール2本の修正前後比較
レーンD (設計)                 : P1-3 の供給設計 + 不変条件テスト一覧 → 承認後に実装
```
4レーンは編集ファイルが重ならないことを確認済み。全 pytest は各レーンで走らせず、
**私が最後にまとめて1回回す** (CPU競合と速度テストの偽陰性を避けるため)。

合流後に zenchi の物理16連鎖と chain_id の対応表を作り、Gate 3R-4 の
「未説明ケース0」を満たしてから報告する。**合格前に Gate 3-2c / production 登録 /
PM100 本番候補A/B へは進まない。**

### 判断待ち (Codex へ)

- K1: 上記の順序変更 (特に**異議1: 115.7%再検算の項目削除**と**異議3: P2-2 の格上げ**) に異議はあるか。
- K2: Gate 3R-4 の「生成量の独立検算 差±5%以内」は、**独立検算の側の規約**
  (実効レート・leftover・全消し) を先に文書で固定しないと合否が動く。
  第10報の実測どおり、実効レートは v51 の当該ラウンドでは一定70だった。
  この規約の確定版を Codex 側で書くか、私が案を出すか。

---

## 第12報 (2026-08-25 / Claude Code) — P1-3 の設計を確定。**SPEC §6.3 の廃止を依頼**

設計書を新規作成した: **`docs/EXCHANGE_GROSS_SUPPLY_DESIGN_2026-08-25.md`**
(status: 設計確定待ち。**実装は未着手**)。

### 実読で確認した gross 可用性 (`src/ojama_accounting.py`)

| 量 | 可用性 | file:line |
|---|---|---|
| gross 生成 (cap 前) | **既にある** (`score_to_ojama` に cap なし) | :712-720 |
| 自己相殺 (uncapped 基準) | **無い**。:741-744 で計算されるが量を捨てている。既存 `total_offset` は **capped forecast 基準 (:732)** で汚染済み | :732, :741-749 |
| 着弾 (uncapped 基準) | **無い**。:536-538 で drain するが累積なし | :536-543 |
| 境界ワイプ回数 | **無い** | :980-1023 |
| uncapped サニティ切り捨て量 | **無い** (黙って min で切っている) | :101, :745-748 |

つまり Codex 指摘のとおり、`_classify_side_delta` の条件追加のような局所修正では救えない。
**供給源を変える指示は設計として正しい。**

### 採用方式: **独立累積カウンタ** (gross event キューは不採用)

Codex 提示の2択のうち**独立累積カウンタ**を推す。理由:
(1) event キューは消費契約 (単一 consumer 前提) が overlay + 診断の複数読者で壊れる、
(2) chain 帰属は `chain_id_resolver` の既存責務と二重化する、
(3) 単調カウンタの差分はフレーム欠落・順序に頑健で、**保存則が恒等式で検査できる**。
このプロジェクトは配線事故 (漏れ/間違いの2種) を繰り返しており、
**消費契約が不要な方式**が全体最適と判断した。

`OjamaAccountSnapshot` は拡張せず、**新 dataclass + 新メソッド `get_gross_counters()`** で供給する。
snapshot 全 field を動的 serialize する経路の有無が未確認のため、
bit-identical を構造で保証する側に倒した。

「同一フレーム incoming=30 + cancel=50」は、自分側 `Δoffset_unc=50` と
相手側 `(Δgenerated − Δoffset_unc)=30` として**別カテゴリの単調カウンタに分離されて残る**。
純差分 −20 に潰れない。

### 依頼 (Codex 管理ファイルのため直接変更していない)

**`docs/EXCHANGE_EPISODE_SPEC_2026-08-24.md` §6.3 (spec:504-511、
「pending_pX_uncapped 差分の判別的再構成」) を廃止し、本設計へ差し替える注記を入れてほしい。**
Fix【1】で導入された当該方式が P1-3 そのものであるため。

### 不変条件テスト (9本、動画不要の合成 fixture)

`OjamaAccountingTracker` を合成 score 列で直接駆動する (rate=70、elapsed=0 で
マージンタイム無効、`score_to_ojama` の換算が地上真値)。両 side の settle カウンタが
同一フレームで満了する score 列を組めば**同時応酬が決定的に再現できる**。

Gate 3R-4「同時応酬で gross 相殺量が失われない」の検収 = **T1 (同時応酬) + T3 (incoming+着弾) + T5 (保存則)**。
T1 は「**旧純差分だと 20 に潰れること**」も併記 assert して回帰の証拠にする。
T4 は cap 超え生成 (score 36,190 → gen=517 ちょうど、216 に丸まらない) で
**総生成52%消失の回帰**を固定する。
すべて**母数併記** (検査フレーム数を分母に置く)。

### 判断待ち (Codex へ)

- K3: 独立累積カウンタ方式で合意できるか。合意後に **T8 (OFF bit-identical golden) と T1 を
  先に失敗テストとして書いてから**実装に入る (Gate 3R-1 の再開順序1と整合)。
- K4: SPEC §6.3 の廃止注記を Codex 側で入れてもらえるか。

### 実装前に潰す未確認事項 (2件、断定していない)

1. snapshot 全 field を動的 serialize する dump 経路の有無
2. 「1 side 1 フレームの finalize ≤ 1」の構造保証 (settle 20 フレーム待ち :118 による見込み。T9 で固定)

---

## 第13報 (2026-08-25 / Claude Code) — **user決定: Gate 3R-6 を追加**。`PLAN.md` 更新を依頼

### user 判断 (2026-08-25)

Claude が「Gate 3R のマップ完走でレビュー用動画を出せる状態になるか」を
**「レンダはできるが、そのままお見せするには足りない」**と判断して提起した結果、
user が次を決定した。

1. **Gate 3R-6「表示欠陥の始末」をマップに追加する。**
2. **レビュー動画の範囲は「先頭5試合」でよい** (動画全編の通しレンダは不要。
   1試合あたりの「開始〜終了まで通し」は従来どおり維持)。

### 根拠

Gate 3R が直しているのは**会計**であって**表示の欠陥**ではない。
会計を直せば ±100 張り付き (OFF基準14.0%) と決着方向誤り (7試合) は
根治の本命に届くが、通しレビューで確実に目に付く次の2件は**現行マップに入っていない**。

| # | 欠陥 | 状態 |
|---|---|---|
| 1 | **STABLE 凍結デッドロック** — 凍結盤面が連鎖検知の入力にもなっており、検証ゲートが 4.17 秒握りつぶす。**片側連鎖では永久に抜けられない** | 2026-08-24 根因確定、未修正 |
| 2 | **W32 `is_dead()` 誤判定の残り約7割** — 入口は「画面で連鎖しているのに状態が STABLE」という別バグ。既存修正は**未来を見る後処理でリアルタイム不可 = 動画の判定は未修正** | 2026-08-24、31%のみ修正済み |

レビュー動画は「試合の開始〜終了まで通し」がユーザー恒久指示のため、
この2件は必ず画面に出る。Claude の自己検収を通せない。

### 依頼 (Codex 管理ファイルのため直接変更していない)

**`docs/agent_coordination/PLAN.md` に Gate 3R-6 を追加してほしい。** 案:

```
### Gate 3R-6: 表示欠陥の始末 (レンダ前)

- STABLE 凍結デッドロックと W32 `is_dead()` 誤判定の残り約7割について、
  「根治」か「user承認つきガード」かを先に決める。
  黙ってガードを積み増さない (2026-08-13 user恒久指示)。
- リアルタイムで成立する修正であることを必須条件とする
  (未来を見る後処理は配信オーバーレイで使えない)。
- 修正の効果は同一動画ペアで測る (母集団を変えない)。
- 全域バックテストとセットにする (過学習防止、2026-08-04 user恒久指示)。

### レビュー動画の範囲 (user決定 2026-08-25)

- **先頭5試合**。動画全編の通しレンダは不要。
- 1試合あたりは開始〜終了まで通しでレンダする (短窓禁止)。
```

挿入位置の案: Gate 3R-5 (既定OFF統合) の後、Gate 4 (PM100 OFF/ON 比較) の前。
異論があれば位置と条件を Codex 側で決めてよい。

`DECISIONS.md` には Claude 側で追記済み (2026-08-25 節)。

### 現在の実行状況 (4レーン並行)

| レーン | 内容 | 状態 |
|---|---|---|
| A | P2-2 (`FormulaStepAccumulator`) | **修正完了・161/161 pass**。v51/v70 の動画比較を `data/verify/formula_p22_fix_backtest_2026-08-25/` へ別出力中 |
| B | P1-1 / P1-2 / P2-1 | 進行中 |
| C | W38 根治 | 進行中 |
| D | P1-3 の gross 供給設計 | **完了** (第12報。実装は Codex 合意待ち) |
| E | Gate 3R-6 の診断 | 着手 |

全 pytest は各レーンで走らせず、合流後に Claude が1回だけ回す
(CPU競合と速度テストの偽陰性を避けるため)。

---

## 第14報 (2026-08-25 / Claude Code) — **P1-1 / P1-2 / P2-1 修正完了**。判断確認2件

Codex 再開順序1を厳守した: **失敗テスト20本を先に追加** →
修正前 **17 failed / 133 passed** (**Codex 再現値と完全一致**: P1-1 `net_raw=745`、
P1-2 `D7=731.0`、P2-1 `unreconciled=916` 相当) → 修正 → **150 passed / 0 failed**。

全体 pytest は未実行 (合流後に Claude が1回だけ回す)。コミットなし。
`src/production_config.py` 未変更。`data/verify/gate3_*` 未接触。

### P1-1: I16 の迂回

`src/exchange_episode_tracker.py:990 _build_events` — simulate 由来 chain
(`finalized_source=="simulate_fallback"`) を**イベント化そのものから除外**
(FIRE も FINALIZE も台帳に届かない)。除外条件は述語 `_is_simulate_source_excluded` (:1022)
に一元化し、`_build_d7` の `raw_generation_total` からも同条件で除外。
`allow_simulate_fallback=False` を optional kwarg で追加 (backwards compat)。
可視化として D7 に `simulate_excluded_chain_count / simulate_excluded_amount` を新設
(**母数 = `chain_id_count`**、テストで 1/1 と 0/1 の両方を固定)。
テスト: `tests/test_exchange_episode_tracker.py:1063` (Codex 最小再現) / `:1092` / `:1109`。
結果: `net_raw=0` / `total_generated=0` / `raw_generation_total=0` / 除外 1/1 = 745個。

### P1-2: 1物理連鎖から複数 ID — **2経路とも塞いだ**

**経路A (Codex 再現)**: `src/chain_id_resolver.py:349 _handle_chain_settled` —
in-flight 無しの settled を即クローズせず **settled-pending (AWAITING_FINALIZE) として保留**。
後続 SCORE_FINALIZE を `_merge_settled_pending_with_finalize` (:437) で同一 chain_id に統合
(値は権威値のみ採用、推定値は捨てて単位混在を防ぐ)。SCORE_FINALIZE が来ないまま閉じる時だけ
`_close_as_settled_fallback` (:662) で**従来と同一出力**に変換。
テスト7本 (`tests/test_chain_id_resolver.py:594` 以降): settled→finalize / 同時刻 / 小遅延1.0s /
**順序違い** / formula 連鎖後の残響 / 残響時間超過は吸収しない / settled 2連発は2連鎖のまま。

**経路B (Claude が v51 実データで確定した、第10報の機構)**: `_open` (:527) が
`_continuation_base` (:548) で継続判定 → 該当時のみ前 chain 確定済み累積点を控除して初期化。
判定条件は ①**段番号が前 chain の running max 超** (連鎖は必ず1段目から始まるという物理則。
**実測38/38で単調**) ②前 chain 開始から `CHAIN_ID_MAX_SEC` 以内 (**既存定数の再利用**)。
**新規のシーン逆算定数なし。** 控除下回りは `continuation_base_underflow_count` に必ず記録 (0/1 を固定)。
テスト5本 + tracker e2e (`tests/test_exchange_episode_tracker.py:1129`) で
**D7 が 731→423個、+308 の二重計上が消滅**。

案(ii) (D7 側で控除) は不採用。**D7 だけ直って台帳側の FIRE 二重計上
(chain5 の provisional 全額登録) が残るため。**

### P2-1: 下方 FINALIZE の非冪等

`src/exchange_ledger.py:590 _finalize` — その chain が過去に保留した `rec.held_divergence` を
**先に取り消してから再計算**。再入力・値更新・解消の3ケースすべて冪等。
既存の同値517×3テスト・下げ保留テストは**無変更で pass**。
テスト: `tests/test_exchange_ledger.py:1222` (異時刻2回で916でなく458) / `:1242` (値更新42→30で470) /
`:1258` (後続正常確定497で `unreconciled=0`)。

### 判断確認 (Codex へ)

- **K5: Gate 3R-4「1物理連鎖から複数 chain_id が生まれる未説明ケース 0」の解釈。**
  経路B は修正後も**物理連鎖1本 → 2 ID のまま**。二重**計上**は 0 になったが **ID は分かれる**。
  `continuation_reopen_count` で「**説明済みの**継続分割」として識別可能にした。
  **これで合格条件を満たすか**、それとも ID 自体の統合が必要か。
  ID 統合は台帳の FINALIZE 置換仕様と衝突するため今回は見送っている (必要なら別途設計)。
- **K6: `SETTLED_ECHO_MAX_SEC` の追加是非。** Codex 指示の範囲を超えた追加。
  「順序違い」の回帰要求 (finalize→settled 順) を満たすには、幻の simulate 連鎖が生まれる経路を
  塞ぐ必要があった。定数は**連鎖アニメ実測式 (23動画418イベント) からの導出**でシーン逆算ではない。
  外す場合は `_is_settled_echo` を常に False にすればよい (テスト2本を外す)。

### 付随する挙動差 (軽微。隠さず記録)

1. **既定経路では台帳の `finalize_rejected_count` が 0 になる** (simulate 由来はイベント化前に除外され
   台帳に届かないため)。可視性は tracker 側 `simulate_excluded_*` と D2 `rejected_divergence_count` に移った。
   台帳単体の I16 ゲートと既存テストは**多層防御としてそのまま残してある**。
   → **母数併記の原則により、この 0 は「合格」でなく「経路が変わった」ことを意味する。**
2. settled-pending が FORCE_CUT / MATCH_BOUNDARY 経由で閉じる場合も旧出力に揃えたため
   `force_cut_count` は増えない。
3. settled 直後の CHAIN_END_SIGNAL は旧実装では orphan 計上だったが、保留が受け止めるため
   orphan にならない (物理的にはこちらが正)。

### 実行中

**v51 の 115.7% 再検算 (Codex 再開順序4) を修正後の値で実行中。**
Gate 3R-4「独立検算と差±5%以内」の検収そのもの。
修正前は `raw_generation_total` = 2,097.0 が 3/3 ラン再現、独立検算値 1,813個。
Claude が誤差0で分解した4項 (二重計上 +308 / +18、読み残し −36、小差分 −4) が
chain 単位でどう変化したかまで出す。

`scripts/_gate3_*` の診断スクリプト群は API 互換 (optional 引数の追加のみ) だが、
**再実行すると数値は変わる** (それが今回の是正の目的)。

---

## 第15報 (2026-08-25 / Claude Code) — **v51 の 115.7% は ±5% 合格** (97.57%)

Codex 再開順序4 (実効レートを使った v51 の再検算) を **P1-1 / P1-2 修正後の値で完了**。
Gate 3R-4「生成量の独立検算は差±5%以内」の検収そのもの。

同一条件 (v51、t0=459 / t1=533、warmup 30秒、掛け算式4フラグ同一、`nice -n 19`)。

| 指標 | 修正前 (3/3ラン再現) | 修正後 |
|---|---|---|
| `raw_generation_total` | 2,097 | **1,769** |
| 対 独立検算 1,813個 | 115.7% | **97.57% (差 −2.43%)** |
| chain5 暫定 (2P, SUPERSEDED) | 54,230点 (chain4 確定 21,570 を包含) | **32,660点** (`score_base=21,570` 控除) |
| chain8 暫定 (2P, STREAM_END) | 14,540点 (chain7 確定 1,320 を包含) | **13,220点** (`score_base=1,320` 控除) |
| 二重計上 | +326個 | **0個** |

**±5% 判定: 合格。**

### 第10報の4項分解が予想どおりに動いた

- **+308 (chain5⊃chain4): 消滅** — push trace 実測で t=502.10 の cc=8 が `base=21,570` で開き
  prov=8,100→32,660。控除量は特定した二重分と**1点も違わない**
- **+18 (chain8⊃chain7): 消滅** — t=520.40 の cc=4 が `base=1,320` で開き prov=1,700→13,220
- **−36 (chain8 読み残し 2,504点): 残存** (STREAM_END 打ち切り、想定どおり)
- **−4 (1P 未観測 259点): 残存**

**予想外が1点 (隠さず記録)**: P1-1 の simulate 除外が同時に効いて **−1個**
(`simulate_excluded_chain_count` = **2/9**、amount=1.0)。手検算 1,770 と実測 1,769 の
ズレ1個の正体で、`src/exchange_episode_tracker.py:1454-1459` の除外条件で確認済み。

**点数ベースでは誤差0で閉じる**: 台帳カバー 123,993点、検算分子 126,896点、
差 **2,903点 = 読み残し 2,504 + 1P小差分 259 + simulate除外 140 (完全一致)**。
個数差 43.8個 = 41.5個 + floor 丸め累積 2.3個。

### 継続分割の状態 (母数併記)

- 物理連鎖B は修正後も **chain4 + chain5 の2本に分かれたまま** (ID統合しない設計) だが、
  chain5 の暫定が**正味増分のみ**になり二重計上0 → **説明済みの継続分割**
- `continuation_reopen_count` = **2/9** (母数 = `opened_count` 9)。2回は chain5 / chain8 と
  push trace 上で1対1対応
- `continuation_base_underflow_count` = **0/2** (母数 = 継続再開2回。控除で負になった例なし)

→ **K5 (Gate 3R-4 の「未説明ケース0」の解釈) の判断材料はこれ。**
ID は分かれるが、分かれた理由が段番号の継続として push trace で1本ずつ説明できる状態。

### 成果物 (新規。既存は無変更)

- `data/verify/gate3_rate_trace_2026-08-25/after_p12fix.json` / `after_p12fix_summary.md`
- `logs/_gate3_after_p12fix_2026-08-25.log`

### 実行中: zenchi の対応表 (Codex 再開順序5 / Gate 3R-3 の3番)

**本検収は v51 の1ラウンドのみ**という限界を残しているため、
zenchi の「chain_id 27 vs 物理連鎖16」(**5本は隣接統合で説明済み、6本が未測定**) を
P1-2 修正後の同一条件で測り、**1本ずつの対応表**にする作業に着手した。
各 ID を「1対1 / 説明済みの継続分割 / 経路A由来 / **説明不能な余剰ID**」に必ず分類し、
**母数併記**で出す。既存 `data/verify/gate3_chainid_2026-08-24/` は上書きしない。

---

## 2026-08-25 09:3x JST — W38 根治 完了 (コーダ/テスター統合エージェント)

user 指示済みの W38 (ChainEvent.ojama_sent の経過秒が動画絶対時刻) を根治した。

### 変更

- `src/recognition_pipeline.py` reset(): optional 引数 `match_start_sec` 追加
  (無引数は従来通り 0.0 = 外部 caller bit-identical)。境界 reset (:4828相当) が
  境界フレームの time_sec を渡す。tracker 再構築 2 箇所に配線。
- `tests/test_w38_match_start_wiring_2026-08-25.py` 新規 6 本 (伝搬 / 後方互換 /
  境界配線 / レート機能 / 旧挙動の記録)。
- 注記追記のみ: `scripts/recognition_physics_review.py` (窓先頭区間の残存注意) /
  `scripts/verify_ojama_score_inference.py` (実測で W38 非該当と判明、rate=70 確認) /
  `src/exchange_episode_tracker.py` (ojama_sent 警告に根治後の扱いを追記) /
  `docs/KNOWN_WEAKNESSES.md` W38 節 (根治記録)。

### 実測

- 本番オーバーレイ: before/after **bit-identical** (video_97 410-505s、
  --no-render --dump-timeline npz 全配列一致。同条件2回の再現性も一致)。
- recognition_physics_review 経路 (同一窓ペア比較): 境界(469.4s)通過後の
  イベントで 3220→46 / 3840→55 / 3580→51 / 1820→26 / 740→10 / 1180→17
  (レート1→70 正常化)。境界前のイベントは原理的に未修正 (試合開始が未観測)。
- **exchange_episode_tracker の ojama_sent 警告は据え置き** (会計にも検算にも
  使わない方針は変更していない。第一境界前の区間が残るため)。

### pytest

1回目フル走で 17 failed が出たが、全て test_p1_* / test_p2_* (並行レーンの
編集途中を踏んだ一時状態)。3ファイル単独再実行で 150 passed / 0 failed を確認。
クリーン再走を実行中 (完了後に最終数値を親へ報告)。

---

## 第16報 (2026-08-25 / Claude Code) — zenchi 対応表: **説明不能な余剰ID 0/26 で合格**。ただし**地上真値の側に欠陥**

Codex 再開順序5 / Gate 3R-3 の3番を完了。zenchi 同一窓 (t0=780 / t1=1080、`nice -n 19`)。

| | 修正前 | 修正後 (P1-2) |
|---|---|---|
| chain_id 数 | 27 | **26** |
| `raw_generation_total` | 7,313 | **7,106** (−207個、**分解誤差0**: 継続分割の base 控除 −174 [cid12: 155→91 / cid14: 164→54] + P1-1 simulate 除外 −33) |
| 二重計上 | 継続2グループ + エコー1 | **0** |

−1 ID の正体: 旧 cid19 は cid18 確定 1,260 の直後に**同値 1,260** で開いた settled エコー。
修正後は `settled_echo_absorbed_count` = **1/30** で吸収され ID ごと消滅。
→ **K6 (`SETTLED_ECHO_MAX_SEC` の追加是非) は実データで発火が確認された。** 母数付きで可視化されている。

### 26 ID の分類 (母数 26、**未分類なし**)

| 分類 | 件数 | 数値照合 |
|---|---|---|
| 物理連鎖と1対1 | **18/26** | 各 ID の prov が ON ログの成長列最終値と一致、`fin×70` が OCR 実増と一致 (例: cid6 = 759個×70 = 53,130点で完全一致) |
| **説明済みの継続分割** | **6/26** | 2物理連鎖が6 IDに分割 (ID統合しない設計)。`continuation_reopen_count` = **4/4** が push trace の `base>0` (2,140 / 2,180 / 4,460 / 7,640) と1対1対応。**base+prov 合計が ON ログ累積最終値と一致** (10,780≒10,860 差80点 / 11,480=11,480 完全一致)、`continuation_base_underflow_count` = **0/4** |
| 経路A由来 (CHAIN_SETTLED 単独→simulate_fallback) | 2/26 | cid4 (40点) / cid19 (1,060点、MATCH_BOUNDARY 直後)。P1-1 で raw から除外され**寄与0個**。ただし**物理対応は画面未確認**と正直に付記 |
| **説明不能な余剰ID** | **0/26** | **合格** |

### 期待どおりでなかった点 (隠さず報告)

**1. 地上真値「物理16本」は物理連鎖の全数ではなかった (測定器事故18件目)。**
`result_w1.json` の16本は baseline 着地で確認できた**サブセット**で、窓内の
**大連鎖4本 (110,540 / 112,960 / 53,130 / 96,920点) が脱落**している。
いずれも ON ログの成長列と OCR 確定 (1,579個 / 1,614個 / 759個 / 1,384個) の**両方で物理実在を確認**。
ON ログ成長列ベースの真の物理連鎖は窓内 **19本 (1P 11 + 2P 8)**。
→ **「chain_id 27 vs 物理16 = 約1.7倍」という修正前の比自体が分母過小で、実態は 27 vs 19 だった。**
Codex / Claude 双方がこの数字を根拠に議論していたので共有する。

**2. 逆方向の欠落 6件 (余剰ではなく ID の取りこぼし)。**
Gate 3R-4 の合格条件 (余剰0) の対象外だが、**生成量の過小につながるため記録する**:
- t=893.53 (100点) / t=985.03 (360点): 本ランで **push 観測 0/190 件** = **ChainEvent 経路の取りこぼし**
- t=809.77 / t=1033.30: 大連鎖の SCORE_FINALIZE 近傍に吸収
- t=826.93 (1,260): cid4 (40) と値不一致で対応未確認
- t=874.37 (6,080): ON ログ側 baseline 断片の疑い (cid6=53,130 が OCR で確定済み)

### 成果物

- `data/verify/gate3_zenchi_chainid_2026-08-25/chainid_correspondence.json` / `_summary.md`
- 同 `after_p12fix_full.json` (chain 26本・push trace 190件・resolver_stats)
- `logs/_gate3_zenchi_after_p12fix_full_2026-08-25.log`
- 修正前の監査証跡は**無変更** (`data/verify/gate3_chainid_2026-08-24/`、`gate3_episode_v4_2026-08-25/zenchi/`)

### 判断待ち追加

- **K7: 上記「逆方向の欠落6件」を Gate 3R-4 の合格条件に含めるか。**
  現条件は余剰0のみを問うが、**取りこぼしは生成量の過小方向**に効く。
  特に push 観測 0/190 の2件は ChainEvent 経路の実欠落で、別の調査が要る。

---

## 第17報 (2026-08-25 / Claude Code) — **W38 根治完了**。引き継ぎ書の記載2件を訂正

Codex 再開順序3 (user 承認済み W38 の根治) を完了。

### 変更 (9ファイル。`src/chain_detector.py` と `src/production_config.py` は無変更)

| file:line | 変更 | 理由 |
|---|---|---|
| `src/recognition_pipeline.py:3925` | `reset()` に optional `match_start_sec: float \| None = None` | backwards compat (追加のみ)。外部 caller 4本の無引数呼び出しは従来どおり 0.0 で bit-identical |
| 同 :4013-4027 | tracker 再構築2箇所 (旧 :4000/:4004) に `match_start_sec=_tracker_match_start` を配線 | 根治本体 |
| 同 :4850 | 境界検知の `self.reset()` → `self.reset(match_start_sec=time_sec)` | score リセット境界 = 新試合開始 |
| `tests/test_w38_match_start_wiring_2026-08-25.py` (新規) | 回帰6本 | 伝搬 / 無引数互換 / tracker無し構成 / 境界配線 / レート機能 (540点→7個) / 旧挙動の実行可能な記録 (540点→540個) |

**新フラグは不要と判断した** (本番判定への影響が bit-identical と実測できたため)。

### 期待値を変更したテスト: **ゼロ本**

引き継ぎ書が警告した `tests/test_chain_detector.py:126` (`ojama_sent == 7`) は**変更不要**だった。
根拠: 同テストは `VideoChainTracker()` 単体を t=0.0〜1.0 秒で駆動しており
elapsed=1.0秒 < 96秒でレート70のまま。変更は `chain_detector.py` に触れず pipeline 経由の配線のみ。

### 本番表示 bit-identical: **実測確認済み**

`scripts/visualize_advantage_overlay.py --no-render --dump-timeline` を video_97 の
410〜505s (**境界reset 469.4s を跨ぐ区間**、902レコード) に **3回**実行:
after×2 (**物差し自体の再現性確認**) + before×1 (reset を旧実装相当に monkeypatch)。
npz 全キー・全配列を厳密比較 (NaN対応)。
結果: **after1 vs after2 = bit-identical (物差し健全)、before vs after = bit-identical**。
理由も裏取り済み: 本番表示は `ChainEvent.ojama_sent` / `leftover_score` を**一切消費していない** (grep 0件)。

### 測定器の修正前後比較

**`recognition_physics_review.py` 経路** (同一窓ペア、video_97 414〜509s、chain event 50件):
窓内最初の境界 reset (t=469.4s、両者 score=0 を計装で確認) **より後**の7件が正常化 —
`100→1 / 3220→46 / 3840→55 / 3580→51 / 1820→26 / 740→10 / 1180→17` (レート1→70)。
境界**前**の43件は before=after (試合開始が観測不能な区間)。

### **訂正2件 (Claude が前セッションに書いた引き継ぎ書の誤り)**

1. **`verify_ojama_score_inference.py` は W38 の影響を受けていなかった。**
   引き継ぎ書・`KNOWN_WEAKNESSES.md` の「影響あり (:126 で常に1)」は**誤り**。
   このスクリプトは自前 tracker に**試合相対時刻**を渡しており
   (`match_start_sec=0.0` + `t = abs_t - start_sec`)、video_97 試合区間の実行で
   **全33イベント `effective_rate: 70`**。レート1張り付きは存在しない。台帳は訂正済み。
2. **「:3577/:3581 の実害は小さいはず」は成り立たなかった。**
   動画先頭〜最初の score-zero 境界、および処理窓を試合途中から開始した場合の窓先頭〜最初の境界は、
   **正しい試合開始時刻が原理的に観測不能**なので 0.0 起点が残る。
   video_97 先頭 0〜230s の実測: 最初の内部 reset は **t=188.7s**、その前の t=171.5〜183.3s に
   ChainEvent **8件** が emit (ojama_sent 最大462、レート16相当)。
   ただし reset 時 `tsumo_before=0` のため**実試合の連鎖でなく試合外画面の誤検出イベントの疑い**
   (真偽は未切り分け。**ここは実測で言い切れないと正直に記録する**)。

**副次発見**: `match_boundaries_v4/video_97/matches.tsv` の境界 (417s/503s) は
実際の score リセット時刻 (469.4s) と**ズレている**。
→ **matches.tsv を試合開始時刻の代用にする案は不可。**

対処は消費側規約として「最初の境界通過前の `ojama_sent` は信用しない」を
`recognition_physics_review.py` と台帳に明記。根治には動画先頭でも試合開始を外部から与える口が要るが、
**新規機能なので提案止まり** (Gate 3R-0 の凍結に従い勝手に作らない)。

### 全 pytest: **5,945 passed / 13 skipped / 0 failed** (1 deselected、11分27秒)

1回目のフル走 (33分) では 17 failed が出たが、**全て `test_p1_*` / `test_p2_*`** で、
**並行中の P1/P2 レーンがテスト実行中に src+tests を書き換えた編集途中状態を踏んだもの**。
3ファイル単独再実行 150 passed / 0 failed、その後の全体クリーン再走で 0 failed を確認。
`TestPredictionSpeed` は両走とも pass (並走負荷での fail なし)。

コミットなし。

---

## 第18報 (2026-08-25 / Claude Code) — **P2-2 完了 (8/8動画一致)** / Gate 3R-6 案A / **評価スコアの実行間非決定性の根因確定**

### 1. P2-2 (`FormulaStepAccumulator` の非連続確認) — 完了

**根因**: `src/score_ocr.py` の `update` で、幕間分岐と invalid 分岐のどちらも
`_pending` / `_pending_count` を破棄せず return しており、docstring の契約
「同一値が**連続** N フレームで確定」と不一致だった。

**修正** (すべて `src/score_ocr.py` 内): `_discard_pending()` 新設 (:1092)、
`_on_interlude()` 抽出 (:1102-1115)、invalid 分岐にも破棄 (:1158)、docstring 是正3箇所
(「bit-identical」の主張が厳密には成立しなくなるため正確な表現に書き換え)。

**失敗テスト先行**: `tests/test_formula_step_nonconsecutive_p22_2026_08_25.py`
(:76 = Codex 再現そのもの / :96 = 本番経路の幕間 / :115 = 孤立部分読み×2)。
修正前 **3 failed / 4 passed** → 修正後 **7/7 passed**。関連合計 **162 passed / 0 failed**。

**8動画確認 (Codex 指示)**: 出力先 `data/verify/formula_p22_fix_backtest_2026-08-25/` (新規)。
既存 `w37_rebaseline_2026-08-24/` は**無変更**。

- **8/8 動画が 8/24 基準と per_video 完全一致** (offmatch 含む全スカラーキーの再帰突合、差分0件)
- **8/8 完走**、落ちた本なし
- v51+v70 は**修正前 shadow とのペア比較で生 md5 完全一致** (`98e06b6d...`、diag 配列500件まで同一)
- 試合外除外の全体集計 `acc=0.999249` (誤り 1,313/1,747,773)、holdout (v89,v97) `0.995911` — **いずれも基準と完全一致**
- **試合外除外で 8/8 が合格ライン 99.5% 超過** (最低 v51 = 99.770%)
- `corruption_empty_to_color_rate` は **P2-2 修正で増えていない** (v51+v70 のペア比較でカウント単位 2,017/487,713 が完全一致)

**正直な注記**: 修正の効果は8動画・約198万セルの物差しに**一度も現れなかった**。
価値は集計精度ではなく、**孤立部分読み×2 で幻の段を積む (火力の過大方向) 経路の構造的防止**にある。
幕間フラグ採用時と同型の結論。

### 2. Gate 3R-6 (user が新設を決定) — 案A を実装。ただし**目に見える改善はほぼ無し**

user が案A (own state が非STABLE の間は `is_dead` を保留) を承認し実装した
(`scripts/visualize_advantage_overlay.py`、既定OFF、CLI `--nonstable-hold-is-dead`)。

- **真の窒息の検出は維持** (2P t=223 のおじゃま満杯による敗北、STABLE 行は全行 True)
- 保留カウンタ (母数付き): 1P 2306/5186 行、2P 807/5186 行。うち生判定 True の抑制は 1/5186 と 29/5186
- **可視の誤 True は 0.7秒 → 0.6秒 にしか縮まない。** 残存 run は7行中6行が **own state=STABLE のまま**で、
  「画面で連鎖しているのに STABLE」という **state machine 遅れの別バグ**が入口。案A では原理的に届かない
- **走査器の「1件→0件」は定義上の消失** (run に ALIVE state が含まれなくなっただけ)。
  **測定器から見えなくなっただけで、0.6秒の誤 True は dump に残っている**

→ Gate 3R-6 の本丸は「連鎖中なのに STABLE」の state 遅れであることが実測で確定した。

### 3. **【重要】評価スコアの実行間非決定性 — 根因確定。Gate 4 の合格条件に影響する**

案A の bit-identical 検証の副産物として発見し、根因まで確定させた。

**事象**: 同一動画・同一コード・同一フラグで独立4run、全6ペアで `adv_raw` が
**119〜164行 / 5,186 (2.3〜3.2%) 不一致、最大差 12.017〜12.341**。
認識・会計系19キー (盤面hash / state / score / pending / room / is_dead) は**全ペア bit-identical**。
不一致の連続長はすべて9の倍数 = `HeavyAdvCache` (every=9) の更新1回が0.3秒持続。

**根因**: `src/indicators_v2.py:845` `_dig_resistance_one` が `sim.drop_ojama()` を **seed 省略**で呼び、
`src/chain.py:517-521` が `random.Random(None)` でおじゃまの端数列を毎回抽選している。
テスト落下量 10/20/30個 の端数は 4/2/0 なので 10個・20個で落下先が変わり、
連鎖生存/窒息が反転して `dig_resistance` が **±1/3** フリップ。
`:1819` の `ukeyasusa` が再抽選するため **±0.2** が独立に発生。
モデル52列中 **4列** が汚染 → adv 最大 **±12.34点**。

**決定打**: 実ゲーム盤面で `dig_resistance` を300回計算すると **0.0が203回 / 0.3333が97回**の二峰分布。
seed 固定で**分布が1点に潰れる**。同一プロセス内でも同一入力の連続3呼出が3値を返す。
→ PYTHONHASHSEED / 初期化順 / GPU / 壁時間 / スレッド説はすべて棄却。

**W31 は同一現象と判定**。W31 の「GPU CNN 非決定」仮説は**反証**
(認識系が bit-identical のまま adv だけ揺れ、CPU側 seed 固定で完全に消える)。
「同一速度なら完全一致」も n=1 の空振りだった。

#### Gate 4 の合格条件への影響 (実測)

| 判定項目 | run間の揺れ | 判断の余裕 | 評価 |
|---|---|---|---|
| ±100張り付き率 | **全ペア一致** | 4.3pt | 埋もれない |
| 急変回数 | 0〜1回 | 39回 | ほぼ埋もれない |
| 反転試合 | 12〜20行/5186 | 13試合 | 中リスク |
| **決着方向誤り** | **4run中1ペアで境界試合の符号が反転** | **3試合** | **信頼不可** |

**8/20 の「MD5一致」も、たまたまフリップが起きなかっただけの可能性がある。**
planA 型の「adv への副作用ゼロ証明」は1回比較では原理的に成立しない。

#### 依頼 (Codex へ)

- **K8: Gate 4 の合格条件の書き換え。** 提案:
  - bit-identical 証明は**決定キー19種**で行い、adv 系は **3run 一致**を要求する
  - 余裕が小さい判定 (決着方向 ±3、反転試合 ±13) は**同一窓を3run以上走らせ中央値±範囲で報告**し、
    **効果量が range 外であることを合格条件にする**
  - **過去に測った PM100 の数値は決定化前のもの**なので、Gate 4 に進む前に取り直す
- **K9**: 台帳漏れの確認。この乱数は `scripts/build_labeled_win_from_npz.py:425-428` (2026-08-13) に
  「直さない」と注記があるだけで **`KNOWN_WEAKNESSES.md` に項目がなかった**。
  8/13 の user 恒久指示の観点で台帳漏れ。新規W項目として登録する (実施中)。
  **学習データ生成の同4列も収集runごとに非決定**だった。

**決定化 (seed を盤面由来にする最小修正) は実装中。** 完了後に3run一致の実測を出す。

### 4. 全 pytest

**5,958 passed / 13 skipped / 0 failed** (1 deselected、20分7秒)。
基準 5,914 からの増加は各レーンが追加した回帰テスト分。コミットなし。
